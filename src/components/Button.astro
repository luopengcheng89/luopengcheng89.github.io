---
interface Props {
  title: string
  href: string
  class?: string
  style?: string
}
const { title, href, ...props } = Astro.props
---
<a href={href} class:list={[props.class, 'inline-block px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition']}>
  {title}
</a>
