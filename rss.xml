<?xml version="1.0" encoding="UTF-8"?><?xml-stylesheet href="/scripts/pretty-feed-v3.xsl" type="text/xsl"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:h="http://www.w3.org/TR/html4/"><channel><title>JohnieXu&apos;s Blog</title><description>Stay hungry, stay foolish</description><link>https://luopengcheng89.github.io</link><item><title>AGENTS.md 用法：Cursor 与 Claude Code 最佳实践</title><link>https://luopengcheng89.github.io/blog/agents-md-cursor-claude-code-practices</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/agents-md-cursor-claude-code-practices</guid><description>以 lynx-stack 项目为例，介绍 AGENTS.md 的定位、写法、分层策略，以及在 Cursor、Claude Code 等 AI 编程工具中的结合方式。</description><pubDate>Wed, 29 Apr 2026 02:35:00 GMT</pubDate><content:encoded>&lt;h1&gt;AGENTS.md 用法：Cursor 与 Claude Code 最佳实践&lt;/h1&gt;
&lt;p&gt;如果说 &lt;code&gt;README.md&lt;/code&gt; 是写给人类开发者的项目入口，&lt;code&gt;AGENTS.md&lt;/code&gt; 就更像是写给 AI 编程代理的“项目操作手册”。它不只是告诉 agent 项目是什么，还要告诉 agent：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;应该用什么包管理器、Node 版本、构建命令和测试命令。&lt;/li&gt;
&lt;li&gt;哪些目录是核心模块，修改时需要理解哪些数据流。&lt;/li&gt;
&lt;li&gt;哪些命令有前置条件，例如测试前必须先 build。&lt;/li&gt;
&lt;li&gt;哪些产物不能手动修改，哪些变更必须同步生成 API 文档或 changeset。&lt;/li&gt;
&lt;li&gt;当进入某个子系统时，是否存在更局部、更具体的规则。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本文结合 &lt;a href=&quot;https://github.com/lynx-family/lynx-stack&quot;&gt;lynx-family/lynx-stack&lt;/a&gt; 的实践，聊聊 &lt;code&gt;AGENTS.md&lt;/code&gt; 应该怎么写，以及如何和 Cursor、Claude Code 等 AI 编程工具配合使用。&lt;/p&gt;
&lt;h2&gt;1. AGENTS.md 解决的核心问题&lt;/h2&gt;
&lt;p&gt;AI 编程工具真正影响产出质量的，不只是模型能力，还有上下文质量。一个大型仓库里，agent 如果不知道项目约定，很容易犯这些错误：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;用错包管理器，例如项目要求 &lt;code&gt;pnpm&lt;/code&gt;，但 agent 直接跑了 &lt;code&gt;npm install&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;跳过必要前置步骤，例如没有先构建 Rust / native bindings 就直接跑测试。&lt;/li&gt;
&lt;li&gt;只看当前文件，忽略 monorepo 中跨 package 的依赖关系。&lt;/li&gt;
&lt;li&gt;修改了生成产物，却没有修改源文件。&lt;/li&gt;
&lt;li&gt;不知道某个子目录的架构，把局部实现当成普通 React 组件来改。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;AGENTS.md&lt;/code&gt; 的价值，就是把这些“资深维护者脑子里的隐性知识”变成可被 agent 自动读取的显性规则。&lt;/p&gt;
&lt;h2&gt;2. 从 lynx-stack 看仓库级 AGENTS.md 应该写什么&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;lynx-stack&lt;/code&gt; 是一个典型的大型 monorepo，包含 ReactLynx、Rspeedy、Lynx for Web、testing utilities 等模块，主要语言是 TypeScript / JavaScript，同时还包含 Rust native bindings。&lt;/p&gt;
&lt;p&gt;它的根目录 &lt;code&gt;AGENTS.md&lt;/code&gt; 做得比较好的地方是：没有泛泛地写“请遵守项目规范”，而是把 agent 最容易做错、最需要提前知道的事情写清楚。&lt;/p&gt;
&lt;h3&gt;2.1 先给仓库画像&lt;/h3&gt;
&lt;p&gt;根目录文档一开始就说明：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;这是 Lynx 的 core JavaScript stack。&lt;/li&gt;
&lt;li&gt;仓库规模是大型 monorepo，包含约 49 个 package。&lt;/li&gt;
&lt;li&gt;主要语言是 TypeScript / JavaScript / Rust。&lt;/li&gt;
&lt;li&gt;构建系统是 Turbo monorepo + pnpm workspaces。&lt;/li&gt;
&lt;li&gt;目标运行时包括 Node.js、浏览器和移动端。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这类信息很重要，因为 agent 在选择修改策略时会受到它影响。比如看到 “Rust native bindings”，agent 就会意识到有些测试失败可能不是普通 TypeScript 编译错误，而是 native 构建链路问题。&lt;/p&gt;
&lt;h3&gt;2.2 把命令写成可执行路径，而不是命令清单&lt;/h3&gt;
&lt;p&gt;lynx-stack 的根目录 &lt;code&gt;AGENTS.md&lt;/code&gt; 不是简单罗列命令，而是强调顺序：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npm install -g corepack
corepack enable
pnpm install --frozen-lockfile
pnpm turbo build
pnpm test
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;并且明确写出：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Always run full build before tests. Watch mode only compiles TypeScript, not Rust components.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;这比“测试命令是 &lt;code&gt;pnpm test&lt;/code&gt;”有用得多。因为 agent 最常见的问题不是不知道测试命令，而是不知道测试命令之前必须做什么。&lt;/p&gt;
&lt;p&gt;一个好的 &lt;code&gt;AGENTS.md&lt;/code&gt; 应该尽量回答：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;第一次进入仓库怎么安装依赖？&lt;/li&gt;
&lt;li&gt;修改后最小验证命令是什么？&lt;/li&gt;
&lt;li&gt;完整验证命令是什么？&lt;/li&gt;
&lt;li&gt;哪些命令慢，需要设置更长 timeout？&lt;/li&gt;
&lt;li&gt;哪些命令会改文件，改完后是否需要提交生成结果？&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;2.3 记录常见失败与恢复方式&lt;/h3&gt;
&lt;p&gt;lynx-stack 还把常见构建问题写进了文档，例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Rust 编译失败时如何安装或更新 Rust toolchain。&lt;/li&gt;
&lt;li&gt;API Report 变化时需要运行 &lt;code&gt;pnpm turbo api-extractor -- --local&lt;/code&gt; 并提交 &lt;code&gt;api.md&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;测试缺少 build artifacts 时，先运行 &lt;code&gt;pnpm turbo build&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;大型构建内存不足时设置 &lt;code&gt;NODE_OPTIONS=&quot;--max-old-space-size=32768&quot;&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这类内容对 agent 特别有价值。因为 agent 遇到报错时，如果上下文里已经有“已知故障模式”，就更可能沿着项目推荐路径修复环境，而不是随意改业务代码绕过错误。&lt;/p&gt;
&lt;h3&gt;2.4 写清楚 CI 验证管线&lt;/h3&gt;
&lt;p&gt;根目录 &lt;code&gt;AGENTS.md&lt;/code&gt; 还总结了 CI 会跑哪些检查：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;code style&lt;/li&gt;
&lt;li&gt;API consistency&lt;/li&gt;
&lt;li&gt;changeset validation&lt;/li&gt;
&lt;li&gt;lint&lt;/li&gt;
&lt;li&gt;TypeScript compilation&lt;/li&gt;
&lt;li&gt;unit tests&lt;/li&gt;
&lt;li&gt;E2E tests&lt;/li&gt;
&lt;li&gt;Rust tests&lt;/li&gt;
&lt;li&gt;type checking&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这能帮助 agent 判断“我的本地验证是否足够接近 CI”。如果一个变更只影响某个小 package，agent 可以选择更聚焦的测试；如果变更影响公共 API，就知道需要额外跑 API extractor。&lt;/p&gt;
&lt;h2&gt;3. 子目录 AGENTS.md：把上下文放到离代码最近的地方&lt;/h2&gt;
&lt;p&gt;大型仓库只写根目录 &lt;code&gt;AGENTS.md&lt;/code&gt; 还不够。根目录适合写全局约定，但子系统的架构细节应该放到对应目录下。&lt;/p&gt;
&lt;p&gt;lynx-stack 在 A2UI 相关目录新增了两个子目录级说明：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-txt&quot;&gt;packages/genui/a2ui/AGENTS.md
packages/genui/a2ui-playground/AGENTS.md
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这两个文件的价值，是让 agent 进入 A2UI 目录时能快速获得局部架构知识。&lt;/p&gt;
&lt;h3&gt;3.1 a2ui：说明运行时数据流&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;packages/genui/a2ui/AGENTS.md&lt;/code&gt; 解释了 &lt;code&gt;@lynx-js/a2ui-reactlynx&lt;/code&gt; 的核心流程：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-txt&quot;&gt;A2UI messages
  -&gt; processor
  -&gt; Surface state
  -&gt; Resource update
  -&gt; A2UIRender
  -&gt; ReactLynx UI
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;它进一步说明：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;processor&lt;/code&gt; 负责处理 &lt;code&gt;createSurface&lt;/code&gt;、&lt;code&gt;updateComponents&lt;/code&gt;、&lt;code&gt;updateDataModel&lt;/code&gt; 等协议消息。&lt;/li&gt;
&lt;li&gt;每个 &lt;code&gt;Surface&lt;/code&gt; 维护 &lt;code&gt;components&lt;/code&gt;、&lt;code&gt;resources&lt;/code&gt; 和 &lt;code&gt;store&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;A2UIRender&lt;/code&gt; 根据 &lt;code&gt;Resource&lt;/code&gt; 渲染 surface root 或单个 component node。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;componentRegistry&lt;/code&gt; 把协议组件名映射到实际 ReactLynx renderer。&lt;/li&gt;
&lt;li&gt;catalog component 会通过副作用注册到 registry。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;如果没有这份文档，agent 很可能只看到一个 &lt;code&gt;index.tsx&lt;/code&gt; 就开始改组件，却不知道组件渲染依赖协议消息、资源对象、数据绑定和模板展开。&lt;/p&gt;
&lt;h3&gt;3.2 a2ui：说明如何扩展 catalog component&lt;/h3&gt;
&lt;p&gt;更实用的是，A2UI 的 &lt;code&gt;AGENTS.md&lt;/code&gt; 还写了新增 catalog component 的步骤：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;新建 &lt;code&gt;src/catalog/Foo/index.tsx&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;组件函数名必须叫 &lt;code&gt;Foo&lt;/code&gt;，和目录名一致。&lt;/li&gt;
&lt;li&gt;新建 &lt;code&gt;src/catalog/Foo.ts&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;加到 &lt;code&gt;src/catalog/all.ts&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;加到 &lt;code&gt;package.json&lt;/code&gt; exports。&lt;/li&gt;
&lt;li&gt;运行 build，确认生成 &lt;code&gt;dist/catalog/Foo/catalog.json&lt;/code&gt;。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这就是典型的 agent 友好信息：不是抽象原则，而是能直接执行的 checklist。&lt;/p&gt;
&lt;h3&gt;3.3 a2ui-playground：说明“看起来像 Web，其实渲染在 Lynx app”&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;packages/genui/a2ui-playground/AGENTS.md&lt;/code&gt; 解决的是另一个常见误区：playground 里有两层东西。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;浏览器里的 React DOM control panel：负责选择 demo、构造 &lt;code&gt;initData&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;真正渲染 A2UI 的 Lynx app：通过 &lt;code&gt;BaseClient&lt;/code&gt;、&lt;code&gt;processor.processMessages(...)&lt;/code&gt; 和 &lt;code&gt;&amp;#x3C;A2UIRender /&gt;&lt;/code&gt; 回放消息并渲染。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;它还指出 &lt;code&gt;/render.html&lt;/code&gt; 是桥接层，会创建 &lt;code&gt;&amp;#x3C;lynx-view&gt;&lt;/code&gt;，把 base64 payload 传给 Lynx runtime。&lt;/p&gt;
&lt;p&gt;这类说明可以显著减少 agent 的误判：如果要改 A2UI 渲染效果，不应该只盯着 control panel；如果要改 demo 切换逻辑，也不应该直接改 runtime。&lt;/p&gt;
&lt;h2&gt;4. Cursor 如何使用 AGENTS.md&lt;/h2&gt;
&lt;p&gt;在 Cursor / Cursor Agent / Cursor Cloud 这类环境中，&lt;code&gt;AGENTS.md&lt;/code&gt; 通常会被作为 agent instructions 或 workspace rules 的一部分读取。&lt;/p&gt;
&lt;p&gt;典型流程可以理解为：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;agent 启动或打开仓库时读取根目录说明。&lt;/li&gt;
&lt;li&gt;当任务涉及某个子目录时，再读取更靠近代码的 &lt;code&gt;AGENTS.md&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;把这些内容加入模型上下文。&lt;/li&gt;
&lt;li&gt;agent 在搜索、修改、测试、提交时遵守这些规则。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;在实际使用中，&lt;code&gt;AGENTS.md&lt;/code&gt; 对 Cursor 最有帮助的内容包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;项目命令：安装、开发、构建、测试、格式化。&lt;/li&gt;
&lt;li&gt;修改边界：哪些目录是生成产物，哪些文件不要手改。&lt;/li&gt;
&lt;li&gt;测试策略：改某类文件应该跑哪些测试。&lt;/li&gt;
&lt;li&gt;子系统架构：某个目录的入口、数据流、常见扩展步骤。&lt;/li&gt;
&lt;li&gt;常见问题：构建失败、缓存失效、依赖安装失败的恢复方式。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;对于 Cursor Cloud 这种自动执行任务的 agent，&lt;code&gt;AGENTS.md&lt;/code&gt; 的作用会更明显。因为它不仅回答“项目是什么”，还影响 agent 是否能一次性完成实现、验证、提交和 PR 描述。&lt;/p&gt;
&lt;h2&gt;5. Claude Code 如何使用 AGENTS.md&lt;/h2&gt;
&lt;p&gt;Claude Code 生态里更常见的项目说明文件是 &lt;code&gt;CLAUDE.md&lt;/code&gt;。很多项目会把 Claude Code 专属规则写在这里，例如：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-txt&quot;&gt;CLAUDE.md
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;不过 &lt;code&gt;AGENTS.md&lt;/code&gt; 正在变成更中立的跨工具约定。实际行为取决于工具版本和运行环境：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Claude Code 通常会自动读取 &lt;code&gt;CLAUDE.md&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;一些 wrapper、agent runner 或团队规范会额外读取 &lt;code&gt;AGENTS.md&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;用户也可以在对话中显式要求 Claude Code 查看 &lt;code&gt;AGENTS.md&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;如果同时存在根目录和子目录说明，通常应让更靠近代码的规则补充全局规则。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;我的建议是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;AGENTS.md&lt;/code&gt; 写跨工具、跨 agent 的通用项目规则。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CLAUDE.md&lt;/code&gt; 写 Claude Code 特有的补充说明，或者保留对 &lt;code&gt;AGENTS.md&lt;/code&gt; 的引用。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;.cursor/rules&lt;/code&gt; 写 Cursor IDE 特有的交互规则、文件匹配规则或 UI 工作流。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;.github/copilot-instructions.md&lt;/code&gt; 写 GitHub Copilot 相关说明。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这样可以避免把所有工具规则混在一起，也能保证核心工程知识只维护一份。&lt;/p&gt;
&lt;h2&gt;6. AGENTS.md 的分层最佳实践&lt;/h2&gt;
&lt;p&gt;一个可维护的 agent 说明体系，可以按三层设计。&lt;/p&gt;
&lt;h3&gt;6.1 根目录：仓库级规则&lt;/h3&gt;
&lt;p&gt;适合写：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;项目概览。&lt;/li&gt;
&lt;li&gt;技术栈。&lt;/li&gt;
&lt;li&gt;安装、构建、测试、格式化命令。&lt;/li&gt;
&lt;li&gt;CI 验证管线。&lt;/li&gt;
&lt;li&gt;目录结构。&lt;/li&gt;
&lt;li&gt;全局编码约定。&lt;/li&gt;
&lt;li&gt;常见环境问题。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;示例：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-md&quot;&gt;# Project Overview

This is an Astro 5 personal blog using the astro-pure theme.

## Commands

- bun install
- bun dev
- bun run build
- bun run check

## Content

Blog posts live in src/content/blog.
Each post must include title, description, publishDate, tags, draft and comment.
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;6.2 子目录：局部架构和操作步骤&lt;/h3&gt;
&lt;p&gt;适合写：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;当前目录负责什么。&lt;/li&gt;
&lt;li&gt;修改入口在哪里。&lt;/li&gt;
&lt;li&gt;数据流怎么走。&lt;/li&gt;
&lt;li&gt;新增一种组件 / 插件 / 页面需要改哪些文件。&lt;/li&gt;
&lt;li&gt;当前目录相关的测试命令。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;比如 &lt;code&gt;src/content/AGENTS.md&lt;/code&gt; 可以写：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-md&quot;&gt;# Content Guidelines

Blog posts are Markdown or MDX files under src/content/blog.
Use Chinese for posts by default.
Keep frontmatter aligned with src/content.config.ts.
Run bun run build after adding or changing public content.
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;6.3 工具专属文件：只写工具差异&lt;/h3&gt;
&lt;p&gt;如果项目同时使用 Cursor、Claude Code、Copilot，可以避免在每个文件里重复项目介绍：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-txt&quot;&gt;AGENTS.md                         # 通用规则
CLAUDE.md                         # Claude Code 补充
.cursor/rules/*.mdc               # Cursor 补充
.github/copilot-instructions.md   # Copilot 补充
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;工具专属文件只写差异，例如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Cursor 是否需要手动截图或录屏。&lt;/li&gt;
&lt;li&gt;Claude Code 是否需要优先使用某些命令。&lt;/li&gt;
&lt;li&gt;Copilot 在生成测试时需要遵守哪些命名模式。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;7. AGENTS.md 应该避免什么&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;AGENTS.md&lt;/code&gt; 不是越长越好。它应该帮助 agent 更快做对事情，而不是把所有文档复制一遍。&lt;/p&gt;
&lt;p&gt;不建议写：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;密钥、token、内部账号。&lt;/li&gt;
&lt;li&gt;和真实 CI 不一致的命令。&lt;/li&gt;
&lt;li&gt;很快会过期的临时说明。&lt;/li&gt;
&lt;li&gt;大段产品背景，但没有操作价值。&lt;/li&gt;
&lt;li&gt;“写高质量代码”这种没有约束力的空话。&lt;/li&gt;
&lt;li&gt;与 README、CONTRIBUTING 完全重复的内容。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;更好的写法是把规则写成可验证的动作：&lt;/p&gt;
&lt;p&gt;| 不推荐 | 推荐 |
| --- | --- |
| 注意测试 | 修改内容后运行 &lt;code&gt;bun run build&lt;/code&gt; |
| 遵守项目结构 | 新博客放在 &lt;code&gt;src/content/blog/&amp;#x3C;slug&gt;/index.md&lt;/code&gt; |
| 不要乱改代码 | 不要手动编辑 &lt;code&gt;dist/&lt;/code&gt;、&lt;code&gt;.astro/&lt;/code&gt;、&lt;code&gt;node_modules/&lt;/code&gt; |
| 保持风格一致 | frontmatter 字段遵循 &lt;code&gt;src/content.config.ts&lt;/code&gt; |&lt;/p&gt;
&lt;h2&gt;8. 给 johniexu.github.io 的落地建议&lt;/h2&gt;
&lt;p&gt;当前这个博客项目是 Astro 5 + astro-pure，内容集中在 &lt;code&gt;src/content/blog&lt;/code&gt; 和 &lt;code&gt;src/content/docs&lt;/code&gt;。相比 lynx-stack，它不是大型 monorepo，但仍然适合加两类 &lt;code&gt;AGENTS.md&lt;/code&gt;：&lt;/p&gt;
&lt;h3&gt;8.1 根目录 AGENTS.md&lt;/h3&gt;
&lt;p&gt;根目录说明应该告诉 agent：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;这是个人博客项目。&lt;/li&gt;
&lt;li&gt;使用 Astro 5、astro-pure、UnoCSS、TypeScript。&lt;/li&gt;
&lt;li&gt;常用命令是 &lt;code&gt;bun install&lt;/code&gt;、&lt;code&gt;bun dev&lt;/code&gt;、&lt;code&gt;bun run build&lt;/code&gt;、&lt;code&gt;bun run check&lt;/code&gt;、&lt;code&gt;bun run lint&lt;/code&gt;、&lt;code&gt;bun run format&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;新增公开内容后至少运行 &lt;code&gt;bun run build&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;部署从 &lt;code&gt;next&lt;/code&gt; 分支触发 GitHub Pages workflow。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;8.2 src/content/AGENTS.md&lt;/h3&gt;
&lt;p&gt;内容目录说明应该告诉 agent：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;博客文章使用 Markdown / MDX。&lt;/li&gt;
&lt;li&gt;推荐结构是 &lt;code&gt;src/content/blog/&amp;#x3C;slug&gt;/index.md&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;frontmatter 必须满足 &lt;code&gt;src/content.config.ts&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;中文技术文章默认设置 &lt;code&gt;language: &apos;zh&apos;&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;description&lt;/code&gt; 控制摘要展示，应该简洁。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;draft: false&lt;/code&gt; 才会公开发布。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这样一来，后续用 Cursor 或 Claude Code 新增文章时，agent 不需要每次都重新摸索 Astro collection schema。&lt;/p&gt;
&lt;h2&gt;9. 一份实用的 AGENTS.md 模板&lt;/h2&gt;
&lt;p&gt;可以从下面这个模板开始，然后按项目真实情况裁剪：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-md&quot;&gt;# Project Name

## Overview

Explain what this repository does, the main stack, and the runtime targets.

## Commands

- Install: `...`
- Dev: `...`
- Build: `...`
- Test: `...`
- Format: `...`

## Architecture

- `src/foo`: ...
- `src/bar`: ...

## Change Guidelines

- Add new feature code in ...
- Do not edit generated files in ...
- Keep public API changes in sync with ...

## Testing

- For content changes, run ...
- For UI changes, run ...
- For API changes, run ...

## Common Issues

- If X fails, try Y.
- If generated files are stale, run Z.
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;模板只是起点。真正有价值的 &lt;code&gt;AGENTS.md&lt;/code&gt;，一定来自项目真实问题：那些新同事会踩的坑、CI 经常失败的点、agent 容易误判的架构边界，都应该优先写进去。&lt;/p&gt;
&lt;h2&gt;10. 总结&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;AGENTS.md&lt;/code&gt; 的本质不是“给 AI 多写一份 README”，而是把项目中会影响 agent 决策的工程知识结构化。&lt;/p&gt;
&lt;p&gt;以 lynx-stack 为例，好的实践包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;根目录写清楚 monorepo 画像、命令顺序、CI 管线和常见故障。&lt;/li&gt;
&lt;li&gt;子目录写清楚局部架构、数据流和扩展 checklist。&lt;/li&gt;
&lt;li&gt;对 agent 友好的说明应该具体、可执行、可验证。&lt;/li&gt;
&lt;li&gt;Cursor、Claude Code、Copilot 等工具可以共享 &lt;code&gt;AGENTS.md&lt;/code&gt;，再通过各自专属文件补充差异。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;当项目规模变大、构建链路变复杂、agent 参与开发越来越多时，&lt;code&gt;AGENTS.md&lt;/code&gt; 会逐渐从“可有可无的说明文件”变成工程协作基础设施的一部分。&lt;/p&gt;</content:encoded><h:img src="/_astro/thumbnail.BSS8RXC9.png"/><enclosure url="/_astro/thumbnail.BSS8RXC9.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>Vue3 watchEffect 用法与实现原理</title><link>https://luopengcheng89.github.io/blog/vue3-watch-effect-principles</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/vue3-watch-effect-principles</guid><description>从日常用法、调度时机、清理机制到 Vue 3 源码，循序渐进拆解 watchEffect 的自动依赖收集与重新执行机制。</description><pubDate>Tue, 28 Apr 2026 08:34:00 GMT</pubDate><content:encoded>&lt;h1&gt;Vue3 watchEffect 用法与实现原理&lt;/h1&gt;
&lt;p&gt;在 Vue 3 的 Composition API 中，&lt;code&gt;watchEffect&lt;/code&gt; 是一个很容易上手、但内部机制非常值得细读的 API。它的使用体验像是“把副作用函数丢给 Vue，Vue 自动知道它依赖了哪些响应式数据”，但这句话背后其实串起了 Vue 3 响应式系统里几个核心部件：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ReactiveEffect&lt;/code&gt;：把一段函数包装成可追踪、可停止、可调度的响应式副作用。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;track&lt;/code&gt; / &lt;code&gt;trigger&lt;/code&gt;：在读取时收集依赖，在写入时触发依赖。&lt;/li&gt;
&lt;li&gt;scheduler：控制副作用重新执行的时机，避免同步多次变更导致重复执行。&lt;/li&gt;
&lt;li&gt;cleanup：在副作用失效前清理上一次副作用留下的异步请求、定时器或订阅。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本文基于 &lt;a href=&quot;https://github.com/vuejs/core&quot;&gt;vuejs/core&lt;/a&gt; 的当前 &lt;code&gt;main&lt;/code&gt; 分支，围绕 &lt;code&gt;packages/runtime-core/src/apiWatch.ts&lt;/code&gt;、&lt;code&gt;packages/reactivity/src/watch.ts&lt;/code&gt;、&lt;code&gt;packages/reactivity/src/effect.ts&lt;/code&gt;、&lt;code&gt;packages/reactivity/src/dep.ts&lt;/code&gt; 等源码，由浅入深分析 &lt;code&gt;watchEffect&lt;/code&gt; 的用法和实现原理，并横向对比 React、Solid、Svelte 等框架里的类似能力。&lt;/p&gt;
&lt;h2&gt;1. 先从使用场景看 watchEffect&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 的类型大致如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;function watchEffect(
  effect: (onCleanup: (cleanupFn: () =&gt; void) =&gt; void) =&gt; void,
  options?: {
    flush?: &apos;pre&apos; | &apos;post&apos; | &apos;sync&apos;
    onTrack?: (event: DebuggerEvent) =&gt; void
    onTrigger?: (event: DebuggerEvent) =&gt; void
  }
): WatchHandle
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;它会立即执行传入的函数，并在函数同步执行期间自动收集读到的响应式依赖；这些依赖后续发生变化时，函数会再次执行。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;import { ref, watchEffect } from &apos;vue&apos;

const count = ref(0)

const stop = watchEffect(() =&gt; {
  console.log(&apos;count changed:&apos;, count.value)
})

count.value++

stop()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这段代码里没有显式告诉 Vue “我要监听 &lt;code&gt;count&lt;/code&gt;”，但 &lt;code&gt;console.log&lt;/code&gt; 读取了 &lt;code&gt;count.value&lt;/code&gt;，所以 &lt;code&gt;count&lt;/code&gt; 会被自动记录为依赖。&lt;/p&gt;
&lt;h3&gt;1.1 适合用 watchEffect 的场景&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 更适合“副作用依赖和副作用逻辑天然写在一起”的场景：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;const todoId = ref(1)
const todo = ref(null)

watchEffect(async (onCleanup) =&gt; {
  const controller = new AbortController()

  onCleanup(() =&gt; {
    controller.abort()
  })

  const res = await fetch(`/api/todos/${todoId.value}`, {
    signal: controller.signal
  })
  todo.value = await res.json()
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;相比下面的 &lt;code&gt;watch&lt;/code&gt;，&lt;code&gt;watchEffect&lt;/code&gt; 少维护了一份 source：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;watch(
  todoId,
  async (id, _oldId, onCleanup) =&gt; {
    const controller = new AbortController()

    onCleanup(() =&gt; {
      controller.abort()
    })

    const res = await fetch(`/api/todos/${id}`, {
      signal: controller.signal
    })
    todo.value = await res.json()
  },
  { immediate: true }
)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;当副作用内部会读取多个响应式字段，或者读取的是深层对象中的少量字段时，&lt;code&gt;watchEffect&lt;/code&gt; 通常比 &lt;code&gt;deep: true&lt;/code&gt; 更经济：它只追踪实际读到的属性，而不是递归遍历整个对象。&lt;/p&gt;
&lt;h3&gt;1.2 不适合用 watchEffect 的场景&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 的代价是依赖不够显式，所以以下场景更适合 &lt;code&gt;watch&lt;/code&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;需要精确控制“哪个 source 变化才触发副作用”。&lt;/li&gt;
&lt;li&gt;需要拿到 &lt;code&gt;newValue&lt;/code&gt; 和 &lt;code&gt;oldValue&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;副作用里会读取一些“不应该触发重新执行”的响应式状态。&lt;/li&gt;
&lt;li&gt;依赖关系复杂到需要让读者一眼看出触发来源。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;可以简单记：&lt;/p&gt;
&lt;p&gt;| API           | 依赖来源         | 首次执行   | 是否能拿旧值 | 适合场景                           |
| ------------- | ---------------- | ---------- | ------------ | ---------------------------------- |
| &lt;code&gt;watchEffect&lt;/code&gt; | 自动收集同步读取 | 立即执行   | 否           | 自动请求、同步外部系统、调试依赖   |
| &lt;code&gt;watch&lt;/code&gt;       | 显式 source      | 默认懒执行 | 是           | 精准监听、比较新旧值、控制触发条件 |&lt;/p&gt;
&lt;h2&gt;2. 两个容易踩坑的语义&lt;/h2&gt;
&lt;h3&gt;2.1 只追踪同步阶段读到的依赖&lt;/h3&gt;
&lt;p&gt;Vue 官方文档特别强调：&lt;code&gt;watchEffect&lt;/code&gt; 使用异步回调时，只会追踪第一个 &lt;code&gt;await&lt;/code&gt; 之前同步读到的依赖。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;watchEffect(async () =&gt; {
  console.log(id.value) // 会被追踪

  await fetch(&apos;/api/profile&apos;)

  console.log(token.value) // 不会被这次 watchEffect 自动追踪
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;原因后面看源码会很清楚：Vue 只能在 &lt;code&gt;ReactiveEffect.run()&lt;/code&gt; 执行期间设置“当前正在收集依赖的 effect”。一旦同步调用栈结束，当前 effect 会被恢复，&lt;code&gt;await&lt;/code&gt; 后面的代码已经不在这次依赖收集上下文里。&lt;/p&gt;
&lt;h3&gt;2.2 cleanup 要在副作用失效前执行&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 的 &lt;code&gt;onCleanup&lt;/code&gt; 用来处理“上一次副作用已经过期”的情况：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;watchEffect((onCleanup) =&gt; {
  const timer = window.setInterval(() =&gt; {
    console.log(count.value)
  }, 1000)

  onCleanup(() =&gt; {
    window.clearInterval(timer)
  })
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;当依赖变化导致 effect 重新执行，或者手动 &lt;code&gt;stop()&lt;/code&gt; 时，Vue 会先执行上一次注册的 cleanup，再运行下一轮 effect。Vue 3.5+ 还提供了 &lt;code&gt;onWatcherCleanup&lt;/code&gt;，可以在 &lt;code&gt;watch&lt;/code&gt; / &lt;code&gt;watchEffect&lt;/code&gt; 同步执行阶段直接注册清理函数。&lt;/p&gt;
&lt;h2&gt;3. 从入口看：watchEffect 只是 doWatch 的特殊分支&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 的运行时入口在 &lt;code&gt;packages/runtime-core/src/apiWatch.ts&lt;/code&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;export function watchEffect(effect: WatchEffect, options?: WatchEffectOptions): WatchHandle {
  return doWatch(effect, null, options)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这里最关键的是第二个参数传了 &lt;code&gt;null&lt;/code&gt;。同一个 &lt;code&gt;doWatch&lt;/code&gt; 同时承载了 &lt;code&gt;watch&lt;/code&gt; 和 &lt;code&gt;watchEffect&lt;/code&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;watch(source, cb, options)&lt;/code&gt;：有 &lt;code&gt;cb&lt;/code&gt;，source 和副作用分离。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;watchEffect(effect, options)&lt;/code&gt;：没有 &lt;code&gt;cb&lt;/code&gt;，传入的函数本身就是副作用。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;doWatch&lt;/code&gt; 会处理运行时层面的能力，比如组件实例、错误处理、SSR、调度时机：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;const runsImmediately = (cb &amp;#x26;&amp;#x26; immediate) || (!cb &amp;#x26;&amp;#x26; flush !== &apos;post&apos;)

if (flush === &apos;post&apos;) {
  baseWatchOptions.scheduler = (job) =&gt; {
    queuePostRenderEffect(job, instance &amp;#x26;&amp;#x26; instance.suspense)
  }
} else if (flush !== &apos;sync&apos;) {
  baseWatchOptions.scheduler = (job, isFirstRun) =&gt; {
    if (isFirstRun) {
      job()
    } else {
      queueJob(job)
    }
  }
}

const watchHandle = baseWatch(source, cb, baseWatchOptions)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;默认 &lt;code&gt;flush&lt;/code&gt; 是 &lt;code&gt;&apos;pre&apos;&lt;/code&gt;，所以 &lt;code&gt;watchEffect&lt;/code&gt; 会立即执行第一次；依赖变化后的重新执行会进入调度队列。&lt;code&gt;flush: &apos;post&apos;&lt;/code&gt; 会放到组件渲染之后，&lt;code&gt;flush: &apos;sync&apos;&lt;/code&gt; 则不走异步队列，依赖一变就同步执行。&lt;/p&gt;
&lt;h2&gt;4. reactivity 层：把函数变成 ReactiveEffect&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;doWatch&lt;/code&gt; 最终调用的是 &lt;code&gt;@vue/reactivity&lt;/code&gt; 包里的 &lt;code&gt;watch&lt;/code&gt;。当它发现 &lt;code&gt;source&lt;/code&gt; 是函数且没有 &lt;code&gt;cb&lt;/code&gt; 时，会进入 &lt;code&gt;watchEffect&lt;/code&gt; 分支：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;if (isFunction(source)) {
  if (cb) {
    getter = call ? () =&gt; call(source, WatchErrorCodes.WATCH_GETTER) : source
  } else {
    getter = () =&gt; {
      if (cleanup) {
        pauseTracking()
        try {
          cleanup()
        } finally {
          resetTracking()
        }
      }

      const currentEffect = activeWatcher
      activeWatcher = effect
      try {
        return call
          ? call(source, WatchErrorCodes.WATCH_CALLBACK, [boundCleanup])
          : source(boundCleanup)
      } finally {
        activeWatcher = currentEffect
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这一段做了三件事：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;每次重新运行 effect 前，先执行上一次注册的 &lt;code&gt;cleanup&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;临时设置 &lt;code&gt;activeWatcher = effect&lt;/code&gt;，让 &lt;code&gt;onWatcherCleanup&lt;/code&gt; 知道当前属于哪个 watcher。&lt;/li&gt;
&lt;li&gt;执行用户传入的 &lt;code&gt;source(boundCleanup)&lt;/code&gt;，也就是我们的 &lt;code&gt;watchEffect&lt;/code&gt; 回调。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;接着 Vue 创建真正的响应式副作用对象：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;effect = new ReactiveEffect(getter)

effect.scheduler = scheduler ? () =&gt; scheduler(job, false) : job

boundCleanup = (fn) =&gt; onWatcherCleanup(fn, false, effect)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;可以把 &lt;code&gt;watchEffect&lt;/code&gt; 理解成：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;用 &lt;code&gt;ReactiveEffect&lt;/code&gt; 包装用户函数；执行时自动收集依赖；依赖触发时，不直接运行用户函数，而是交给 scheduler 决定何时运行。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;5. 自动依赖收集：为什么读 count.value 就会被记录&lt;/h2&gt;
&lt;p&gt;以 &lt;code&gt;ref&lt;/code&gt; 为例，&lt;code&gt;count.value&lt;/code&gt; 的 getter 会调用 &lt;code&gt;dep.track()&lt;/code&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;get value() {
  this.dep.track()
  return this._value
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;reactive&lt;/code&gt; 对象也类似，Proxy 的 &lt;code&gt;get&lt;/code&gt; 拦截里会调用 &lt;code&gt;track(target, TrackOpTypes.GET, key)&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;真正的依赖关系存储在 &lt;code&gt;packages/reactivity/src/dep.ts&lt;/code&gt;。Vue 3 当前实现使用 &lt;code&gt;WeakMap -&gt; Map -&gt; Dep&lt;/code&gt; 找到某个对象某个 key 对应的依赖集合：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;export const targetMap: WeakMap&amp;#x3C;object, KeyToDepMap&gt; = new WeakMap()

export function track(target: object, type: TrackOpTypes, key: unknown): void {
  if (shouldTrack &amp;#x26;&amp;#x26; activeSub) {
    let depsMap = targetMap.get(target)
    if (!depsMap) {
      targetMap.set(target, (depsMap = new Map()))
    }

    let dep = depsMap.get(key)
    if (!dep) {
      depsMap.set(key, (dep = new Dep()))
    }

    dep.track()
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这里的 &lt;code&gt;activeSub&lt;/code&gt; 就是当前正在执行的 &lt;code&gt;ReactiveEffect&lt;/code&gt;。而 &lt;code&gt;activeSub&lt;/code&gt; 是在 &lt;code&gt;ReactiveEffect.run()&lt;/code&gt; 中设置的：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;run(): T {
  this.flags |= EffectFlags.RUNNING
  cleanupEffect(this)
  prepareDeps(this)

  const prevEffect = activeSub
  const prevShouldTrack = shouldTrack
  activeSub = this
  shouldTrack = true

  try {
    return this.fn()
  } finally {
    cleanupDeps(this)
    activeSub = prevEffect
    shouldTrack = prevShouldTrack
    this.flags &amp;#x26;= ~EffectFlags.RUNNING
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;因此 &lt;code&gt;watchEffect(() =&gt; count.value)&lt;/code&gt; 的完整链路是：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;watchEffect&lt;/code&gt; 创建 &lt;code&gt;ReactiveEffect(getter)&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;初次运行 &lt;code&gt;effect.run()&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ReactiveEffect.run()&lt;/code&gt; 把 &lt;code&gt;activeSub&lt;/code&gt; 设置为当前 effect。&lt;/li&gt;
&lt;li&gt;用户函数读取 &lt;code&gt;count.value&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ref.value&lt;/code&gt; getter 调用 &lt;code&gt;dep.track()&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;dep.track()&lt;/code&gt; 发现当前有 &lt;code&gt;activeSub&lt;/code&gt;，于是把 &lt;code&gt;count.value&lt;/code&gt; 对应的 &lt;code&gt;Dep&lt;/code&gt; 和当前 effect 建立订阅关系。&lt;/li&gt;
&lt;li&gt;执行结束后恢复上一个 &lt;code&gt;activeSub&lt;/code&gt;，清理本轮没再用到的旧依赖。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这就是“自动依赖收集”的本质：不是静态分析代码，也不是编译器猜测变量，而是在运行时通过 getter/Proxy 捕获真实读取行为。&lt;/p&gt;
&lt;h2&gt;6. 触发更新：写入 count.value 后发生了什么&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;ref.value&lt;/code&gt; 的 setter 会在值变化后调用 &lt;code&gt;dep.trigger()&lt;/code&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;set value(newValue) {
  if (hasChanged(newValue, oldValue)) {
    this._rawValue = newValue
    this._value = toReactive(newValue)
    this.dep.trigger()
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;reactive&lt;/code&gt; 对象的 &lt;code&gt;set&lt;/code&gt;、&lt;code&gt;deleteProperty&lt;/code&gt;、集合操作也会走到 &lt;code&gt;trigger(...)&lt;/code&gt;。触发时，Vue 会找到这个 key 关联的 &lt;code&gt;Dep&lt;/code&gt;，再通知其中订阅的 effect：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;trigger(debugInfo?: DebuggerEventExtraInfo): void {
  this.version++
  globalVersion++
  this.notify(debugInfo)
}

notify(debugInfo?: DebuggerEventExtraInfo): void {
  startBatch()
  try {
    for (let link = this.subs; link; link = link.prevSub) {
      link.sub.notify()
    }
  } finally {
    endBatch()
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;ReactiveEffect.notify()&lt;/code&gt; 不会立刻执行用户函数，而是先进入批处理：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;notify(): void {
  if (this.flags &amp;#x26; EffectFlags.RUNNING &amp;#x26;&amp;#x26; !(this.flags &amp;#x26; EffectFlags.ALLOW_RECURSE)) {
    return
  }

  if (!(this.flags &amp;#x26; EffectFlags.NOTIFIED)) {
    batch(this)
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;批处理结束后，&lt;code&gt;ReactiveEffect.trigger()&lt;/code&gt; 再根据是否有 scheduler 决定执行方式：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;trigger(): void {
  if (this.flags &amp;#x26; EffectFlags.PAUSED) {
    pausedQueueEffects.add(this)
  } else if (this.scheduler) {
    this.scheduler()
  } else {
    this.runIfDirty()
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这解释了两个现象：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;同一个 tick 内多次同步修改同一个依赖，默认不会导致 &lt;code&gt;watchEffect&lt;/code&gt; 重复无意义地执行很多次。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;flush: &apos;sync&apos;&lt;/code&gt; 绕过调度队列，适合极少数需要同步失效的缓存场景，但不适合监听会被高频同步修改的数据结构。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;7. flush 时机：pre、post、sync 的真正区别&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 的 &lt;code&gt;flush&lt;/code&gt; 不是“是否执行”，而是“依赖变化后排到哪里执行”。&lt;/p&gt;
&lt;h3&gt;7.1 默认 pre：组件更新前&lt;/h3&gt;
&lt;p&gt;默认模式会在第一次运行时直接执行：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;baseWatchOptions.scheduler = (job, isFirstRun) =&gt; {
  if (isFirstRun) {
    job()
  } else {
    queueJob(job)
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;随后重新执行会进入 &lt;code&gt;queueJob&lt;/code&gt;。&lt;code&gt;doWatch&lt;/code&gt; 还会给 job 标记 &lt;code&gt;PRE&lt;/code&gt;，并把组件实例 &lt;code&gt;uid&lt;/code&gt; 设置为 job id：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;if (isPre) {
  job.flags! |= SchedulerJobFlags.PRE
  if (instance) {
    job.id = instance.uid
    job.i = instance
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;调度器会按 id 排序，保证父子组件和 pre watcher 的执行顺序稳定。默认 watcher 会在所属组件 DOM 更新前运行，所以不适合在里面读取更新后的 DOM。&lt;/p&gt;
&lt;h3&gt;7.2 post：组件更新后&lt;/h3&gt;
&lt;p&gt;如果需要访问更新后的 DOM，应使用：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;watchEffect(
  () =&gt; {
    console.log(el.value?.offsetHeight)
  },
  { flush: &apos;post&apos; }
)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;或者语义更明确的别名：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;watchPostEffect(() =&gt; {
  console.log(el.value?.offsetHeight)
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码里 &lt;code&gt;flush: &apos;post&apos;&lt;/code&gt; 会使用 &lt;code&gt;queuePostRenderEffect&lt;/code&gt;，它会在渲染完成后执行。&lt;/p&gt;
&lt;h3&gt;7.3 sync：同步触发&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;watchSyncEffect&lt;/code&gt; 等价于 &lt;code&gt;watchEffect(..., { flush: &apos;sync&apos; })&lt;/code&gt;。它不进入 &lt;code&gt;queueJob&lt;/code&gt;，依赖一变就同步触发：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;watchSyncEffect(() =&gt; {
  cache.invalidate(key.value)
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个模式要谨慎使用。如果同步 push 一千次数组，普通 watcher 可以被批处理合并，sync watcher 则会更容易出现性能问题和中间状态问题。&lt;/p&gt;
&lt;h2&gt;8. cleanup、pause、resume、stop 如何工作&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 返回的是一个可调用的 handle：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;const handle = watchEffect(() =&gt; {})

handle.pause()
handle.resume()
handle.stop()
handle() // 等价于 stop()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;在 &lt;code&gt;@vue/reactivity&lt;/code&gt; 的 &lt;code&gt;watch&lt;/code&gt; 实现中：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;const watchHandle: WatchHandle = () =&gt; {
  effect.stop()
  if (scope &amp;#x26;&amp;#x26; scope.active) {
    remove(scope.effects, effect)
  }
}

watchHandle.pause = effect.pause.bind(effect)
watchHandle.resume = effect.resume.bind(effect)
watchHandle.stop = watchHandle
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;stop()&lt;/code&gt; 会断开当前 effect 和所有 dep 的订阅关系，并执行 &lt;code&gt;onStop&lt;/code&gt;。&lt;code&gt;watchEffect&lt;/code&gt; 注册的 cleanup 最终挂在 &lt;code&gt;effect.onStop&lt;/code&gt; 上：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;cleanup = effect.onStop = () =&gt; {
  const cleanups = cleanupMap.get(effect)
  if (cleanups) {
    for (const cleanup of cleanups) cleanup()
    cleanupMap.delete(effect)
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;pause()&lt;/code&gt; / &lt;code&gt;resume()&lt;/code&gt; 则通过 &lt;code&gt;EffectFlags.PAUSED&lt;/code&gt; 控制触发行为：暂停期间依赖变化不会立即执行；恢复时如果暂停期间发生过触发，会补跑一次。&lt;/p&gt;
&lt;h2&gt;9. 和 computed 的区别&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 和 &lt;code&gt;computed&lt;/code&gt; 都基于 &lt;code&gt;ReactiveEffect&lt;/code&gt;，也都会自动收集依赖，但目的完全不同：&lt;/p&gt;
&lt;p&gt;| 能力             | &lt;code&gt;computed&lt;/code&gt;                   | &lt;code&gt;watchEffect&lt;/code&gt;         |
| ---------------- | ---------------------------- | --------------------- |
| 目标             | 产生可缓存的派生值           | 执行副作用            |
| 是否应该有副作用 | 不应该                       | 本来就是副作用        |
| 返回值           | 一个 ref-like 结果           | 停止/暂停/恢复 handle |
| 执行策略         | 懒计算，读取 &lt;code&gt;.value&lt;/code&gt; 时刷新 | 创建后立即执行        |
| 依赖变化后       | 标记 dirty，必要时重新计算   | 重新执行用户副作用    |&lt;/p&gt;
&lt;p&gt;如果一段逻辑是“根据 A、B 算出 C”，优先用 &lt;code&gt;computed&lt;/code&gt;；如果是“当 A、B 变化时请求接口、写日志、同步 DOM、订阅外部资源”，才考虑 &lt;code&gt;watchEffect&lt;/code&gt;。&lt;/p&gt;
&lt;h2&gt;10. 横向对比其他框架&lt;/h2&gt;
&lt;h3&gt;10.1 React useEffect：依赖数组是显式契约&lt;/h3&gt;
&lt;p&gt;React 的 &lt;code&gt;useEffect&lt;/code&gt; 依赖数组是显式声明：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-tsx&quot;&gt;useEffect(() =&gt; {
  document.title = `${count}`
}, [count])
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;React 不会在运行时追踪 &lt;code&gt;effect&lt;/code&gt; 里读了哪些状态。依赖数组写少了会产生闭包陈旧问题，写多了可能导致不必要执行，所以 React 社区需要 &lt;code&gt;eslint-plugin-react-hooks&lt;/code&gt; 帮助检查依赖。&lt;/p&gt;
&lt;p&gt;Vue 的 &lt;code&gt;watchEffect&lt;/code&gt; 则更像运行时依赖追踪：读取了什么，就订阅什么。好处是少写依赖数组，坏处是依赖隐藏在函数体内部，复杂副作用的触发来源不如 &lt;code&gt;watch&lt;/code&gt; 或 React 依赖数组直观。&lt;/p&gt;
&lt;h3&gt;10.2 Solid createEffect：同样自动追踪，但粒度更细&lt;/h3&gt;
&lt;p&gt;Solid 的 &lt;code&gt;createEffect&lt;/code&gt; 和 Vue 的 &lt;code&gt;watchEffect&lt;/code&gt; 在心智模型上更接近：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;createEffect(() =&gt; {
  console.log(count())
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;它也通过运行时读取 signal 来自动建立依赖。不过 Solid 的渲染模型本身就是细粒度响应式，组件函数不是像 React 那样反复整体执行；Vue 则把细粒度响应式和组件级渲染 effect 结合起来，通过 scheduler 协调 watcher、组件更新和 post flush callback。&lt;/p&gt;
&lt;h3&gt;10.3 Svelte $effect：编译器参与更多&lt;/h3&gt;
&lt;p&gt;Svelte 5 的 &lt;code&gt;$effect&lt;/code&gt; 也可以写出类似自动依赖的副作用：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;$effect(() =&gt; {
  console.log(count)
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Svelte 的特色是编译器参与度更高，会把组件代码编译成更直接的更新逻辑。Vue 3 则主要依靠运行时 Proxy/ref 追踪，在无需编译器特殊语法的情况下，让普通 JavaScript 访问也能参与响应式。&lt;/p&gt;
&lt;h2&gt;11. 用一段伪代码总结 watchEffect&lt;/h2&gt;
&lt;p&gt;把细节压缩后，&lt;code&gt;watchEffect&lt;/code&gt; 大致可以看成：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;function watchEffect(userEffect, options) {
  let cleanup

  const getter = () =&gt; {
    if (cleanup) cleanup()

    userEffect((fn) =&gt; {
      cleanup = fn
    })
  }

  const effect = new ReactiveEffect(getter)

  effect.scheduler = createScheduler(options.flush)

  effect.run()

  return () =&gt; effect.stop()
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;ReactiveEffect.run()&lt;/code&gt; 内部再做：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;function run() {
  activeSub = currentEffect
  try {
    getter()
  } finally {
    activeSub = previousEffect
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;响应式读取时：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;function track(target, key) {
  if (activeSub) {
    deps[target][key].add(activeSub)
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;响应式写入时：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;function trigger(target, key) {
  for (const effect of deps[target][key]) {
    effect.scheduler ? effect.scheduler() : effect.run()
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;真实 Vue 源码比这复杂得多：它要处理嵌套 effect、依赖清理、批处理、组件更新顺序、递归保护、调试钩子、SSR、effect scope、暂停恢复等。但核心闭环始终是这四步：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;运行 effect -&gt; 读取时 track -&gt; 写入时 trigger -&gt; scheduler 决定何时重新运行 effect。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;12. 实战建议&lt;/h2&gt;
&lt;p&gt;最后给出一些使用建议：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;优先保持副作用小而清晰&lt;/strong&gt;：&lt;code&gt;watchEffect&lt;/code&gt; 里读取的响应式数据越多，触发来源越隐蔽。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;异步请求一定考虑 cleanup&lt;/strong&gt;：避免旧请求晚返回后覆盖新状态。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;需要旧值就用 watch&lt;/strong&gt;：不要在 &lt;code&gt;watchEffect&lt;/code&gt; 里手写额外变量模拟 &lt;code&gt;oldValue&lt;/code&gt;，可读性通常更差。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;读 DOM 用 post&lt;/strong&gt;：需要访问组件更新后的 DOM 时使用 &lt;code&gt;flush: &apos;post&apos;&lt;/code&gt; 或 &lt;code&gt;watchPostEffect&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;谨慎使用 sync&lt;/strong&gt;：只有缓存失效这类非常明确的同步场景才考虑 &lt;code&gt;watchSyncEffect&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;复杂场景用 onTrack/onTrigger 调试&lt;/strong&gt;：它们可以告诉你 effect 追踪了什么、被什么触发。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;结语&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;watchEffect&lt;/code&gt; 的 API 很简单，但它正好站在 Vue 3 响应式系统和运行时调度系统的交界处。理解它，可以顺着一条很清晰的线索看懂 Vue 3 的响应式核心：&lt;code&gt;ReactiveEffect&lt;/code&gt; 负责描述“谁依赖谁”，&lt;code&gt;track&lt;/code&gt; / &lt;code&gt;trigger&lt;/code&gt; 负责建立和触发依赖关系，scheduler 负责把重新执行安排在正确的时机，cleanup 负责让副作用具备失效语义。&lt;/p&gt;
&lt;p&gt;当你只是想“依赖什么就自动重新做什么”时，&lt;code&gt;watchEffect&lt;/code&gt; 很顺手；当你需要更强的边界、更明确的触发条件和新旧值时，&lt;code&gt;watch&lt;/code&gt; 仍然是更合适的工具。理解二者背后的实现差异，才能在业务代码里做出更稳定的选择。&lt;/p&gt;</content:encoded><h:img src="undefined"/><enclosure url="undefined"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>Lynx 移动端 HTTP 请求方案对比：TanStack Query vs Axios</title><link>https://luopengcheng89.github.io/blog/lynx-http-client-comparison</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/lynx-http-client-comparison</guid><description>深入对比 TanStack Query 和 Axios 在 Lynx（ReactLynx）项目中的使用方案，分析各自优缺点及适用场景</description><pubDate>Mon, 27 Apr 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;Lynx 移动端 HTTP 请求方案对比：TanStack Query vs Axios&lt;/h1&gt;
&lt;h2&gt;前言&lt;/h2&gt;
&lt;p&gt;在 Lynx（字节跳动的跨端框架）项目中进行网络请求时，开发者通常面临两个主要选择：原生 Fetch API 配合 TanStack Query，或者使用 Axios 进行封装。本文将深入对比这两种方案，帮助你在项目中做出合适的技术选型。&lt;/p&gt;
&lt;h2&gt;方案一：TanStack Query + Fetch&lt;/h2&gt;
&lt;h3&gt;为什么推荐 TanStack Query？&lt;/h3&gt;
&lt;p&gt;TanStack Query 是 React 生态系统中最流行的服务端状态管理库，它不仅仅是一个 HTTP 客户端，更是数据获取和缓存管理的完整解决方案。官方文档明确推荐在 ReactLynx 中使用 TanStack Query。&lt;/p&gt;
&lt;h3&gt;核心特性&lt;/h3&gt;
&lt;p&gt;| 特性 | 说明 |
|------|------|
| 自动缓存 | 开箱即用的请求缓存和自动重新获取 |
| 乐观更新 | 支持 mutations 的乐观更新模式 |
| 后台刷新 | 窗口聚焦时自动刷新 stale 数据 |
| 依赖查询 | 支持查询之间的依赖关系 |
| 分页 &amp;#x26; 无限滚动 | 内置支持分页和无限滚动场景 |&lt;/p&gt;
&lt;h3&gt;基础用法&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { QueryClient, QueryClientProvider, useQuery } from &quot;@tanstack/react-query&quot;;

const queryClient = new QueryClient();

// 封装请求函数
interface Post {
  userId: number;
  id: number;
  title: string;
  body: string;
}

const fetchPosts = async (): Promise&amp;#x3C;Post[]&gt; =&gt; {
  const response = await fetch(&quot;https://jsonplaceholder.typicode.com/posts&quot;);
  if (!response.ok) {
    throw new Error(&quot;Failed to fetch posts&quot;);
  }
  return response.json();
};

// 在组件中使用
function PostList() {
  const { data, isLoading, isError, error, refetch } = useQuery({
    queryKey: [&quot;posts&quot;],
    queryFn: fetchPosts,
    staleTime: 5 * 60 * 1000, // 5分钟内不重新获取
  });

  if (isLoading) return &amp;#x3C;text&gt;Loading...&amp;#x3C;/text&gt;;
  if (isError) return &amp;#x3C;text&gt;Error: {error.message}&amp;#x3C;/text&gt;;

  return (
    &amp;#x3C;scroll-view scroll-y&gt;
      {data?.map((post) =&gt; (
        &amp;#x3C;view key={post.id}&gt;
          &amp;#x3C;text&gt;{post.title}&amp;#x3C;/text&gt;
        &amp;#x3C;/view&gt;
      ))}
    &amp;#x3C;/scroll-view&gt;
  );
}

// 完整应用包装
root.render(
  &amp;#x3C;QueryClientProvider client={queryClient}&gt;
    &amp;#x3C;PostList /&gt;
  &amp;#x3C;/QueryClientProvider&gt;
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;高级用法：Mutation 与乐观更新&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const useDeletePost = () =&gt; {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: async (postId: number) =&gt; {
      const response = await fetch(
        `https://jsonplaceholder.typicode.com/posts/${postId}`,
        { method: &quot;DELETE&quot; }
      );
      if (!response.ok) throw new Error(&quot;Failed to delete&quot;);
      return postId;
    },

    // 乐观更新：在请求完成前就更新 UI
    onMutate: async (postId) =&gt; {
      // 取消任何现有的刷新操作
      await queryClient.cancelQueries({ queryKey: [&quot;posts&quot;] });

      // 快照当前数据用于回滚
      const previousPosts = queryClient.getQueryData&amp;#x3C;Post[]&gt;([&quot;posts&quot;]);

      // 立即更新缓存
      queryClient.setQueryData&amp;#x3C;Post[]&gt;([&quot;posts&quot;], (old) =&gt;
        old ? old.filter((p) =&gt; p.id !== postId) : []
      );

      return { previousPosts };
    },

    // 失败时回滚
    onError: (err, postId, context) =&gt; {
      if (context?.previousPosts) {
        queryClient.setQueryData([&quot;posts&quot;], context.previousPosts);
      }
    },

    // 请求完成后刷新数据
    onSettled: () =&gt; {
      queryClient.invalidateQueries({ queryKey: [&quot;posts&quot;] });
    },
  });
};

// 组件中使用
function DeleteButton({ postId }) {
  const deletePost = useDeletePost();

  return (
    &amp;#x3C;view bindtap={() =&gt; deletePost.mutate(postId)}&gt;
      &amp;#x3C;text&gt;Delete&amp;#x3C;/text&gt;
    &amp;#x3C;/view&gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;分页查询&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const usePostsPage = (page: number) =&gt; {
  return useQuery({
    queryKey: [&quot;posts&quot;, page],
    queryFn: async () =&gt; {
      const response = await fetch(
        `https://jsonplaceholder.typicode.com/posts?_page=${page}&amp;#x26;_limit=10`
      );
      return response.json();
    },
    keepPreviousData: true, // 保持前一页数据直到新数据加载完成
  });
};

// 使用
function PostPage({ pageNum }) {
  const { data, isFetching } = usePostsPage(pageNum);
  return (
    // ...
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;不足之处&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;包体积&lt;/strong&gt;：作为完整的解决方案，库体积相对较大&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;概念门槛&lt;/strong&gt;：需要理解缓存、失效、重置等核心概念&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;移动端适配&lt;/strong&gt;：某些 web 生态的库可能需要适配 Lynx 环境&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2&gt;方案二：Axios 封装&lt;/h2&gt;
&lt;h3&gt;为什么考虑 Axios？&lt;/h3&gt;
&lt;p&gt;Axios 是历史悠久的 HTTP 客户端库，API 设计直观，提供请求拦截器、取消请求、自动转换等功能。如果你已经有现成的 Axios 使用经验，或者需要更精细的 HTTP 控制，Axios 是一个合理的选择。&lt;/p&gt;
&lt;h3&gt;核心特性&lt;/h3&gt;
&lt;p&gt;| 特性 | 说明 |
|------|------|
| 请求/响应拦截器 | 统一的请求前后处理 |
| 自动 JSON 转换 | 自动序列化请求体和解析响应 |
| 请求取消 | 使用 CancelToken 取消请求 |
| 错误处理 | 统一的错误处理结构 |
| 浏览器兼容 | 良好的跨环境兼容性 |&lt;/p&gt;
&lt;h3&gt;基础封装&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import axios, { AxiosInstance, AxiosRequestConfig, AxiosError } from &quot;axios&quot;;

// 创建实例
const createApiClient = (baseURL: string): AxiosInstance =&gt; {
  const client = axios.create({
    baseURL,
    timeout: 10000,
    headers: {
      &quot;Content-Type&quot;: &quot;application/json&quot;,
    },
  });

  // 请求拦截器
  client.interceptors.request.use(
    (config) =&gt; {
      // 添加 token
      const token = getAuthToken();
      if (token) {
        config.headers.Authorization = `Bearer ${token}`;
      }
      // 请求日志
      console.log(`[Request] ${config.method?.toUpperCase()} ${config.url}`);
      return config;
    },
    (error) =&gt; Promise.reject(error)
  );

  // 响应拦截器
  client.interceptors.response.use(
    (response) =&gt; {
      console.log(`[Response] ${response.status} ${response.config.url}`);
      return response.data;
    },
    (error: AxiosError) =&gt; {
      // 统一错误处理
      if (error.response) {
        switch (error.response.status) {
          case 401:
            handleUnauthorized();
            break;
          case 403:
            handleForbidden();
            break;
          case 404:
            handleNotFound();
            break;
          case 500:
            handleServerError();
            break;
        }
      } else if (error.request) {
        // 请求已发出但没有收到响应
        console.error(&quot;Network Error:&quot;, error.message);
      }
      return Promise.reject(error);
    }
  );

  return client;
};

// 使用工厂函数
const api = createApiClient(&quot;https://api.example.com&quot;);

// 封装通用请求方法
export const http = {
  get: &amp;#x3C;T&gt;(url: string, config?: AxiosRequestConfig) =&gt;
    api.get&amp;#x3C;T&gt;(url, config),

  post: &amp;#x3C;T&gt;(url: string, data?: unknown, config?: AxiosRequestConfig) =&gt;
    api.post&amp;#x3C;T&gt;(url, data, config),

  put: &amp;#x3C;T&gt;(url: string, data?: unknown, config?: AxiosRequestConfig) =&gt;
    api.put&amp;#x3C;T&gt;(url, data, config),

  delete: &amp;#x3C;T&gt;(url: string, config?: AxiosRequestConfig) =&gt;
    api.delete&amp;#x3C;T&gt;(url, config),

  patch: &amp;#x3C;T&gt;(url: string, data?: unknown, config?: AxiosRequestConfig) =&gt;
    api.patch&amp;#x3C;T&gt;(url, data, config),
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;请求函数封装&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// types.ts
interface ApiResponse&amp;#x3C;T&gt; {
  data: T;
  code: number;
  message: string;
}

interface User {
  id: number;
  name: string;
  email: string;
}

// userApi.ts
export const userApi = {
  list: () =&gt; http.get&amp;#x3C;ApiResponse&amp;#x3C;User[]&gt;&gt;(&quot;/users&quot;),

  getById: (id: number) =&gt; http.get&amp;#x3C;ApiResponse&amp;#x3C;User&gt;&gt;(`/users/${id}`),

  create: (user: Omit&amp;#x3C;User, &quot;id&quot;&gt;) =&gt;
    http.post&amp;#x3C;ApiResponse&amp;#x3C;User&gt;&gt;(&quot;/users&quot;, user),

  update: (id: number, user: Partial&amp;#x3C;User&gt;) =&gt;
    http.put&amp;#x3C;ApiResponse&amp;#x3C;User&gt;&gt;(`/users/${id}`, user),

  delete: (id: number) =&gt; http.delete&amp;#x3C;ApiResponse&amp;#x3C;void&gt;&gt;(`/users/${id}`),
};

// 组件中使用
function UserList() {
  const [users, setUsers] = useState&amp;#x3C;User[]&gt;([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState&amp;#x3C;string | null&gt;(null);

  useEffect(() =&gt; {
    userApi
      .list()
      .then((res) =&gt; {
        if (res.code === 0) {
          setUsers(res.data);
        } else {
          setError(res.message);
        }
      })
      .catch((err) =&gt; setError(err.message))
      .finally(() =&gt; setLoading(false));
  }, []);

  // ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;请求取消&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 创建取消令牌
const controller = new AbortController();

// 发送可取消的请求
const fetchUser = async () =&gt; {
  try {
    const response = await axios.get(&quot;/api/user/1&quot;, {
      signal: controller.signal,
    });
    return response.data;
  } catch (error) {
    if (axios.isCancel(error)) {
      console.log(&quot;Request was cancelled&quot;);
    }
    throw error;
  }
};

// 取消请求
const cancelRequest = () =&gt; {
  controller.abort();
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;与 TanStack Query 结合&lt;/h3&gt;
&lt;p&gt;如果你既想用 Axios 的便利，又想享受 TanStack Query 的缓存管理，可以结合使用：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { useQuery } from &quot;@tanstack/react-query&quot;;
import axios from &quot;axios&quot;;

// 封装 Axios 为 queryFn
const fetchWithAxios = async &amp;#x3C;T&gt;(url: string): Promise&amp;#x3C;T&gt; =&gt; {
  const response = await axios.get&amp;#x3C;T&gt;(url);
  return response.data;
};

// 组件中使用
function PostList() {
  const { data, isLoading } = useQuery({
    queryKey: [&quot;posts&quot;],
    queryFn: () =&gt; fetchWithAxios&amp;#x3C;Post[]&gt;(&quot;https://jsonplaceholder.typicode.com/posts&quot;),
  });

  // ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;hr&gt;
&lt;h2&gt;对比总结&lt;/h2&gt;
&lt;h3&gt;功能对比&lt;/h3&gt;
&lt;p&gt;| 功能 | TanStack Query | Axios |
|------|---------------|-------|
| HTTP 请求 | ❌ 需配合 fetch/axios | ✅ 原生支持 |
| 缓存管理 | ✅ 自动缓存 | ❌ 需自行实现 |
| 乐观更新 | ✅ 内置支持 | ❌ 需自行实现 |
| 后台刷新 | ✅ 支持 | ❌ 需自行实现 |
| 请求拦截 | ❌ 需配合 fetch | ✅ 原生支持 |
| 取消请求 | ✅ 内置 | ✅ 原生 |
| 包体积 | ~14KB (gzipped) | ~14KB (gzipped) |
| 学习曲线 | 较陡 | 平缓 |&lt;/p&gt;
&lt;h3&gt;使用场景建议&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;推荐使用 TanStack Query + Fetch：&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;复杂的数据获取场景（分页、无限滚动）&lt;/li&gt;
&lt;li&gt;需要乐观更新提升用户体验&lt;/li&gt;
&lt;li&gt;多处重复获取相同数据的场景&lt;/li&gt;
&lt;li&gt;需要缓存和自动刷新功能&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;推荐使用 Axios：&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;已经大量使用 Axios 的存量项目迁移&lt;/li&gt;
&lt;li&gt;需要细粒度的 HTTP 控制&lt;/li&gt;
&lt;li&gt;请求/响应需要特殊处理（加密、压缩等）&lt;/li&gt;
&lt;li&gt;团队对 Axios 熟悉度高&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;混合方案&lt;/h3&gt;
&lt;p&gt;实际上，最佳实践可能是结合两者：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 统一封装
import { QueryClient } from &quot;@tanstack/react-query&quot;;
import axios from &quot;axios&quot;;

const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,
      retry: 2,
    },
  },
});

// Axios 实例
const http = axios.create({
  baseURL: &quot;/api&quot;,
  timeout: 10000,
});

// 封装查询函数
const queryFn = async &amp;#x3C;T&gt;(url: string): Promise&amp;#x3C;T&gt; =&gt; {
  const response = await http.get&amp;#x3C;T&gt;(url);
  return response.data;
};

// 使用
const { data } = useQuery({
  queryKey: [&quot;posts&quot;],
  queryFn: () =&gt; queryFn(&quot;/posts&quot;),
});
&lt;/code&gt;&lt;/pre&gt;
&lt;hr&gt;
&lt;h2&gt;Lynx Fetch API 与 Web Fetch 的兼容性差异&lt;/h2&gt;
&lt;p&gt;在 Web 生态中，Axios 等库对 &lt;code&gt;XMLHttpRequest&lt;/code&gt; 和 &lt;code&gt;fetch&lt;/code&gt; 进行了大量兼容性处理，以弥合不同浏览器和环境之间的差异。Lynx 只支持 &lt;code&gt;fetch&lt;/code&gt; API，这意味着许多 Axios 在 Web 端做的工作在 Lynx 环境中需要手动处理或依赖其他方式。&lt;/p&gt;
&lt;h3&gt;Axios 在 Web 端做的兼容性处理（Lynx 中需自行处理的部分）&lt;/h3&gt;
&lt;p&gt;| 功能 | Web Axios 处理方式 | Lynx Fetch 现状 |
|------|-------------------|-----------------|
| &lt;strong&gt;JSON 自动转换&lt;/strong&gt; | 请求时自动 &lt;code&gt;JSON.stringify&lt;/code&gt;，响应时自动 &lt;code&gt;response.json()&lt;/code&gt; | 需手动调用 &lt;code&gt;.json()&lt;/code&gt; |
| &lt;strong&gt;超时控制&lt;/strong&gt; | &lt;code&gt;axios.timeout&lt;/code&gt; 配置 | 需使用 &lt;code&gt;AbortController&lt;/code&gt; 自行实现 |
| &lt;strong&gt;请求取消&lt;/strong&gt; | &lt;code&gt;CancelToken&lt;/code&gt; 机制 | 原生支持 &lt;code&gt;AbortController&lt;/code&gt; |
| &lt;strong&gt;HTTP Basic Auth&lt;/strong&gt; | 自动处理 &lt;code&gt;Authorization&lt;/code&gt; 头 | 需手动设置 |
| &lt;strong&gt;XSRF Token&lt;/strong&gt; | 自动从 cookie 读取并添加到 header | 需自行实现 |
| &lt;strong&gt;请求进度&lt;/strong&gt; | 支持 &lt;code&gt;onUploadProgress&lt;/code&gt; | &lt;strong&gt;不支持&lt;/strong&gt; |
| &lt;strong&gt;响应进度&lt;/strong&gt; | 支持 &lt;code&gt;onDownloadProgress&lt;/code&gt; | &lt;strong&gt;不支持&lt;/strong&gt; |
| &lt;strong&gt;FormData 上传&lt;/strong&gt; | 自动处理 &lt;code&gt;multipart/form-data&lt;/code&gt; | 需手动构建 |
| &lt;strong&gt;URL 编码&lt;/strong&gt; | 自动处理 &lt;code&gt;application/x-www-form-urlencoded&lt;/code&gt; | 需手动处理 |
| &lt;strong&gt;重试机制&lt;/strong&gt; | 需插件支持 | 需自行实现 |
| &lt;strong&gt;请求拦截器&lt;/strong&gt; | 原生支持 | 需封装或使用库 |
| &lt;strong&gt;响应拦截器&lt;/strong&gt; | 原生支持 | 需封装或使用库 |
| &lt;strong&gt;错误类型区分&lt;/strong&gt; | 区分网络错误、HTTP 错误等 | 需自行判断 |&lt;/p&gt;
&lt;h3&gt;详细差异说明&lt;/h3&gt;
&lt;h4&gt;1. JSON 自动转换&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;Web Axios（自动处理）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Axios 自动序列化，自动解析
axios.post(&apos;/api/user&apos;, { name: &apos;test&apos; })
  .then(res =&gt; console.log(res.data)); // 直接得到对象
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Lynx Fetch（需手动处理）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 需手动 JSON.stringify
fetch(&apos;/api/user&apos;, {
  method: &apos;POST&apos;,
  headers: { &apos;Content-Type&apos;: &apos;application/json&apos; },
  body: JSON.stringify({ name: &apos;test&apos; })
}).then(res =&gt; res.json()) // 手动调用 .json()
  .then(data =&gt; console.log(data));
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;2. 超时控制&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;Web Axios：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;axios.get(&apos;/api/user&apos;, { timeout: 5000 });
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Lynx Fetch：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const controller = new AbortController();
setTimeout(() =&gt; controller.abort(), 5000);

fetch(&apos;/api/user&apos;, { signal: controller.signal })
  .catch(err =&gt; {
    if (err.name === &apos;AbortError&apos;) {
      console.log(&apos;Request timeout&apos;);
    }
  });
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;3. 请求/响应拦截器&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;Web Axios（原生支持）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;axios.interceptors.request.use(config =&gt; {
  config.headers[&apos;Authorization&apos;] = &apos;Bearer token&apos;;
  return config;
});

axios.interceptors.response.use(
  response =&gt; response.data, // 转换响应
  error =&gt; { /* 统一错误处理 */ }
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Lynx Fetch（封装方案）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 封装 fetch 拦截器
class FetchClient {
  private interceptors = {
    request: [],
    response: []
  };

  async request(url, options = {}) {
    // 请求拦截
    let config = { ...options };
    for (const fn of this.interceptors.request) {
      config = await fn(config);
    }

    const response = await fetch(url, {
      ...config,
      headers: {
        &apos;Content-Type&apos;: &apos;application/json&apos;,
        ...config.headers
      }
    });

    // 响应拦截
    let data = await response.json();
    for (const fn of this.interceptors.response) {
      data = await fn(data, response);
    }

    return data;
  }

  // 添加拦截器方法
  addRequestInterceptor(fn) {
    this.interceptors.request.push(fn);
  }

  addResponseInterceptor(fn) {
    this.interceptors.response.push(fn);
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;4. FormData 上传&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;Web Axios（自动处理）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const formData = new FormData();
formData.append(&apos;file&apos;, file);
formData.append(&apos;name&apos;, &apos;test&apos;);

axios.post(&apos;/api/upload&apos;, formData); // 自动设置 Content-Type
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Lynx Fetch（需手动处理）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const formData = new FormData();
formData.append(&apos;file&apos;, file);
formData.append(&apos;name&apos;, &apos;test&apos;);

fetch(&apos;/api/upload&apos;, {
  method: &apos;POST&apos;,
  body: formData
  // 注意：不需要手动设置 Content-Type，fetch 会自动设置
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;5. XSRF Token 处理&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;Web Axios：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Axios 自动从 cookie 中读取 xsrf-token 并添加到 header
axios.get(&apos;/api/user&apos;);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Lynx Fetch（需自行实现）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;function getCookie(name: string): string | null {
  const match = document.cookie.match(new RegExp(`(^| )${name}=([^;]+)`));
  return match ? match[2] : null;
}

fetch(&apos;/api/user&apos;, {
  headers: {
    &apos;X-XSRF-TOKEN&apos;: getCookie(&apos;XSRF-TOKEN&apos;)
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;6. 错误类型区分&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;Web Axios：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;axios.get(&apos;/api/user&apos;)
  .catch(error =&gt; {
    if (error.response) {
      // 服务器返回错误状态码
      console.log(error.response.status);
    } else if (error.request) {
      // 请求已发出但没有收到响应
      console.log(&apos;Network error&apos;);
    } else {
      // 请求配置出错
      console.log(&apos;Request error&apos;);
    }
  });
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Lynx Fetch（需自行判断）：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;fetch(&apos;/api/user&apos;)
  .catch(error =&gt; {
    if (error.name === &apos;AbortError&apos;) {
      console.log(&apos;Request timeout&apos;);
    } else if (error.message) {
      // 网络错误或 Fetch 自身错误
      console.log(&apos;Fetch error:&apos;, error.message);
    }
  });

// 检查响应状态需在 response.ok 中判断
const response = await fetch(&apos;/api/user&apos;).catch(error =&gt; {
  throw error;
});

if (!response.ok) {
  console.log(&apos;HTTP error:&apos;, response.status);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;进度监控不支持的解决方案&lt;/h3&gt;
&lt;p&gt;Lynx Fetch &lt;strong&gt;不支持&lt;/strong&gt; &lt;code&gt;onUploadProgress&lt;/code&gt; 和 &lt;code&gt;onDownloadProgress&lt;/code&gt;，这是与 Web Axios 的重要差异。对于需要显示上传/下载进度的场景，可以考虑：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;使用原生 XMLHttpRequest&lt;/strong&gt;（如果有暴露）：&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 如果 Lynx 环境支持 XMLHttpRequest
const xhr = new XMLHttpRequest();
xhr.upload.onprogress = (e) =&gt; {
  if (e.lengthComputable) {
    const percent = (e.loaded / e.total) * 100;
    console.log(`Upload: ${percent}%`);
  }
};
&lt;/code&gt;&lt;/pre&gt;
&lt;ol start=&quot;2&quot;&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;使用分段上传&lt;/strong&gt;：将大文件分成小块上传，服务端返回已接收的部分。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;使用 Server-Sent Events&lt;/strong&gt;：服务器推送进度状态，客户端监听更新。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;完整的 Fetch 封装示例&lt;/h3&gt;
&lt;p&gt;以下是综合处理的封装方案，模拟了 Axios 的大部分功能：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;interface FetchOptions extends RequestInit {
  timeout?: number;
  params?: Record&amp;#x3C;string, string&gt;;
}

interface FetchError extends Error {
  status?: number;
  timeout?: boolean;
}

class LynxFetchClient {
  private baseURL: string;

  constructor(baseURL: string) {
    this.baseURL = baseURL;
  }

  private async request&amp;#x3C;T&gt;(
    method: string,
    url: string,
    options: FetchOptions = {}
  ): Promise&amp;#x3C;T&gt; {
    const { timeout = 10000, params, headers, body, ...rest } = options;

    // 构建完整 URL
    let fullUrl = `${this.baseURL}${url}`;
    if (params) {
      const searchParams = new URLSearchParams(params);
      fullUrl += `?${searchParams.toString()}`;
    }

    // 构建 headers
    const mergedHeaders: Record&amp;#x3C;string, string&gt; = {
      &apos;Content-Type&apos;: &apos;application/json&apos;,
      ...(headers as Record&amp;#x3C;string, string&gt;),
    };

    // 添加 token（示例）
    const token = this.getToken();
    if (token) {
      mergedHeaders[&apos;Authorization&apos;] = `Bearer ${token}`;
    }

    // 超时控制
    const controller = new AbortController();
    const timeoutId = setTimeout(() =&gt; controller.abort(), timeout);

    try {
      const response = await fetch(fullUrl, {
        method,
        headers: mergedHeaders,
        body: body ? (typeof body === &apos;string&apos; ? body : JSON.stringify(body)) : undefined,
        signal: controller.signal,
        ...rest,
      });

      clearTimeout(timeoutId);

      if (!response.ok) {
        const error: FetchError = new Error(`HTTP ${response.status}`);
        error.status = response.status;
        throw error;
      }

      return response.json();
    } catch (error: any) {
      clearTimeout(timeoutId);

      if (error.name === &apos;AbortError&apos;) {
        const timeoutError: FetchError = new Error(&apos;Request timeout&apos;);
        timeoutError.timeout = true;
        throw timeoutError;
      }
      throw error;
    }
  }

  private getToken(): string | null {
    // 实现获取 token 的逻辑
    return null;
  }

  get&amp;#x3C;T&gt;(url: string, options?: FetchOptions) {
    return this.request&amp;#x3C;T&gt;(&apos;GET&apos;, url, options);
  }

  post&amp;#x3C;T&gt;(url: string, data?: unknown, options?: FetchOptions) {
    return this.request&amp;#x3C;T&gt;(&apos;POST&apos;, url, { ...options, body: data });
  }

  put&amp;#x3C;T&gt;(url: string, data?: unknown, options?: FetchOptions) {
    return this.request&amp;#x3C;T&gt;(&apos;PUT&apos;, url, { ...options, body: data });
  }

  delete&amp;#x3C;T&gt;(url: string, options?: FetchOptions) {
    return this.request&amp;#x3C;T&gt;(&apos;DELETE&apos;, url, options);
  }

  patch&amp;#x3C;T&gt;(url: string, data?: unknown, options?: FetchOptions) {
    return this.request&amp;#x3C;T&gt;(&apos;PATCH&apos;, url, { ...options, body: data });
  }
}

// 使用
const api = new LynxFetchClient(&apos;https://api.example.com&apos;);

api.get&amp;#x3C;User[]&gt;(&apos;/users&apos;).then(users =&gt; {
  console.log(users);
}).catch(error =&gt; {
  if (error.timeout) {
    console.log(&apos;请求超时&apos;);
  } else if (error.status) {
    console.log(`HTTP 错误: ${error.status}`);
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;hr&gt;
&lt;h2&gt;结论&lt;/h2&gt;
&lt;p&gt;对于 Lynx/ReactLynx 项目，&lt;strong&gt;推荐优先考虑 TanStack Query + Fetch 方案&lt;/strong&gt;。它提供了完整的数据获取解决方案，能够显著提升开发效率和用户体验。Axios 则适合需要精细 HTTP 控制或已有存量项目的场景。两者也可以结合使用，取长补短。&lt;/p&gt;
&lt;p&gt;最终的技术选型应该基于项目实际需求、团队技术背景和长期维护成本来综合考虑。&lt;/p&gt;</content:encoded><h:img src="undefined"/><enclosure url="undefined"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>XState核心概念与实战应用</title><link>https://luopengcheng89.github.io/blog/xstate-core-concepts-and-usage</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/xstate-core-concepts-and-usage</guid><description>从一个实际项目出发，深入理解状态管理库XState的核心概念、设计模式和最佳实践</description><pubDate>Tue, 31 Mar 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;XState核心概念与实战应用&lt;/h1&gt;
&lt;p&gt;状态管理是现代前端应用中不可或缺的一部分。在复杂的应用场景中，简单的状态更新往往无法满足需求，我们需要一种更强大、更可预测的方式来管理应用的状态。XState正是为此而生的一个状态机库，它基于状态机理论，提供了强大的状态管理能力。&lt;/p&gt;
&lt;p&gt;本文将基于一个实际项目（&lt;a href=&quot;https://github.com/JohnieXu/public_actions&quot;&gt;public_actions&lt;/a&gt;）来深入理解XState的核心概念和最佳实践。&lt;/p&gt;
&lt;h2&gt;1. 什么是XState？&lt;/h2&gt;
&lt;p&gt;XState是一个基于状态机理论的状态管理库，它使用有限状态自动机（Finite State Machine）的模型来管理应用的状态。与传统的Redux等状态管理库相比，XState具有以下优势：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;可预测性&lt;/strong&gt;：状态转换是明确的，不会有意外的状态变化&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;可视化&lt;/strong&gt;：状态机可以图形化展示，便于理解和调试&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;复杂逻辑&lt;/strong&gt;：适合处理复杂的状态转换和业务逻辑&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;异步操作&lt;/strong&gt;：内置对异步操作的支持，如API调用、定时器等&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;2. 核心概念&lt;/h2&gt;
&lt;h3&gt;2.1 State（状态）&lt;/h3&gt;
&lt;p&gt;状态是XState中的基本概念，表示系统在某个时刻的条件。状态可以是简单的，也可以是嵌套的：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 简单状态
states: {
  idle: {},
  loading: {},
  success: {},
  error: {}
}

// 嵌套状态
states: {
  authentication: {
    initial: &apos;loggedOut&apos;,
    states: {
      loggedOut: {},
      loggedIn: {
        initial: &apos;loading&apos;,
        states: {
          loading: {},
          ready: {},
          error: {}
        }
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2.2 Event（事件）&lt;/h3&gt;
&lt;p&gt;事件是触发状态转换的信号。事件可以是外部用户交互，也可以是内部状态变化：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const machine = createMachine({
  id: &apos;light&apos;,
  initial: &apos;green&apos;,
  states: {
    green: {
      on: {
        TIMER: &apos;yellow&apos;
      }
    },
    yellow: {
      on: {
        TIMER: &apos;red&apos;
      }
    },
    red: {
      on: {
        TIMER: &apos;green&apos;
      }
    }
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2.3 Transition（转换）&lt;/h3&gt;
&lt;p&gt;转换定义了从一个状态到另一个状态的路径。转换可以由事件触发，也可以自动发生：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 事件触发的转换
on: {
  &apos;FETCH_DATA&apos;: {
    target: &apos;loading&apos;
  }
}

// 自动转换（立即执行）
always: &apos;nextState&apos;

// 条件转换
cond: &apos;isValid&apos;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2.4 Context（上下文）&lt;/h3&gt;
&lt;p&gt;Context是状态机的数据存储，类似于Redux中的store：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const machine = createMachine({
  context: {
    user: null,
    error: null,
    data: []
  },
  // ...
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2.5 Actions（动作）&lt;/h3&gt;
&lt;p&gt;动作是在状态转换过程中执行的副作用，可以是同步或异步的：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;{
  // 分配动作（更新上下文）
  actions: assign({
    user: (context, event) =&gt; event.user
  }),

  // 调用服务（异步）
  invoke: {
    src: &apos;fetchData&apos;,
    onDone: {
      target: &apos;success&apos;,
      actions: assign({
        data: (context, event) =&gt; event.data
      })
    },
    onError: {
      target: &apos;error&apos;,
      actions: assign({
        error: (context, event) =&gt; event.data
      })
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2.6 Guards（守卫）&lt;/h3&gt;
&lt;p&gt;守卫是条件判断，用于控制转换的发生：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;{
  cond: &apos;hasValidToken&apos;,
  target: &apos;authenticated&apos;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;3. 实战应用：自动化工作流&lt;/h2&gt;
&lt;p&gt;基于public_actions项目，我们来看看如何使用XState来构建自动化工作流。&lt;/p&gt;
&lt;h3&gt;3.1 基本机器结构&lt;/h3&gt;
&lt;p&gt;每个平台的自动化任务都有一个独立的XState机器：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { createMachine, assign } from &apos;xstate&apos;;
import { setup } from &apos;xstate/fsm&apos;;

export const hiFinMachine = setup({
  types: {
    context: {} as HiFinContext,
    input: {} as HiFinInput,
    events: {} as HiFinEvent
  },
  actors: {
    doCheckin: doCheckinActor,
    doSendEmail: doSendEmailActor
  },
  actions: {
    assignCheckinResult: assign({
      checkinResult: (_, event) =&gt; event.data
    })
  }
}).createMachine({
  id: &apos;hiFin&apos;,
  initial: &apos;initial&apos;,
  context: {
    checkinResult: null,
    error: null,
    cookies: []
  },
  states: {
    initial: {
      always: &apos;checkin&apos;
    },
    checkin: {
      invoke: {
        src: &apos;doCheckin&apos;,
        onDone: {
          target: &apos;checkin success&apos;,
          actions: &apos;assignCheckinResult&apos;
        },
        onError: {
          target: &apos;checkin error&apos;,
          actions: assign({
            error: (_, event) =&gt; event.data
          })
        }
      }
    },
    &apos;checkin success&apos;: {
      invoke: {
        src: &apos;doSendEmail&apos;,
        input: (context) =&gt; ({
          type: &apos;success&apos;,
          result: context.checkinResult
        }),
        onDone: &apos;done&apos;
      }
    },
    &apos;checkin error&apos;: {
      invoke: {
        src: &apos;doSendEmail&apos;,
        input: (context) =&gt; ({
          type: &apos;error&apos;,
          error: context.error
        }),
        onDone: &apos;done&apos;
      }
    },
    done: {
      type: &apos;final&apos;
    }
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3.2 关键设计模式&lt;/h3&gt;
&lt;h4&gt;模式1：线性工作流&lt;/h4&gt;
&lt;p&gt;自动化任务通常遵循简单的线性流程：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;初始状态 → 执行动作 → 成功状态 → 通知 → 最终状态
           ↓
         失败状态 → 错误通知 → 最终状态
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;模式2：执行者模式&lt;/h4&gt;
&lt;p&gt;使用&lt;code&gt;invoke&lt;/code&gt;来执行异步操作，并处理成功/失败情况：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;invoke: {
  src: &apos;doCheckin&apos;,  // 执行异步操作
  onDone: {         // 成功时的处理
    target: &apos;checkin success&apos;,
    actions: assign({ /* 更新上下文 */ })
  },
  onError: {        // 失败时的处理
    target: &apos;checkin error&apos;,
    actions: assign({ error: (_, event) =&gt; event.data })
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;模式3：邮件通知模式&lt;/h4&gt;
&lt;p&gt;无论成功还是失败，都要发送通知：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 成功通知
&apos;checkin success&apos;: {
  invoke: {
    src: &apos;doSendEmail&apos;,
    input: (context) =&gt; ({
      type: &apos;success&apos;,
      result: context.checkinResult
    })
  }
}

// 失败通知
&apos;checkin error&apos;: {
  invoke: {
    src: &apos;doSendEmail&apos;,
    input: (context) =&gt; ({
      type: &apos;error&apos;,
      error: context.error
    })
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3.3 实用工具：单例运行器&lt;/h3&gt;
&lt;p&gt;为了让机器能够独立运行并提供超时控制，项目实现了一个单例运行器：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// src/utils/machine.ts
import { createActor, fromPromise, createMachine } from &apos;xstate&apos;;

export function singletonRunner&amp;#x3C;TInput, TOutput&gt;(
  machine: ReturnType&amp;#x3C;typeof createMachine&amp;#x3C;any, any, any&gt;&gt;,
  options: {
    input: TInput;
    timeout?: number;
  }
): Promise&amp;#x3C;TOutput&gt; {
  const actor = createActor(machine, {
    input: options.input
  });

  return new Promise((resolve, reject) =&gt; {
    const timeoutId = options.timeout
      ? setTimeout(() =&gt; {
          actor.stop();
          reject(new Error(&apos;Timeout&apos;));
        }, options.timeout)
      : null;

    actor.subscribe((state) =&gt; {
      if (state.matches(&apos;done&apos;)) {
        if (timeoutId) clearTimeout(timeoutId);
        resolve(state.context.output);
      }
    });

    actor.start();
  });
}

export function runMachine&amp;#x3C;TInput, TOutput&gt;(
  machine: ReturnType&amp;#x3C;typeof createMachine&amp;#x3C;any, any, any&gt;&gt;,
  input: TInput,
  options?: {
    timeout?: number;
  }
) {
  return singletonRunner&amp;#x3C;TInput, TOutput&gt;(machine, {
    input,
    timeout: options?.timeout || 30000 // 默认30秒超时
  });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3.4 API集成&lt;/h3&gt;
&lt;p&gt;将API调用封装为XState的执行者：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// src/utils/api.ts
import { fromPromise } from &apos;xstate&apos;;

export const doCheckinActor = fromPromise(async () =&gt; {
  const response = await fetch(&apos;https://api.example.com/checkin&apos;, {
    method: &apos;POST&apos;,
    headers: {
      &apos;Content-Type&apos;: &apos;application/json&apos;,
      &apos;Cookie&apos;: getCookieString()
    }
  });

  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }

  return response.json();
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;4. 最佳实践&lt;/h2&gt;
&lt;h3&gt;4.1 类型安全&lt;/h3&gt;
&lt;p&gt;充分利用TypeScript的类型系统：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;interface HiFinContext {
  checkinResult: CheckinResult | null;
  error: string | null;
  cookies: string[];
}

interface HiFinInput {
  userId: string;
  password: string;
}

interface HiFinEvent {
  type: &apos;checkin&apos;;
  data?: any;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.2 状态分离&lt;/h3&gt;
&lt;p&gt;将不同状态分离，避免状态过度复杂：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// ❌ 不好的设计
{
  initial: &apos;idle&apos;,
  states: {
    idle: {
      on: {
        FETCH: &apos;loading&apos;
      }
    },
    loading: {
      invoke: {
        src: &apos;fetchData&apos;
      }
    },
    loaded: {
      // ...复杂的处理逻辑
    },
    error: {}
  }
}

// ✅ 好的设计
{
  initial: &apos;idle&apos;,
  states: {
    idle: {
      on: {
        FETCH: &apos;loading&apos;
      }
    },
    loading: {
      invoke: {
        src: &apos;fetchData&apos;,
        onDone: &apos;loaded&apos;,
        onError: &apos;error&apos;
      }
    },
    loaded: {
      // 处理成功状态
    },
    error: {
      // 处理错误状态
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.3 错误处理&lt;/h3&gt;
&lt;p&gt;始终处理错误状态，并提供恢复机制：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;{
  invoke: {
    src: &apos;riskyOperation&apos;,
    onDone: &apos;success&apos;,
    onError: {
      target: &apos;retry&apos;,
      // 可以尝试重试
    }
  }
}

// 或者
{
  &apos;retry attempt&apos;: {
    invoke: {
      src: &apos;riskyOperation&apos;,
      onDone: &apos;success&apos;,
      onError: {
        target: &apos;error&apos;,
        actions: assign({
          error: (_, event) =&gt; event.data,
          retryCount: (context) =&gt; context.retryCount + 1
        })
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.4 动作管理&lt;/h3&gt;
&lt;p&gt;将动作集中管理，便于维护：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const actions = {
  assignUser: assign({
    user: (_, event) =&gt; event.user
  }),
  logAction: (context, event) =&gt; {
    console.log(&apos;Action:&apos;, event.type);
  },
  notify: (context, event) =&gt; {
    sendNotification(`State changed to: ${event.type}`);
  }
};

export const machine = createMachine({
  // ...
  actions: {
    ...actions
  },
  // ...
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;5. 性能优化&lt;/h2&gt;
&lt;h3&gt;5.1 机器缓存&lt;/h3&gt;
&lt;p&gt;对于不会频繁变化的机器，可以缓存实例：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const machineCache = new WeakMap();

function getCachedMachine(config) {
  if (machineCache.has(config)) {
    return machineCache.get(config);
  }

  const machine = createMachine(config);
  machineCache.set(config, machine);
  return machine;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;5.2 状态树扁平化&lt;/h3&gt;
&lt;p&gt;避免过深的嵌套状态：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// ❌ 过深的嵌套
{
  initial: &apos;auth&apos;,
  states: {
    auth: {
      initial: &apos;checking&apos;,
      states: {
        checking: {
          // ...
        },
        notAuthenticated: {
          // ...
        }
      }
    }
  }
}

// ✅ 扁平化的状态
{
  initial: &apos;auth.checking&apos;,
  states: {
    &apos;auth.checking&apos;: {},
    &apos;auth.notAuthenticated&apos;: {},
    &apos;authenticated&apos;: {}
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;5.3 订阅优化&lt;/h3&gt;
&lt;p&gt;合理使用订阅，避免不必要的重渲染：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 只在特定状态变化时订阅
actor.subscribe((state) =&gt; {
  if (state.matches(&apos;success&apos;)) {
    handleSuccess(state.context);
  } else if (state.matches(&apos;error&apos;)) {
    handleError(state.context.error);
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;6. 调试技巧&lt;/h2&gt;
&lt;h3&gt;6.1 使用XState可视化工具&lt;/h3&gt;
&lt;p&gt;&lt;a href=&quot;https://stately.ai/viz&quot;&gt;XState Visualizer&lt;/a&gt;可以帮助你可视化状态机：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# 安装 CLI 工具
npm install -g @xstate/inspect

# 运行调试模式
node -r @xstate/inspect your-app.js
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;6.2 日志记录&lt;/h3&gt;
&lt;p&gt;添加日志动作来跟踪状态变化：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;const actions = {
  logState: (context, event) =&gt; {
    console.log(`Transitioning to: ${event.type}`, context);
  }
};

export const machine = createMachine({
  // ...
  on: {
    FETCH: {
      target: &apos;loading&apos;,
      actions: &apos;logState&apos;
    }
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;6.3 使用React DevTools&lt;/h3&gt;
&lt;p&gt;如果你使用React集成，可以使用React DevTools来观察状态变化：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { useMachine } from &apos;@xstate/react&apos;;

function MyComponent() {
  const [state, send] = useMachine(machine);

  // 在devtools中观察状态
  console.log(state);

  return (
    &amp;#x3C;button onClick={() =&gt; send({ type: &apos;FETCH&apos; })}&gt;
      Fetch Data
    &amp;#x3C;/button&gt;
  );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;7. 总结&lt;/h2&gt;
&lt;p&gt;XState是一个强大的状态管理工具，特别适合处理复杂的状态逻辑。通过本文的介绍，我们了解了：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;核心概念&lt;/strong&gt;：状态、事件、转换、上下文、动作和守卫&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;设计模式&lt;/strong&gt;：线性工作流、执行者模式、邮件通知模式&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;最佳实践&lt;/strong&gt;：类型安全、状态分离、错误处理、动作管理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;性能优化&lt;/strong&gt;：机器缓存、状态扁平化、订阅优化&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;调试技巧&lt;/strong&gt;：可视化工具、日志记录、DevTools&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;在实际项目中，XState可以帮助我们构建更加可维护、可预测的状态逻辑。正如public_actions项目所示，XState非常适合自动化工作流、表单状态管理、游戏逻辑等场景。&lt;/p&gt;
&lt;h2&gt;8. 参考资料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://xstate.js.org/docs/&quot;&gt;XState官方文档&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://stately.ai/viz&quot;&gt;XState Visualizer&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://en.wikipedia.org/wiki/Finite-state_machine&quot;&gt;State Machine Theory&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://xstate.js.org/docs/guides/typescript.html&quot;&gt;TypeScript Integration&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;希望这篇文章能帮助你更好地理解和使用XState。如果你有任何问题或建议，欢迎在评论区交流！&lt;/p&gt;</content:encoded><h:img src="undefined"/><enclosure url="undefined"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>Shell中的Vim快捷键指南</title><link>https://luopengcheng89.github.io/blog/shell-vim-shortcuts-guide</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/shell-vim-shortcuts-guide</guid><description>在shell中使用vim快捷键的快速参考指南，提高终端操作效率</description><pubDate>Wed, 25 Mar 2026 08:29:19 GMT</pubDate><content:encoded>&lt;h1&gt;Shell中的Vim快捷键指南&lt;/h1&gt;
&lt;p&gt;在日常开发中，我们经常需要在shell中编辑命令行。掌握vim快捷键可以大大提高终端操作效率。本文整理了常用的vim快捷键，方便快速查看回顾。&lt;/p&gt;
&lt;h2&gt;基础移动&lt;/h2&gt;
&lt;h3&gt;光标移动&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;h&lt;/code&gt; - 向左移动一个字符&lt;/li&gt;
&lt;li&gt;&lt;code&gt;j&lt;/code&gt; - 向下移动一行&lt;/li&gt;
&lt;li&gt;&lt;code&gt;k&lt;/code&gt; - 向上移动一行&lt;/li&gt;
&lt;li&gt;&lt;code&gt;l&lt;/code&gt; - 向右移动一个字符&lt;/li&gt;
&lt;li&gt;&lt;code&gt;w&lt;/code&gt; - 移动到下一个单词的开头&lt;/li&gt;
&lt;li&gt;&lt;code&gt;b&lt;/code&gt; - 移动到上一个单词的开头&lt;/li&gt;
&lt;li&gt;&lt;code&gt;e&lt;/code&gt; - 移动到当前单词的结尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;0&lt;/code&gt; - 移动到行首&lt;/li&gt;
&lt;li&gt;&lt;code&gt;$&lt;/code&gt; - 移动到行尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;^&lt;/code&gt; - 移动到行首的非空白字符&lt;/li&gt;
&lt;li&gt;&lt;code&gt;G&lt;/code&gt; - 移动到文件末尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;gg&lt;/code&gt; - 移动到文件开头&lt;/li&gt;
&lt;li&gt;&lt;code&gt;nG&lt;/code&gt; - 移动到第n行&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;单词内移动&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ge&lt;/code&gt; - 移动到上一个单词的结尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;gE&lt;/code&gt; - 移动到上一个长单词（包含下划线）的结尾&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;编辑操作&lt;/h2&gt;
&lt;h3&gt;删除操作&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;x&lt;/code&gt; - 删除当前字符&lt;/li&gt;
&lt;li&gt;&lt;code&gt;X&lt;/code&gt; - 删除前一个字符&lt;/li&gt;
&lt;li&gt;&lt;code&gt;dd&lt;/code&gt; - 删除当前行&lt;/li&gt;
&lt;li&gt;&lt;code&gt;d0&lt;/code&gt; - 删除到行首&lt;/li&gt;
&lt;li&gt;&lt;code&gt;d$&lt;/code&gt; - 删除到行尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;dw&lt;/code&gt; - 删除一个单词&lt;/li&gt;
&lt;li&gt;&lt;code&gt;db&lt;/code&gt; - 删除前一个单词&lt;/li&gt;
&lt;li&gt;&lt;code&gt;de&lt;/code&gt; - 删除到单词结尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;dgg&lt;/code&gt; - 删除到文件开头&lt;/li&gt;
&lt;li&gt;&lt;code&gt;dG&lt;/code&gt; - 删除到文件末尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ndd&lt;/code&gt; - 删除n行&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;复制和粘贴&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;yy&lt;/code&gt; - 复制当前行&lt;/li&gt;
&lt;li&gt;&lt;code&gt;y0&lt;/code&gt; - 复制到行首&lt;/li&gt;
&lt;li&gt;&lt;code&gt;y$&lt;/code&gt; - 复制到行尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;yw&lt;/code&gt; - 复制一个单词&lt;/li&gt;
&lt;li&gt;&lt;code&gt;p&lt;/code&gt; - 粘贴到光标后&lt;/li&gt;
&lt;li&gt;&lt;code&gt;P&lt;/code&gt; - 粘贴到光标前&lt;/li&gt;
&lt;li&gt;&lt;code&gt;nyy&lt;/code&gt; - 复制n行&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;修改和替换&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;i&lt;/code&gt; - 在光标前插入&lt;/li&gt;
&lt;li&gt;&lt;code&gt;a&lt;/code&gt; - 在光标后插入&lt;/li&gt;
&lt;li&gt;&lt;code&gt;I&lt;/code&gt; - 在行首插入&lt;/li&gt;
&lt;li&gt;&lt;code&gt;A&lt;/code&gt; - 在行尾插入&lt;/li&gt;
&lt;li&gt;&lt;code&gt;o&lt;/code&gt; - 在下一行插入&lt;/li&gt;
&lt;li&gt;&lt;code&gt;O&lt;/code&gt; - 在上一行插入&lt;/li&gt;
&lt;li&gt;&lt;code&gt;r&lt;/code&gt; - 替换当前字符&lt;/li&gt;
&lt;li&gt;&lt;code&gt;R&lt;/code&gt; - 进入替换模式&lt;/li&gt;
&lt;li&gt;&lt;code&gt;cw&lt;/code&gt; - 修改一个单词&lt;/li&gt;
&lt;li&gt;&lt;code&gt;c$&lt;/code&gt; - 修改到行尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;c0&lt;/code&gt; - 修改到行首&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;查找和替换&lt;/h2&gt;
&lt;h3&gt;查找&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;/pattern&lt;/code&gt; - 向下查找&lt;/li&gt;
&lt;li&gt;&lt;code&gt;?pattern&lt;/code&gt; - 向上查找&lt;/li&gt;
&lt;li&gt;&lt;code&gt;n&lt;/code&gt; - 查找下一个&lt;/li&gt;
&lt;li&gt;&lt;code&gt;N&lt;/code&gt; - 查找上一个&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;替换&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;:s/old/new&lt;/code&gt; - 替换当前行的第一个匹配&lt;/li&gt;
&lt;li&gt;&lt;code&gt;:s/old/new/g&lt;/code&gt; - 替换当前行的所有匹配&lt;/li&gt;
&lt;li&gt;&lt;code&gt;:%s/old/new/g&lt;/code&gt; - 替换全文的所有匹配&lt;/li&gt;
&lt;li&gt;&lt;code&gt;:%s/old/new/gc&lt;/code&gt; - 替换全文并确认&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;撤销和重做&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;u&lt;/code&gt; - 撤销&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + r&lt;/code&gt; - 重做&lt;/li&gt;
&lt;li&gt;&lt;code&gt;U&lt;/code&gt; - 撤销对整行的修改&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;其他实用快捷键&lt;/h2&gt;
&lt;h3&gt;操作历史&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Ctrl + p&lt;/code&gt; - 上一条命令&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + n&lt;/code&gt; - 下一条命令&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + f&lt;/code&gt; - 前进&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + b&lt;/code&gt; - 后退&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;文件操作&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;Ctrl + d&lt;/code&gt; - 退出（EOF）&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + c&lt;/code&gt; - 中断当前命令&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + z&lt;/code&gt; - 挂起当前进程&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + l&lt;/code&gt; - 清屏&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + a&lt;/code&gt; - 移动到行首&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + e&lt;/code&gt; - 移动到行尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + u&lt;/code&gt; - 删除到行首&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + k&lt;/code&gt; - 删除到行尾&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + w&lt;/code&gt; - 删除前一个单词&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;高级技巧&lt;/h3&gt;
&lt;h4&gt;可视模式&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;v&lt;/code&gt; - 字符可视模式&lt;/li&gt;
&lt;li&gt;&lt;code&gt;V&lt;/code&gt; - 行可视模式&lt;/li&gt;
&lt;li&gt;&lt;code&gt;Ctrl + v&lt;/code&gt; - 块可视模式&lt;/li&gt;
&lt;li&gt;&lt;code&gt;o&lt;/code&gt; - 切换选择端点&lt;/li&gt;
&lt;li&gt;&lt;code&gt;O&lt;/code&gt; - 切换选择端点（另一端）&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;标记&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ma&lt;/code&gt; - 在当前位置设置标记a&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&apos;a&lt;/code&gt; - 跳转到标记a&lt;/li&gt;
&lt;li&gt;&lt;code&gt;`&lt;/code&gt; - 跳转到上次跳转的位置&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;宏录制&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;qa&lt;/code&gt; - 开始录制宏到a&lt;/li&gt;
&lt;li&gt;&lt;code&gt;q&lt;/code&gt; - 停止录制&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@a&lt;/code&gt; - 执行宏a&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@@&lt;/code&gt; - 重复上次执行的宏&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;实用技巧总结&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;快速修改&lt;/strong&gt;：使用 &lt;code&gt;cw&lt;/code&gt; 修改单词比 &lt;code&gt;dlle&lt;/code&gt; 更快&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;快速删除&lt;/strong&gt;：&lt;code&gt;dd&lt;/code&gt; 删除整行比 &lt;code&gt;D&lt;/code&gt; 更直观&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;快速复制&lt;/strong&gt;：&lt;code&gt;yy&lt;/code&gt; 复制整行，&lt;code&gt;yw&lt;/code&gt; 复制单词&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;快速移动&lt;/strong&gt;：&lt;code&gt;0&lt;/code&gt; 到行首，&lt;code&gt;$&lt;/code&gt; 到行尾，&lt;code&gt;gg&lt;/code&gt; 到文件开头&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;快速跳转&lt;/strong&gt;：&lt;code&gt;nG&lt;/code&gt; 快速跳转到指定行&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;快速查找&lt;/strong&gt;：使用 &lt;code&gt;/&lt;/code&gt; 向下查找，&lt;code&gt;n&lt;/code&gt; 查找下一个&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;练习建议&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;从基础移动开始，先熟练 &lt;code&gt;hjkl&lt;/code&gt; 和 &lt;code&gt;wbe$&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;逐渐练习删除和复制操作&lt;/li&gt;
&lt;li&gt;练习查找替换功能&lt;/li&gt;
&lt;li&gt;尝试使用可视模式进行块操作&lt;/li&gt;
&lt;li&gt;最后学习宏录制功能&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;总结&lt;/h2&gt;
&lt;p&gt;掌握这些vim快捷键可以让你在shell中的操作更加流畅高效。建议多加练习，逐渐形成肌肉记忆。将常用的快捷键记在笔记本上，方便随时查阅。&lt;/p&gt;
&lt;p&gt;记住，熟能生巧，坚持练习才是掌握快捷键的关键！&lt;/p&gt;</content:encoded><h:img src="undefined"/><enclosure url="undefined"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>CommonJS 与 ESM 模块异同及 vite 的处理</title><link>https://luopengcheng89.github.io/blog/esm_module_vite_esbuild</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/esm_module_vite_esbuild</guid><description>本文将深入探讨 CommonJS 和 ESM 的异同，分析它们各自的特点和适用场景，并揭示 Vite 如何巧妙地处理 CommonJS 模块，让开发者能够无缝使用庞大的 npm 生态系统。</description><pubDate>Mon, 26 Aug 2024 16:58:24 GMT</pubDate><content:encoded>&lt;h2&gt;引言&lt;/h2&gt;
&lt;p&gt;在现代 JavaScript 开发中，模块系统是构建复杂应用的基石。然而，JavaScript 生态系统中存在着两种主流的模块格式：CommonJS (CJS) 和 ECMAScript Modules (ESM)。这两种格式在设计理念、语法和工作方式上有着根本性的差异，导致了它们在浏览器和 Node.js 环境中的兼容性问题。&lt;/p&gt;
&lt;p&gt;本文将深入探讨 CommonJS 和 ESM 的异同，分析它们各自的特点和适用场景，并揭示 Vite 如何巧妙地处理 CommonJS 模块，让开发者能够无缝使用庞大的 npm 生态系统。&lt;/p&gt;
&lt;h2&gt;一、CommonJS 与 ESM 的全面对比&lt;/h2&gt;
&lt;h3&gt;1.1 语法差异&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;CommonJS (CJS) 语法：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// 导出
module.exports = { value: 42 };
exports.value = 42;

// 导入
const module = require(&apos;./module&apos;);
const { value } = require(&apos;./module&apos;);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;ES Module (ESM) 语法：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// 导出
export default { value: 42 };
export const value = 42;

// 导入
import module from &apos;./module&apos;;
import { value } from &apos;./module&apos;;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;1.2 核心差异对比&lt;/h3&gt;
&lt;p&gt;| 特性 | CommonJS (CJS) | ES Module (ESM) |
|------|----------------|-----------------|
| &lt;strong&gt;加载方式&lt;/strong&gt; | 同步加载（运行时） | 异步加载（编译时静态解析） |
| &lt;strong&gt;值传递&lt;/strong&gt; | 值的拷贝 | 值的引用（活的绑定） |
| &lt;strong&gt;动态性&lt;/strong&gt; | 高度动态（条件导入、动态路径） | 静态结构（路径必须为字面量） |
| &lt;strong&gt;设计初衷&lt;/strong&gt; | 服务器端（Node.js） | 浏览器端（语言标准） |
| &lt;strong&gt;严格模式&lt;/strong&gt; | 可选 | 默认启用且不可关闭 |
| &lt;strong&gt;循环引用&lt;/strong&gt; | 处理困难，容易出错 | 原生支持，是定义特性 |
| &lt;strong&gt;Tree-Shaking&lt;/strong&gt; | 困难 | 天然支持 |
| &lt;strong&gt;顶级 &lt;code&gt;this&lt;/code&gt;&lt;/strong&gt; | 指向 &lt;code&gt;module.exports&lt;/code&gt; | &lt;code&gt;undefined&lt;/code&gt; |&lt;/p&gt;
&lt;h3&gt;1.3 值的拷贝 vs 值的引用&lt;/h3&gt;
&lt;p&gt;这是两者最关键的语义差异，通过代码示例可以清晰看出：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;CommonJS (值的拷贝)：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// counter.cjs
let count = 0;
function increment() { count++; }
module.exports = { count, increment };

// main.cjs
const { count, increment } = require(&apos;./counter.cjs&apos;);
console.log(count); // 输出: 0
increment();
console.log(count); // 输出: 0 (未改变，导入的是原始值的拷贝)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;ES Module (值的引用)：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// counter.mjs
export let count = 0;
export function increment() { count++; }

// main.mjs
import { count, increment } from &apos;./counter.mjs&apos;;
console.log(count); // 输出: 0
increment();
console.log(count); // 输出: 1 (改变，导入的是值的引用)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;1.4 动态导入机制&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;CommonJS 的动态 &lt;code&gt;require&lt;/code&gt;：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// 可以根据条件动态加载模块
let myModule;
if (condition) {
  myModule = require(&apos;./moduleA&apos;);
} else {
  myModule = require(&apos;./moduleB&apos;);
}

// 动态路径
const path = &apos;./&apos; + moduleName;
const module = require(path);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;ES Module 的静态 &lt;code&gt;import&lt;/code&gt; 和动态 &lt;code&gt;import()&lt;/code&gt;：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// 静态导入必须在顶层
import moduleA from &apos;./moduleA&apos;;

// 动态导入返回 Promise
if (condition) {
  import(&apos;./moduleA.mjs&apos;).then(module =&gt; {
    // 使用模块
  });
}

// 使用 async/await
const loadModule = async () =&gt; {
  const module = await import(`./${moduleName}.mjs`);
};
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;二、Vite 如何处理 CommonJS 模块&lt;/h2&gt;
&lt;h3&gt;2.1 问题背景&lt;/h3&gt;
&lt;p&gt;Vite 是专为 ESM 设计的构建工具，其开发服务器依赖于浏览器原生 ESM 的特性。然而，npm 生态系统中有大量包仍然是 CommonJS 格式，这导致了兼容性问题。&lt;/p&gt;
&lt;h3&gt;2.2 解决方案：预构建&lt;/h3&gt;
&lt;p&gt;Vite 的解决方案是预构建（Pre-Bundling），这个过程由 esbuild（一个极快的 JavaScript 打包器）完成，主要步骤包括：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;依赖扫描&lt;/strong&gt;：Vite 分析项目源码，找出所有依赖的 CommonJS 模块&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;格式转换&lt;/strong&gt;：使用 esbuild 将 CommonJS 模块转换为 ESM 格式&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;模块合并&lt;/strong&gt;：将多个内部模块的包打包成单个文件&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;存储缓存&lt;/strong&gt;：转换后的模块存储在 &lt;code&gt;node_modules/.vite/deps&lt;/code&gt; 目录中&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;2.3 预构建的优势&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;性能提升&lt;/strong&gt;：减少浏览器请求数量，将多个文件合并为一个&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;兼容性保证&lt;/strong&gt;：确保 CommonJS 模块在 ESM 环境中正常工作&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;缓存机制&lt;/strong&gt;：首次构建后结果被缓存，提高后续启动速度&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;2.4 实际工作流程&lt;/h3&gt;
&lt;p&gt;当你在 Vite 项目中导入一个 CommonJS 包时：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;import _ from &apos;lodash&apos;; // 一个 CommonJS 包
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Vite 会：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在预构建阶段检测到 &lt;code&gt;lodash&lt;/code&gt; 是 CommonJS 格式&lt;/li&gt;
&lt;li&gt;使用 esbuild 将其转换为 ESM 格式&lt;/li&gt;
&lt;li&gt;将转换后的模块存储在 &lt;code&gt;node_modules/.vite/deps/lodash.js&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;重写你的导入语句指向转换后的模块&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;三、esbuild 的转换过程&lt;/h2&gt;
&lt;h3&gt;3.1 转换策略&lt;/h3&gt;
&lt;p&gt;esbuild 的转换过程不仅仅是语法替换，而是深度的语义转换：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;导出分析&lt;/strong&gt;：识别 &lt;code&gt;module.exports&lt;/code&gt; 和 &lt;code&gt;exports&lt;/code&gt; 的使用模式&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;导入转换&lt;/strong&gt;：将 &lt;code&gt;require()&lt;/code&gt; 调用转换为 &lt;code&gt;import&lt;/code&gt; 语句&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;作用域保持&lt;/strong&gt;：保留顶级作用域的逻辑执行顺序&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;模拟机制&lt;/strong&gt;：模拟 CommonJS 的缓存和循环引用行为&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;3.2 转换示例&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;原始 CommonJS 模块：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// math.cjs
const PI = 3.14159;

function add(a, b) {
  return a + b;
}

function multiply(a, b) {
  return a * b;
}

module.exports = {
  add,
  multiply,
  PI
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;esbuild 转换后的 ESM 模块：&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// math.mjs (转换后)
var __getOwnPropNames = Object.getOwnPropertyNames;
var __commonJS = function(cb) { /* CommonJS 模拟函数 */ };

var require_math = __commonJS(function(exports, module) {
  const PI = 3.14159;
  
  function add(a, b) {
    return a + b;
  }
  
  function multiply(a, b) {
    return a * b;
  }
  
  module.exports = {
    add,
    multiply,
    PI
  };
});

// ESM 导出
export default require_math();
export const { add, multiply, PI } = require_math();
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3.3 处理复杂情况&lt;/h3&gt;
&lt;p&gt;esbuild 能够智能处理各种复杂场景：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;条件导出&lt;/strong&gt;：根据环境变量选择不同的导出内容&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;动态 require&lt;/strong&gt;：将函数内的 &lt;code&gt;require()&lt;/code&gt; 调用转换为动态 &lt;code&gt;import()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;循环依赖&lt;/strong&gt;：生成额外的代码来正确处理循环引用&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;混合使用&lt;/strong&gt;：处理模块中同时使用 CJS 和 ESM 语法的情况&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;四、实践建议与最佳实践&lt;/h2&gt;
&lt;h3&gt;4.1 对于库开发者&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;提供双模式导出&lt;/strong&gt;：在 &lt;code&gt;package.json&lt;/code&gt; 中同时指定 &lt;code&gt;main&lt;/code&gt; (CJS) 和 &lt;code&gt;module&lt;/code&gt; (ESM) 字段&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;使用导出映射&lt;/strong&gt;：利用 &lt;code&gt;exports&lt;/code&gt; 字段提供更精细的导出控制&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;避免混合使用&lt;/strong&gt;：在同一个库中尽量避免混合使用 CJS 和 ESM 语法&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code class=&quot;language-json&quot;&gt;{
  &quot;name&quot;: &quot;my-library&quot;,
  &quot;main&quot;: &quot;./dist/index.cjs&quot;,
  &quot;module&quot;: &quot;./dist/index.mjs&quot;,
  &quot;exports&quot;: {
    &quot;.&quot;: {
      &quot;import&quot;: &quot;./dist/index.mjs&quot;,
      &quot;require&quot;: &quot;./dist/index.cjs&quot;
    },
    &quot;./feature&quot;: {
      &quot;import&quot;: &quot;./dist/feature.mjs&quot;,
      &quot;require&quot;: &quot;./dist/feature.cjs&quot;
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.2 对于应用开发者&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;优先选择 ESM 包&lt;/strong&gt;：在选择依赖时优先考虑提供 ESM 版本的包&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;注意混合使用&lt;/strong&gt;：避免在同一个文件中混合使用 &lt;code&gt;import&lt;/code&gt; 和 &lt;code&gt;require&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;配置优化&lt;/strong&gt;：在必要时使用 &lt;code&gt;optimizeDeps.include&lt;/code&gt; 强制预构建特定依赖&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// vite.config.js
import { defineConfig } from &apos;vite&apos;;

export default defineConfig({
  optimizeDeps: {
    include: [&apos;some-cjs-package&apos;], // 强制预构建
    exclude: [&apos;some-esm-package&apos;] // 排除无需预构建的包
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.3 调试与故障排除&lt;/h3&gt;
&lt;p&gt;如果遇到 CommonJS 相关的问题：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;检查预构建结果&lt;/strong&gt;：查看 &lt;code&gt;node_modules/.vite/deps&lt;/code&gt; 目录中的转换结果&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;强制重新构建&lt;/strong&gt;：使用 &lt;code&gt;vite --force&lt;/code&gt; 命令强制重新预构建&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;手动排除问题&lt;/strong&gt;：使用 &lt;code&gt;optimizeDeps.exclude&lt;/code&gt; 排除有问题的包，然后手动处理&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;五、未来展望&lt;/h2&gt;
&lt;p&gt;随着 JavaScript 生态的发展，ESM 正在成为主流格式：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Node.js 的 ESM 支持&lt;/strong&gt;：新版 Node.js 不断改进对 ESM 的支持&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;工具链优化&lt;/strong&gt;：构建工具正在优化对混合代码库的处理&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;生态迁移&lt;/strong&gt;：越来越多的 npm 包提供纯 ESM 版本&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;然而，CommonJS 由于其在 npm 生态中的深厚基础，仍将在相当长的时间内继续存在。工具如 Vite 的价值就在于桥接这两个世界，让开发者能够平稳过渡。&lt;/p&gt;
&lt;h2&gt;结论&lt;/h2&gt;
&lt;p&gt;CommonJS 和 ESM 的差异反映了 JavaScript 生态系统的演进历程。虽然它们在设计理念和实现方式上有显著不同，但通过像 Vite 这样的现代构建工具，开发者可以几乎无感知地使用这两种模块系统。&lt;/p&gt;
&lt;p&gt;理解这两种格式的底层差异有助于我们编写更健壮的代码，做出更合理的架构决策，并更好地调试遇到的问题。随着生态系统的不断发展，ESM 终将成为主导，但 CommonJS 的遗产仍将在未来多年内影响着 JavaScript 的开发方式。&lt;/p&gt;
&lt;p&gt;Vite 通过其创新的预构建机制，不仅解决了技术兼容性问题，更为我们提供了一窥 JavaScript 模块系统未来发展的窗口。它提醒我们，良好的工具设计可以在尊重历史的同时，优雅地引领我们走向未来。&lt;/p&gt;</content:encoded><h:img src="/_astro/thumbnail.Couev2ys.png"/><enclosure url="/_astro/thumbnail.Couev2ys.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>Node.js模块导入路径别名方案</title><link>https://luopengcheng89.github.io/blog/nodejs_module_alias</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/nodejs_module_alias</guid><description>深入探讨了Node.js项目中路径别名导入方案的实现与优化。针对不使用构建打包器的传统Node.js项目（如CLI工具），介绍了三种主流路径别名解决方案：tsc-alias、tsconfig-paths和module-alias，并对其原理和使用场景进行了详细对比分析。</description><pubDate>Wed, 12 Jun 2024 14:46:42 GMT</pubDate><content:encoded>&lt;h2&gt;前言&lt;/h2&gt;
&lt;h3&gt;路径别名是什么&lt;/h3&gt;
&lt;p&gt;一般 &lt;code&gt;pnpm create vite&lt;/code&gt; 创建的前端项目都内置了对路径别名的支持，路径别名配置的写法：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;// vite.config.js 或 vite.config.ts
import { defineConfig } from &apos;vite&apos;;
import { fileURLToPath, URL } from &apos;url&apos;;

export default defineConfig({
  resolve: {
    alias: {
      &apos;@&apos;: fileURLToPath(new URL(&apos;./src&apos;, import.meta.url)),
    },
  },
});

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;在代码中可以使用 &lt;code&gt;@&lt;/code&gt; 前缀导入模块：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;// 使用 @ 别名
import MyComponent from &apos;@/components/MyComponent.vue&apos;;
import { formatDate } from &apos;@/utils/helper.js&apos;;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;对比未使用别名的用法：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;import MyComponent from &apos;../../../../components/MyComponent.vue&apos;;
import { formatDate } from &apos;../../../../utils/helper.js&apos;;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;导入路径&lt;/strong&gt; 别名可以彻底告别繁琐的  &lt;code&gt;../../&lt;/code&gt; ，让导入语句清晰易懂。&lt;/p&gt;
&lt;h3&gt;普通项目存在问题&lt;/h3&gt;
&lt;p&gt;当我们在开发&lt;strong&gt;不带编译打包器的项目&lt;/strong&gt;时，路径别名功能在 Node.js 下就不支持了。比如：下面使用 TypeScript 开发 CLI 工具项目，使用路径别名导入模块执行时报错。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/16b49128-fb55-4ef6-a1e2-a513a62994fc.BDFIwfCG_2h6TAk.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;本文会分析这一场景下路径别名导入功能的实现，介绍各实现的基本原理和用法。&lt;/p&gt;
&lt;h2&gt;最佳方案（tsc-alias）&lt;/h2&gt;
&lt;p&gt;目前找到的最佳方案是使用  &lt;a href=&quot;https://github.com/justkey007/tsc-alias.git&quot;&gt;&lt;code&gt;tsc-alias&lt;/code&gt;&lt;/a&gt;  来处理。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;tsc-alias&lt;/code&gt; 读取并解析编译后 JS 代码中的 &lt;code&gt;import&lt;/code&gt; 或 &lt;code&gt;require&lt;/code&gt; 模块导入写法，将路径别名替换为相对路径。&lt;/p&gt;
&lt;p&gt;需要增加一步编译流程：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-json&quot;&gt;// package.json
// 脚本名称可以随意，不需要叫做 build
{
    &quot;script&quot;: {
        &quot;build&quot;: &quot;tsc-alias -p tsconfig.json&quot;
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;TS 项目 &lt;code&gt;tsconfig.json&lt;/code&gt; 里面至少需要配置 &lt;code&gt;paths&lt;/code&gt; 和 &lt;code&gt;outDir&lt;/code&gt;，同样 JS 项目使用 &lt;code&gt;jsconfig.json&lt;/code&gt; 配置。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;paths&lt;/code&gt;: &lt;code&gt;tsc-alias&lt;/code&gt; 据此解析别名配置
&lt;code&gt;outDir&lt;/code&gt;：&lt;code&gt;tsc-alias&lt;/code&gt;据此查找需要转换的代码，在 TS 项目对应编译输出的代码路径&lt;/p&gt;
&lt;p&gt;完整代码 demo 在&lt;a href=&quot;https://github.com/JohnieXu/demo/tree/next/tsconfig-paths&quot;&gt;这里&lt;/a&gt;可以找到，一个真实项目 &lt;a href=&quot;https://github.com/JohnieXu/public_actions&quot;&gt;JohnieXu/public_actions&lt;/a&gt;。&lt;/p&gt;
&lt;h2&gt;其他方案分析&lt;/h2&gt;
&lt;p&gt;路径别名的本质是&lt;strong&gt;通过别名到真实路径的映射查找出真实文件路径的过程&lt;/strong&gt;。Webpack、Rollup 等打包工具各自实现了这个转换的过程，但在&lt;strong&gt;原生 Node.js 中不支持通过路径别名查找模块&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;要 Node.js 支持路径别名，有两种实现思路：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;运行时&lt;/strong&gt;：修改 require 的运行时实现，将别名转换为真实文件路径再 require&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;编译时&lt;/strong&gt;：本地编译构建时先把别名映射转换到真实文件路径，require 中使用真实文件路径（相对路径）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;基于这个思路，有下面三种具体实现。&lt;/p&gt;
&lt;h2&gt;tsconfig-paths&lt;/h2&gt;
&lt;p&gt;https://github.com/jonaskello/tsconfig-paths&lt;/p&gt;
&lt;h3&gt;实现原理&lt;/h3&gt;
&lt;p&gt;修改 Node.js 内置模块 &lt;code&gt;module&lt;/code&gt; 的静态方法 &lt;code&gt;Module._resolveFilename&lt;/code&gt;，用 require 导入模块时会走到这个方法。&lt;/p&gt;
&lt;p&gt;依次判断导入的模块是否内置模块、是否匹配上别名，匹配上别名则转换为对应的相对路径，再交给 Node.js 处理。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/d9230bbf-684f-4dce-b59d-5ac697d244eb.N5a_3DrD_1cHxIU.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;使用方法&lt;/h3&gt;
&lt;p&gt;在入口 JS 文件顶部引入  &lt;code&gt;require(&apos;tsconfig-paths/register&apos;)&lt;/code&gt;  ，TS 源代码和编译后的 JS 代码还是保持别名导入。&lt;/p&gt;
&lt;p&gt;源代码：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;import { log } from &quot;@@utils/index.js&quot;

function main() {
  log(&apos;home&apos;)
}

main()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;编译之后：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;&quot;use strict&quot;;
Object.defineProperty(exports, &quot;__esModule&quot;, { value: true });
const index_js_1 = require(&quot;@@utils/index.js&quot;);
function main() {
    (0, index_js_1.log)(&apos;home&apos;);
}
main();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/e670cb74-37dd-464e-a741-a9675782650.BKxohU9B_WEIcb.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;module-alias&lt;/h2&gt;
&lt;p&gt;https://github.com/ilearnio/module-alias&lt;/p&gt;
&lt;h3&gt;实现原理&lt;/h3&gt;
&lt;p&gt;和 tsconfig-paths 实现相似，都是对 &lt;code&gt;Module._resolveFilename&lt;/code&gt; 方法进行 hook，以实现在 require 之前根据别名配置转换路径。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/deb12fa2-9066-4ad7-9691-1982cac517e9.BWQnIWvY_Z2aKssr.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;不过，这里别名配置的解析和 tsconfig-paths 不同，是从 &lt;code&gt;pacakge.json&lt;/code&gt; 的 &lt;code&gt;_moduleAliases&lt;/code&gt; 配置项解析的。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/efb19602-2dbe-4358-a24f-f1abceda97cb.C28e1m8K_2o7pgh.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;另外，&lt;strong&gt;自定义模块目录&lt;/strong&gt;是通过对 &lt;code&gt;Module._nodeModulePath&lt;/code&gt; 方法 hook 来实现的。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/2dd9c808-7049-4d31-a05a-14187af16aff.Q49nfQDc_VTINJ.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;使用方法&lt;/h3&gt;
&lt;p&gt;在  &lt;code&gt;package.json&lt;/code&gt;  中添加路径别名的配置，需要和  &lt;code&gt;tsconfig.json&lt;/code&gt;  中的  &lt;code&gt;paths&lt;/code&gt;  配置保持一致。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-json&quot;&gt;{
    &quot;_moduleAliases&quot;: {
        &quot;@@root&quot;: &quot;.&quot;,
        &quot;@@utils&quot;: &quot;utils&quot;
     }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;然后在入口 JS 文件顶部执行  &lt;code&gt;require(&apos;module-alias/register&apos;)&lt;/code&gt; ，TS 源代码和编译后的 JS 代码还是保持别名导入。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;自定义模块目录&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;还支持配置自定义的模块目录，作用和 &lt;code&gt;node_modules&lt;/code&gt; 一样，例如：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-json&quot;&gt;{
    &quot;_moduleDirectories&quot;: [&quot;miniprogram_dist&quot;, &quot;uni_modules&quot;]
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;可以将私有的 npm 包安装到这些目录中，在代码中使用包名直接引入。&lt;/p&gt;
&lt;h2&gt;tsc-alias&lt;/h2&gt;
&lt;p&gt;https://github.com/justkey007/tsc-alias&lt;/p&gt;
&lt;h3&gt;基本原理&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;tsc-alias&lt;/code&gt; 通过读取解析编译后 JS 代码中的 &lt;code&gt;import&lt;/code&gt; &lt;code&gt;require&lt;/code&gt; 写法，将路径别名替换为相对路径，编译输出新代码。&lt;/p&gt;
&lt;p&gt;替换的过程采用正则表达式处理，没有使用复杂的 AST 解析，也不会对代码做任何压缩、编译、poflyfill 等其他操作。&lt;/p&gt;
&lt;h3&gt;使用方法&lt;/h3&gt;
&lt;p&gt;修改构建脚本，在 &lt;code&gt;tsc&lt;/code&gt; 编译后执行  &lt;code&gt;tsc-alias -p tsconfig.json&lt;/code&gt;  。TS 配置项的 paths 对应所有可用的别名。&lt;/p&gt;
&lt;p&gt;TS 配置（tsconfig.json）：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-json&quot;&gt;{
    &quot;compilerOptions&quot;: {
        &quot;paths&quot;: {
            &quot;@@utils/*&quot;: [&quot;./src/utils/*&quot;]
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源代码：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-ts&quot;&gt;import { log } from &quot;@@utils/index.js&quot;

function main() {
  log(&apos;home&apos;)
}

main()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;编译之后：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;import { log } from &quot;../utils/index.js&quot;;
function main() {
    log(&apos;home&apos;);
}
main();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码使用 &lt;code&gt;require(&apos;@@utils/index.js&apos;)&lt;/code&gt; 语法也支持。&lt;/p&gt;
&lt;h2&gt;总结&lt;/h2&gt;
&lt;p&gt;在基于 Node.js 运行的CLI、后端服务等项目，由于没有采用构建打包工具，对路径别名导入的支持需要第三方库实现。实现方案有文中提及的多种，比较推荐的是 &lt;code&gt;tsc-alias&lt;/code&gt; 实现，没有运行时代码。&lt;/p&gt;
&lt;p&gt;最后，整理了三者横向对比，供大家查阅。&lt;/p&gt;
&lt;p&gt;| 功能          | tsconfig-paths                                                                                                                                | module-alias                                                                                                               | tsc-alias                                                           |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 解决阶段        | 运行时 (Runtime)                                                                                                                                 | 运行时 (Runtime)                                                                                                              | 编译后/构建时 (Post-compile)                                              |
| 核心机制        | 动态修改 Node.js 模块加载。通过 require 钩子（Hook）修补 Node.js 的 Module._resolveFilename 方法。                                                                 | 动态修改 Node.js 模块加载。通过修补 Node.js 的内部方法 Module._resolveFilename 和 Module._nodeModulePaths 来注册别名。                              | 静态重写 JS 文件内容。在 tsc 编译完成后，将编译出的 JavaScript 文件中的别名导入路径替换为运行时可识别的相对路径。 |
| 配置来源        | 直接读取 tsconfig.json 或 jsconfig.json 中的 paths。                                                                                                  | 读取 package.json 中的 _moduleAliases 字段。也可以通过编程方式配置。                                                                          | 读取 tsconfig.json 或 jsconfig.json 中的 paths。                          |
| 集成方式        | 在应用的主文件最顶部调用 require(&apos;tsconfig-paths/register&apos;)。通过 Node.js 的 -r (require) 选项进行注册：node -r tsconfig-paths/register main.js。或与 ts-node 一起使用。 | 在应用的主文件最顶部调用 require(&apos;module-alias/register&apos;)。或通过 Node.js 的 -r (require) 选项进行注册：node -r module-alias/register main.js。 | 作为构建流程的后处理步骤运行：tsc-alias -p tsconfig.json。                          |
| 对TS配置的依赖    | 强依赖：旨在实现 tsconfig.json paths 的运行时功能。                                                                                                          | 不依赖：使用自己的配置格式，与 tsconfig.json 的 paths 字段不直接关联。                                                                             | 强依赖：用于处理 tsconfig.json paths 编译后的结果。                                |
| 已知限制        | 与 TypeScript 路径配置保持同步，是 Node.js 环境下路径别名最常用的运行时解决方案。                                                                                           | 通用性高，允许注册自定义模块目录（类似于 node_modules）。                                                                                        | 编译时完成替换，没有运行时依赖。生成的 JS 文件是“纯净”的相对路径，更适用于最终部署环境。                     |
| 与Webpack的关系 | 不直接兼容，对于 Webpack，官方推荐使用配套的 tsconfig-paths-webpack-plugin。                                                                                     | 不兼容：明确指出设计用于服务器端，不适用于前端 WebPack 项目；WebPack 建议使用其内置的 resolve.alias 机制。                                                      | 兼容：由于输出是纯 JS，WebPack 无需特殊插件即可打包。                                    |
| 模块格式支持      | 支持 CJS，不支持 ESM                                                                                                                                | 支持 CJS，不支持 ESM                                                                                                             | ESM/CJS 都支持                                                         |&lt;/p&gt;
&lt;h2&gt;参考资料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;https://stackoverflow.com/questions/58809944/cannot-find-module-typescript-path-alias-error&lt;/li&gt;
&lt;li&gt;https://www.geeksforgeeks.org/node-js/how-to-have-path-alias-in-node-js/&lt;/li&gt;
&lt;li&gt;https://stackoverflow.com/questions/76765069/node-js-typescript-how-to-setup-path-alias&lt;/li&gt;
&lt;li&gt;https://www.typescriptlang.org/tsconfig/#paths&lt;/li&gt;
&lt;/ul&gt;</content:encoded><h:img src="/_astro/thumbnail.BpdVNfVk.png"/><enclosure url="/_astro/thumbnail.BpdVNfVk.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>GitHub双重验证（2FA）免手机号验证方案</title><link>https://luopengcheng89.github.io/blog/github_2fa_solution</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/github_2fa_solution</guid><description>提供两种GitHub双重验证（2FA）免手机号验证方案。</description><pubDate>Wed, 25 Oct 2023 18:43:53 GMT</pubDate><content:encoded>&lt;h2&gt;背景&lt;/h2&gt;
&lt;p&gt;GitHub 开始逐步要求账号登录时启用&lt;strong&gt;双重验证（2FA）&lt;/strong&gt;，并且在 2023 年底过后，&lt;strong&gt;没有启用双重验证的账号将不能使用&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/71b1f8af-152e-4485-ac85-0baa6cefcedf.CDvNyWZE_2stgPw.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;看来，启用双重验证势在必行，但是在启用双重验证的过程中&lt;strong&gt;不支持国内的手机号&lt;/strong&gt;来接收验证码 : (。&lt;/p&gt;
&lt;p&gt;经过一番尝试，终于找到了两种解决方法，见下文。&lt;/p&gt;
&lt;h2&gt;解决方法&lt;/h2&gt;
&lt;p&gt;按照官方文档说明，要启用双重验证方法有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;GitHub APP&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;短信验证码&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;TOTP应用&lt;/strong&gt;的一次性验证码&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;GitHub APP 在国内应用商店无法下载，同时国内手机号无法接收短信验证码，所以我们使用[[TOTP应用]]接收一次性验证码来解决。&lt;/p&gt;
&lt;p&gt;并且，使用TOTP应用接收验证码方法是&lt;strong&gt;完全免费&lt;/strong&gt;的。&lt;/p&gt;
&lt;h3&gt;方法一：微软Authenticator应用接收验证码&lt;/h3&gt;
&lt;p&gt;微软的&lt;a href=&quot;https://support.microsoft.com/zh-cn/account-billing/%E4%B8%8B%E8%BD%BD%E5%B9%B6%E5%AE%89%E8%A3%85microsoft-authenticator%E5%BA%94%E7%94%A8-351498fc-850a-45da-b7b6-27e523b8702a&quot;&gt;Authenticator&lt;/a&gt;应用就是一个TOTP应用，支持Android、iOS系统，国内应用商店均可下载。
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/14dab25c-e076-407d-a49e-4292dd956bd1.Dp43sNxD_Z2qWh0o.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;安装后初次打开出现报错：“由于 Google Pay 服务当前在此设备上不可用，因此无法使用一些功能”，可以直接忽略。&lt;/p&gt;
&lt;p&gt;在 GitHub 网页上 &lt;code&gt;首页&gt;个人头像&gt;Settings&gt;Password and authentication&gt;Two-factor authentication&lt;/code&gt; 位置找到启用双重验证的入口。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/cff5a093-b424-462e-b325-70d48da91dfc.Bs7MitwL_Z2nUqQ6.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/c0784b48-c4c1-4dcb-be25-fd31d450edc2.CHDJxXt0_1JNvm5.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;打开微软 Authenticator 应用，点击右上角“+”选择“其他账户(Google、Facebook 等)”唤起相机扫码，扫描上面页面的二维码，成功后就在微软 Authenticator 应用上&lt;strong&gt;添加了&lt;/strong&gt;你的 GitHub 账号。&lt;/p&gt;
&lt;p&gt;在微软 Authenticator 应用点击刚添加的应用，生成一个&lt;strong&gt;一次密码代码&lt;/strong&gt;，在上面页面输入这个验证码，验证通过后就将 GitHub 账号&lt;strong&gt;绑定&lt;/strong&gt;到了微软 Authenticator 应用了。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;一定注意：上面这两步操作时一定不要刷新上图这个网页&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;绑定成功会到这个页面。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/7329fd28-449d-4c65-a245-5f90cea78dac.BQonHUzp_2909j3.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;GitHub 给出了一个&lt;strong&gt;Recovery Codes&lt;/strong&gt; 的文件（类似于私钥，一定要保存好）。&lt;/p&gt;
&lt;p&gt;点击“I have saved my recovery codes”，跳转到绑定成功页面。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/4824e705-8b16-4d73-bf9a-a6dc3b8f2fd8.tuZl3SYS_U1fg7.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;后面在登录 GitHub 时若出现要填写验证码的地方，输入手机上微软 Authenticator 应用生成的验证码即可。&lt;/p&gt;
&lt;h3&gt;方法二：Chrome/Edge插件接收验证码&lt;/h3&gt;
&lt;p&gt;Chrome扩展：&lt;a href=&quot;https://chrome.google.com/webstore/detail/authenticator/bhghoamapcdpbohphigoooaddinpkbai&quot;&gt;身份验证器 - Chrome 应用商店 (google.com)&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Edge扩展：&lt;a href=&quot;https://microsoftedge.microsoft.com/addons/detail/authenticator-2fa-client/ocglkepbibnalbgmbachknglpdipeoio&quot;&gt;Authenticator: 2FA Client - Microsoft Edge Addons&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;根据浏览器自行安装对应版本插件，这个插件相当于一个简洁版的微软 Authenticator 应用。&lt;/p&gt;
&lt;p&gt;按照&lt;strong&gt;方法一&lt;/strong&gt;打开 GitHub 的启用双重验证页面，然后点击插件弹窗右上角的扫码按钮，拖拽框选二维码，即可&lt;strong&gt;添加&lt;/strong&gt; GitHub 账号，然后在网页验证码窗口输入插件给出的一次性验证码，即可完成&lt;strong&gt;绑定&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/8cb9754e-0418-4c4d-b117-6fe3fd0dabe5.D01R0_TM_Z1uM4Nn.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;绑定成功 GitHub 同样会给出一个&lt;strong&gt;Recovery Codes&lt;/strong&gt;的文件（类似于私钥，一定要保存好）。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/4824e705-8b16-4d73-bf9a-a6dc3b8f2fd8.tuZl3SYS_U1fg7.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;看到上面页面表示&lt;strong&gt;绑定成功&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;后面在登录 GitHub 时若出现要填写验证码的地方，填写这个插件自动生成的验证码即可。
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/bfe9a076-27fe-4a44-b20d-6510e2356f18.mFSboi2h_tld6v.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;恢复双重验证&lt;/h2&gt;
&lt;p&gt;如果微软 Authenticator 应用、浏览器插件中的 GitHub 账号被删掉了，或者应用、插件本身被卸载了，则需要进行这一步操作来&lt;strong&gt;重新启用双重验证&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;从 &lt;strong&gt;Recovery Codes&lt;/strong&gt; 文件随便复制一行，登录进GitHub。
![[Pasted image 20231024190647.png]]
进入设置页面，点击 “Edit”，重新添加、绑定验证码生成设备（微软 Authenticator 应用或者浏览器插件），步骤和&lt;strong&gt;解决方法&lt;/strong&gt;中完全一样。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/5431ce20-acdb-43ee-a9d3-9dc9cd514f6c.DViM9ARD_ZjxHs.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;重新添加、绑定验证码生成设备之后，&lt;strong&gt;Recovery Codes&lt;/strong&gt; 文件和之前一样不会重新生成。&lt;/p&gt;
&lt;h2&gt;参考资料&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;https://docs.github.com/zh/authentication/securing-your-account-with-two-factor-authentication-2fa/configuring-two-factor-authentication&lt;/li&gt;
&lt;li&gt;https://support.microsoft.com/zh-cn/account-billing/%E4%B8%8B%E8%BD%BD%E5%B9%B6%E5%AE%89%E8%A3%85microsoft-authenticator%E5%BA%94%E7%94%A8-351498fc-850a-45da-b7b6-27e523b8702a&lt;/li&gt;
&lt;li&gt;https://github.blog/2023-03-09-raising-the-bar-for-software-security-github-2fa-begins-march-13/&lt;/li&gt;
&lt;/ul&gt;</content:encoded><h:img src="/_astro/thumbnail.Pin7LPNg.png"/><enclosure url="/_astro/thumbnail.Pin7LPNg.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>写一个 CLI 工具抓取奇舞周刊文章链接</title><link>https://luopengcheng89.github.io/blog/75_action</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/75_action</guid><description>本文介绍基于Node.js开发的CLI工具75_action，用于解决奇舞周刊文章阅读不便问题。核心功能包括抓取全部文章链接、随机返回N篇文章、本地缓存（24小时有效期）及定时抓取（GitHub Actions）。</description><pubDate>Tue, 19 Sep 2023 15:18:18 GMT</pubDate><content:encoded>&lt;h2&gt;引言&lt;/h2&gt;
&lt;p&gt;前端er 应该都知道奇舞周刊，这是一个技术类博客，上面汇聚了一大批优秀博客作者投稿的技术类文章。我本人每隔几天就会去看看上面的文章，但是它的官网经常打不开，每次想看文章的时候还得一页一页的翻才能找到想看的文章。或者，有时候就想随便找一篇文章看一看作为知识扩展或是温故知新。&lt;/p&gt;
&lt;p&gt;基于阅读文章的便利性的考虑，于是开始了本次 CLI 工具的探索，这款工具的核心功能是以开发者的角度来快速的找出奇舞周刊上发布的文章链接。&lt;/p&gt;
&lt;h2&gt;主要功能&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;抓取全部文章链接&lt;/li&gt;
&lt;li&gt;随机 N 篇文章链接&lt;/li&gt;
&lt;li&gt;定时任务自动抓取&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;抓取全部文章链接&lt;/h3&gt;
&lt;p&gt;这个功能目的主要是抓取文章链接数据，为CLI 工具开发提供数据支撑，以及为后续开发关键词检索、文章内容爬取、文章推荐等功能做铺垫。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx 75_action fetch
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;文章数据本地缓存&lt;/h3&gt;
&lt;p&gt;作为命令行工具使用时，从官网抓取数据的过程实测会消耗 20s+ 的时间，因此采用本地文件来缓存已经抓取的文章数据。&lt;code&gt;npx 75_action random &amp;#x3C;N&gt;&lt;/code&gt; 命令执行完成后会自定缓存抓取数据到本地，缓存有效期为 24h。&lt;/p&gt;
&lt;h3&gt;随机 N 篇文章链接&lt;/h3&gt;
&lt;p&gt;CLI 工具主要功能之一，运行命令即可随机返回 N 篇文章的数据。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx 75_action random &amp;#x3C;N&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;定时任务自动抓取&lt;/h3&gt;
&lt;p&gt;借助 Github Actions 配置定时任务，每天0 8 16点执行自定执行[[#抓取全部文章链接]]任务，并将抓取到的文章数据打包上传至 GitHub 可供下载使用。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/33d0fb86dfe9563e295e8d421f3d535c.Cn9u-C1g_ZK4MGS.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;方案设计&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;获取文章数据&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/7d617548894f165ebeef138fad6b8ea6.BcCAApLt_Z1DMpR8.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;ol start=&quot;2&quot;&gt;
&lt;li&gt;CLI 工具&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/thumbnail.B0zSw5Wg_Z2bAHMs.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;ol start=&quot;3&quot;&gt;
&lt;li&gt;缓存策略&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/a0eb860b15eab6acf71ded08d51872ed.8n_j9CGE_oDne7.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;功能实现&lt;/h2&gt;
&lt;h3&gt;文章数据抓取&lt;/h3&gt;
&lt;p&gt;对应源码在这里查看：&lt;a href=&quot;https://github.com/JohnieXu/75_action/blob/master/lib/fetch.js&quot;&gt;https://github.com/JohnieXu/75_action/blob/master/lib/fetch.js&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;抓取奇舞周刊官网首页 HTML 并解析出文章集合数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function getCollections() {
  return fetch(homeUrl)
  .then(res =&gt; res.text())
  .then(res =&gt; {
    if (!res) {
      return Promise.reject(new Error(&apos;获取网页内容失败&apos;))
    }
    return cheerio.load(res)
  })
  .then($ =&gt; {
    const list = $(&apos;ol.issue-list &gt; li&apos;)
    const collections = list.map((i, l) =&gt; {
      const title = $(l).find(&apos;a&apos;).attr(&apos;title&apos;)
      const url = $(l).find(&apos;a&apos;).attr(&apos;href&apos;)
      const date = $(l).find(&apos;.date&apos;).attr(&apos;datetime&apos;)
      return { title, url, date }
    })
    return collections
  })
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;抓取集合 URL 页面的 HTML 并解析出集合下文章数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function getArticleDoc(url) {
  return fetch(homeUrl + url)
  .then(res =&gt; res.text())
  .then(res =&gt; {
    if (!res) {
      return Promise.reject(new Error(&quot;获取网页内容失败&quot;))
    }
    return cheerio.load(res)
  })
}

function getArticles(doc) {
  const $ = doc
  const el = $(&apos;ul &gt; li.article&apos;)
  const list = el.map((i, l) =&gt; {
    return {
      title: $(l).find(&apos;h3.title &gt; a&apos;).text(),
      url: $(l).find(&apos;h3.title &gt; a&apos;).attr(&apos;href&apos;),
      desc: $(l).find(&apos;.desc&apos;).text()
    }
  })
  return list
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;整合文章数据并排序输出&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;getArticleDoc(url).then(getArticles).then(list =&gt; list.map((_, item) =&gt; ({ ...item, issue: title, date }))).then(list =&gt; {
  all = [...all, ...list]
}) // 整合文章数据

all = all.sort((a, b) =&gt; b.date.localeCompare(a.date)) // date倒序排列
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;文章的 date 字段是对应集合的发布日期（期刊日期），例如：&lt;code&gt;2021-12-17&lt;/code&gt;，需要进行日期倒序排列，使用 &lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/String/localeCompare&quot;&gt;String.prototype.localeCompare()&lt;/a&gt;  进行字符串排序。&lt;/p&gt;
&lt;h3&gt;文章数据缓存&lt;/h3&gt;
&lt;p&gt;对应源码在这里查看：&lt;a href=&quot;https://github.com/JohnieXu/75_action/blob/master/lib/file.js&quot;&gt;https://github.com/JohnieXu/75_action/blob/master/lib/file.js&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;缓存文件及有效期&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;const CACHE_FILE = &apos;./.75_action/.data.json&apos;
const CACHE_TIME = 1000 * 60 * 60 * 24; // 缓存24h
const homeDir = require(&apos;os&apos;).homedir()
const p = path.resolve(homeDir, CACHE_FILE) // 缓存文件路径在用户家目录
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;读取缓存文件的修改时间判断是否过期（不存在缓存文件也算缓存过期）&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function isCacheOutDate() {
  const p = path.resolve(require(&apos;os&apos;).homedir(), CACHE_FILE)
  if (!fs.existsSync(p)) {
    return true
  }
  const stat = fs.statSync(p)
  const lastModified = stat.mtime
  const now = new Date()
  return now - lastModified &gt;= CACHE_TIME
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;未过期则读取缓存文件作为抓取到的文章数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function getHomeFileJson() {
  const homeDir = require(&apos;os&apos;).homedir()
  const p = path.resolve(homeDir, CACHE_FILE)
  const jsonStr = fs.readFileSync(p)
  let json
  try {
    json = JSON.parse(jsonStr)
  } catch(e) {
    console.error(e)
    json = []
  }
  return json
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;抓取到文章数据后写入本地缓存&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function writeFileToHome(json) {
  const homeDir = require(&apos;os&apos;).homedir()
  const p = path.resolve(homeDir, CACHE_FILE) // 写入路径为用户家目录
  return mkdirp(path.dirname(p)).then(() =&gt; {
    fs.writeFileSync(p, JSON.stringify(json, null, 2)) // 使用 JSON 格式序列化
  })
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;CLI 工具开发&lt;/h3&gt;
&lt;h4&gt;配置 bin 入口&lt;/h4&gt;
&lt;p&gt;运行 &lt;code&gt;npx 75_action&lt;/code&gt; 命令即使用 Node.js 执行此处指向的 &lt;code&gt;75_action.js&lt;/code&gt; 脚本&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-json&quot;&gt;{
  &quot;bin&quot;: {
    &quot;75_action&quot;: &quot;bin/75_action.js&quot;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;指向的脚本文件源码在这里查看：&lt;a href=&quot;https://github.com/JohnieXu/75_action/blob/master/bin/75_action.js&quot;&gt;https://github.com/JohnieXu/75_action/blob/master/bin/75_action.js&lt;/a&gt;&lt;/p&gt;
&lt;h4&gt;令行参数&lt;/h4&gt;
&lt;blockquote&gt;
&lt;p&gt;使用 commander 库注册 CLI 命令和解析命令参数&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;const program = require(&apos;commander&apos;)

// 注册命令
program.command(&apos;random [number]&apos;)
       .description(&apos;随机获取n篇文章链接&apos;)
       .option(&apos;-d, --debug&apos;, &apos;开启debug模式&apos;)
       .action((number, options) =&gt; {
         number = number || 1
         if (options.debug) {
           console.log(number, options)
         }
         fetch({ save: &apos;home&apos;, progress: true }).then(({ collections, articles }) =&gt; {
           const selected = random(number, articles)
           console.log(JSON.stringify(selected, null, 2))
           process.exit()
         }).catch((e) =&gt; {
           console.log(e)
           process.exit(1)
         })
       })

program.command(&apos;fetch&apos;)
       .description(&apos;重新抓取文章链接&apos;)
       .option(&apos;-d, --debug&apos;, &apos;开启debug模式&apos;)
       .action((options) =&gt; {
          if (options.debug) {
            console.log(options)
          }
          fetch({ save: &apos;home&apos;, progress: true, reload: true }).then(({ collections, articles }) =&gt; {
            console.log(`抓取完成，总共${collections.length}个集合，${articles.length}篇文章`)
            process.exit()
          })
       })

program.parse(process.argv)
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;命令行进度条&lt;/h4&gt;
&lt;blockquote&gt;
&lt;p&gt;使用 &lt;code&gt;cli-progress&lt;/code&gt; 库实现命令行进度条效果&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/e7783ce7b33058328fed0ffdd7837906._tF6kksS_Z17HHpl.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;const cliProgress = require(&apos;cli-progress&apos;)
const bar1 = new cliProgress.SingleBar({}, cliProgress.Presets.shades_classic)
bar1.start(collections.length, 0) // 文章集合数设置为进度总值
bar1.update(doneLen)              // 抓取完成任一集合文章后更新进度条
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;定时抓取数据&lt;/h3&gt;
&lt;p&gt;此功能使用 GitHub Actions 自动执行定时任务，在项目中添加对应的 yml 配置文件即可，对应源码在这里查看：&lt;a href=&quot;https://github.com/JohnieXu/75_action/blob/master/.github/workflows/fetch.yml&quot;&gt;https://github.com/JohnieXu/75_action/blob/master/.github/workflows/fetch.yml&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yml&quot;&gt;name: FETCH
on:
  push:
    branches:
      - master
  schedule:
    - cron: &quot;0 0,8,16 * * *&quot; # 每天0 8 16点执行（有8小时时差）

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
        cache: &apos;npm&apos;
    - run: npm i -g yarn
    - run: yarn
    - run: node index.js
    - name: Save
      uses: actions/upload-artifact@v2
      with:
        path: data.json
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;使用 &lt;code&gt;actions/checkout&lt;/code&gt; 克隆仓库源码，使用 &lt;code&gt;actions/setup-node&lt;/code&gt; 来切换 Node.js 版本为 16.X，最后使用 &lt;code&gt;actions/upload-artifact&lt;/code&gt; 将执行 &lt;code&gt;node index.js&lt;/code&gt; 命令生成的 &lt;code&gt;data.json&lt;/code&gt; 文件打包输出上传至  GitHub。&lt;/p&gt;
&lt;p&gt;执行效果&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/9f9a66da9bce88f0c073c03a5c86b4bb.uIbdmfgs_dre4p.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;Npm 包发布&lt;/h3&gt;
&lt;p&gt;要确保此项目支持命令 &lt;code&gt;npx 75_action&lt;/code&gt; 执行，需要将此项目发布到 npm 官方仓库下，项目名称为 &lt;code&gt;75_action&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;发布流程如下（部分命令根据实际情况选择） ，其中 &lt;code&gt;nrm&lt;/code&gt; 用法可在这里查看：&lt;a href=&quot;https://www.npmjs.com/package/nrm&quot;&gt;https://www.npmjs.com/package/nrm&lt;/a&gt; 。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;nrm use npm # 切换 npm 源为官方
npm login	   # 登录 npm 账号
npm run publish# 发布
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;成品展示&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;以下命令均在终端执行，并且需要 Node.js 版本至少 10.X 及以上，并且终端内 node、npx 命令可正常使用&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;随机一篇文章&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx 75_action random
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;随机 5 篇文章&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx 75_action random 5
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;随机 N 篇文章（N 为正整数）&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx 75_action random N
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;抓取并更新本地文章数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx 75_action fetch
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;结语&lt;/h2&gt;
&lt;p&gt;本文实现了一个用于抓取奇舞周刊文章标题、描述及文章原始链接的 CLI 工具，该工具依托于 Node.js 运行。基本满足了快速获取奇舞周刊文章链接的需求，同时文章数据还能够缓存在本地，有效提升了使用体验。另外，还有一些进阶功能未开发，比如：&lt;strong&gt;根据文章标题进行关键词搜索，返回最新一期文章集合，根据文章标题进行分类，文章链接有效性检测&lt;/strong&gt;等。&lt;/p&gt;
&lt;p&gt;上述这些未开发的功能后续会视情况陆续进行开发，也欢迎各位关注此项目的后续进展，项目地址在这里：&lt;a href=&quot;https://github.com/JohnieXu/75_action&quot;&gt;https://github.com/JohnieXu/75_action&lt;/a&gt; 。&lt;/p&gt;
&lt;h2&gt;参考资料&lt;/h2&gt;
&lt;p&gt;[1] &lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/String/localeCompare&quot;&gt;String.prototype.localeCompare()&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;[2] &lt;a href=&quot;https://github.com/cheeriojs/cheerio/wiki/Chinese-README&quot;&gt;cheerio 使用文档&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;[3] &lt;a href=&quot;https://github.com/tj/commander.js/blob/master/Readme_zh-CN.md&quot;&gt;commander 使用文档&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;[4] &lt;a href=&quot;https://github.com/npkgz/cli-progress#cli-progress&quot;&gt;cli-progress 使用文档&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;[5] &lt;a href=&quot;https://docs.github.com/cn/actions/quickstart&quot;&gt;GitHub Actions 使用教程&lt;/a&gt;&lt;/p&gt;</content:encoded><h:img src="/_astro/thumbnail.B0zSw5Wg.png"/><enclosure url="/_astro/thumbnail.B0zSw5Wg.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>纯前端实现图片色卡提取功能</title><link>https://luopengcheng89.github.io/blog/dominant_color_in_frontend</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/dominant_color_in_frontend</guid><description>本文使用 Go 打包输出了一份 Wasm 二进制文件，前端只需要初始化 Wasm 实例调用其挂载到 window 对象上的方法即可，不需要服务端提供任何接口、也不需要部署 Go 程序。</description><pubDate>Sat, 09 Sep 2023 17:18:31 GMT</pubDate><content:encoded>&lt;h2&gt;前言&lt;/h2&gt;
&lt;p&gt;某天，我在使用 PS 时接触到一个提取色卡的插件。突然间很好奇这个插件是如何实现这个功能的呢？经过一番折腾，最终我成功地使用纯客户端技术实现了提取色卡的功能。&lt;/p&gt;
&lt;p&gt;色卡提取如下图所示：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/9e864e28b4d72b3cdc24f58106e01889.CcVb0h64_1jvCk2.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;上图框选的部分正是色卡，是根据选择的图片实时分析计算得出的，色卡数据会随着图片编辑、修改而更新。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/b271aa40be1008ce5dcfd51d790b681a.D1ZXYaWf_lgMv1.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;实现的简单 demo 效果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/e9aaefd491c02f0f8379114df531d087.BdF08nM4_2nx1UW.webp&quot; alt=&quot;image&quot;&gt;
通过【选择文件】选择本地的图片后，点击【提取主色调】会计算出图片中像素点占比最多的颜色值（即：&lt;strong&gt;主色调&lt;/strong&gt;），并其显示出来。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/596682075b1813670cde5759c4928430.D88FZ3nc_19aAMo.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;点击【提取色卡】，则会分析图片中像素点占比排名靠前的多个颜色值（即：&lt;strong&gt;色卡&lt;/strong&gt;），并将其显示出来。&lt;/p&gt;
&lt;p&gt;在线预览地址：&lt;a href=&quot;https://dominant-color.surge.sh/&quot;&gt;https://dominant-color.surge.sh/&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;主要思路&lt;/h2&gt;
&lt;p&gt;本次考虑采用 &lt;code&gt;Go + WebAssembly + JS&lt;/code&gt; 实现这个功能，关键流程如下图：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/58bb591dcab84ef27a8e41fd6f66000d.DdtnB_xf_oRzX6.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;WebAssembly 简介：&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;下文中简称为：&lt;strong&gt;Wasm&lt;/strong&gt;，是 Go 部分编译输出的一个二进制模块，在 JS 环境中运行，Go 部分实现的功能最终是通过 Wasm 在浏览器中运行的。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;JS 部分主要负责：&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Wasm 实例的初始化&lt;/li&gt;
&lt;li&gt;图片文件的选择、转换为 ArrayBuffer 传输给 Wasm 实例&lt;/li&gt;
&lt;li&gt;回显 Wasm 计算得到的色值&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Go 部分主要负责：&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;编译 Go 代码并输出 Wasm 模块&lt;/li&gt;
&lt;li&gt;接收 JS 传递的 ArrayBuffer 数据&lt;/li&gt;
&lt;li&gt;通过算法分析 ArrayBuffer 数据中色彩的权重信息&lt;/li&gt;
&lt;li&gt;计算主色调值的字符串传递给 JS（提取主色调）&lt;/li&gt;
&lt;li&gt;计算色卡颜色值的数组传递给 JS（提取色卡）&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;其中，关键逻辑的实现见下文分析。&lt;/p&gt;
&lt;h2&gt;关键步骤实现&lt;/h2&gt;
&lt;h3&gt;1.File 转为 ArrayBuffer&lt;/h3&gt;
&lt;p&gt;使用 HTML5 的 &lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/Web/API/FileReader&quot;&gt;FileReader API&lt;/a&gt; 读取 &lt;code&gt;Input&lt;/code&gt; 控件选择的 &lt;code&gt;File&lt;/code&gt; 文件对象为 &lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/ArrayBuffer&quot;&gt;ArrayBuffer&lt;/a&gt; 对象。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;code&gt;input&lt;/code&gt; 控件选择文件比较简单，其实现略过&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;转换方法封装如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function fileToArrayBuffer(file) {
  return new Promise((resolve, reject) =&gt; {
    const reader = new FileReader();
    reader.onload = () =&gt; {
      resolve(reader.result); // reader.result 就是 ArrayBuffer 对象
    };
    reader.onerror = reject;
    reader.readAsArrayBuffer(file);
  });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2.JS 的数据如何传递到 Wasm&lt;/h3&gt;
&lt;p&gt;前面读取 &lt;code&gt;File&lt;/code&gt; 对象得到的是 JS 环境中的 &lt;code&gt;ArrayBuffer&lt;/code&gt; 对象，是一个二进制对象和 Go 里面的字节数组 &lt;code&gt;[]byte&lt;/code&gt; 相对应。&lt;/p&gt;
&lt;p&gt;对于 &lt;code&gt;ArrayBuffer&lt;/code&gt; 这种体积较大的数据需要借助 &lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/WebAssembly/JavaScript_interface/Memory&quot;&gt;WebAssembly.Memory()&lt;/a&gt; 进行传递，之前的文章介绍过 &lt;a href=&quot;https://juejin.cn/post/7219899306945806394#heading-21&quot;&gt;JS 与 Wasm 之间的数据传递&lt;/a&gt;，不再赘述。&lt;/p&gt;
&lt;p&gt;传递  &lt;code&gt;ArrayBuffer&lt;/code&gt;  过程原理概括为下图所示：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/472509e60d61fcacfda942bc7b63c499.B3d5nx0N_1QrkbA.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;在 JS 环境将 &lt;code&gt;ArrayBuffer&lt;/code&gt; 写入到 &lt;code&gt;Memory&lt;/code&gt; 中，会得到写入时的偏移位置、&lt;code&gt;ArrayBuffer&lt;/code&gt; 字节数量两个参数；在调用 Wasm 导出的方法时传入这两个参数，在 Wasm 中可以根据这两个参数从 &lt;code&gt;Memory&lt;/code&gt; 中读取写入的 &lt;code&gt;ArrayBuffer&lt;/code&gt; 数据。&lt;/p&gt;
&lt;p&gt;这一过程，在 Go 里面已经有官方的封装—— &lt;a href=&quot;https://pkg.go.dev/syscall/js&quot;&gt;&lt;code&gt;syscall/js&lt;/code&gt;&lt;/a&gt;，我们直接按规范使用就可以。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-go&quot;&gt;// 在 JS 的 window 对象上添加名为 getDominantColor 的方法
js.Global().Set(&quot;getDominantColor&quot;, js.FuncOf(getDominantColor))

// args 参数对应 JS 中调用时传入的参数
func getDominantColor(this js.Value, args []js.Value) interface{} {
	// 相当于 JS 中执行：new Uint8Array(args[0])
	byteArray := js.Global().Get(&quot;Uint8Array&quot;).New(args[0])
	buffer := make([]byte, byteArray.Length())
	// 将 Unint8Array 转换为 []byte
	js.CopyBytesToGo(buffer, byteArray)
	// 省略其他代码...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;在 Go 里面通过 &lt;code&gt;js.Global().Get(&quot;Uint8Array&quot;)&lt;/code&gt; 将接收到的 &lt;code&gt;ArrayBuffer&lt;/code&gt; 转为 &lt;code&gt;Uint8Array&lt;/code&gt; ，然后 &lt;code&gt;js.CopyBytesToGo&lt;/code&gt; 将 &lt;code&gt;Uint8Array&lt;/code&gt; 拷贝为 Go 里面的字节数组 &lt;code&gt;[]byte&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;在 JS 里面使用如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;// 省略了 Wasm 实例初始化过程
window.getDominantColor(arrayBuffer)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3.Go 函数返回数组给 JS&lt;/h3&gt;
&lt;p&gt;JS 中调用原生 Wasm 导出的函数，得到的返回值只能是 &lt;code&gt;i32&lt;/code&gt; 类型，相应地 Go 的 Wasm 函数返回值只能是 JS 的基本数据类型。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;原生 Wasm 导出的函数&lt;/strong&gt;是指：直接使用原始的 wat 文本描述语言编写的 Wasm 模块、或者经过 Go 等三方语言编译输出&lt;strong&gt;不带辅助函数&lt;/strong&gt;的 Wasm 模块&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;数组不属于基础类型，没法直接当做函数返回值返回给 JS 环境，需要使用 &lt;code&gt;js.Global()&lt;/code&gt; 调用 JS 环境的 &lt;code&gt;Array&lt;/code&gt; 方法创建一个数组实例来返回。&lt;/p&gt;
&lt;p&gt;示例代码如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-go&quot;&gt;func getColorPalette(this js.Value, args []js.Value) interface{} {
	// 省略其他代码...
	colors := dominantcolor.FindN(image, count)
	// 相当于在 JS 环境执行：let colorsArray = new Array(colors.length)
	// 得到的 colorsArray 就是 JS 中的数组
	colorsArray := js.Global().Get(&quot;Array&quot;).New((len(colors)))
	// 遍历 colors 切片，给 colorsArray 数组元素赋值
	for i, color := range colors {
		colorsArray.SetIndex(i, dominantcolor2.Hex(color))
	}
	// 返回 colorsArray 数组
	return colorsArray
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.k-均值算法分析图片像素&lt;/h3&gt;
&lt;p&gt;前面得到的字节数组 &lt;code&gt;[]byte&lt;/code&gt;，也就是图片的像素点数据的集合，那如何计算图片中占比较多的颜色值？&lt;/p&gt;
&lt;p&gt;对像素点集合包含的所有颜色值进行排序，取出排序靠前的几个颜色值作为色卡值，这样可行吗？&lt;/p&gt;
&lt;p&gt;这种方案显然是&lt;strong&gt;不准确的&lt;/strong&gt;，如果图片中的颜色值不存在渐变过度，只是简单的几个大色块是可行的。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/e790db8e0ad292abf384b0be083d32f7.B-s2mB9M_ZO9hAO.webp&quot; alt=&quot;image&quot;&gt;
这种简单的色块图片可以识别&lt;/p&gt;
&lt;p&gt;但是，一般的图片都存在人眼无法分辨的渐变过渡色，比如：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/ce9ad2f474369bf55a4178f1fc6ad7ed.D27ZFo2M_61Cqe.webp&quot; alt=&quot;image&quot;&gt;
这种带有渐变色的图片就识别不了&lt;/p&gt;
&lt;p&gt;例如：这里非常接近的两个红色人眼应该看做一个颜色，但是上面说的识别算法却只能当做两个颜色。&lt;/p&gt;
&lt;p&gt;因此，我们&lt;strong&gt;需要将某一个范围之内的颜色算作一个颜色&lt;/strong&gt;再去排序，而这就是 &lt;code&gt;k-均值算法&lt;/code&gt; 的作用了。&lt;/p&gt;
&lt;h4&gt;算法简介&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;k-均值算法&lt;/strong&gt;是一种常用的聚类算法，用于将数据集划分为 k 个不同的&lt;strong&gt;簇&lt;/strong&gt;。它的主要思想是通过计算数据点之间的距离，将数据点划分到距离最近的&lt;strong&gt;簇中心&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;算法步骤如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;随机选择 k 个数据点作为初始的簇中心。&lt;/li&gt;
&lt;li&gt;对于每个数据点，计算其与各个簇中心的距离，并将其划分到距离最近的簇中心所在的簇。&lt;/li&gt;
&lt;li&gt;更新每个簇的中心点，即将簇中所有数据点的均值作为新的簇中心。&lt;/li&gt;
&lt;li&gt;重复步骤 2 和 3，直到簇中心不再发生变化或达到预定的迭代次数。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/1446e68a039c63c037afa5e35f7526f1.Bu7x_aWG_1JlhoQ.webp&quot; alt=&quot;image&quot;&gt;
k-均值算法示意图&lt;/p&gt;
&lt;h4&gt;如何使用&lt;/h4&gt;
&lt;p&gt;使用 &lt;code&gt;k-均值算法&lt;/code&gt; 将图片的像素点分类聚合成不同簇，每一个簇中心代表着图片中占比较大的一个色值。&lt;/p&gt;
&lt;p&gt;我在 github 找到了一个 Go 语言比较可靠的实现：&lt;a href=&quot;https://github.com/cenkalti/dominantcolor&quot;&gt;dominantcolor&lt;/a&gt;，使用方法如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-go&quot;&gt;// buffer 就是前面得到的 []byte
// 将 []byte 转为 Image
image, err := ConvertBytesToImage(buffer)
// 使用 dominantcolor 提供的 Find 方法查找图片主色调
color := dominantcolor.Find(image)
// 使用 dominantcolor 提供的 FindN 方法查找色卡
colors := dominantcolor2.FindN(image, count)
// 将 RGA 结构转为字符形式，例如：#AA33FF
colorStr := dominantcolor.Hex(color)
// 省略将 colors 转换数组返回给 JS 的逻辑
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;综上，我们就实现了图片主色调、色卡提取的所有关键功能，完整代码如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-go&quot;&gt;package main

import (
	&quot;bytes&quot;
	&quot;image&quot;
	&quot;image/jpeg&quot;
	&quot;syscall/js&quot;

	&quot;github.com/johniexu/dominantcolor&quot;
)

// 将[]byte转换为图像
func ConvertBytesToImage(data []byte) (image.Image, error) {
	img, err := jpeg.Decode(bytes.NewReader(data))
	if err != nil {
		return nil, err
	}
	return img, nil
}

// 提取主色调
func getDominantColor(this js.Value, args []js.Value) interface{} {
	// 将ArrayBuffer转换为字节数组
	byteArray := js.Global().Get(&quot;Uint8Array&quot;).New(args[0])
	buffer := make([]byte, byteArray.Length())
	js.CopyBytesToGo(buffer, byteArray)
	image, err := ConvertBytesToImage(buffer)
	if err != nil {
		println(err.Error())
		return nil
	}
	color := dominantcolor.Find(image)
	colorStr := dominantcolor.Hex(color)
	println(&quot;success&quot;)
	return colorStr
}

// 提取色卡
func getColorPalette(this js.Value, args []js.Value) interface{} {
	println(&quot;doing&quot;)
	// 将ArrayBuffer转换为字节数组
	byteArray := js.Global().Get(&quot;Uint8Array&quot;).New(args[0])
	count := args[1].Int()
	buffer := make([]byte, byteArray.Length())
	js.CopyBytesToGo(buffer, byteArray)
	image, err := ConvertBytesToImage(buffer)
	if err != nil {
		println(err.Error())
		return nil
	}
	colors := dominantcolor2.FindN(image, count)
	colorsArray := js.Global().Get(&quot;Array&quot;).New((len(colors)))

	for i, color := range colors {
		colorsArray.SetIndex(i, dominantcolor2.Hex(color))
	}
	println(&quot;success&quot;)
	return colorsArray
}

func main() {
	js.Global().Set(&quot;getDominantColor&quot;, js.FuncOf(getDominantColor))
	js.Global().Set(&quot;getColorPalette&quot;, js.FuncOf(getColorPalette))
	// 确保 main 方法中挂载到 window 对象上的方法可以正常执行
	select {}
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;编译输出 Wasm&lt;/h3&gt;
&lt;p&gt;编译上面代码只需执行下面命令即可，需要注意的是本机安装的 Go 需要使用比较新的支持 Wasm 编译目标的版本。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;编译生成 Wasm 之后在浏览器等 JS 环境运行 Wasm 时不需要安装 Go 开发环境&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;我本机 Go 的版本：
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/10ef454af5104a0e86a6d773787225ee.BfmQkkrG_Z1qvGgB.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;编译为 Wasm 二进制文件命令：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;GOOS=js GOARCH=wasm go build -o ./build/dominant_color.wasm ./wasm
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;另外，如果使用 &lt;code&gt;VS Code&lt;/code&gt; 开发这个 Go 项目，可能会遇到如下报错：
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/461096a59e799878b249ef8059632b47.BXV_EoWW_ZQwGNl.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;这是因为 &lt;code&gt;VS Code&lt;/code&gt; 安装的 Go 的插件默认的 &lt;code&gt;GOOS&lt;/code&gt;、&lt;code&gt;GOARCH&lt;/code&gt; 与上述编译命令的设置不一致。&lt;/p&gt;
&lt;p&gt;解决方法为：增加 &lt;code&gt;VS Code&lt;/code&gt; 配置文件告知插件项目的 &lt;code&gt;GOOS&lt;/code&gt;、&lt;code&gt;GOARCH&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/3193105c8fe330ab5d21e6761ad14452.COtyF5r0_Z2klUsz.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;JS 环境使用 Wasm 模块&lt;/h3&gt;
&lt;p&gt;加载上面编译得到的 &lt;code&gt;dominant_color.wasm&lt;/code&gt; 文件&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;if (!WebAssembly.instantiateStreaming) { // polyfill
    WebAssembly.instantiateStreaming = async (resp, importObject) =&gt; {
        const source = await (await resp).arrayBuffer();
        return await WebAssembly.instantiate(source, importObject);
    };
}
// Go 来自于 html 中通过 script 加载的 wasm_exec.js 文件
const go = new Go();
WebAssembly.instantiateStreaming(fetch(&quot;./dominant_color.wasm&quot;), go.importObject).then((result) =&gt; {
  // go.run 相当于执行 Go 里面的入口函数 main，
  // 执行完成后会在 window 上挂载方法：getDominantColor
  return go.run(result.instance);
}).then(() =&gt; {
  toggleLoading(false)
}).catch(e =&gt; {
  console.log(e)
  window.alert(&quot;出错了，请检查控制台&quot;)
  toggleLoading(false)
});
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;上面 &lt;code&gt;const go = new Go();&lt;/code&gt;  里面的 &lt;code&gt;Go&lt;/code&gt; 来自于 &lt;code&gt;wasm_exec.js&lt;/code&gt; 文件，这是从 Go 开发环境安装目录下拷贝出来的一个文件，它是初始化 Wasm 运行时环境的&lt;strong&gt;胶水代码&lt;/strong&gt;，目前使用 Go 开发 Wasm 是必须的。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;待加载完成后执行 &lt;code&gt;window.getDominantColor&lt;/code&gt; 可以获取图片的主色调，调用 &lt;code&gt;window.getColorPalette&lt;/code&gt; 可以获取图片的色卡数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function fileToArrayBuffer(file) {
  return new Promise((resolve, reject) =&gt; {
    const reader = new FileReader();
    reader.onload = () =&gt; {
      resolve(reader.result);
    };
    reader.onerror = reject;
    reader.readAsArrayBuffer(file);
  });
}
// file 是用户选择的图片文件
const arrayBuffer = await fileToArrayBuffer(file);
// 调用 Wasm 挂载到 window 上的 getDominantColor
const color = window.getDominantColor(arrayBuffer);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;至此，获取单张图片的主色调、色卡功能已实现，完整的代码可以在这里查看：&lt;a href=&quot;https://github.com/JohnieXu/demo/tree/20230819/dominantcolor_demo&quot;&gt;https://github.com/JohnieXu/demo/tree/20230819/dominantcolor_demo&lt;/a&gt;。&lt;/p&gt;
&lt;h2&gt;效果对比&lt;/h2&gt;
&lt;p&gt;文中 demo 提取的色卡和我使用 PS 插件提取的色卡对比如下：&lt;/p&gt;
&lt;p&gt;Demo 提取色卡
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/thumbnail.DmZTGc1F_Z1WMevz.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;PS 插件提取色卡
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/9e864e28b4d72b3cdc24f58106e01889.CcVb0h64_1jvCk2.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;可见，两者提取到的颜色值还是很接近的。&lt;/p&gt;
&lt;h2&gt;一些想法&lt;/h2&gt;
&lt;h3&gt;1.打包成通用包&lt;/h3&gt;
&lt;p&gt;本文使用 Go 项目打包成了一个独立的二进制的 Wasm 文件，任何有 Wasm 运行时环境的平台都可以使用，因此理论上这个功能可以完全做成一个绝对通用模块。&lt;/p&gt;
&lt;p&gt;目前了解到支持 Wasm 运行时环境的平台有：Node.js、Electron、微信小程序、JVM。&lt;/p&gt;
&lt;h3&gt;2.移植到微信小程序&lt;/h3&gt;
&lt;p&gt;微信小程序也支持 Wasm 运行了，那么本 demo 中的提取主色调、提取色卡功能可以尝试移植到微信小程序中。&lt;/p&gt;
&lt;h3&gt;3.扩展更多功能&lt;/h3&gt;
&lt;p&gt;通过 Go 开发 Wasm 可以将很多已有 Go 项目的库编译打包成其他环境可运行的 Wasm，因此可以借此实现一些 Web 环境不擅长的功能，例如：图片处理、视频处理、文件加密等。&lt;/p&gt;
&lt;p&gt;目前正在尝试这些：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Go 的图片处理相关方法扩展到 Web 环境&lt;/li&gt;
&lt;li&gt;Go 的加密算法扩展到 Web 环境&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;4.兼容性处理&lt;/h3&gt;
&lt;p&gt;文中的实现方式是依赖于 Wasm 环境的，目前来看还是有有一些兼容性问题。生产环境需使用要使用&lt;strong&gt;纯 JS 实现&lt;/strong&gt;的方案来做兼容性处理。&lt;/p&gt;
&lt;p&gt;兼容性见下图：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0ab7c74e4d038a31352a025e2b218888.BbPA5mJL_1mI54t.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;目前在安卓上最新版微信也支持：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/d9e02b6ccf0f96e3c21650d83fc457b7.5vNlD4uF_PYdlz.webp&quot; alt=&quot;image&quot;&gt;
微信浏览器使用正常&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;iOS 和电脑版微信没有试过，大家感兴趣可以尝试一下看是否支持&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;总结&lt;/h2&gt;
&lt;p&gt;本文使用 Go 打包输出了一份 Wasm 二进制文件，前端只需要初始化 Wasm 实例调用其挂载到 window 对象上的方法即可，不需要服务端提供任何接口、也不需要部署 Go 程序，所以算作&lt;strong&gt;纯前端实现&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;另外文中 demo 例子采用了 WebAssembly 技术，也算是对 WebAssembly 技术在前端场景的使用补充了一个案例。&lt;/p&gt;
&lt;p&gt;经过这个色卡提取功能的开发，我也深刻体会到 WebAssembly 在&lt;strong&gt;通用性、性能、移植现有项目&lt;/strong&gt;方面真的是大有可为。我后续还会继续研究这项技术，欢迎大家跟进😊😊。&lt;/p&gt;
&lt;h2&gt;参考资料&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;a href=&quot;https://zh.wikipedia.org/wiki/K-%E5%B9%B3%E5%9D%87%E7%AE%97%E6%B3%95&quot;&gt;https://zh.wikipedia.org/wiki/K-%E5%B9%B3%E5%9D%87%E7%AE%97%E6%B3%95&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/Zyzle/image-kmeans&quot;&gt;https://github.com/Zyzle/image-kmeans&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://zyzle.dev/projects/image-kmeans&quot;&gt;https://zyzle.dev/projects/image-kmeans&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/WebAssembly&quot;&gt;https://developer.mozilla.org/zh-CN/docs/WebAssembly&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;</content:encoded><h:img src="/_astro/thumbnail.DmZTGc1F.png"/><enclosure url="/_astro/thumbnail.DmZTGc1F.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>纯前端实现图片 Exif 数据编辑</title><link>https://luopengcheng89.github.io/blog/exif_edit_in_frontend</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/exif_edit_in_frontend</guid><description>本文重点介绍了图片的 Exif 数据的二进制结构，Exif 数据是如何嵌入到 JPEG 格式的图片中的。最后也使用 Vue 实现了一个可以读取、编辑并保存 JPEG 图片 Exif 数据的应用，同时可以打包为 Vue 组件和 Web Component 方便第三方复用。</description><pubDate>Sat, 09 Sep 2023 17:15:39 GMT</pubDate><content:encoded>&lt;h2&gt;前言&lt;/h2&gt;
&lt;p&gt;前段时间小米出的水印边框照片模板被大家玩的不亦乐乎，本来想着写一个实现合成各厂商带水印边框照片的工具。这个头脑一热想要开发的工具最终也没有着手实现，不过这里面有一个知识点倒是令我很感兴趣。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/b13d320d62439d89e85f3f3b5d6b71e9.DoEOFpcb_1D9V5F.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;这一点就是：&lt;strong&gt;合成水印边框通常会自动带上照片的拍摄数据——光圈、快门、感光度、品牌 LOGO，那这些数据都是从哪里获取照片里面又是怎么存储的呢？&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在查询了大量资料之后得知，&lt;strong&gt;这些参数都来自于 Exif 数据&lt;/strong&gt;，于是对 Exif 的技术标准、读写 Exif 的 JS 框架做了一些研究，这篇文章就是据此研究总结而来。&lt;/p&gt;
&lt;p&gt;最后，笔者还写了一个 Exif 数据编辑的 Vue 组件——&lt;a href=&quot;https://github.com/JohnieXu/exif-edit&quot;&gt;exif-edit&lt;/a&gt;。
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/thumbnail.Cts2ZRCm_ZPtUJM.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;图片格式与 Exif 数据&lt;/h2&gt;
&lt;p&gt;图像元数据有两种格式：Exif、XMP，而 Exif 就是其中一种。&lt;/p&gt;
&lt;p&gt;Exif 全称是&lt;strong&gt;可交换图像文件格式&lt;/strong&gt;（英语：Exchangeable image file format，官方简称 &lt;strong&gt;Exif&lt;/strong&gt;），是用于记录数码照片的属性和拍摄数据的。属性主要是指照片的分辨率大小、缩略图，而拍摄数据主要是指光圈、快门、感光度、焦距、设备型号、镜头、GPS 位置等。&lt;/p&gt;
&lt;p&gt;维基百科上给出的 Exif 包含的信息示例如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/8f8e817290bfebbb5ba1612f2796d16e.IW2yXpGY_ZFTtYU.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;常见图片格式有 JPEG、PNG、GIF、HEIF、DNG、TIFF 等，其中已经明确能够支持写入 Exif 数据的格式有：JPEG、DNG、TIFF、HEIF。&lt;/p&gt;
&lt;p&gt;各图片格式对于 Exif 数据支持情况如下：&lt;/p&gt;
&lt;p&gt;| 格式     | JPEG | PNG    | GIF    | HEIF | DNG  | TIFF | BPG    | BMP |
| -------- | ---- | ------ | ------ | ---- | ---- | ---- | ------ | --- |
| Exif | 支持 | 不支持 | 不支持 | 支持 | 支持 | 支持 | 不支持 |   不支持  |&lt;/p&gt;
&lt;p&gt;目前在互联网上最常见的图片格式是 JPEG 格式（文件名后缀 &lt;code&gt;.jpeg&lt;/code&gt;），又称为 JPG 格式（文件名后缀 &lt;code&gt;.jpg&lt;/code&gt;），我们暂且只分析 JPEG 图片中的 Exif 数据。&lt;/p&gt;
&lt;h2&gt;Exif 格式规范&lt;/h2&gt;
&lt;p&gt;Exif 数据能够嵌入到不同格式的图片中，那各个格式的图片是如何识别并解析 Exif 数据的呢？这就涉及到 Exif 数据的格式规范了。&lt;/p&gt;
&lt;p&gt;可以简要描述为：&lt;strong&gt;Exif 信息数据以两个字节长度的特定标记开头，这个标记之后的两个字节记录Exif数据总的字节长度&lt;/strong&gt;。有了 Exif 标记和所有数据总字节长度，就能知道哪些是 Exif 数据了。&lt;/p&gt;
&lt;p&gt;这个标记是 2 字节长的 &lt;code&gt;0xFFE1&lt;/code&gt;，这是十六进制表示，表示的是什么意思呢？且看下文解释。&lt;/p&gt;
&lt;p&gt;我们知道计算机世界里一个字节是由 8 个二进制位组成，即 &lt;code&gt;1Byte = 8bit&lt;/code&gt; （&lt;code&gt;Btye&lt;/code&gt; 可以简写为 &lt;code&gt;B&lt;/code&gt;，&lt;code&gt;bit&lt;/code&gt; 可以简写为 &lt;code&gt;b&lt;/code&gt;），8 个二进制位的所有排列组合有 &lt;code&gt;2**8 = 256&lt;/code&gt; 种结果；二进制中一个位有 2 种可能结果，而假设一个位有 16 种可能结果的话，8 个二进制位则可以用 2 个 十六进制位表示，因为 2 个 16 进制位所有的排列组合正好有 &lt;code&gt;16**2 = 256&lt;/code&gt; 种结果。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/a70fa1e6b7246bdea52f299e2d10d9b1.BL9v1O6q_ZgSzLO.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;这里的一个位有 16 种可能结果就是&lt;strong&gt;十六进制&lt;/strong&gt;，一个位的 16 种可能结果分别用 &lt;code&gt;1,2,3....10,A,B,C,D,E,F&lt;/code&gt; 表示，所以一个字节用十六进制表示&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/01d000543429f055558f4356fbf5121d.wUT2ia48_R0nVb.webp&quot; alt=&quot;image&quot;&gt;
1 字节的 8 个二进制为都为 1 的话，可以用字符串表示为 &lt;code&gt;11111111&lt;/code&gt; ，这是 256 种可能结果中的第 256 个，所以相当于十进制的 255（从 0 开始算的话，是十进制 255）。&lt;/p&gt;
&lt;p&gt;按照十六进制换算的话，这 1 字节包含 2 个位，表示上述 256 的话需要第 1、2 位都是 16，可用 &lt;code&gt;FF&lt;/code&gt; 表示。&lt;/p&gt;
&lt;p&gt;并且，在 JS 的 Number 类型中，以 &lt;code&gt;0x&lt;/code&gt; 开头来表示十六进制，字符串中则以 &lt;code&gt;\x&lt;/code&gt; 开头来表示十六进制。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/325b7c3b22ab03de9f5a73191d167801.DfRKVmM9_2qBi10.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;综上，&lt;strong&gt;二进制 &lt;code&gt;11111111&lt;/code&gt; 等价于十六进制的 &lt;code&gt;FF&lt;/code&gt;，即 &lt;code&gt;0xFF&lt;/code&gt;。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/fc97dfc47b899f3f24deded6932cfdd4.CQG100tE_ZLH4gD.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;所以，Exif 数据的开始标记 &lt;code&gt;0xFFE1&lt;/code&gt; 表达的意思如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;这是十六进制表示，包含 2 个字节的数据&lt;/li&gt;
&lt;li&gt;第 1 字节对应二进制数 &lt;code&gt;11111111&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;第 2 字节对应二进制数 &lt;code&gt;11100001&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;该标记之后的 2 个字节表示 Exif 数据的总长度&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/2cfccc08e3395badf5f96d79782815ae.CnT_cQhX_ZJOOAx.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;Exif 数据在 JPEG 图片中的位置示意图如下，&lt;code&gt;0xFFE1&lt;/code&gt; 标记之后的两个字节 &lt;code&gt;XXXX&lt;/code&gt; 表示 Exif 数据总的字节长度，这两个字节的最大值为 &lt;code&gt;0xFFFF&lt;/code&gt;，相当于十进制的 &lt;code&gt;2**16 = 65536&lt;/code&gt;，也就是 &lt;code&gt;65536B = 64kB&lt;/code&gt;，即 Exif 数据长度最大为 64kB。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/cd7fd4ef888ce9c995eb2b0a39cddfe6.-9355kMS_Z2qQ01W.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;所以，&lt;strong&gt;解析 JPEG 图片中的 Exif 信息只需要找到标记 &lt;code&gt;0xFFE1&lt;/code&gt; 的位置，然后根据其后 2 字节的 &lt;code&gt;0xXXXX&lt;/code&gt; 表示的数据大小往后找出 Exif 数据部分即可&lt;/strong&gt;。同样，对于其他支持写入 Exif 信息的图片格式可使用同样方法找到 Exif 信息。&lt;/p&gt;
&lt;p&gt;这个标记 &lt;code&gt;0xFFE1&lt;/code&gt; 是 Exif 技术标准规定的，是 Exif 数据专用标记，称为 &lt;code&gt;APP1&lt;/code&gt;，当 Exif 数据嵌入到其他的图片格式中时，Exif 数据的开始标记也是这个；同样的标记还有 &lt;code&gt;APP0&lt;/code&gt; 等，这里不做赘述。&lt;/p&gt;
&lt;p&gt;![image](./cd7fd4ef888ce9c995eb2b0a39cddfe6 copy.png)&lt;/p&gt;
&lt;p&gt;图中 APP1 Data 就是 Exif 的数据部分，它内部又是如何记录照片的属性和拍摄数据的呢？这就涉及到 Exif 数据部分的结构，见下文介绍。&lt;/p&gt;
&lt;h3&gt;Exif 数据部分结构&lt;/h3&gt;
&lt;p&gt;这里说的 Exif 数据部分指的是上面 APP1 Data 部分，其以 2 字节的 &lt;code&gt;xxxx&lt;/code&gt; 开头表示 APP1 Data 部分总字节长度，随后跟上占用 4 字节的字符串 &lt;code&gt;Exif&lt;/code&gt; 和占用 2 字节的固定值 &lt;code&gt;0x00&lt;/code&gt;，随后就是 TIFF 格式的数据了。
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/6d9f534292b9ad3decb20f976f354933.BotNqa0C_Z1tYOyJ.webp&quot; alt=&quot;image&quot;&gt;
这里字符串 &lt;code&gt;Exif&lt;/code&gt; 是 ASCII 编码，一个字符占用 1 个字节，总共占用 4 个字节。&lt;/p&gt;
&lt;h2&gt;TIFF 格式规范&lt;/h2&gt;
&lt;p&gt;TIFF 可以理解成和 MySQL 数据库中表一样，是一条一条记录数据的，一条记录代表了一个数据体，称为：Tag。下图中黄色部分为一个 Tag 的结构，Tag 分为 Header 部分和 IFD 部分。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/2a7a0c0ad9fc9d7ef14c4f1edfd920ac.B8OlNmsJ_BeCas.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;一个 Tag 的拥有一个 8 字节的 TIFF Header（头部），和 IFD 数据部分。头部主要记录了当前 Tag 的序号、数据格式以及下一个 Tag 的偏移值，通过偏移值将多条 Tag 依次串联起来；通过头部指定了数据格式，每一个 Tag 所占用的字节数也就确定了，因此 TIFF 不需要像 Exif 一样标记数据部分的字节大小，即没有 64kB 的大小限制。&lt;/p&gt;
&lt;p&gt;上图中最关键的绿色部分记录了图片的属性、拍摄参数（即通常说的 Exif 参数），每一条的数据值的字节大小不一样，所以各个位置的字节代表的含义不一样，这里暂不展开。图中这样绿色的一条数据我们称为：Entry。&lt;/p&gt;
&lt;p&gt;一个 Entry 展开为如下图，光圈、快门等拍摄参数根据标签序号映射向左边 IFD Data 数据中查找即可。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/d7dde3494249cf63bad5bfa48801cac5.CpMoAHWH_Z7Iip3.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;例如：要查找图片的感光度 ISO，先从标签序号映射找到标签序号 &lt;code&gt;0x8827&lt;/code&gt;，然后从 IFD Data 中找到标签序号和次序号对应的 Entry，取值数据值部分，然后根据数据格式映射做格式转换，最后就可以知道感光度 ISO 的值是多少。&lt;/p&gt;
&lt;h2&gt;Exif 数据读取写入&lt;/h2&gt;
&lt;p&gt;知道了 Exif 数据在二进制数据中的存储规范了，那在前端项目中如何实现读取、写入 Exif 数据呢？且看下文解释。&lt;/p&gt;
&lt;h3&gt;主要思路&lt;/h3&gt;
&lt;p&gt;从本地图片文件解析 Exif 数据的过程如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/daa367b8022d3f4125119272d85dafb8.CixUWw5i_Z19OmFu.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;编辑 Exif 数据并保存到文件的过程如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/69da6401e7c01214001fa6e249fccbfd.DNcuiWu7_1DTXrY.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;上述过程中从二进制到解析出 Exif 拍摄参数和 Exif 拍摄参数序列化为二进制格式的过程为核心点，已在 github 上找到了开源的工具库来实现，后续文章再讲解如何实现这一过程。&lt;/p&gt;
&lt;h3&gt;piexifjs 工具库&lt;/h3&gt;
&lt;p&gt;piexifjs 是一个专门读取和编辑 JPEG 图片中 Exif 数据的工具库，源码在这里：&lt;a href=&quot;https://github.com/hMatoba/piexifjs&quot;&gt;https://github.com/hMatoba/piexifjs&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;读取 Exif 数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;import piexifjs, { piexif } from &apos;piexifjs&apos;
piexifjs.load(imgData) // imgData 为 JPEG 图片的 base64 字符串

// piexifjs 解析出来的 Exif 参数对象可读性不强，需要格式化处理一下
const parseExifData = (exifData) =&gt; {
  if (!exifData) { return null }
  const M = exifData[&apos;0th&apos;][piexif.ImageIFD.Model]
  const F = exifData.Exif[piexif.ExifIFD.FNumber]
  const S = exifData.Exif[piexif.ExifIFD.ExposureTime]
  const ISO = exifData.Exif[piexif.ExifIFD.ISOSpeedRatings]
  const L = exifData.Exif[piexif.ExifIFD.FocalLength]
  const LEN = exifData.Exif[piexif.ExifIFD.LensModel]
  const T = exifData.Exif[piexif.ExifIFD.DateTimeOriginal]
  console.log(&apos;parseExifData\nM, F, S, ISO, L, LEN, T\n&apos;, M, F, S, ISO, L, LEN, T)
  return {
    M: M || null,
    F: F &amp;#x26;&amp;#x26; F[0] &amp;#x26;&amp;#x26; F[1] ? F[0] / F[1] : null,
    S: S &amp;#x26;&amp;#x26; S[0] &amp;#x26;&amp;#x26; S[1] ? S[1] : null,
    ISO: ISO || null,
    L: L &amp;#x26;&amp;#x26; L[0] &amp;#x26;&amp;#x26; L[1] ? L[0] / L[1] : null,
    LEN: LEN || null,
    T: T || null
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;写入 Exif 数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;import piexifjs, { piexif } from &apos;piexifjs&apos;

// 将 Exif 参数对象按 TIFF 格式序列化
const exifStr = piexifjs.dump({ &apos;0th&apos;: th, Exif: exif })

// 将序列化结果写入 JPEG 图片base64 字符串中，返回新的 base64 字符串
const nb64 = piexifjs.insert(exifStr, imgData)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;序列化时传入的 Exif 对象有格式要求，需要转换一下格式&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;const th = {
	[piexif.ImageIFD.Model]: M || this.exif.M
}
const _F = F || this.exif.F
const _S = S || this.exif.S
const _L = L || this.exif.L
const exif = {
	[piexif.ExifIFD.ExifVersion]: this.exif.version,
	// 光圈值包含小数时需要x100
	[piexif.ExifIFD.FNumber]: `${_F}`.includes(&apos;.&apos;) ? [_F * 100, 1 * 100] : [_F, 1],
	[piexif.ExifIFD.ExposureTime]: [1, Number(_S)],
	[piexif.ExifIFD.ISOSpeed]: Number(ISO || this.exif.ISO),
	[piexif.ExifIFD.ISOSpeedRatings]: Number(ISO || this.exif.ISO),
	[piexif.ExifIFD.FocalLength]: [Number(_L) * 10, 10],
	[piexif.ExifIFD.LensModel]: LEN || this.exif.LEN,
	[piexif.ExifIFD.DateTimeOriginal]: T || this.exif.T,
	[piexif.ExifIFD.DateTimeDigitized]: T || this.exif.T
  }
  removeNull(th)
  removeNull(exif)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Exif 数据读取编辑应用开发&lt;/h2&gt;
&lt;p&gt;封装一个 ExifEdit 组件，实现功能有： Exif 读取并查看、编辑、保存修改后数据为 base64 、未传入 base64 图片数据时可通过交互选择本地图片文件。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/57f61dab9d40c8d9b4fb361c1b5b9ee6.DvfxvN92_ZFtoe7.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;ExifEdit 组件还同时拥有清空、重置、复制、粘贴、保存功能。&lt;/p&gt;
&lt;h3&gt;清空&lt;/h3&gt;
&lt;p&gt;清空选择的文件、或者传入组件的图片 base64 数据，恢复为组件默认的无数据状态，可以点击从本地文件选择图片。&lt;/p&gt;
&lt;h3&gt;重置&lt;/h3&gt;
&lt;p&gt;放弃当前对 Exif 数据的修改，重新从选择的文件或者传入组件的 base64 数据解析 Exif 数据。&lt;/p&gt;
&lt;h3&gt;复制&lt;/h3&gt;
&lt;p&gt;复制当前解析到的 Exif 数据的JSON 字符串到系统剪贴板。&lt;/p&gt;
&lt;h3&gt;粘贴&lt;/h3&gt;
&lt;p&gt;尝试从系统剪贴板读取数据，进行 JSON 序列化，若符合 Exif 数据结构，则将其关联到当前 Exif 数据上填充在表单上，但是不会自动像图片 dataURL 中插入 Exif，也不会触发组件的 change 事件。&lt;/p&gt;
&lt;h3&gt;保存&lt;/h3&gt;
&lt;p&gt;将表单绑定的 Exif 数据进行序列化，并将序列化结果插入到传入的 base64 字符串或者从选择的文件读取到的 dataURL 中，若插入成功，则会触发组件的 change 事件。&lt;/p&gt;
&lt;h2&gt;Exif 读取功能实现&lt;/h2&gt;
&lt;p&gt;通过 ExifEdit 组件接受图片 dataURL 数据或者通过交互选择本地文件，读取为 dataURL 字符串，然后调用 piexifjs 库获取对应的 Exif 数据对象，然后解析 Exif 数据对象中的设备型号、拍摄参数等。&lt;/p&gt;
&lt;h2&gt;Exif 写入功能实现&lt;/h2&gt;
&lt;p&gt;收集页面表单的数据并做校验，校验通过后根据 piexifjs 库对 Exif 数据的格式要求做数据转换，然后依次调用 &lt;code&gt;piexifjs.dump&lt;/code&gt; &lt;code&gt;piexifjs.insert&lt;/code&gt; 写入 Exif 二进制数据，写入之后返回的是插入了 Exif 数据的 dataURL。&lt;/p&gt;
&lt;h2&gt;保存文件功能实现&lt;/h2&gt;
&lt;p&gt;ExifEdit 组件在完成 Exif 数据写入功能后仅返回 dataURL 数据，组件不实现文件保存功能，在写入 Exif 数据成功后会向外 emit 一个 &lt;code&gt;change&lt;/code&gt; 事件，事件携带上 dataURL 和新的文件名。&lt;/p&gt;
&lt;p&gt;当该项目作为组件库 lib 被第三方使用时，需要使用方自行监听 change 事件，并使用事件传递的 dataURL 去实现保存到本地功能。&lt;/p&gt;
&lt;p&gt;不过当前项目在 &lt;code&gt;App.vue&lt;/code&gt; 中实现了保存文件功能，实现方法如下：&lt;/p&gt;
&lt;p&gt;监听 ExifEdit 组件的 change 事件，使用 &lt;code&gt;downloadjs&lt;/code&gt; 库将得到的 dataURL 保存到本地。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-vue&quot;&gt;import download from &apos;downloadjs&apos;
export default {
	methods: {
		handleExifEditChange ({ b64: imgData, fileName }) {
			download(imgData, fileName, &apos;image/jpeg&apos;)
		}
	}
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;若 ExifEdit 通过 UI 交互引导用户选择了本地文件，则明文件会使用 &lt;code&gt;${用户选择的文件名去掉后缀}_1.jpg&lt;/code&gt; 作为新文件名，否则使用 &lt;code&gt;${Date.now()}.jpg&lt;/code&gt; 做新文件名。&lt;/p&gt;
&lt;h2&gt;打包为 Vue 组件&lt;/h2&gt;
&lt;p&gt;笔者在查找 Exif 相关资料时并未发现有项目将 Exif 数据读写封装成 Vue 组件，因此就有了这一想法。&lt;/p&gt;
&lt;p&gt;打包成 Vue 组件也比较简单，组件库的打包使用 vue-cli 脚手架工具，执行下面打包命令即可。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npm run build:lib
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;打包为 Web Component 组件&lt;/h2&gt;
&lt;p&gt;基于 vue-cli 搭建的项目可以很方便的打包成 Web Component 组件，同时 Web Component 组件是浏览器内核支持的自定义组件，与 Vue、React、Angular 等 UI 框架无关，所以考虑打包为 Web Component 组件。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npm run build:wc
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Web Component 组件可能有兼容性问题，具体见下方 MDN 截图&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/2516c02008bf80a4de8aa4d82a731879.0IEus8Ze_Zsq1MU.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;结语&lt;/h2&gt;
&lt;p&gt;本文重点介绍了图片的 Exif 数据的二进制结构，Exif 数据是如何嵌入到 JPEG 格式的图片中的。最后也使用 Vue 实现了一个可以读取、编辑并保存 JPEG 图片 Exif 数据的应用，同时可以打包为 Vue 组件和 Web Component 方便第三方复用。&lt;/p&gt;
&lt;p&gt;另外，这个应用在读取文件、解析 Exif 过程的性能优化、组件 E2E 测试方面还有欠缺，并且不支持 TIFF 格式图片的 Exif 数据读取便秘和预览功能，后续会再考虑补充完善。&lt;/p&gt;
&lt;p&gt;最后，这个项目代码是开源的，地址在这里：&lt;a href=&quot;https://github.com/JohnieXu/exif-edit&quot;&gt;https://github.com/JohnieXu/exif-edit&lt;/a&gt;，欢迎各位大佬参与贡献 PR。&lt;/p&gt;
&lt;h2&gt;参考资料&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;TIFF6技术标准 &lt;a href=&quot;https://web.archive.org/web/20120703095221/http://partners.adobe.com/public/developer/en/tiff/TIFF6.pdf&quot;&gt;https://web.archive.org/web/20120703095221/http://partners.adobe.com/public/developer/en/tiff/TIFF6.pdf&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Exif 文件格式 &lt;a href=&quot;https://web.archive.org/web/20111025004429/http://park2.wakwak.com/~tsuruzoh/Computer/Digicams/exif-e.html&quot;&gt;https://web.archive.org/web/20111025004429/http://park2.wakwak.com/~tsuruzoh/Computer/Digicams/exif-e.html&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;JS 中的 unicode 编码 &lt;a href=&quot;https://www.ruanyifeng.com/blog/2014/12/unicode.html&quot;&gt;https://www.ruanyifeng.com/blog/2014/12/unicode.html&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Exif 维基百科 &lt;a href=&quot;https://zh.wikipedia.org/wiki/Exif&quot;&gt;https://zh.wikipedia.org/wiki/Exif&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;piexifjs 文档 &lt;a href=&quot;http://piexifjs.readthedocs.org/en/latest/index.html&quot;&gt;http://piexifjs.readthedocs.org/en/latest/index.html&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;</content:encoded><h:img src="/_astro/thumbnail.Cts2ZRCm.png"/><enclosure url="/_astro/thumbnail.Cts2ZRCm.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>Git 常用命令整理</title><link>https://luopengcheng89.github.io/blog/git</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/git</guid><description>通过具体的命令示例和适用场景说明，详细介绍了各种Git操作的使用方法，适合Git初学者和需要深入了解Git高级功能的开发者阅读。</description><pubDate>Wed, 01 Mar 2017 16:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;一、配置命令&lt;/h2&gt;
&lt;h3&gt;配置用户名和邮箱&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git config --global user.name &quot;JohnieXu&quot; // 配置全局用户名为JohnieXu
git config --global user.email &quot;281910378@qq.com&quot; // 配置全局邮箱为281910378@qq.com
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;查看配置&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git config -l // 查看git全局配置信息
or
git config --list
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;配置公钥&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;ssh-keygen -t rsa -C &quot;281910378@qq.com&quot; // 采用rsa加密算法生成git公钥，前提是用户名和邮箱已经设置好且此处邮箱与前面配置邮箱保持一致
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;二、常用命令&lt;/h2&gt;
&lt;h3&gt;初始化项目&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;git init
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;克隆项目&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git clone https://github.com/JohnieXu/eleme-web-vue.git
or
git clone git@git.github.com:JohnieXu/eleme-web-vue.git
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;查看项目信息&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;综合信息&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git status // 查看当前项目的所有综合信息
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;commit提交信息&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git log --graph --pretty=oneline --abbrev-commit // 以图形的形式显示commit的提交记录
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;推送更新&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git add .&lt;/code&gt; =&gt; &lt;code&gt;git commit&lt;/code&gt; =&gt; &lt;code&gt;git push&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git add . // 添加所有修改修改过的文件,仅添加**当前目录下**的所有修改过的文件,主要运行git命令的路径
git commit -m &apos;fix a bug&apos; // 将修改提交到(本地项目的)暂存区, 提交的描述内容为&quot;fix a bug&quot;
git push -u origin master // 推送当前分支下已提交的更新到origin仓库的master分支下,同时设置当前提交的origin和master为默认设置
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;检查更新&lt;/h3&gt;
&lt;p&gt;在多人协作开发中，可以通过以下方式在不&lt;code&gt;commit&lt;/code&gt;、&lt;code&gt;stash&lt;/code&gt;当前更改的情况下查看远程仓库是否有更新&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git remote update // 拉取远程仓库更新，此操作不影响当前修改
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;管理修改&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;已修改文件未添加到暂存区&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout -- . // 撤销当前所有文件做的修改
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;-- file&lt;/code&gt;: file为需要撤销修改的文件路径&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;已修改文件并已添加到暂存区&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;// 撤销全部文件
git reset HEAD // 撤回暂存区的所有文件到工作区(没有撤销对文件的修改)
git checkout -- . // 撤销所有文件的修改
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;// 撤销指定文件
git reset HEAD readme.txt // 将暂存区的readme.txt文件撤回工作区
git checkout -- readme.txt // 撤销readme.txt文件的修改
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;已提交commit未push到远程仓库&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git reflog // 查看最近的commit列表
git reset --hard commit_id // 退回到commit_id对应的commit版本(不建议添加--hard参数)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;commit_id:&lt;/code&gt;需要退回的版本的commit的ID(hash值)&lt;/p&gt;
&lt;h3&gt;分支管理&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;查看分支&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git branch // 查看所有分支, *对应当前分支
git remote -v // 查看关联的远程分支详情
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;创建分支&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git branch dev // 创建dev分支
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;切换分支&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout dev // 切换到dev分支
or
git checkout -b dev // 创建并切换到dev分支
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;合并分支&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout master // 切换到master分支
git merge dev // 将dev分支合并到master分支
or
git merge --no-ff -m &quot;merge with no-ff&quot; dev // 非fast-forward模式合并dev分支到master分支
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;--no-ff&lt;/code&gt;: 合并方式为禁用&lt;code&gt;Fast-forward&lt;/code&gt;, 即合并的时候会另外提交一个commit记录&lt;/p&gt;
&lt;p&gt;&lt;code&gt;-m&lt;/code&gt;: 本次合并commit的描述内容&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;推送分支&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git push origin master // 将master分支推送到远程的origin关联的仓库上
or
git push -u origin master // 将master分支推送到远程的origin关联的仓库上, 设置默认推送master至origin
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;-u&lt;/code&gt;: 第一次推送时候加上&lt;code&gt;-u&lt;/code&gt;的话, 将第一次的推送源和分支保存为默认设置, 下次&lt;code&gt;git push&lt;/code&gt;就相当于&lt;code&gt;git push origin master&lt;/code&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;删除分支&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git branch -d dev // 删除dev分支(dev已经被合并)
git branch -D dev // 强制删除dev分支(dev分支未被合并)

&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;Bug分支&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;应用场景&lt;/strong&gt;: 当前正在自己的分支-dev-xu上进行新功能开发, master分支发现Bug需要立即修复。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{1,7}&quot;&gt;git stash // 将当前的工作区储存起来, git status查看工作区是干净的
git checkout master // 切换到master分支
git checkout -b bug-101 // 从master分支创建新的对应的bug-101分支
git add . &amp;#x26;&amp;#x26; git commit -m &apos;fix bug 101&apos; // 修复bug并提交修改
git checkout master &amp;#x26;&amp;#x26; git merge --no-ff -m &apos;merged bug fix 101&apos; bug-101 // 将bug-101分支的修改合并到主分支
git branch -d bug-101 // 删除bug-101分支
git checkout dev-xu // 切回之前自己在开发的分支
git stash pop // 恢复之前工作区以便继续新功能的开发, 同时删除stash记录(git stash list看不到stash的任何内容) &amp;#x3C;==&gt; git stash apply &amp;#x26;&amp;#x26; git stash drop
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;分支策略&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;code&gt;master分支&lt;/code&gt;: 最稳定的分支，仅用来发布新版本，平时对代码做的修改不能直接往上push&lt;/p&gt;
&lt;p&gt;&lt;code&gt;dev分支&lt;/code&gt;: 是不稳定的, 一般在新版本发布的时候把dev分支合并到master上，在master分支发布新版本&lt;/p&gt;
&lt;p&gt;&lt;code&gt;个人分支&lt;/code&gt;: 每个人都有自己的分支，提交代码的时候往dev分支上合并&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/thumbnail.DqOP6Bd4_2x5EQT.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;标签管理&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;创建标签&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;适应场景&lt;/strong&gt;: 对当前最新的提交commit打标签&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{2}&quot;&gt;git checkout master // 切换到需要打标签的分支
git tag v1.0 // 打一个名为v1.0的标签
git tag // 查看已打标签列表
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;适应场景&lt;/strong&gt;: 对当历史提交的commit打标签&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{2}&quot;&gt;git log --pretty=oneline --abbrev-commit // 以简洁的形式列出所有commit记录
git tag v0.9 commit_id // 对commit_id对应的这次commit打一个名为v0.9的标签
git tag // 查看标签记录
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;带有说明的标签&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;$ git tag -a v0.1 -m &quot;version 0.1 released&quot; commit_id // 创建带有说明的标签
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;查看标签&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git show v0.9 // 查看v0.9这一个标签的详情
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;-a&lt;/code&gt;: 指定标签名&lt;/p&gt;
&lt;p&gt;&lt;code&gt;-m&lt;/code&gt;: 指定说明文字&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;推送标签&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git push origin v1.0 // 推送v1.0这一个标签到origin关联的远程仓库
git push origin --tags // 一次性推送所有标签到origin关联的远程仓库
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;删除标签&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 要删除的标签仅创建在本地未推送到远程仓库&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git tag -d v0.9 // 删除v0.9这一个标签(仅本地删除该标签)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 要删除的标签已经推送到远程仓库, 并且也要删除远程仓库上相应的标签&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{2}&quot;&gt;git tag -d v0.9 // 本地删除v0.9这一个标签
git push origin :refs/tags/v0.9 // 删除origin关联的远程仓库的v0.9这一标签(删除也用push), 这是github删除远程仓库标签, coding等其他的远程仓库未测试
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;三、进阶命令&lt;/h2&gt;
&lt;h3&gt;多个远程仓库管理&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 当前本地的git项目是clone自&lt;code&gt;https://github.com/JohnieXu/eleme-web-vue.git&lt;/code&gt;, 同时这个项目又需要同步关联coding上的另一个远程仓库&lt;code&gt;https://git.coding.net/JohnieXu/eleme-web-vue.git&lt;/code&gt;, 要求每次&lt;code&gt;push&lt;/code&gt;代码同步&lt;code&gt;push&lt;/code&gt;到两个远程仓库。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{3-5}&quot;&gt;git remote add origin https://github.com/JohnieXu/eleme-web-vue.git
git remote add coding https://git.coding.net/JohnieXu/eleme-web-vue.git
git remote add all https://github.com/JohnieXu/eleme-web-vue.git // 将all关联到github上的远程仓库
git remote set-url --add --push all https://github.com/JohnieXu/eleme-web-vue.git // 在all上追加push对应的远程仓库地址为github的地址
git remote set-url --add --push all https://git.coding.net/JohnieXu/eleme-web-vue.git // 在all上追加push对应的远程仓库地址为github的地址
git add . &amp;#x26;&amp;#x26; git commit -m &apos;init&apos; &amp;#x26;&amp;#x26; git push -u all master // all分支同时关联了origin和coding两个远程仓库, 推送all即可同步push代码到两个远程仓库
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;暂存修改&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 当前&lt;code&gt;git&lt;/code&gt;项目有&lt;code&gt;master&lt;/code&gt;,&lt;code&gt;dev&lt;/code&gt;两个分支, 无意中在本地&lt;code&gt;pull&lt;/code&gt;的&lt;code&gt;master&lt;/code&gt;分支上做了&lt;strong&gt;有用的&lt;/strong&gt;代码修改, 临时需要处理&lt;code&gt;master&lt;/code&gt;分支上的一个&lt;code&gt;bug&lt;/code&gt;; 同时, 由于之前在&lt;code&gt;master&lt;/code&gt;分支上的修改正常应该放在&lt;code&gt;dev&lt;/code&gt;分支上, 需要在处理完&lt;code&gt;bug&lt;/code&gt;后将&lt;code&gt;master&lt;/code&gt;分支上的修改放到&lt;code&gt;dev&lt;/code&gt;分支上。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{5-6}&quot;&gt;git stash // 暂存master分支上的修改(效果和git checkout -- .相似, 但是对代码的修改并未删除)
git checkout -b bug // 根据master分支创建新的临时bug分支
git add . &amp;#x26;&amp;#x26; git commit -m &apos;commit&apos; // 修复bug并commit修改
git checkout master &amp;#x26;&amp;#x26; git merge bug &amp;#x26;&amp;#x26; git branch -d bug // 合并bug分支到master分支, 删除bug分支
git checkout dev // 切换到dev分支, 此分支为之前对master分支的修改正常所在分支
git stash pop // 将之前暂存的修改放到dev分支, 此时处于处于modified状态, 需要git add . &amp;#x26;&amp;#x26; git commit
...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;补充说明:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;git stash list&lt;/code&gt;: 查看所有的暂存信息&lt;/li&gt;
&lt;li&gt;&lt;code&gt;git stash apply stash@{0}&lt;/code&gt;: 恢复&lt;code&gt;stash@{0}&lt;/code&gt;这个暂存, 也可用&lt;code&gt;git stash apply&lt;/code&gt;即默认恢复第一个暂存, 此命令并未删除&lt;code&gt;stash&lt;/code&gt;记录, 需用&lt;code&gt;git stash drop&lt;/code&gt;删除暂存记录&lt;/li&gt;
&lt;li&gt;&lt;code&gt;git stash pop&lt;/code&gt;: 恢复到第一个暂存并删除这条暂存记录&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;撤回已移除文件&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 在多人协作开发时， 他人误将之前未完善的文件删除了， 同时（在自己提交代码之前）推送到了远程仓库， 而自己想找回被删掉的文件； 或者只想找回自己之前删掉的文件。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{2}&quot;&gt;git log --diff-filter=D --summary // 查找删除文件的记录， 获取对应的commitID及要恢复的文件路径filepath
git checkout &amp;#x3C;commitID&gt;~1 filepath // 恢复filepath这个文件， 原理： 撤回filepath这个文件到上次(～1)提交的commit状态
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;撤回commit记录&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;本地仓库里面进行了大量代码更新，并且已经commit过了，或者commit过很多次，然后推送到了远程仓库，后来发现这其中有些commit存在较大问题，希望退回远程仓库提交记录；&lt;/li&gt;
&lt;li&gt;在一个本地仓库里面错误的关联了另一个远程仓库，在推送代码到远程仓库时候没有确认远程仓库地址是否正确，导致错误的将错误的代码推送到了远程仓库，希望回退远程仓库提交记录。&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git log --graph // 查看commit记录，找到希望保留的最近一次推送记录的commitId
git reset &amp;#x3C;commitID&gt; --hard // 将本地仓库切换到刚刚找到的commitID，此时本地仓库已经退回至之前的commit记录
git push origin &amp;#x3C;branchName&gt; -f // 将本地仓库强制推送到远程仓库，本地已经回退的提交记录将覆盖远程仓库
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;补充说明:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;commitID&lt;/code&gt;：希望退回到的提交记录ID&lt;/li&gt;
&lt;li&gt;&lt;code&gt;branchName&lt;/code&gt;：需要推送的分支名&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;::: tip 找回误删的commit
如果错误的将某个commit记录撤回了，可以通过&lt;code&gt;git reflog&lt;/code&gt;查看最近所有的commit操作记录，通过记录的commitId使用上面第二步进行恢复被误删的commit
:::&lt;/p&gt;
&lt;h3&gt;更新fork的项目&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 在github上fork了一份他人的项目， 后来他人的项目更新了， 自己fork的这一份也想更新。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{3,5}&quot;&gt;git remote -v // 查看当本地项目(fork的项目)分支详情
git remote add upstream https://github.com/JohnieXu/eleme-web-vue.git // 创建远程仓库源upstream并关联到原始仓库
git fetch upstream // 拉取原始仓库
git checkout master // 切换到本地项目需要更新的分支
git rebase upstream/master // 将原始仓库更新合并到本地需要更新仓库
git push origin master // 推送更新到本地仓库关联的远程仓库(自己github上fork的仓库)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;子模块管理&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;Submodules rely on nesting repos: you have repos within… repos. The module has its own repo, somewhere inside the working directory of its container repo.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;blockquote&gt;
&lt;p&gt;子模块是一个独立的git仓库，拥有独立的commit记录，拉取主项目不会自动更新子模块&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 一个主项目中依赖了其他子项目，分别由不同人员协作开发&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash{3}&quot;&gt;git submodule add https://github.com/JohnieXu/cars-component.git // 添加子模块
git submodule update --init --recursive
 // 更新一个含有submodule的远程仓库，同时拉取其submodule --recursive为可选
git clone --recursive https://github.com/JohnieXu/cars-component.git // 克隆一个含有submodule的远程仓库，同时拉取其submodule
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;移除子模块&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git submodule deinit path/to/module // 移除子模块关联
git rm path/to/module // 移除子模块文件夹
git commit -am update // commit
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;合并其他分支的文件&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;: 在一个项目的多个分支中同时有个多个feature等分支，想把某一个分支的部分文件的修改合并到另一个分支上&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout --patch [branch_name] [target_file_path] // 接下来根据提示确认要合并的修改
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;补充说明:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;branch_name&lt;/code&gt;：需要合并的文件所在分支&lt;/li&gt;
&lt;li&gt;&lt;code&gt;target_file_path&lt;/code&gt;：需要合并的文件的地址&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;切换到远程特定分支&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;适用场景&lt;/strong&gt;:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在进行项目克隆时使用常规的&lt;code&gt;git clone ....&lt;/code&gt;命令克隆的完整的项目，是包含所有的分支和标签的，但是用&lt;code&gt;git branch&lt;/code&gt;查看只会看到master分支；&lt;/li&gt;
&lt;li&gt;另一种情况是在克隆项目为了减小克隆文件大小，采用&lt;code&gt;git clone --depth=1 ...&lt;/code&gt;只克隆&lt;code&gt;master&lt;/code&gt;分支上最近一次&lt;code&gt;commit&lt;/code&gt;，这时候项目只有&lt;code&gt;master&lt;/code&gt;分支；&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这时候希望切换到远程仓库其他的分支，而直接&lt;code&gt;git checkout branch_name&lt;/code&gt;是不生效的（本地没有对应分支）&lt;/p&gt;
&lt;p&gt;情况一：（直接 &lt;code&gt;git clone&lt;/code&gt;）&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout -b 3.x-stable origin/3.x-stable // 拉取远程的3.x-stable分支并关联到本地3.x-stable分支
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;实际操作结果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/9f575ce8-4ec3-4986-8111-3878f798f8b1.Btuk30CL_Z26VHTs.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;情况二：（&lt;code&gt;git clone --depth=1&lt;/code&gt;）&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;#git branch -va // 查看全部本地和远程分支，发现远程也没有3.x-stalbe分支
git remote set-branches origin &quot;3.x-stable&quot;
git fetch --depth=1 origin 3.x-stable // 拉取上一步自行关联的3.x-stable分支，这是 origin 生成 3.x-stable 分支
git checkout -b 3.x-stable origin/3.x-stable // 本地创建3.x-stable分支，并关联到 origin 上的 3.x-stable 分支
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;实际操作结果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/8b28328b-6d05-488c-82dd-b5a5fa49a42b.38oBGH-b_Z1S7dLG.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/2de6df20-a65d-4563-a8c0-5ed8173ee083.7_fGjFZ6_Z4hckA.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;拉取ant-design3.x-stable分支&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;更新克隆不完整的仓库&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;使用场景&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在&lt;a href=&quot;#%E5%88%87%E6%8D%A2%E5%88%B0%E8%BF%9C%E7%A8%8B%E7%89%B9%E5%AE%9A%E5%88%86%E6%94%AF&quot;&gt;上一步&lt;/a&gt;中克隆远程仓库时使用了&lt;code&gt;--depth=1&lt;/code&gt;等命令，导致本地仓库不是完整的git仓库，后续希望看到完整的commit提交记录&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git fetch --unshallow // 使用--unshallow属性来合并到完整的git仓库
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;实际操作结果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/5aa61a5b-0044-439b-8a6a-b15b51c63366.BuJ7TM4P_ZWTYeC.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;使用&lt;code&gt;git fetch --unshallow&lt;/code&gt;拉取完整的vue-router项目，后续的以及之前的commit记录都可以看到&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;创建git离线包bundle&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;使用场景&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;可以将本地仓库打包成一个 bundle发送给没有远程仓库权限或远程仓库暂时无法使用的的协作者。&lt;/p&gt;
&lt;p&gt;详见：&lt;a href=&quot;https://git-scm.com/book/zh/v2/Git-%E5%B7%A5%E5%85%B7-%E6%89%93%E5%8C%85&quot;&gt;Git 工具 - 打包&lt;/a&gt;&lt;/p&gt;
&lt;h4&gt;创建离线包&lt;/h4&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git bundle create develop.bundle HEAD develop // 将 develop 分支记录打包为 develop.bundle 压缩包
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;需要包含 HEAD 指向，下面根据这个 HEAD 指向来直接克隆生成项目。&lt;/p&gt;
&lt;h4&gt;使用离线包克隆项目&lt;/h4&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git bundle clone path/to/develop.bundle demos // 从 develop.bundle 中克隆生成 demos 项目
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;此时 demos 项目的 origin 将关联到 develop.bundle 文件所在地址&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/d2b99797-4d59-4e23-ba90-38c1a42d3ade.BYhdynAs_1rdsxH.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git bundle create develop.bundle -20 develop // 将 develop 分支最近的 20 条 commit 记录打包
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;此时把这个新生成的文件发送给他人并覆盖掉其用于创建 demos 项目的 develop.bundle 文件，然后可以使用&lt;code&gt;git fetch origin develop&lt;/code&gt; &lt;code&gt;git pull origin develop&lt;/code&gt;命令来更新项目&lt;/p&gt;
&lt;h4&gt;使用离线包更新仓库&lt;/h4&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git fetch origin develop // 从 origin 关联的 develop.bundle 中更新 git 仓库
git pull origin develop // 将 git 仓库中的 develop 分支记录更新到工作空间
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git bundle verify path/to/develop.bundle // 检查收到的离线包是否有效及是否可以合并到当前项目
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;a href=&quot;https://git-scm.com/docs/git-bundle&quot;&gt;git bundle 命令文档地址&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;按时间范围筛选commit记录&lt;/h3&gt;
&lt;p&gt;按时间范围过滤提交日志，并且显示提交信息和变更的文件名&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git log --since=&quot;2023-4-23 00:00:00&quot; --until=&quot;2023-4-23 23:59:59&quot; --pretty=format:&quot;%h - %an, %ar : %s&quot; --name-status --author=&quot;徐志勇&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/4bda467d-5ed7-4666-8b0e-20a55803ccaf.B9VjLlb__ZzNCx5.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;清理忽略但本地存在文件、目录&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;清理 &lt;code&gt;.gitignore&lt;/code&gt; 标记忽略，但本地存在的文件、目录&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;使用场景&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;一般前端项目执行构建命令，例如： &lt;code&gt;npm run build&lt;/code&gt; 会输出 &lt;code&gt;dist&lt;/code&gt; 等目录，这个目录一般会被 &lt;code&gt;.gitignore&lt;/code&gt; 标记为忽略，通过下面命令可以快速删除 &lt;code&gt;dist&lt;/code&gt; 等目录。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git clean -X -f [path]
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;-X&lt;/code&gt;: 用于指定仅删除忽略文件中列出的文件和目录&lt;/li&gt;
&lt;li&gt;&lt;code&gt;-f&lt;/code&gt;: 用于强制执行删除操作&lt;/li&gt;
&lt;li&gt;&lt;code&gt;[path]&lt;/code&gt;: 可选的路径参数，传递时只会清理这个路径下的文件和目录&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;清理之前错误提交的文件&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;使用场景&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;之前提交时候将 &lt;code&gt;dist&lt;/code&gt; &lt;code&gt;lib&lt;/code&gt; 等编译输出的文件、目录提交到了 git 中，后来修改 &lt;code&gt;.gitignore&lt;/code&gt; 文件将其标记为忽略，这时候需要将 &lt;code&gt;dist&lt;/code&gt; &lt;code&gt;lib&lt;/code&gt; 等文件、目录删除并提交。下面命令可以快捷的执行：&lt;strong&gt;删除&lt;/strong&gt;、&lt;strong&gt;添加暂存区&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git rm -r --cached /path/to/ignored_folder/
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;-r&lt;/code&gt;: 表示递归地删除文件夹及其内容&lt;/li&gt;
&lt;li&gt;&lt;code&gt;--cached&lt;/code&gt;: 表示只从 Git 缓存中删除文件，而不会从文件系统中删除它们&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/path/to/ignored_folder/&lt;/code&gt;: 要删除的文件夹的路径&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;执行 &lt;code&gt;git rm -r --cached tsdoc-demo/lib/&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/8fc2ade7-10b2-4557-9cd2-ff842ff30d84.t-H_ZF9D_1NpGs8.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;执行之后的状态：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/d40d9aec-847f-4fb3-9618-b3394f4cbd07.BxAg07w-_2ljXfJ.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;::: warning
相当于执行：&lt;strong&gt;删除&lt;/strong&gt;、&lt;strong&gt;添加暂存区&lt;/strong&gt;，后续需要你自己 commit &lt;strong&gt;提交&lt;/strong&gt;
:::&lt;/p&gt;
&lt;h3&gt;基于tag创建新分支&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;使用场景&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在业务上需要基于之前某次发版的tag代码来修复bug或添加新需求，修改的代码需要放到新的分支上提交并发布。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# 基于tag创建新分支
git checkout -b [new_branch_name] [tag_name]

# 推送新创建的分支
git push -u origin [new_branch_name]
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;-b&lt;/code&gt;: 表示创建新分支&lt;/li&gt;
&lt;li&gt;&lt;code&gt;new_branch_name&lt;/code&gt;:d 新分支的名字&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tag_name&lt;/code&gt;: tag的名字&lt;/li&gt;
&lt;li&gt;&lt;code&gt;-u&lt;/code&gt;: 表示将&lt;code&gt;[new_branch_name]&lt;/code&gt;分支关联到远程仓库，否则直接&lt;code&gt;git push&lt;/code&gt;会报错：&lt;code&gt;fatal: The current branch xxx has no upstream branch.&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;四、实际使用流程&lt;/h2&gt;
&lt;h3&gt;基本配置&lt;/h3&gt;
&lt;p&gt;下面几种场景的&lt;strong&gt;操作前提&lt;/strong&gt;都需要先完成基本配置：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;配置用户名和邮箱&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;#%E9%85%8D%E7%BD%AE%E7%94%A8%E6%88%B7%E5%90%8D%E5%92%8C%E9%82%AE%E7%AE%B1&quot;&gt;配置用户名和邮箱&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git config --global user.name &quot;JohnieXu&quot; // 配置全局用户名为JohnieXu
git config --global user.email &quot;281910378@qq.com&quot; // 配置全局邮箱为281910378@qq.com
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;配置公钥(建议)&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;#%E9%85%8D%E7%BD%AE%E5%85%AC%E9%92%A5&quot;&gt;生成公钥文件&lt;/a&gt;，然后在git服务器中保存本地生成的公钥文件内容，公钥文件一般位于:&lt;/p&gt;
&lt;p&gt;macos: &lt;code&gt;~/.ssh/id_rsa.pub&lt;/code&gt;
windows: &lt;code&gt;用户目录/.ssh/id_rsa.pub&lt;/code&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;场景一：从远程克隆项目&lt;/h3&gt;
&lt;p&gt;这是最常见的使用场景，用于从公司现有项目/github开源项目/本地git仓库克隆项目继续进行开发，涉及如何&lt;code&gt;分支管理&lt;/code&gt; &lt;code&gt;提交更新&lt;/code&gt; &lt;code&gt;如何解决代码冲突&lt;/code&gt;等&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;从远程克隆项目&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;默认拉取整个git项目将是处于master分支，然后在本地进行分支等操作&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git clone git clone https://github.com/JohnieXu/eleme-web-vue.git // 默认拉取master分支
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;切换分支&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;一般多人协作开发的项目都有多个分支，发时候切换到对应的开发分支&lt;/p&gt;
&lt;p&gt;切换到开发分支(假设是dev分支)&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout dev // 假设当前开发分支在dev分支上
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;创建本地个人分支&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;主要根据开发分支创建自己的个人分支，后面对代码的修改都在这个分支上完成，这个分支不用提交到远程仓库；开发完成功能后需要合并更新到开发分支。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout -b chenxing // 从当前分支（dev）创建新的分支：chenxing，同时会切换到chenxing这一分支
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;此时处于&lt;code&gt;chenxing&lt;/code&gt;这一分支，目前项目内容与&lt;code&gt;dev&lt;/code&gt;分支一模一样，然后可以进入项目开发了。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;向git提交记录&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git add . // 把当前目录下所有修改的文件添加进暂存区
git commit -m &quot;这里是写一些描述信息&quot; // 提交commit记录
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;完成这一步操作后，对项目的修改就被添加进git的chenxing分支了，使用&lt;code&gt;git status&lt;/code&gt;命令会看到分支内容是干净的。&lt;/p&gt;
&lt;p&gt;假如这时候预期安排开发的功能都完成了，或者他人需要看到你最近更新的功能，就需要进行下一步操作了，不然则继续接着开发。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;合并更新到开发分支&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git checkout dev // 切换到dev分支
git merge chenxing // 将自己分支的更新合并到dev分支
git branch -d chenxing // 删除自己的分支
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;拉取远程仓库更新&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git pull origin dev // 将远程仓库dev分支更新拉取到本地并尝试快速合并本地更新
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;如果命令行提示快速合并成功，则可以直接推送代码到远程仓库，如果提示有代码冲突则进入下面解决冲突的步骤。&lt;/p&gt;
&lt;p&gt;拉取远程更新时候遇到冲突会有类似下面这样的提示：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/5d72eaa2-15e1-41bd-b3d1-9ab54341d1f8.B6dSa21-_Z15el1T.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;运行命令&lt;code&gt;git status&lt;/code&gt;，会看到类似下面展示的冲突文件提示：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/7fa5ccf5-1baf-492d-927e-eeeaf0de642f.BONktKpz_G5PRg.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;解决冲突&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;最简单快速的解决冲突方法就是采用&lt;a href=&quot;https://code.visualstudio.com/&quot;&gt;vscode编辑器&lt;/a&gt;配合&lt;a href=&quot;https://marketplace.visualstudio.com/items?itemName=eamodio.gitlens&quot;&gt;gitlen插件&lt;/a&gt;快速定位和修复冲突文件，然后再次提交commit修复冲突。&lt;/p&gt;
&lt;p&gt;在vscode编辑器中，文件带有&lt;code&gt;C&lt;/code&gt;(conflict首字母)标识的文件为存在冲突的文件&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/e482005b-506e-4b2a-ba92-05fcfaf29c91.BKR3AEkX_ZlX5A3.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;点开冲突文件，通过&lt;code&gt;gitlen&lt;/code&gt;可以看到冲突情况如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/1521f01a-6f01-4d7e-83df-29c9682bcc57.DGR4dsH-_EhhNl.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;文件中存在三种特殊的标识：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;=======&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;&gt;&gt;&gt;&gt;&gt;&gt;&gt;&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;对应的说明在下图的标注中，冲突指的是对应的&lt;code&gt;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C; HEAD&lt;/code&gt;和&lt;code&gt;=======&lt;/code&gt;之间的内容与&lt;code&gt;=======&lt;/code&gt;和&lt;code&gt;&gt;&gt;&gt;&gt;&gt;&gt;&gt; 46e233a7b4b601b896c13a6e2c577d12c87f3c17&lt;/code&gt;之间的内容不一致，需要删除你不需要的部分，最后&lt;strong&gt;确保代码中不再有上面的任何一个特殊标记&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/39633bfe-48f6-4b37-96b2-af9d834259e8.DGxuTxcb_Z24H7XJ.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;:::tip 注意
代码冲突解决完毕 === 不存在任何的&lt;code&gt;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&amp;#x3C;&lt;/code&gt; &lt;code&gt;=======&lt;/code&gt; &lt;code&gt;&gt;&gt;&gt;&gt;&gt;&gt;&gt;&lt;/code&gt;标识
:::&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;推送更到到远程仓库&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;手动修复完冲突后，需要添加文件然后提交commit，然后可以在vscode的文件管理器中看到文件没有&lt;code&gt;C&lt;/code&gt;标识，然后将更新推送到远程仓库&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git add . // 添加本次修复冲突更改的文件
git commit -m &quot;fix conflict&quot; // 提交更新
git push origin dev // 推送更新到远程仓库的dev分支
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;场景二：仅本地使用&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;初始化git&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git init
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;添加修改文件&amp;#x26;提交&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git add . // 添加修改的文件
git commit -m &quot;本次更新的一些描述信息&quot; // 提交更新到本地git仓库
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;修复最后一次的commit信息&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git commit --amend // 然后在vim编辑界面编辑commit信息即可
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;删除git仓库信息&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;当需要完全删除git版本/提交记录等时候使用；仅仅删除git仓库记录的内容，文件不会删除；可以再次初始化git重新添加git管理&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;rm -rf .git // 删除git仓库
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;打包bundle&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;将某个分支下的一个commitId或HEAD指向的内容压缩成bundle包，可以快速与他人共享特定部分的代码，在没有git服务器的时候也可以通过这种方式来推送更新内容给他人。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git bundle create name.bundle HEAD dev // 将dev分支HEAD指向处的内容（包含之前的commit记录，不包含其他分支）打包成name.bundle文件，存在当前目录下
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;通过bundle文件克隆项目&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这种方式克隆克隆的项目只有bundle中的部分分支和部分commit记录&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git clone path/to/name.bundle test // 从name.bundle文件中克隆处项目存在./test文件夹下
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;场景三：本地使用然后同步至远程&lt;/h3&gt;
&lt;p&gt;本地存在一个git项目，需要推送到git服务器中与他人共享&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;git remote add origin https://github.com/JohnieXu/eleme-web-vue.git // 将远程仓库关联到本地git仓库的origin上，远程仓库必须为空项目
git push origin master // 推送本地master分支到远程仓库origin的master上
git push origin dev // 推送本地dev分支到远程仓库origin的dev上
git push origin master --tags // 当存在tags时可以顺便推送tags到远程仓库
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;后面的操作与&lt;a href=&quot;#%E5%9C%BA%E6%99%AF%E4%B8%80%EF%BC%9A%E4%BB%8E%E8%BF%9C%E7%A8%8B%E5%85%8B%E9%9A%86%E9%A1%B9%E7%9B%AE&quot;&gt;场景一&lt;/a&gt;的用法一致不再赘述&lt;/p&gt;
&lt;h2&gt;五、Git不是万能的&lt;/h2&gt;
&lt;p&gt;:::tip
这里主要说的是对Git设计思想方面的思考（就是一些废话，可略过）
:::&lt;/p&gt;
&lt;h3&gt;如何理解分支&lt;/h3&gt;
&lt;p&gt;我想Git的核心就是&lt;code&gt;master&lt;/code&gt;分支， &lt;code&gt;master&lt;/code&gt;分支就像是生命的成长历程，当你在面对重大抉择的时候就该考虑创建不同分支进行不同尝试，其他分支可以看做一个&lt;strong&gt;试错&lt;/strong&gt;或者一个新的&lt;strong&gt;生活方式&lt;/strong&gt;，最后都会回到生命的本质&lt;code&gt;master&lt;/code&gt;分支。&lt;/p&gt;
&lt;p&gt;分支另一个意义在于在不同的阶段做不同的尝试，甚至你可以将某个分支暂存很多年，即使如此，Git也不会遗忘它。某一天你突然想到某件种事情、某种尝试等，&lt;code&gt;master&lt;/code&gt;也会欣然接受（可能会有临时的拒绝）。&lt;/p&gt;
&lt;h3&gt;如何理解commit&lt;/h3&gt;
&lt;p&gt;如果说分支是一个生命历程的一部分，那&lt;code&gt;commit&lt;/code&gt;就是每天的日程记录、重要事件记录等，有了&lt;code&gt;commit&lt;/code&gt;多年之后仍然能够回忆起当时这么做的原因等。&lt;/p&gt;
&lt;h3&gt;如何理解tag&lt;/h3&gt;
&lt;p&gt;随着&lt;code&gt;master&lt;/code&gt;分支的不断更新，总会在某个时间点恰好满足了当时的开发需求，于是此时就可以打个&lt;code&gt;tag&lt;/code&gt;标签，可类比于自己特定情况下达成的一个小目标。&lt;/p&gt;
&lt;p&gt;Git本身就是一套管理规则，纵使你提交了无数个&lt;code&gt;commit&lt;/code&gt;， 打了无数个&lt;code&gt;tag&lt;/code&gt;，它也不会帮你成为“海贼王”找到&lt;code&gt;one piece&lt;/code&gt;。就像各种技术的设计思想一样，Git本身也是源自于生活，也最终会回到生活。生活一样也没有万能的东西，且行且珍惜～&lt;/p&gt;</content:encoded><h:img src="/_astro/thumbnail.DqOP6Bd4.png"/><enclosure url="/_astro/thumbnail.DqOP6Bd4.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>深入理解 JS 原型与继承</title><link>https://luopengcheng89.github.io/blog/js_prototype</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/js_prototype</guid><description>每个构造函数都有一个原型对象，原型对象都包含一个指向构造函数的指针，实例都包含一个指向原型对象的内部指针。</description><pubDate>Sun, 03 May 2020 17:08:54 GMT</pubDate><content:encoded>&lt;h2&gt;前言&lt;/h2&gt;
&lt;p&gt;最近在整体地复习一遍现代前端必备的核心知识点，将会整理成一个前端分析总结文章系列。这篇是其中的第三篇，主要是总结下JS中原型与继承等核心知识点。（另外，此系列文章也可以在语雀专栏——&lt;a href=&quot;https://www.yuque.com/johniexu/frontend&quot;&gt;硬核前端系列&lt;/a&gt;查看）。&lt;/p&gt;
&lt;h2&gt;一、原型机制&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;“每个构造函数都有一个原型对象，原型对象都包含一个指向构造函数的指针，实例都包含一个指向原型对象的内部指针。”
——《JavaScript高级程序设计》&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;核心总结&lt;/h3&gt;
&lt;p&gt;实例对象是通过 &lt;code&gt;new&lt;/code&gt; 操作符来操作构造函数 &lt;code&gt;constructor&lt;/code&gt; 生成的。实例对象具有 &lt;code&gt;__proto__&lt;/code&gt; 属性，构造函数具有 &lt;code&gt;prototype&lt;/code&gt; 属性。&lt;/p&gt;
&lt;p&gt;原型（&lt;code&gt;prototype&lt;/code&gt;）本身也是一个对象，称为&lt;strong&gt;原型对象&lt;/strong&gt;。构造函数上的属性 &lt;code&gt;prototype&lt;/code&gt; 指向原型对象，实例上的属性 &lt;code&gt;__proto__&lt;/code&gt; 指向原型对象。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/thumbnail.UfARAG0n_Z4oPvF.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;code&gt;prototype&lt;/code&gt; 与 &lt;code&gt;___proto___&lt;/code&gt; 的指向关系图&lt;/p&gt;
&lt;p&gt;借助原型实现继承的核心思想是：对象在查找属性和方法时首先看对象自身是否存在，不存在则去原型对象上查找，若未找到则继续到原型对象的原型对象上查找，依次进行下去直到查找到内置对象&lt;code&gt;Object&lt;/code&gt;。即，&lt;strong&gt;实例属性访问是沿着原型链向上递归查找&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/2623680a-41fb-4a08-9048-dff736105bd8.gxz3mb7R_Z1rvmot.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;实例属性访问时沿原型链递归查找&lt;/p&gt;
&lt;p&gt;原型对象也是对象也存在自己的原型对象，这里的原型对象形成的链条就是&lt;strong&gt;原型链&lt;/strong&gt;。内置对象 &lt;code&gt;Object&lt;/code&gt; 的原型对象是 &lt;code&gt;null&lt;/code&gt;，&lt;code&gt;Object&lt;/code&gt; 对象是所有对象最顶层的原型对象。&lt;/p&gt;
&lt;p&gt;完整的构造函数、原型、原型链组成及之间的关系如下图所示：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/30a1bb57-0d4e-49ac-ad03-4581324e4401.DpDmVtrW_Zufdsf.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;实例分析&lt;/h3&gt;
&lt;p&gt;以下面这个基础的继承实现为例：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function constructorFn (state, data) {
    this.state = state;
    this.data = data;
    this.isPlay = function () {
        return this.state + &apos; is &apos; + this.data;
    }
}
var instance1 = new constructorFn (&apos;1&apos;, &apos;doing&apos;);
var instance2 = new constructorFn (&apos;2&apos;, &apos;done&apos;);
console.log(instance1.isPlay()); // 1 is doing
console.log(instance2.isPlay()); // 2 is done
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这里分别生成了两个实例：&lt;code&gt;instance1&lt;/code&gt; 和 &lt;code&gt;instance2&lt;/code&gt;，其构造函数通过对 &lt;code&gt;this&lt;/code&gt; 进行赋值使得各自实例有各自的独立属性和方法。&lt;/p&gt;
&lt;p&gt;这种情况下的实例、原型对象、构造函数之间的关系图如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/1dd05efd-d977-4994-97c0-35cce5d973de.CscDAvLV_ZaPsBM.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;实例、原型对象、构造函数之间的关系图&lt;/p&gt;
&lt;p&gt;上面例子中的 &lt;code&gt;isPlay&lt;/code&gt; 方法在各自的实例对象上进行了重复定义，方法的逻辑是一样的可以进行复用，下面采用 &lt;code&gt;prototype&lt;/code&gt; 来优化。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function constructorFn (state, data) {
    this.state = state;
    this.data = data;
}
constructorFn.prototype.isPlay = function () {
    return this.state + &apos; is &apos; + this.data;
}
var instance1 = new constructorFn (&apos;1&apos;, &apos;doing&apos;);
var instance2 = new constructorFn (&apos;2&apos;, &apos;done&apos;);
instance1.isDoing = &apos;nonono!&apos;;
instance2.isDoing = &apos;nonono!&apos;;
console.log(instance1.isPlay()); // 1 is doing
console.log(instance2.isPlay()); // 2 is done
console.log(instance1.isDoing); // nonono!
console.log(instance2.isDoing); // nonono!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这里将 &lt;code&gt;isPlay&lt;/code&gt; 方法存到构造函数的原型对象上面，然后分别给两个实例对象添加 &lt;code&gt;isDoing&lt;/code&gt; 属性。此时实例对象的 &lt;code&gt;isPlay&lt;/code&gt; 方法是通过其 &lt;code&gt;__proto__&lt;/code&gt; 指向的原型对象而访问到构造函数原型 &lt;code&gt;prototype&lt;/code&gt; 上的 &lt;code&gt;isPlay&lt;/code&gt; 方法，而实例对象的 &lt;code&gt;isDoing&lt;/code&gt; 属性是其实例对象本身的属性。&lt;/p&gt;
&lt;p&gt;这种情况下的实例、原型对象、构造函数之间的关系图如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/2bae0844-c97a-4529-b9a8-ebd7a240b193.CylGbxB1_Z2oS8He.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;实例、原型对象、构造函数之间的关系图&lt;/p&gt;
&lt;p&gt;这时候通过构造函数的 &lt;code&gt;prototype&lt;/code&gt; 修改原型对象属性，所有继承自原型的属性都被修改，而实例对象自身的属性不会改变。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;constructorFn.prototype.isDoing = &apos;yesyesyes!&apos;;
console.log(instance1.isDoing); // yesyesyes!
console.log(instance2.isDoing); // yesyesyes!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;同样，通过实例的 &lt;code&gt;__proto__&lt;/code&gt; 修改原型对象属性，所有继承自原型的属性都被修改，而实例对象自身的属性不会改变。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;- constructorFn.prototype.isDoing = &apos;yesyesyes!&apos;;
+ instance1.__proto__.isDoing = &apos;yesyesyes!&apos;;
console.log(instance1.isDoing); // yesyesyes！
console.log(instance2.isDoing); // yesyesyes！
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;上述情况下的内存模型如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/45e39817-7c06-4f5a-9d7d-ed185f246864.DxgG0r5A_Z12CuEd.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;上述实例部分内存模型&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;多个实例对象的 &lt;code&gt;___proto___&lt;/code&gt; 属性通过指针指向同一个原型对象——构造函数的原型对象；而实例本身的属性则是指向存储在对象本身。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;如果直接修改实例自身的属性 &lt;code&gt;isDoing&lt;/code&gt; ，则另一个实例的属性不会跟着修改。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;- constructorFn.prototype.isDoing = &apos;yesyesyes!&apos;;
+ instance1.isDoing = &apos;yesyesyes!&apos;;
console.log(instance1.isDoing); // yesyesyes!
console.log(instance2.isDoing); // nonono!
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;属性方法的查找过程&lt;/h4&gt;
&lt;p&gt;从对象本身开始，沿着原型组成的原型链逐级往上查找所访问的属性，找到相应的属性就返回，若直到 &lt;code&gt;Object.prototype&lt;/code&gt; 还未找到则返回 undefined。这里属性方法沿原型链的查找过程就是所谓的属性继承、方法重写以及继承方案的本质。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0e1a5b12-c8ee-4c00-8e67-d7e8d986f209.DPkwxbD3_ZN0hVc.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;属性沿着原型链查找示意图&lt;/p&gt;
&lt;h2&gt;二、new的本质&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;var obj = {};
obj.__proto__ =  constructorFn.prototype;
constructorFn.call(obj);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;new&lt;/code&gt; 主要做了以下四件事情&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;创建一个空对象&lt;/li&gt;
&lt;li&gt;将上面创建对象的原型 &lt;code&gt;__proto__&lt;/code&gt; 指向构造函数的原型 &lt;code&gt;prototype&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;将构造函数上下文 &lt;code&gt;this&lt;/code&gt; 指向创建的对象然后执行&lt;/li&gt;
&lt;li&gt;返回上面创建的对象&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;实例分析&lt;/h3&gt;
&lt;p&gt;还是使用原型机制中用过的例子进行分析&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function constructorFn (state, data) {
    this.state = state;
    this.data = data;
}
constructorFn.prototype.isPlay = function () {
    return this.state + &apos; is &apos; + this.data;
}
constructorFn.prototype.isDoing = &apos;nonono!&apos;;
var instance1 = new constructorFn (&apos;1&apos;, &apos;doing&apos;);
var instance2 = new constructorFn (&apos;2&apos;, &apos;done&apos;);
console.log(instance1.isPlay()); // 1 is doing
console.log(instance2.isPlay()); // 2 is done
console.log(instance1.isDoing); // nonono!
console.log(instance2.isDoing); // nonono!
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;对于 &lt;code&gt;var instance1 = new constructorFn (&apos;1&apos;, &apos;doing&apos;);&lt;/code&gt; 执行过程如下图所示。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/1c454a56-eb16-42d1-a221-f816fc1f0ec7.CLtfI-Aw_2qHiLG.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;new 的执行过程图示&lt;/p&gt;
&lt;h2&gt;三、this 的指向问题&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;this 永远指向函数的直接调用者&lt;/li&gt;
&lt;li&gt;如果存在 new 关键字，则 this 指向 new 出来的那个对象&lt;/li&gt;
&lt;li&gt;在事件中，this 指向触发这个事件的对象，特殊的是，IE 中的 attachEvent 中的 this 总是指向全局对象 window&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function constructorFn (state, data) {
    this.data = data;
    this.state = state;
  	console.log(this);
}
var obj = {
	constructorFn
};
constructorFn(&apos;a&apos;, &apos;b&apos;); // Window
obj.constructorFn(&apos;a&apos;, &apos;b&apos;); // obj
var instance1 = new constructorFn(&apos;a&apos;, &apos;b&apos;); // instance1
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;对于 &lt;code&gt;constructorFn(&apos;a&apos;, &apos;b&apos;);&lt;/code&gt; 函数体内部 this 指向 Window 对象；对于 &lt;code&gt;obj.constructorFn(&apos;a&apos;, &apos;b&apos;);&lt;/code&gt; 函数体内部 this 绑定到了 obj；对于 &lt;code&gt;var instance1 = new constructorFn(&apos;a&apos;, &apos;b&apos;);&lt;/code&gt; 函数体内部 this 绑定到了 new 出来的那个对象 instance1。&lt;/p&gt;
&lt;h3&gt;函数嵌套的情况&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function a() {
  return () =&gt; {
    return () =&gt; {
      console.log(this)
    }
  }
}
console.log(a()()())
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;首先箭头函数其实是没有 this 的，箭头函数中的 this 只取决包裹箭头函数的第一个普通函数的 this。在这个例子中，因为包裹箭头函数的第一个普通函数是 a，所以此时的 this 是 window。另外对箭头函数使用 bind这类函数是无效的。&lt;/p&gt;
&lt;p&gt;最后种情况也就是 bind 这些改变上下文的 API 了，对于这些函数来说，this 取决于第一个参数，如果第一个参数为空，那么就是 window。&lt;/p&gt;
&lt;h3&gt;call、apply、bind&lt;/h3&gt;
&lt;p&gt;三者都是将第一个参数作为上下文绑定到其调用者的上下文 this 上，如果第一个参数不存在则默认绑定 Window 到调用者的上下文 this。&lt;/p&gt;
&lt;p&gt;call 以散列值的形式将函数参数传入前面调用函数并执行；apply 以数组的形式将函数参数传入前面调用函数并执行；bind 仅绑定前面函数的上下分 this 不执行前面函数。&lt;/p&gt;
&lt;h3&gt;使用多次 bind 绑定 this&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;let a = {}
let fn = function () { console.log(this) }
fn.bind().bind(a)() // Window
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;上述代码等价于：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;let fn2 = function fn1() {
  return function() {
    return fn.apply()
  }.apply(a)
}
fn2()
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;就是说：&lt;strong&gt;多次使用 bind 绑定 this 只有第一次绑定 this 生效&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;如果存在多次绑定函数的上下文 this，则按照优先级进行判断&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;首先，new 的方式优先级最高，接下来是 bind 这些函数，然后是 obj.foo() 这种调用方式，最后是 foo 这种调用方式，同时，箭头函数的 this 一旦被绑定，就不会再被任何方式所改变。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;四、类型判断&lt;/h2&gt;
&lt;h3&gt;instanceof&lt;/h3&gt;
&lt;p&gt;主要用法如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function constructorFn (state, data) {
    this.data = data;
    this.state = state;
}
var instance1 = new constructorFn(&apos;a&apos;, &apos;b&apos;)
console.log(instance1 instanceof constructorFn) // true || false
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;内部原理如下：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;instanceof 主要的实现原理就是只要右边变量的 prototype 在左边变量的原型链上即可。因此，instanceof 在查找的过程中会遍历左边变量的原型链，直到找到右边变量的 prototype，如果查找失败，则会返回 false。&lt;/strong&gt;&lt;/p&gt;
&lt;h3&gt;Object.prototype.toString.call()&lt;/h3&gt;
&lt;p&gt;主要用法如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function constructorFn (state, data) {
    this.data = data;
    this.state = state;
}
var instance1 = new constructorFn(&apos;a&apos;, &apos;b&apos;);
var array1 = [1, 2, 3];
var boolean1 = true;
var string1 = &apos;a&apos;;
var number1 = 123;
var set1 = new Set();
var map1 = new Map();
class Class1 {};
var symbol1 = new Symbol();
console.log(Object.prototype.toString.call(instance1)); 	// [object Object]
console.log(Object.prototype.toString.call(array1)); 			// [object Array]
console.log(Object.prototype.toString.call(boolean1)); 		// [object Boolean]
console.log(Object.prototype.toString.call(string1)); 		// [object String]
console.log(Object.prototype.toString.call(number1)); 		// [object Number]
console.log(Object.prototype.toString.call(constructorFn));// [object Function]
console.log(Object.prototype.toString.call(null)); 				// [object Null]
console.log(Object.prototype.toString.call(set1)); 				// [object Set]
console.log(Object.prototype.toString.call(map1)); 				// [object Map]
console.log(Object.prototype.toString.call(Class1)); 			// [object Function]
console.log(Object.prototype.toString.call(symbol1)); 		// [object Symbol]
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;用法原理如下：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;对象可以有自己的 toString 方法，也可以由从父类继承过来的 toString 方法，这些方法的执行逻辑可能被更改过，估计直接通过 &lt;code&gt;instance1.toString() 方&lt;/code&gt;法不能准确获取对象类型信息。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;这种用法的思路是将 &lt;code&gt;Object.prototype.toString&lt;/code&gt; 方法内部 this 绑定到当前的对象上调用，这样不管当前的对象有没有提供或继承别的 toString 方法只会执行 &lt;code&gt;Object.prototype&lt;/code&gt; 上的 toString 方法，确保了可以准确打印对象的类型信息。&lt;/p&gt;
&lt;h2&gt;五、常用继承方案&lt;/h2&gt;
&lt;h3&gt;原型链继承&lt;/h3&gt;
&lt;p&gt;将子类的原型 prototype 指向父类的实例对象来实现父类属性和方法的继承；因为父类实例对象的构造函数 constructor 指向了父类原型，所以需要将子类原型构造函数 constructor 指向子类构造函数。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function Animal (name) {
    this.name = name;
}
Animal.prototype = {
    canRun: function () {
        console.log(&apos;it can run!&apos;);
    }
}
function Cat () {
    this.speak = &apos;喵！&apos;;
} 
Cat.prototype = new Animal(&apos;miao&apos;);
Cat.prototype.constructor = Cat;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;call、apply 实现继承&lt;/h3&gt;
&lt;p&gt;通过 call、apply 改变函数的 this 指向，来将子类的 this 指向父类，在父类构造函数用当前子类 this 执行完成后，当前子类 this 即有了父类定义的属性和方法。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function Animal (name) {
    this.name = name;
}
Animal.prototype = {
  canRun: function () {
    console.log(&apos;it can run!&apos;);
  }
}
function Cat (name) {
  Animal.call(this, name);
  this.speak = &apos;喵！&apos;;
} 
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;组合继承&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;原型链继承与 call、apply 实现继承的结合应用&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;核心是在子类的构造函数中通过 &lt;code&gt;Animal.call(this)&lt;/code&gt; 继承父类的属性，然后改变子类的原型为父类实例对象来继承父类的方法。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function Animal(name) {
  this.name = name
}
Animal.prototype.getName = function() {
  console.log(this.name)
}
function Cat(name) {
  Animal.call(this, name)
}
Cat.prototype = new Animal()

const cat1 = new Cat(1)

cat1.getName() // 1
cat1 instanceof Animal // true
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这种继承方式优点在于构造函数可以传参，不会与父类引用属性共享，可以复用父类的函数，但是也存在一个缺点就是在继承父类函数的时候调用了父类构造函数，导致子类的原型上多了不需要的父类属性，存在内存上的浪费。&lt;/p&gt;
&lt;h3&gt;class 实现继承&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;说明下： es6 中的 class 类其实只是语法糖，上面打印 class 的类型信息可以发现其本质还是函数，只不过通过 extends、super等关键字对原型和构造函数的操作进行了简化。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;class Animal {
  constructor(name) {
    this.name = name
  }
  getValue() {
    console.log(this.name)
  }
}
class Cat extends Animal {
  constructor(name) {
    super(name)
    this.name = name
  }
}
let cat1 = new Cat(1)
cat1.getName() // 1
cat1 instanceof Animal // true
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;class 实现继承的核心在于使用 extends 表明继承自哪个父类，并且在子类构造函数中必须调用 super，因为这段代码可以看成 &lt;code&gt;Animal.call(this, value)&lt;/code&gt;。&lt;/p&gt;
&lt;h3&gt;TS 中 class 实现继承&lt;/h3&gt;
&lt;p&gt;TS 中的 class 继承其实是向 ECMAScript 规范靠近的，两者用法并无二致。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;class Animal {
  public name: string | null = null
  constructor(name: string) {
    this.name = name
  }
}
class Cat extends Animal {
  constructor(name: string) {
    super(name)
  }
  getName () {
    console.log(this.name)
  }
}
let cat1 = new Cat(&apos;1&apos;)
cat1.getName() // 1
cat1 instanceof Animal // true
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;参考文章&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://www.typescriptlang.org/v2/docs/handbook/classes.html#public-private-and-protected-modifiers&quot;&gt;https://www.typescriptlang.org/v2/docs/handbook/classes.html#public-private-and-protected-modifiers&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Inheritance_and_the_prototype_chain&quot;&gt;https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Inheritance_and_the_prototype_chain&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;http://zzfed.com/#/detail/5bab326aa696b41dfdc00fe8&quot;&gt;http://zzfed.com/#/detail/5bab326aa696b41dfdc00fe8&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</content:encoded><h:img src="/_astro/thumbnail.UfARAG0n.png"/><enclosure url="/_astro/thumbnail.UfARAG0n.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>全面分析总结BFC原理及实践</title><link>https://luopengcheng89.github.io/blog/bfc</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/bfc</guid><description>这篇文章主要就是彻底分析清楚BFC原理、总结BFC经典场景中的用法，最后在实践中总结BFC在H5、小程序开发中的应用。</description><pubDate>Mon, 02 Mar 2020 14:51:02 GMT</pubDate><content:encoded>&lt;h2&gt;前言&lt;/h2&gt;
&lt;p&gt;经常在面试中被问到“如何清除浮动？”、“为什么 &lt;code&gt;overflow: hidden&lt;/code&gt; 可以清除浮动？”等等比较基础的问题。虽然这些题目案在各种写面试题的文章中都有提供答案，但这种教科书式的问答肯定不是我们的目的，与其记住答案不如彻底掌握其核心原理——&lt;strong&gt;块级格式化上下文（BFC）&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;这篇文章主要就是彻底分析清楚BFC原理、总结BFC经典场景中的用法，最后在实践中总结BFC在H5、小程序开发中的应用。&lt;/p&gt;
&lt;h2&gt;一、BFC作用&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;清除浮动&lt;/strong&gt;：BFC会包含创建它的元素内部的所有内容（包含浮动元素）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;外边距折叠&lt;/strong&gt;：解决同一BFC容器中的相邻元素间的外边距折叠问题&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;左图右文布局&lt;/strong&gt;：利用浮动元产生BFC以及BFC之间不会互相覆盖实现左图右文布局&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;:::tip
以上BFC的作用可以心里默想一遍如何实现，下文将会有实例说明
:::&lt;/p&gt;
&lt;h2&gt;二、如何产生BFC&lt;/h2&gt;
&lt;p&gt;| 名称 | 如何产生 | 备注 |
| :---: | :---: | :---: |
| html根元素 | html元素本身就是一个BFC元素 | 记住就好，body元素不是BFC |
| 浮动元素 | 元素的float属性不是none | 常用 |
| 绝对定位元素 | position属性为absolute或fixed | 常用 |
| 行内块元素 | display属性为inline-block | 常用 |
| 表格元素 | 表格元素默认display属性即是BFC | 表格单元格、表格标题也是 |
| 弹性元素 | display属性为flex或inline-flex元素的直接子元素 | 非常重要（flex布局常用） |
| 网格元素 | display属性为grid或inline-grid元素的直接子元素 | 新特性用得少 |
| 多列容器 | 元素的column-count或column-width不为auto，包括 column-count为1 | 新特性用得少 |
| overflow属性 | overflow属性值不为visible的元素 | 常用oveflfow：visible |
| display属性 | display属性值为flow-root的元素，flow-root: 一个新的display属性的值，它可以创建无副作用的BFC | 用得少 |
| contain属性 | contain属性值为layout、content、paint的元素 | 用得少 |&lt;/p&gt;
&lt;p&gt;总结成一句话：&lt;strong&gt;凡脱离文档流都可以产生BFC&lt;/strong&gt;&lt;/p&gt;
&lt;h2&gt;三、BFC原理&lt;/h2&gt;
&lt;h3&gt;三种文档定位方式&lt;/h3&gt;
&lt;p&gt;在讲BFC的原理之前先看看html文档的三种定位方式&lt;/p&gt;
&lt;h4&gt;普通流&lt;/h4&gt;
&lt;blockquote&gt;
&lt;p&gt;在普通流中，元素按照其在HTML中的先后位置自上而下布局，在这个过程中，行内元素水平排列，直到当行被占满然后换行，块级元素则会被渲染为完整的一个新行，除非另外指定，否则所有元素默认都是普通流定位，也就是说普通流中元素的位置由该元素在HTML文档中的位置决定。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4&gt;浮动&lt;/h4&gt;
&lt;blockquote&gt;
&lt;p&gt;在浮动定位中，元素首先按照普通流的位置出现，然后根据浮动的方向尽可能的向左边或右边偏移，其效果与印刷排版中的文本环绕相似。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4&gt;绝对定位&lt;/h4&gt;
&lt;blockquote&gt;
&lt;p&gt;在绝对定位中，元素会整体脱离普通流，因此绝对定位元素不会对其兄弟元素造成影响，而元素具体的位置由绝对定位的坐标决定。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;BFC产生作用的原因&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;其实BFC是上面三种布局方式中的普通流，BFC会产生一个独立的容器，该容器内部的元素不会在布局上影响到外部的元素，在外部的普通流看来它和其他普通流元素无差别，文档最终会按照上面说的普通流计算布局。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjerebmcj30s70jbjsa.jpg&quot; alt=&quot;00831rSTly1gcfjerebmcj30s70jbjsa.jpg&quot;&gt;&lt;/p&gt;
&lt;h3&gt;BFC的注意事项&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;块格式化上下文对浮动定位与清除浮动都很重要。浮动定位和清除浮动时只会应用于同一个BFC内的元素。&lt;/p&gt;
&lt;p&gt;浮动不会影响其它BFC中元素的布局，而清除浮动只能清除同一BFC中在它前面的元素的浮动。&lt;/p&gt;
&lt;p&gt;外边距折叠也只会发生在属于同一BFC的块级元素之间。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;四、BFC常见用法&lt;/h2&gt;
&lt;p&gt;为了更好的理解BFC，我们先看看下面这些常见的用法。&lt;/p&gt;
&lt;h3&gt;🌰清除浮动防止高度塌陷&lt;/h3&gt;
&lt;p&gt;按照上面三种定位方式中说的，浮动元素会脱离普通文档流，导致外部元素的高度计算不包括浮动元素本身高度，形成高度塌陷。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjf6gqisj30s70jbq2y.jpg&quot; alt=&quot;00831rSTly1gcfjf6gqisj30s70jbq2y.jpg&quot;&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;div class=&quot;container&quot;&gt;
  &amp;#x3C;div class=&quot;box box1&quot;&gt;&amp;#x3C;/div&gt;
&amp;#x3C;/div&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-less&quot;&gt;.box {
  width: 20px;
  height: 50px;
  float: left; // 脱离了文档流形成了一个BFC
	border: 4px solid green;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;采用上述产生BFC元素的方法之一使外层元素产生BFC，可以防止元素高度塌陷。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-less&quot;&gt;.container {
	overflow: hidden; // 外层容器产生BFC
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjfn88zyj30s70jbwel.jpg&quot; alt=&quot;00831rSTly1gcfjfn88zyj30s70jbwel.jpg&quot;&gt;&lt;/p&gt;
&lt;h3&gt;🌰处理外边距折叠的问题&lt;/h3&gt;
&lt;p&gt;在普通文档流中，元素（非BFC元素）的外边距margin会自动折叠，产生如下现象。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjg2llwij30s70jbmxb.jpg&quot; alt=&quot;00831rSTly1gcfjg2llwij30s70jbmxb.jpg&quot;&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;div class=&quot;container&quot;&gt;
  &amp;#x3C;div class=&quot;box box1 m20&quot;&gt;&amp;#x3C;/div&gt;
  &amp;#x3C;div class=&quot;box box2 m20&quot;&gt;&amp;#x3C;/div&gt;
&amp;#x3C;/div&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-less&quot;&gt;.m20 {
  margin: 20px;
}
.box {
	border: 1px solid green;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个是html的特性不算bug，但是我们更希望margin不产生折叠效果，通过利用BFC元素之间的外边距不会折叠的特性来实现。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;div class=&quot;container&quot;&gt;
  &amp;#x3C;div class=&quot;box box1 m20&quot;&gt;&amp;#x3C;/div&gt;
  &amp;#x3C;div class=&quot;box box2 m20&quot;&gt;&amp;#x3C;/div&gt;
&amp;#x3C;/div&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-less&quot;&gt;.m20 {
  margin: 20px;
}
.box {
  overflow: hidden; // 使得两个box成为BFC元素
	border: 4px solid green;
}

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;使两个box形成BFC或者分别用两个BFC包裹box，然后形成的两个BFC之间的外边距不会折叠，修复后效果如下图。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjgfwd9lj30s70jbwen.jpg&quot; alt=&quot;00831rSTly1gcfjgfwd9lj30s70jbwen.jpg&quot;&gt;&lt;/p&gt;
&lt;h2&gt;五、扩展一下&lt;/h2&gt;
&lt;p&gt;存在块级格式化上下文BFC，则对应存在内联格式化上下文IFC、网格格式化上下文GFC、自适应格式化上下文FFC，这些都可以统称为格式化上下文。&lt;/p&gt;
&lt;h3&gt;IFC&lt;/h3&gt;
&lt;p&gt;内联格式化上下文，IFC 的 line box（线框）高度由其包含行内元素中最高的实际高度计算而来，不受到竖直方向的 padding、margin 影响。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjgv7m78j30ms0gbq37.jpg&quot; alt=&quot;00831rSTly1gcfjgv7m78j30ms0gbq37.jpg&quot;&gt;&lt;/p&gt;
&lt;p&gt;当在内联元素中插入块级元素会产生什么效果呢？在内联元素中插入位置前后的内联元素各自形成一个IFC，然后按钮文档流形式进行布局，效果如下图。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjh6gad6j30s70jbdg5.jpg&quot; alt=&quot;00831rSTly1gcfjh6gad6j30s70jbdg5.jpg&quot;&gt;&lt;/p&gt;
&lt;h3&gt;GFC&lt;/h3&gt;
&lt;p&gt;网格布局格式化上下文（&lt;code&gt;display: grid&lt;/code&gt;）&lt;/p&gt;
&lt;h3&gt;FFC&lt;/h3&gt;
&lt;p&gt;自适应格式化上下文（&lt;code&gt;display: flex&lt;/code&gt;）&lt;/p&gt;
&lt;h2&gt;六、常见问题分析&lt;/h2&gt;
&lt;h3&gt;为什么overflow: hidden可以去起到清除浮动的效果？&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;overflow: hidden使得外层元素产生了一个BFC，BFC的高度计算包含其内部的浮动元素，从而达到清除浮动效果&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;div style=&quot;border: 5px solid #6EBD91; overflow: hidden;&quot;&gt;
  &amp;#x3C;div style=&quot;float: left; border: 5px solid #F4D491;&quot;&gt;Lorem ipsum dolor sit amet consectetur adipisicing elit. Accusamus, maxime fuga assumenda excepturi, exercitationem rerum quae voluptates sunt perspiciatis cupiditate sed itaque officia, placeat minus iure quod expedita earum nam.Eum aliquam totam iure mollitia deserunt, minus repellendus. Harum ullam tenetur, impedit aliquam nobis ut dignissimos eligendi, expedita illum iste esse odio ab quos explicabo, odit architecto. Tempora, hic facilis?&amp;#x3C;/div&gt;
&amp;#x3C;/div&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjhktx6nj31fw076gn8.jpg&quot; alt=&quot;00831rSTly1gcfjhktx6nj31fw076gn8.jpg&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;分析过程&lt;/strong&gt;
外层&lt;code&gt;overflow: hidden&lt;/code&gt;的元素产生了一个BFC，本身&lt;code&gt;float: left&lt;/code&gt;的元素也产生了一个BFC，外层BFC进行尺寸计算时包含内层BFC尺寸。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjhzce41j30s20b83zx.jpg&quot; alt=&quot;00831rSTly1gcfjhzce41j30s20b83zx.jpg&quot;&gt;&lt;/p&gt;
&lt;h3&gt;如何实现两列自实行布局？&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;不考虑使用UI框架中的布局组件情况下，直接使用float: left + 固定左侧宽度 + 右边列margin-left&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;div&gt;
  &amp;#x3C;img style=&quot;float: left; width: 140px;&quot; src=&quot;https://tva1.sinaimg.cn/large/00831rSTly1gcel7v9ji4j3041041wec.jpg&quot; alt=&quot;&quot;&gt;
  &amp;#x3C;div style=&quot;margin-left: 150px;&quot;&gt;
    &amp;#x3C;p&gt;Lorem ipsum dolor sit amet consectetur adipisicing elit. Quos eligendi numquam nihil excepturi sint reiciendis iusto maiores nostrum fugiat harum?&amp;#x3C;/p&gt;
    &amp;#x3C;p&gt;Lorem ipsum dolor sit amet consectetur adipisicing elit. Quam, perspiciatis magnam consectetur corrupti suscipit a ratione sunt commodi beatae ad!&amp;#x3C;/p&gt;
  &amp;#x3C;/div&gt;
&amp;#x3C;/div&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;实现效果如下图：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjieiybuj312e09kq3r.jpg&quot; alt=&quot;00831rSTly1gcfjieiybuj312e09kq3r.jpg&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;分析过程&lt;/strong&gt;
**
左侧部分使用float: left会产生一个BFC元素，这个BFC与右侧块级元素div一起进行普通流定位；&lt;strong&gt;由于是两个按照普通流定位这个两个元素之前不会产生覆盖&lt;/strong&gt;，即使不固定左侧元素宽度和右侧块级元素给margin-left也可以实现效果（这里给宽度是为了控制左侧固定尺寸）。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjiuoztej31rc0hc0v6.jpg&quot; alt=&quot;00831rSTly1gcfjiuoztej31rc0hc0v6.jpg&quot;&gt;&lt;/p&gt;
&lt;h3&gt;内联元素中使用块级元素会产生什么效果？&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;内联元素中插入块级元素会在插入的块级元素前后各产生一个匿名块与插入的块按照普通流进行定位&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjj7cuddj31r00880u0.jpg&quot; alt=&quot;00831rSTly1gcfjj7cuddj31r00880u0.jpg&quot;&gt;&lt;/p&gt;
&lt;h3&gt;内联元素中使用插入浮动元素会产生什么效果？&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;内联元素使用了display: inline-block会产生一个IFC，其内部的浮动会内部进行浮动定位，然后整个IFC看成一个块级元素与外部进行定位&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;span style=&quot;display: inline-block; background-color: #6EBD91;&quot;&gt;
  Lorem ipsum dolor sit amet.
  &amp;#x3C;span style=&quot;float: left; background-color: chocolate;&quot;&gt;Lorem, ipsum.&amp;#x3C;/span&gt;
  &amp;#x3C;div style=&quot;background-color: #F4D491;&quot;&gt;Lorem, ipsum dolor sit amet consectetur adipisicing elit. Repellat labore, ipsa quo possimus mollitia, officiis quia provident inventore placeat nulla, rem velit ratione ducimus. Facilis eos repudiandae debitis quam voluptatem.&amp;#x3C;/div&gt;
  Lorem ipsum dolor sit amet consectetur.
&amp;#x3C;/span&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;./00831rSTly1gcfjdpqgqlj31ra0b840p.jpg&quot; alt=&quot;00831rSTly1gcfjdpqgqlj31ra0b840p.jpg&quot;&gt;&lt;/p&gt;
&lt;p&gt;以上效果可以&lt;a href=&quot;http://jsrun.pro/4vfKp&quot;&gt;在线预览&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;参考文章&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/Web/Guide/CSS/Block_formatting_context&quot;&gt;MDN-格式化上下文&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/Advanced-Frontend/Daily-Interview-Question/issues/59&quot;&gt;gihtub-介绍下BFC及其应用&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://zhuanlan.zhihu.com/p/25321647&quot;&gt;知乎专栏-10分钟理解BFC原理&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</content:encoded><h:img src="/_astro/thumbnail.dgicA_g5.jpg"/><enclosure url="/_astro/thumbnail.dgicA_g5.jpg"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>这次一定彻底弄懂 DOM 事件机制</title><link>https://luopengcheng89.github.io/blog/dom_event</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/dom_event</guid><description>一次彻底弄懂 DOM 事件机制。</description><pubDate>Wed, 26 Feb 2020 14:39:10 GMT</pubDate><content:encoded>&lt;h2&gt;前言&lt;/h2&gt;
&lt;p&gt;在网页端、移动端H5、小程序等各个终端环境的前端开发中随处可见事件的运用，可见事件机制的是前端这一块的重中之重。经过我研读了大量博客文章以及开源框架源码后，这次算是对DOM事件机制有了更新更全面的的认识。&lt;/p&gt;
&lt;p&gt;下文主要通过一个例子带你&lt;strong&gt;分析DOM事件的传递、事件处理&lt;/strong&gt;，然后在此基础上再&lt;strong&gt;深入总结对事件监听的优化方案&lt;/strong&gt;，最终总结下DOM事件机制&lt;strong&gt;在开源框架中的实战应用&lt;/strong&gt;。&lt;/p&gt;
&lt;h2&gt;一、从实例看事件传递&lt;/h2&gt;
&lt;p&gt;以下面这个普通的html文件为例&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;!DOCTYPE html&gt;
&amp;#x3C;html lang=&quot;en&quot; onclick=&quot;handleClickHtml()&quot;&gt;
&amp;#x3C;head&gt;
  &amp;#x3C;meta charset=&quot;UTF-8&quot;&gt;
  &amp;#x3C;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;
  &amp;#x3C;title&gt;Document&amp;#x3C;/title&gt;
&amp;#x3C;/head&gt;
&amp;#x3C;body onclick=&quot;handleBodyClick()&quot;&gt;
  &amp;#x3C;div id=&quot;div1&quot; onclick=&quot;handleClick2()&quot;&gt;
    &amp;#x3C;button id=&quot;button1&quot; onclick=&quot;handleClick1()&quot;&gt;handleClick1&amp;#x3C;/button&gt;
  &amp;#x3C;/div&gt;
  &amp;#x3C;script&gt;
    function handleClick1(e) {
      console.log(&apos;click1&apos;)
    }
    function handleClick2(e) {
      console.log(&apos;click2&apos;)
    }
    function handleBodyClick(e) {
      console.log(&apos;body clicked&apos;)
    }
    function handleClickHtml(e) {
      console.log(&apos;html clicked&apos;)
    }
    document.addEventListener(&apos;click&apos;, e =&gt; {
      console.log(&apos;document clicked&apos;)
    })
  &amp;#x3C;/script&gt;
&amp;#x3C;/body&gt;
&amp;#x3C;/html&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;例子中给&lt;code&gt;document、html、body、div、button&lt;/code&gt;都添加了点击事件，其执行结果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0082zybply1gca5zepk57j30n006u74g.C0AR5knl_Iewyo.webp&quot; alt=&quot;执行结果&quot;&gt;&lt;/p&gt;
&lt;h3&gt;事件传递&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;事件产生后，从window对象自上而下向目标节点传递，抵达目标节点后会沿着相反方向传递&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0082zybply1gca5xm2gchj30ml0gljs9.DdSYfs_P_ZwrOCb.webp&quot; alt=&quot;DOM事件传递&quot;&gt;&lt;/p&gt;
&lt;h4&gt;事件传递路径如何确定？&lt;/h4&gt;
&lt;p&gt;由鼠标、键盘、触摸屏、窗口缩放、图片加载、节点焦点变化等触发事件，每个触发的事件都有一个target——事件目标，根据target往html根节点遍历就可以确定节点嵌套层级关系，从而确定此事件的传递路径。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;事件传递路径由浏览器、webview来确定&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4&gt;事件传递为何要来回走两遍？&lt;/h4&gt;
&lt;p&gt;一个事件来回走两遍是便于事件响应时机的控制（下文的事件冒泡与事件捕获的应用中有体现），同时也存在事件冒泡的浏览器历史原因（与IE、Netscape有关，不赘述）。&lt;/p&gt;
&lt;h3&gt;事件响应&lt;/h3&gt;
&lt;p&gt;事件在从自上而下的传递过程中会&lt;strong&gt;判断当前node节点是否绑定对应事件的监听器&lt;/strong&gt;，若有则执行响应监听器的事件处理程序（也就是绑定的事件要执行的逻辑），这里的执行就是所谓的事件响应。&lt;/p&gt;
&lt;p&gt;事件的响应过于与事件的类型是捕获事件、冒泡事件有关，同时是否组件冒泡还会影响该事件的后续传递过程，具体执行流程总结成如下图所示：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/thumbnail.CIJLfWj2_Z2iMNNa.webp&quot; alt=&quot;事件响应逻辑&quot;&gt;&lt;/p&gt;
&lt;h3&gt;事件捕获与冒泡&lt;/h3&gt;
&lt;p&gt;根据事件传递中的两种传递路径分别分为：&lt;strong&gt;由上至下——事件捕获阶段，由下至上——事件冒泡阶段&lt;/strong&gt;，指的是一个事件由window（上图并未标出window）到target之间的传递的过程。&lt;/p&gt;
&lt;p&gt;可以通过阻止事件冒泡让事件不再继续完成由下至上的过程，这会让target的父节点绑定冒泡阶段的事件监听器不响应事件。&lt;/p&gt;
&lt;p&gt;可以通过指定事件在捕获阶段执行。&lt;/p&gt;
&lt;h4&gt;🌰冒泡的应用&lt;/h4&gt;
&lt;p&gt;看下面这个例子，可以通过&lt;code&gt;stopPropagation&lt;/code&gt;来阻止事件继续往上冒泡，&lt;code&gt;window、document、html&lt;/code&gt;上添加的点击事件均不会生效&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;!-- 省略了部分代码 --&gt;
&amp;#x3C;div id=&quot;div1&quot;&gt;
  &amp;#x3C;button id=&quot;button1&quot;&gt;button&amp;#x3C;/button&gt;
&amp;#x3C;/div&gt;
&amp;#x3C;script&gt;
  var div1 = document.getElementById(&apos;div1&apos;)
  var button1 = document.getElementById(&apos;button1&apos;)
  div1.addEventListener(&apos;click&apos;, (e) =&gt; {
    console.log(e.currentTarget)
  }, false)
  button1.addEventListener(&apos;click&apos;, (e) =&gt; {
    console.log(e.currentTarget)
    e.stopPropagation() // 关键代码：阻止了click事件继续往上冒泡
  }, false)
    
  // 以下是监听html、document的点击事件
  function handleClickHtml(e) {
    console.log(&apos;html clicked&apos;)
  }
  document.addEventListener(&apos;click&apos;, e =&gt; {
    console.log(&apos;document clicked&apos;)
  })
&amp;#x3C;/script&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;点击button的打印结果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0082zybply1gca61b2enzj30n603wglj.rR1c3VHM_1Ui0YI.webp&quot; alt=&quot;执行结果&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;addEventListener需要用attachEvent进行兼容性处理，第三个参数默认值为false表示在事件冒泡阶段响应事件&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4&gt;🌰捕获的应用&lt;/h4&gt;
&lt;p&gt;还是上面这个例子，只更改div1部分的事件监听，的第三个参数为true来开启事件捕获&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;!-- 省略了部分代码 --&gt;
&amp;#x3C;div id=&quot;div1&quot;&gt;
  &amp;#x3C;button id=&quot;button1&quot;&gt;button&amp;#x3C;/button&gt;
&amp;#x3C;/div&gt;
&amp;#x3C;script&gt;
  var div1 = document.getElementById(&apos;div1&apos;)
  var button1 = document.getElementById(&apos;button1&apos;)
  div1.addEventListener(&apos;click&apos;, (e) =&gt; {
    console.log(e.currentTarget)
  }, true) // true：表示div1在事件的捕获阶段响应事件
  button1.addEventListener(&apos;click&apos;, (e) =&gt; {
    console.log(e.currentTarget)
    e.stopPropagation() // 阻止事件冒泡
  }, false)
  
  // 以下是监听html、document的点击事件
  function handleClickHtml(e) {
    console.log(&apos;html clicked&apos;)
  }
  document.addEventListener(&apos;click&apos;, e =&gt; {
    console.log(&apos;document clicked&apos;)
  })
&amp;#x3C;/script&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;点击button的打印结果如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0082zybply1gca626mdjej30n404eglo.NwnXERpO_Z1WMiGp.webp&quot; alt=&quot;执行结果&quot;&gt;&lt;/p&gt;
&lt;p&gt;依次执行了div1和button1绑定的点击事件，注意这里两者的&lt;strong&gt;执行顺序&lt;/strong&gt;很关键。事件在捕获阶段遇到div1节点时执行打印，然后在button处（处于目标阶段）执行打印，然后事件被阻止冒泡不再往上传递。&lt;/p&gt;
&lt;h2&gt;二、事件性能优化&lt;/h2&gt;
&lt;h3&gt;大批量事件监听性能问题&lt;/h3&gt;
&lt;p&gt;考虑以下场景：&lt;/p&gt;
&lt;p&gt;有个一个长消息列表实时接受新的消息，滚动到底部时加载更多消息，点击消息可进入回话窗口页面，消息左滑消息此条消息&lt;/p&gt;
&lt;p&gt;问题：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;如何给此消息列表添加左滑事件？&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;抽象来看这个问题就是如何高效的给大批量（甚至是无限量）节点添加事件，这里暂时不考虑vue、react等框架&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4&gt;性能问题原因&lt;/h4&gt;
&lt;p&gt;每注册一个事件监听监听都需要使用一定内存，同时在dom节点多了之后事件经过的捕获、冒泡阶段要处理事件传递也就慢了&lt;/p&gt;
&lt;h3&gt;事件委托&lt;/h3&gt;
&lt;p&gt;父节点嵌套的子节点的触发的事件会通过事件冒泡到达父节点，事件处理程序不直接绑定到子节点，统一由公共父节点进行事件监听。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;e.target表示触发事件的元素，通过e.target可以判断具体响应那个元素的事件&lt;/strong&gt;
&lt;strong&gt;e.currentTarget表示事件绑定的元素，在事件委托情况下指向同一个元素&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0082zybply1gca63h6wufj30mn0hagm5.R5LEjmEE_2vpQUL.webp&quot; alt=&quot;事件委托&quot;&gt;&lt;/p&gt;
&lt;h4&gt;采用事件委托改造本文例子&lt;/h4&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;!-- 省略了部分代码 --&gt;
&amp;#x3C;div id=&quot;div1&quot;&gt;
  &amp;#x3C;button id=&quot;button1&quot;&gt;button&amp;#x3C;/button&gt;
&amp;#x3C;/div&gt;
&amp;#x3C;script&gt;
  document.addEventListener(&apos;click&apos;, e =&gt; {
    // e.target：触发事件的元素
    // e.currentTarget：事件监听器绑定的元素
    // console.log(e.target, e.currentTarget)
    if (e.target.id === &apos;button1&apos;) {
      return console.log(&apos;button1 clicked&apos;)
    }
    if (e.target.id === &apos;div1&apos;) {
      return console.log(&apos;div1 clicked&apos;)
    }
    if (e.target.tagName === &apos;HTML&apos;) {
      return console.log(&apos;html clicked&apos;)
    }
    if (e.target.tagName === &apos;BODY&apos;) {
      return cosnole.log(&apos;body clicked&apos;)
    }
  })
&amp;#x3C;/script&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;依次点击&lt;code&gt;html、div1、button1&lt;/code&gt;会依次打印结果：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0082zybply1gca63yca8pj30n2054t8q.DpEsAc9d_Z20KpMg.webp&quot; alt=&quot;执行结果&quot;&gt;&lt;/p&gt;
&lt;h3&gt;虚拟滚动&lt;/h3&gt;
&lt;p&gt;上面事件委托可以解决大批量节点事件监听的性能问题，但是DOM节点量过大导致界面滚动不流畅的问题没解决。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;虚拟滚动是指在列表滚动事件中动态计算出当前窗口中能够显示的列表节点，其前后窗口不显示的节点各用一个div进行占位处理，并调整占位div的样式确模拟的列表样式与原始列表无异处。&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;虚拟滚动已经是一个完美解决长列表渲染性能的方案，针对react、vue均有对应库实现，此处不再赘述&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;React事件代理&lt;/h3&gt;
&lt;p&gt;事件委托也叫事件代理，在react中的合成事件则是完全采用了事件代理&lt;/p&gt;
&lt;p&gt;基于react16的&lt;code&gt;ant-design-pro&lt;/code&gt;项目的事件绑定截图如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/0082zybply1gca64qg4t8j30rw0di0ti.DZueQ2SR_Z1Ahemp.webp&quot; alt=&quot;React事件代理&quot;&gt;&lt;/p&gt;
&lt;p&gt;直接通过onClick绑定的事件通过react处理最终会统一绑定到document上，正是使用的事件委托。&lt;/p&gt;
&lt;h2&gt;三、实战中的应用&lt;/h2&gt;
&lt;h3&gt;小程序中阻止事件冒泡阻止弹窗滚动穿透&lt;/h3&gt;
&lt;p&gt;最新版微信小程序中绑定事件有两种类型：&lt;code&gt;bind:methodName、catch:methodName、mut-bind:methodName、mut-catch:methodName&lt;/code&gt;，其中区别最大的是前两者。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;methodName表示移动端事件名，例如：tap、touchmove等&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;code&gt;bind:methodName&lt;/code&gt;&lt;strong&gt;采用冒泡绑定事件，&lt;/strong&gt;&lt;code&gt;catch:methodName&lt;/code&gt;**采用冒泡绑定事件并阻止冒泡&lt;/p&gt;
&lt;h4&gt;vant-weapp中防止滚动穿透&lt;/h4&gt;
&lt;p&gt;实现思路：&lt;strong&gt;弹框采用一层遮罩层overlay遮盖，遮罩层宽高设为窗口宽高同大小，采用&lt;code&gt;catch:touchmove&lt;/code&gt;绑定该层的touchmove事件为空函数防止其冒泡&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;给overlay组件外层绑定touchmove为空函数并阻止冒泡：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// overlay/index.wxml
&amp;#x3C;van-transition
  show=&quot;{{ show }}&quot;
  custom-class=&quot;van-overlay&quot;
  custom-style=&quot;z-index: {{ zIndex }}; {{ customStyle }}&quot;
  duration=&quot;{{ duration }}&quot;
  bind:tap=&quot;onClick&quot;
  catch:touchmove=&quot;noop&quot;
  /*
   * 通过catch:touchmove来阻止冒泡至overlay层的事件继续往上冒泡，
   * 实现内层嵌套的内容可以响应touchmove事件而外层（页面）滚动事件不响应），
   * 以此来解决滚动事件穿透
  */
&gt;
  &amp;#x3C;slot&gt;&amp;#x3C;/slot&gt;
&amp;#x3C;/van-transition&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;overlay组件外层fixed定位固定宽高防止滚动：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-less&quot;&gt;// overlay/index.less
.van-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  .theme(background-color, &apos;@overlay-background-color&apos;);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;移动端H5框架中事件冒泡阻止弹窗滚动穿透&lt;/h3&gt;
&lt;h4&gt;vant中防止滚动穿透&lt;/h4&gt;
&lt;p&gt;实现思路：与vant-weapp小程序框架中相似，&lt;strong&gt;弹框采用一层遮罩层overlay遮盖，遮罩层宽高设为窗口宽高同大小，采用&lt;code&gt;@touchmove&lt;/code&gt;绑定该层的touchmove事件，在事件中用event.stopPropagation阻止事件冒泡&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;给overlay组件外层绑定touchmove事件阻止冒泡：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;return (
  &amp;#x3C;transition name=&quot;van-fade&quot;&gt;
    &amp;#x3C;div
      vShow={props.show}
      style={style}
      class={[bem(), props.className]}
      onTouchmove={preventTouchMove}
      {...inherit(ctx, true)}
    /&gt;
  &amp;#x3C;/transition&gt;
);

const preventTouchMove(e: Event): void = () =&gt; {
  event.stopPropagation();
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;overlay组件外层fixed定位固定宽高防止滚动：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-less&quot;&gt;.van-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  .theme(background-color, &apos;@overlay-background-color&apos;);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;参考文章&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://developer.mozilla.org/zh-CN/docs/Web/Events&quot;&gt;MDN-事件参考&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://blog.csdn.net/howgod/article/details/97615209&quot;&gt;浪里行舟-DOM事件机制&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://blog.csdn.net/huangpin815/article/details/69787924&quot;&gt;CSDN-事件冒泡（IE事件流），事件捕获和DOM事件流&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</content:encoded><h:img src="/_astro/thumbnail.CIJLfWj2.jpg"/><enclosure url="/_astro/thumbnail.CIJLfWj2.jpg"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>聊一聊实现 Vue 路由组件缓存遇到的’坑‘</title><link>https://luopengcheng89.github.io/blog/the_pitfalls_encountered_in_implementing_vue_routing_component_caching</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/the_pitfalls_encountered_in_implementing_vue_routing_component_caching</guid><description>本文详细分析了在iview-admin@2.0项目中使用keep-alive+vue-router实现路由组件缓存时遇到的缓存失效问题，探讨了iview-admin路由缓存实现原理及局限性，并提出了路由分类划分和动态注册的优化方案。</description><pubDate>Thu, 20 Feb 2020 11:51:02 GMT</pubDate><content:encoded>&lt;h2&gt;背景介绍&lt;/h2&gt;
&lt;p&gt;最近在做公司后台管理系统项目时遇到一个比较怪异的bug——采用&lt;code&gt;keep-alive+vue-router&lt;/code&gt;实现的路由缓存遇到&lt;strong&gt;缓存不生效&lt;/strong&gt;的情况。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/thumbnail.FpNboBm7_Z1spjmr.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h2&gt;项目背景&lt;/h2&gt;
&lt;p&gt;此项目是基于&lt;a href=&quot;https://github.com/iview/iview-admin/tree/2.0&quot;&gt;iview-admin@2.0&lt;/a&gt;进行开发的，全局状态管理采用vuex分module实现，路由采用vue-router进行路由配置表的方式实现。&lt;/p&gt;
&lt;p&gt;项目属于基于&lt;a href=&quot;https://www.cnblogs.com/niuli1987/p/9871182.html&quot;&gt;RBAC&lt;/a&gt;的后台管理系统，需要多用户多角色的权限控制，同时路由功能菜单以及展示的字段需按角色划分，因此左侧系统菜单根据接口进行动态设置。&lt;/p&gt;
&lt;p&gt;项目中可以进行菜单的图标、跳转路径、是否隐藏等进行配置，路径对应的前端路由则由前端的路由记录进行管理，即菜单管理功能设置的跳转路径是否可用由此路由记录决定。&lt;/p&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h2&gt;解决过程&lt;/h2&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h3&gt;核实路由缓存用法问题&lt;/h3&gt;
&lt;p&gt;发现这个bug之后梳理了一遍路由组件缓存的实现方案，同时仔细核对了一遍&lt;a href=&quot;https://router.vuejs.org/zh/api/#router-view&quot;&gt;vue-router的官方文档&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;官方推荐的用法如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-vue&quot;&gt;&amp;#x3C;transition&gt;
  &amp;#x3C;keep-alive&gt;
    &amp;#x3C;router-view&gt;&amp;#x3C;/router-view&gt;
  &amp;#x3C;/keep-alive&gt;
&amp;#x3C;/transition&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;可以排除基本用法的问题，如是继续翻阅&lt;a href=&quot;https://cn.vuejs.org/v2/guide/components-dynamic-async.html#%E5%9C%A8%E5%8A%A8%E6%80%81%E7%BB%84%E4%BB%B6%E4%B8%8A%E4%BD%BF%E7%94%A8-keep-alive&quot;&gt;vue的官方文档&lt;/a&gt;，keep-alive的缓存匹配原理是根据组件的name作为key来进行缓存，官方的提示&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/7f402f00-11d4-4cef-8543-79929256dd6d.B5gPu2dA_1kjrM6.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;随即检查代码中的路由记录的name以及对应component的name，发现都有定义好，因此&lt;strong&gt;排除基本用法的错误导致。&lt;/strong&gt;&lt;/p&gt;
&lt;h2&gt;iview-admin的不完善导致&lt;/h2&gt;
&lt;p&gt;进行上述问题排查后定位到此问题出在keep-alive组件的include参数处，keep-alive的参数说明如下：&lt;/p&gt;
&lt;blockquote&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;include&lt;/code&gt; - 字符串或正则表达式。只有名称匹配的组件会被缓存。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;exclude&lt;/code&gt; - 字符串或正则表达式。任何名称匹配的组件都不会被缓存。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;max&lt;/code&gt; - 数字。最多可以缓存多少组件实例。&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
&lt;p&gt;iview-admin@2.0中通过路由的meata参数——notCache来控制路由对应的组件是否进行缓存，其源码中有如下注释说明：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;em&gt;设为true后页面在切换标签后不会缓存，如果需要缓存，无需设置这个字段，而且需要设置页面组件name属性和路由配置的name一致&lt;/em&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;其文档文档上关于路由meta参数的说明&lt;a href=&quot;https://github.com/iview/iview-admin/blob/2.0/src/router/routers.js#L13&quot;&gt;在这里&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;其注释说明说要让组件实现缓存必须要保证页面组件的name和路由配置的name一致，而该项目中路由配置根据由后台设置功能决定，故修改不能缓存的页面的路由配置的namne即可解决问题。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;仅仅是解决了此bug总感觉还不够，不如顺便研究一个iview-admin的源码是如何实现路由缓存的。&lt;/p&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h4&gt;iview-admin实现路由组件缓存原理&lt;/h4&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/04e33871-b8b0-41f7-8ffa-7f45bfded077.KUH-sjJi_qQJw.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;iview-admin中将navTagList、menuList等数据保存在全局的vuex的app模块中，navTagList是当前的当前打开过的标签页数组根据路由变化动态更新；menuList是当前登录用户的菜单数组，默认根据路由记录的meta参数的access字段进行过滤&lt;/p&gt;
&lt;p&gt;cacheList是app模块的的getters之一，用户从tagNavList中计算出当前打开的标签页需要用keep-alive缓存的组件name数组，此处采用的是动态修改keep-alive组件的exclude的值来动态更新路由的缓存规则&lt;/p&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h3&gt;iview-admin的一些局限&lt;/h3&gt;
&lt;p&gt;iview-admin中的用户权限路由控制思路是：采用路由meata参数的access数组来标签当前路由可访问的用户角色，根据路由记录计算出对应用户的menuList进行渲染。&lt;/p&gt;
&lt;p&gt;这个实现方案存在一些不足：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;路由采用配置表的方式由前端进行配置，路由需要更新时需要前端更新代码进行打包部署的整套项目发布流程&lt;/li&gt;
&lt;li&gt;菜单完全由路由配置即用户角色计算得出，后端不能够灵活的重置页面菜单显示&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;基于以上这些考虑对iview-admin默认的权限控制、路由配置进行如下的优化。&lt;/p&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h3&gt;考虑优化方案&lt;/h3&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h4&gt;路由分类划分&lt;/h4&gt;
&lt;p&gt;项目中的路由组件包括Man.vue进行布局控制，404.vue 500.vue等处理前端路由异常的展示，login.vue error_log.vue等展示用户注册登录日志记录等页面，这些页面路由通常不会经常更新不需要进行动态注册，而像其他的复杂业务逻辑处理页面需要接口进行动态配置。&lt;/p&gt;
&lt;p&gt;故将路由进行拆分，基础路由——routers-common直接配置到router中，业务路由——routers进行动态注册&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/fa71a7f7-6846-4a58-a155-c7d83514d5cd.o1rfu5Xk_1G9G3m.webp&quot; alt=&quot;image.png&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h4&gt;路由动态注册&lt;/h4&gt;
&lt;p&gt;vuex的store中的user模块添加获取用户路由配置的action——getUserRoutes，在用户登录成功后或者或者Main.vue的mounted生命周期中根据用户token调用接口获取用户路由配置&lt;/p&gt;
&lt;p&gt;调用&lt;a href=&quot;https://router.vuejs.org/zh/api/#router-addroutes&quot;&gt;router.addRoutes&lt;/a&gt;方法进行路由的动态注册。&lt;/p&gt;
&lt;p&gt;&lt;/p&gt;
&lt;h2&gt;更进一步&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;后续考虑更进一步的研究vue-router的view部分的源码，彻底弄懂router-view如何与keep-alive实现关联，后续的更新将尽快更新到此处。&lt;/p&gt;
&lt;/blockquote&gt;</content:encoded><h:img src="/_astro/thumbnail.FpNboBm7.png"/><enclosure url="/_astro/thumbnail.FpNboBm7.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>带你全面分析 vue-router 源码（万字长文）</title><link>https://luopengcheng89.github.io/blog/source_code_of_vue_router</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/source_code_of_vue_router</guid><description>深入剖析vue-router@3.1.3源码实现，涵盖路由实例初始化、动态路由匹配、嵌套路由、导航守卫机制、路由懒加载及router-view组件工作原理等核心功能的实现细节。</description><pubDate>Thu, 13 Feb 2020 12:03:55 GMT</pubDate><content:encoded>&lt;h1&gt;前言&lt;/h1&gt;
&lt;p&gt;在&lt;a href=&quot;https://www.yuque.com/johniexu/frontend/dhgooo&quot;&gt;前一篇文章&lt;/a&gt;中遇到的vue路由组件缓存问题已经解决并对项目进行了适当的优化改进，但是并未开探寻究vue-router的源码，这篇就继续上次的话题，深入分析vue-router源码中对导航守卫、动态参数匹配、过渡效果和异步组件等的实现。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;本文分析的源码为vue-router@3.1.3，vue@2.6.11&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h1&gt;万丈高楼平地起&lt;/h1&gt;
&lt;p&gt;参考官网的&lt;a href=&quot;https://v3.router.vuejs.org/zh/guide/#%E8%B5%B7%E6%AD%A5&quot;&gt;起步中&lt;/a&gt;的使用实例，vue-router的基本思路是根据路由记录生成VueRouter实例并传入Vue的app实例的router属性上，同时使用router-view来挂载路由匹配的路由组件到页面某一位置。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const Foo = { template: &apos;&amp;#x3C;div&gt;foo&amp;#x3C;/div&gt;&apos; }
const Bar = { template: &apos;&amp;#x3C;div&gt;bar&amp;#x3C;/div&gt;&apos; }

const routes = [
  { path: &apos;/foo&apos;, component: Foo },
  { path: &apos;/bar&apos;, component: Bar }
]

const router = new VueRouter({
  routes // (缩写) 相当于 routes: routes
})

const app = new Vue({
  router
}).$mount(&apos;#app&apos;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;使用流程&lt;/h2&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/852cd383-a782-414c-9933-149d19db4198.DUC2mqZz_Z1fYbTl.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;这里vue-router的设计哲学与react-routerV4不太一样，前者是以路由配置来统一配置路由，后者是路由即组件的概念（不需要统一的路由配置，不过自行封装成路由配置）&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;核心特性&lt;/h2&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/9516091f-4193-41fc-b668-a8bd5e511ffe.BiJ8UifU_ZSIeOA.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;以上这些是vue-router提供的核心功能，完整的用法说明可以参考&lt;a href=&quot;https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html#%E5%93%8D%E5%BA%94%E8%B7%AF%E7%94%B1%E5%8F%82%E6%95%B0%E7%9A%84%E5%8F%98%E5%8C%96&quot;&gt;官方文档&lt;/a&gt;，下面将逐步分析vue-router源码中是如何实现以上这些核心功能的。&lt;/p&gt;
&lt;h1&gt;阅读源码的前置条件&lt;/h1&gt;
&lt;h2&gt;源码目录结构&lt;/h2&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/ac0c7e0f-57c4-4553-aaaf-f1fbe751faa0.DKzl1RG9_VJSK8.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;源码结构算是秉承着vue系列的一目了然的特性，主要分为组件link和view、维护路由的history、vue插件的注册方法install.js、模块导出文件index.js&lt;/p&gt;
&lt;h2&gt;基础概念——路由实例router&lt;/h2&gt;
&lt;p&gt;路由实例router是在使用vue-router的时候通过传入路由记录等配置而生成的实例对象，重点在于其VueRouter类的实现。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/3208bfb8-d96d-4e3f-bdd0-aa6d2687235d.CDgkq2q1_Z1noknp.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h3&gt;init方法的实现&lt;/h3&gt;
&lt;p&gt;这里的init方法与install.js中注册的全局mixin关联最大，是vue组件在create时执行的初始化路由方法，需要重点分析一下。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;init (app: any /* Vue component instance */) {
  this.apps.push(app)

  app.$once(&apos;hook:destroyed&apos;, () =&gt; {
    const index = this.apps.indexOf(app)
    if (index &gt; -1) this.apps.splice(index, 1)
    if (this.app === app) this.app = this.apps[0] || null
  })

  if (this.app) {
    return
  }

  this.app = app

  const history = this.history

  if (history instanceof HTML5History) {
    history.transitionTo(history.getCurrentLocation())
  } else if (history instanceof HashHistory) {
    const setupHashListener = () =&gt; {
      history.setupListeners()
    }
    history.transitionTo(
      history.getCurrentLocation(),
      setupHashListener,
      setupHashListener
    )
  }

  history.listen(route =&gt; {
    this.apps.forEach((app) =&gt; {
      app._route = route
    })
  })
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/index.js#L83&quot;&gt;L83&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;这里的app是vue组件的实例，通过 &lt;code&gt;app.$once(&apos;hook:destroyed&apos;, () =&gt; {}&lt;/code&gt; 声明式地注册组件destroyed生命周期钩子，保证对应组件销毁时组件app实例从&lt;code&gt;router.apps&lt;/code&gt;上移除。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;保证路由初仅始化一次&lt;/strong&gt;：由于init是被全局注册的mixin调用，此处通过this.app是否存在的判断逻辑保证路由初始化仅仅在根组件 &lt;code&gt;&amp;#x3C;App /&gt;&lt;/code&gt; 上初始化一次，&lt;code&gt;this.app&lt;/code&gt;最后保存的根据组件实例。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;触发路由变化&amp;#x26;开始路由监听&lt;/strong&gt;：使用 &lt;code&gt;history.transitionTo&lt;/code&gt; 分路由模式触发路由变化，使用 &lt;code&gt;history.listen&lt;/code&gt; 监听路由变化来更新根组件实例 &lt;code&gt;app._route&lt;/code&gt; 是当前跳转的路由。&lt;/p&gt;
&lt;h2&gt;基础概念——路由匹配器matcher&lt;/h2&gt;
&lt;p&gt;路由匹配器macther是由create-matcher生成一个对象，其将传入VueRouter类的路由记录进行内部转换，对外提供根据location匹配路由方法——match、注册路由方法——addRoutes。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/create-matcher.js#L26&quot;&gt;match&lt;/a&gt;方法：根据内部的路由映射匹配location对应的路由对象route&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/create-matcher.js#L22&quot;&gt;addRoutes&lt;/a&gt;方法：将路由记录添加到matcher实例的路由映射中&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;生成matcher&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// src/index.js
constructor (options: RouterOptions = {}) {
    ...
    this.matcher = createMatcher(options.routes || [], this)
  	...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/index.js#L42&quot;&gt;L42&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;code&gt;options.routes&lt;/code&gt;为进行 &lt;code&gt;new VueRoute&lt;/code&gt; 操作是传入的路由记录&lt;/p&gt;
&lt;h3&gt;createMatcher内部&lt;/h3&gt;
&lt;p&gt;createMatcher来自于&lt;code&gt;import { createMatcher } from &apos;./create-matcher&apos;&lt;/code&gt;, 内部进行路由地址到路由对象的转换、路由记录的映射、路由参数处理等操作&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// src/create-matcher.js
export function createMatcher (
  routes: Array&amp;#x3C;RouteConfig&gt;,
  router: VueRouter
): Matcher {
  ...
  function addRoutes (routes) {
    createRouteMap(routes, pathList, pathMap, nameMap)
  }
  function match (
    raw: RawLocation,
    currentRoute?: Route,
    redirectedFrom?: Location
  ): Route {
    ...
  }
  function _createRoute (
    record: ?RouteRecord,
    location: Location,
    redirectedFrom?: Location
  ): Route {
    ...
  }
    
  return {
    match,
    addRoutes
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/create-matcher.js#L16&quot;&gt;L16&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;createRoute：将外部传入的路由记录转换成统一的route对象，传入&lt;strong&gt;组件实例的$route&lt;/strong&gt;就是此处返回的
alias：处理路由别名
nameMap：处理命名路由
&lt;strong&gt;路由参数解析&lt;/strong&gt;：解析路由location.params、query参数、hash等，&lt;strong&gt;动态路由匹配正是来自此处&lt;/strong&gt;&lt;/p&gt;
&lt;h1&gt;动态路由匹配&amp;#x26;嵌套路由&lt;/h1&gt;
&lt;h2&gt;动态路由匹配&lt;/h2&gt;
&lt;p&gt;动态路由匹配指的是可以在路径中设置多段参数，参数将会被设置到 &lt;code&gt;$route.params&lt;/code&gt; 上，例如：&lt;/p&gt;
&lt;p&gt;| &lt;strong&gt;模式&lt;/strong&gt; | &lt;strong&gt;匹配路径&lt;/strong&gt; | &lt;strong&gt;$route.params&lt;/strong&gt; |
| :---: | :---: | :---: |
| /user/:username | /user/evan | { username: &apos;evan&apos; } |
| /user/:username/post/:post_id | /user/evan/post/123 | { username: &apos;evan&apos;, post_id: &apos;123&apos; } |&lt;/p&gt;
&lt;p&gt;参考：&lt;a href=&quot;https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html#%E5%8A%A8%E6%80%81%E8%B7%AF%E7%94%B1%E5%8C%B9%E9%85%8D&quot;&gt;官网例子&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;嵌套路由&lt;/h2&gt;
&lt;p&gt;嵌套路由指的是路由可以像组件一样具有嵌套关系，一条路由记录下可以通过 &lt;code&gt;children&lt;/code&gt; 属性嵌套由多个子路由记录组成的数组，例如：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const router = new VueRouter({
  routes: [
    { path: &apos;/user/:id&apos;, component: User,
      children: [
        {
          // 当 /user/:id/profile 匹配成功，
          // UserProfile 会被渲染在 User 的 &amp;#x3C;router-view&gt; 中
          path: &apos;profile&apos;,
          component: UserProfile
        },
        {
          // 当 /user/:id/posts 匹配成功
          // UserPosts 会被渲染在 User 的 &amp;#x3C;router-view&gt; 中
          path: &apos;posts&apos;,
          component: UserPosts
        }
      ]
    }
  ]
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;参考：&lt;a href=&quot;https://router.vuejs.org/zh/guide/essentials/nested-routes.html&quot;&gt;官网例子&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;在项目中只要使用vue-router，几乎不可避免要使用到动态路由匹配和嵌套路由，可见两个功能在vue-router是何等重要，在研究其源码时这两个功能肯定是要研究的，下面将探究上述功能在vue-router是如何实现的。&lt;/p&gt;
&lt;h2&gt;主要实现思路&lt;/h2&gt;
&lt;p&gt;要实现动态路由匹配主要是要实现路由记录的path属性与实际的路由路径的参数进行匹配，而要实现嵌套路由则需要根据嵌套规则对路由记录解析，这两个都在create-route-map进行实现，实现的思路如下：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/00a39db2-760f-43d5-bc9f-a472c4450710.Dcv113Mc_Z1UoWSu.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;create-route-map中的核心代码如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;export function createRouteMap (
  routes: Array&amp;#x3C;RouteConfig&gt;,
  oldPathList?: Array&amp;#x3C;string&gt;,
  oldPathMap?: Dictionary&amp;#x3C;RouteRecord&gt;,
  oldNameMap?: Dictionary&amp;#x3C;RouteRecord&gt;
): {
  pathList: Array&amp;#x3C;string&gt;,
  pathMap: Dictionary&amp;#x3C;RouteRecord&gt;,
  nameMap: Dictionary&amp;#x3C;RouteRecord&gt;
} {
 	...
 	routes.forEach(route =&gt; {
    addRouteRecord(pathList, pathMap, nameMap, route)
  })
  ...
  /**
   * TODO:
   * 处理路由的优先级循序：将路由记录中的通配符*表示的路由按循序移动到路由记录末尾
   * 采用的哪种排序算法？
   */
  for (let i = 0, l = pathList.length; i &amp;#x3C; l; i++) {
    if (pathList[i] === &apos;*&apos;) {
      pathList.push(pathList.splice(i, 1)[0])
      l--
      i--
    }
  }
  ...
  /**
   * TODO:
   * 路由记录，将路由所有的路由记录映射到pathMap、nameMap中，pathMap：按路径映射，nameMap：按名称映射，pathList所有路由path组成的数组
   * 处理嵌套路由：递归调用此方法，parent表示父级路由
   * 处理路由别名：把路径别名看成是指向同一个组件的路由记录，由此方法处理一遍这个别名组成的路由
   * 处理路由名称：若存在路由名称，则将该路由映射到nameMap中存储
   */
  function addRouteRecord (
    pathList: Array&amp;#x3C;string&gt;,
    pathMap: Dictionary&amp;#x3C;RouteRecord&gt;,
    nameMap: Dictionary&amp;#x3C;RouteRecord&gt;,
    route: RouteConfig,
    parent?: RouteRecord,
    matchAs?: string
  ) {
    ...
  }
  ...
  return {
    pathList,
    pathMap,
    nameMap
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/create-route-map.js#L7&quot;&gt;L7&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;code&gt;createRouteMap&lt;/code&gt;方法主要是遍历路由配置routes，调用 &lt;code&gt;addRouteRecord&lt;/code&gt; 方法来处理路由，处理完路由后得到 &lt;code&gt;pathList&lt;/code&gt; &lt;code&gt;pathMap&lt;/code&gt; &lt;code&gt;nameMap&lt;/code&gt; ，并将其组成对象并返回。&lt;/p&gt;
&lt;h3&gt;动态路由匹配的实现&lt;/h3&gt;
&lt;p&gt;在 &lt;code&gt;addRouteRecord&lt;/code&gt; 方法处理路由的实现中由将 &lt;code&gt;route.path&lt;/code&gt; 使用path-to-regexp转换成正则表达式， &lt;code&gt;record&lt;/code&gt; 是处理完成后保存在 &lt;code&gt;pahtMap&lt;/code&gt; &lt;code&gt;nameMap&lt;/code&gt; 映射中的值。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const record: RouteRecord = {
	...
  regex: compileRouteRegex(normalizedPath, pathToRegexpOptions),
  ...
}
...
/**
 * TODO:
 * 调用path-to-regexp生成路由匹配用的正则
 */
function compileRouteRegex (
  path: string,
  pathToRegexpOptions: PathToRegexpOptions
): RouteRegExp {
  const regex = Regexp(path, [], pathToRegexpOptions)
  if (process.env.NODE_ENV !== &apos;production&apos;) {
    const keys: any = Object.create(null)
    regex.keys.forEach(key =&gt; {
      warn(
        !keys[key.name],
        `Duplicate param keys in route with path: &quot;${path}&quot;`
      )
      keys[key.name] = true
    })
  }
  return regex
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/create-route-map.js#L178&quot;&gt;L178&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;然后在create-matcher提供的match方法中根据 &lt;code&gt;route.name&lt;/code&gt; 、 &lt;code&gt;route.path&lt;/code&gt; 进行路由匹配，&lt;strong&gt;匹配的时候回调用上述得到的正则表达式进行路由匹配及参数解析&lt;/strong&gt;，从而得到路径或者路由名称匹配的路由以及动态参数。&lt;/p&gt;
&lt;h3&gt;嵌套路由的实现&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;addRouteRecord&lt;/code&gt; 方法实现嵌套路由部分的源码如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;export function createRouteMap (
  routes: Array&amp;#x3C;RouteConfig&gt;,
  oldPathList?: Array&amp;#x3C;string&gt;,
  oldPathMap?: Dictionary&amp;#x3C;RouteRecord&gt;,
  oldNameMap?: Dictionary&amp;#x3C;RouteRecord&gt;
): {
  pathList: Array&amp;#x3C;string&gt;,
  pathMap: Dictionary&amp;#x3C;RouteRecord&gt;,
  nameMap: Dictionary&amp;#x3C;RouteRecord&gt;
} {
 	...
  if (route.children) {
    route.children.forEach(child =&gt; {
      const childMatchAs = matchAs
        ? cleanPath(`${matchAs}/${child.path}`)
        : undefined
      addRouteRecord(pathList, pathMap, nameMap, child, record, childMatchAs)
    })
  }
  ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;&quot;&gt;L102&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;路由记录的children表示当前路由下嵌套的路由记录，当其存在时递归处理路由；处理子路由时会拼接完整的路由path放入 &lt;code&gt;pathMap&lt;/code&gt; &lt;code&gt;nameMap&lt;/code&gt; 。&lt;/p&gt;
&lt;p&gt;故不论是否嵌套路由与否，最后都是进行评级的路由映射，统一路由match方法进行路由匹配。&lt;/p&gt;
&lt;h1&gt;导航守卫机制&lt;/h1&gt;
&lt;p&gt;导航守卫给给路由使用者传入自定义的控制路由跳转逻辑的钩子方法，通过 &lt;code&gt;next&lt;/code&gt; 方法串行执行下一个路由的匹配逻辑，根据定义导航守卫的位置不同可以将其分为三类：&lt;strong&gt;全局导航守卫、路由独享守卫、组件内部导航守卫&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/83b03bac-1783-42ca-aba7-35ec8952d325.BqFTayYE_1X2LvS.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;导航守卫的注册&lt;/h2&gt;
&lt;p&gt;用注册导航守卫方法或导航守卫配置会被注册到执行队列中，在路由跳转时根据根据路由配置映射计算出组件实例的更新、替换、重用等情况，然后在对应组件上遍历执行导航守卫队列。&lt;/p&gt;
&lt;h3&gt;注册全局导航守卫&lt;/h3&gt;
&lt;p&gt;全局导航守卫分为：全局前置导航守卫、全局解析守卫、全局后置守卫，分别通过 &lt;code&gt;router.beforeEach&lt;/code&gt; &lt;code&gt;router.beforeResolve&lt;/code&gt; &lt;code&gt;router.afterEach&lt;/code&gt; 进行注册。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// src/index.js
beforeEach (fn: Function): Function {
  return registerHook(this.beforeHooks, fn)
}

beforeResolve (fn: Function): Function {
  return registerHook(this.resolveHooks, fn)
}

afterEach (fn: Function): Function {
  return registerHook(this.afterHooks, fn)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/index.js#L133&quot;&gt;L133&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;注册全局导航守卫是调用 &lt;code&gt;registerHook&lt;/code&gt; 方法向钩子函数队列中推入钩子函数，同时返回删除钩子函数的方法。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;这是常见队列入栈出栈用法，vue源码中很常见的一种用法&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;registerHook方法源码如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// src/index.js
function registerHook (list: Array&amp;#x3C;any&gt;, fn: Function): Function {
  list.push(fn)
  return () =&gt; {
    const i = list.indexOf(fn)
    if (i &gt; -1) list.splice(i, 1)
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;注册路由独享守卫&lt;/h3&gt;
&lt;p&gt;路由独享守卫是以路由配置的形式进行注册，例如：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const router = new VueRouter({
  routes: [
    {
      path: &apos;/foo&apos;,
      component: Foo,
      beforeEnter: (to, from, next) =&gt; {
        // ...
      }
    }
  ]
})
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;注册组件内部守卫&lt;/h3&gt;
&lt;p&gt;组件内部守卫是通过配置组件的导航守卫属性进行注册，例如：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const Foo = {
  template: `...`,
  beforeRouteEnter (to, from, next) {
    // 在渲染该组件的对应路由被 confirm 前调用
    // 不！能！获取组件实例 `this`
    // 因为当守卫执行前，组件实例还没被创建
  },
  beforeRouteUpdate (to, from, next) {
    // 在当前路由改变，但是该组件被复用时调用
    // 举例来说，对于一个带有动态参数的路径 /foo/:id，在 /foo/1 和 /foo/2 之间跳转的时候，
    // 由于会渲染同样的 Foo 组件，因此组件实例会被复用。而这个钩子就会在这个情况下被调用。
    // 可以访问组件实例 `this`
  },
  beforeRouteLeave (to, from, next) {
    // 导航离开该组件的对应路由时调用
    // 可以访问组件实例 `this`
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;导航守卫的解析流程&lt;/h2&gt;
&lt;p&gt;将上面在全局注册、路由配置注册、组件内部注册的导航守卫解析出来，按照&lt;strong&gt;导航钩子解析顺序&lt;/strong&gt;推进队列中&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const queue: Array&amp;#x3C;?NavigationGuard&gt; = [].concat(
  // in-component leave guards
  extractLeaveGuards(deactivated),  // 失效组件的beforeRouterLeave
  // global before hooks
  this.router.beforeHooks,					// 全局的前置钩子beforeEach
  // in-component update hooks
  extractUpdateHooks(updated),			// 重用的组件beforeRouteUpdate
  // in-config enter guards
  activated.map(m =&gt; m.beforeEnter),// 路由配置的beforeRouteEnter
  // async components
  resolveAsyncComponents(activated) // 路由配置中异步组件的加载解析
)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/history/base.js#L133&quot;&gt;L133&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;导航钩子的解析流程&lt;/strong&gt;
&lt;img src=&quot;https://luopengcheng89.github.io/_astro/3cf41362-5479-4926-afa5-eb867f07fddc.BL7VjFMc_ZqRnvs.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;导航钩子解析对应的源码&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// 执行前置守卫
runQueue(queue, iterator, () =&gt; {
  const postEnterCbs = []
  const isValid = () =&gt; this.current === route
  const enterGuards = extractEnterGuards(activated, postEnterCbs, isValid)
  const queue = enterGuards.concat(this.router.resolveHooks)
  // 执行解析守卫
  runQueue(queue, iterator, () =&gt; {
    if (this.pending !== route) {
      return abort()
    }
    this.pending = null
    onComplete(route)
    if (this.router.app) {
      this.router.app.$nextTick(() =&gt; {
        // 执行后置守卫
        postEnterCbs.forEach(cb =&gt; {
          cb()
        })
      })
    }
  })
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/history/base.js#L179&quot;&gt;L179&lt;/a&gt;&lt;/p&gt;
&lt;h1&gt;路由懒加载&lt;/h1&gt;
&lt;p&gt;路由懒加载是以路由为基础单位对页面代码进行分包，在匹配到对应路由时候在异步下载对应路由组件的代码，以vue-cli新建的项目可以直接使用&lt;a href=&quot;https://webpack.js.org/guides/code-splitting/&quot;&gt;webpack 的 code-splitting 功能&lt;/a&gt;，一个结合&lt;a href=&quot;https://cn.vuejs.org/v2/guide/components-dynamic-async.html#%E5%BC%82%E6%AD%A5%E7%BB%84%E4%BB%B6&quot;&gt;vue异步组件&lt;/a&gt;+ES新语法的路由懒加载的例子如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;vue VueRouter({
	routes: [
    {
     	path: &apos;/foot&apos;,
      component: () =&gt; import(&apos;./my-async-component&apos;)
    }
  ]
})
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;异步组件的加载需要关注其加载状态，在vue-router中的异步组件的加载状态只有loading、error等，而在vue-router是重新实现的组件懒加载实现了更细致的组件加载状态控制、路由解析控制。&lt;/p&gt;
&lt;p&gt;vue-router的异步组件解析在&lt;a href=&quot;#5Pm5h&quot;&gt;导航守卫队列的解析流程&lt;/a&gt;里面，其中重点的解析异步组件的方法源码如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// util/resolve-components.js
/**
 * TODO:
 * 异步路由解析：重写异步组件的resolve、reject方法，添加了组件加载状态控制、路由解析控制；对异步组件的传统写法及promise写进行兼容
 */
export function resolveAsyncComponents (matched: Array&amp;#x3C;RouteRecord&gt;): Function {
  return (to, from, next) =&gt; {
    let hasAsync = false
    let pending = 0
    let error = null

    flatMapComponents(matched, (def, _, match, key) =&gt; {
      if (typeof def === &apos;function&apos; &amp;#x26;&amp;#x26; def.cid === undefined) {
        hasAsync = true
        pending++
        // 重写vue异步组件的resolve和reject方法
        const resolve = once(resolvedDef =&gt; {
          if (isESModule(resolvedDef)) {
            resolvedDef = resolvedDef.default
          }
          // save resolved on async factory in case it&apos;s used elsewhere
          def.resolved = typeof resolvedDef === &apos;function&apos;
            ? resolvedDef
            : _Vue.extend(resolvedDef)
          match.components[key] = resolvedDef
          pending--
          if (pending &amp;#x3C;= 0) {
            next()
          }
        })

        const reject = once(reason =&gt; {
          const msg = `Failed to resolve async component ${key}: ${reason}`
          process.env.NODE_ENV !== &apos;production&apos; &amp;#x26;&amp;#x26; warn(false, msg)
          if (!error) {
            error = isError(reason)
              ? reason
              : new Error(msg)
            next(error)
          }
        })

        let res
        try {
          res = def(resolve, reject)
        } catch (e) {
          reject(e)
        }
        // 兼容异步组件的promise写法
        if (res) {
          if (typeof res.then === &apos;function&apos;) {
            res.then(resolve, reject)
          } else {
            // new syntax in Vue 2.3
            const comp = res.component
            if (comp &amp;#x26;&amp;#x26; typeof comp.then === &apos;function&apos;) {
              comp.then(resolve, reject)
            }
          }
        }
      }
    })
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/util/resolve-components.js#L6&quot;&gt;L6&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;重写了vue异步组件加载的resolve和reject方法来实现对路由解析是否进入下一个匹配的控制，加入了路由匹配的组件解析失败的异常处理，同时还对异步组件的promise写法也进行了兼容。&lt;/p&gt;
&lt;h1&gt;router-view组件&lt;/h1&gt;
&lt;p&gt;router-view是vue-router提供的两个核心组件之一，它是一个函数式组件不存在自己的组件实例，只负责调用父组件上存储的 &lt;code&gt;keepAlive&lt;/code&gt; &lt;code&gt;$route.match&lt;/code&gt; 等相关的属性/方法来控制路由对应的组件的渲染情况。&lt;/p&gt;
&lt;p&gt;router-view组件可以嵌套来配合实现嵌套路由，其自身所在的页面位置最终是其匹配上的路由组件所挂载的位置。&lt;/p&gt;
&lt;p&gt;其源码render部分的核心源码如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;render (_, { props, children, parent, data }) {
  // 标识当前组件是router-view
  data.routerView = true
  const h = parent.$createElement
  const name = props.name
  const route = parent.$route
  const cache = parent._routerViewCache || (parent._routerViewCache = {})
  
  let depth = 0
  let inactive = false
  // 由router-view组件向上遍历直到跟组件，遇到其他的router-view组件则路由深度+1
  // vnodeData.keepAlivepj
  while (parent &amp;#x26;&amp;#x26; parent._routerRoot !== parent) {
    const vnodeData = parent.$vnode ? parent.$vnode.data : {}
    if (vnodeData.routerView) {
      depth++
    }
    if (vnodeData.keepAlive &amp;#x26;&amp;#x26; parent._directInactive &amp;#x26;&amp;#x26; parent._inactive) {
      inactive = true
    }
    parent = parent.$parent
  }
  data.routerViewDepth = depth

	// 启用缓存时
  if (inactive) {
    const cachedData = cache[name]
    const cachedComponent = cachedData &amp;#x26;&amp;#x26; cachedData.component
    if (cachedComponent) {
      if (cachedData.configProps) {
        fillPropsinData(cachedComponent, data, cachedData.route, cachedData.configProps)
      }
      return h(cachedComponent, data, children)
    } else {
      return h()
    }
  }

  const matched = route.matched[depth]
  const component = matched &amp;#x26;&amp;#x26; matched.components[name]
  
  if (!matched || !component) {
    cache[name] = null
    return h()
  }

  cache[name] = { component }

  // 往父组件注册registerRouteInstance方法
  data.registerRouteInstance = (vm, val) =&gt; {
    // val could be undefined for unregistration
    const current = matched.instances[name]
    if (
      (val &amp;#x26;&amp;#x26; current !== vm) ||
      (!val &amp;#x26;&amp;#x26; current === vm)
    ) {
      matched.instances[name] = val
    }
  }
	...
  return h(component, data, children)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/components/view.js#L13&quot;&gt;L13&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;路由缓存的判断&lt;/h2&gt;
&lt;p&gt;parent表示router-view组件的直接父级组件实例，从当router-view往外层组件遍历，遇到router-view则说明存在嵌套路由，路由深度+1，同时若满足条件则表示路由启用了缓存；&lt;/p&gt;
&lt;p&gt;即如下结构会使用路由缓存&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;#x3C;keep-alive&gt;
  &amp;#x3C;router-view&gt;&amp;#x3C;/router-view&gt;
&amp;#x3C;/keep-alive&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;缓存的路由组件实例存在父级组件实例上，若启用了路由缓存则用父级缓存的已匹配的路由组件进行渲染，无则用 &lt;code&gt;$route.match&lt;/code&gt; 来匹配 &lt;code&gt;matcher&lt;/code&gt; 中匹配上的路由进行渲染。&lt;/p&gt;
&lt;p&gt;**&lt;code&gt;parent._inactive&lt;/code&gt;**由vue核心模块的&lt;code&gt;observer/scheduler&lt;/code&gt;调度器更新
&lt;code&gt;**parent._directInactive**&lt;/code&gt;由vue核心模块的&lt;code&gt;instance/lifecycle&lt;/code&gt;更新，两者都是用于标识当前组件是否处于active状态，具体区别可参考这个issue&lt;a href=&quot;https://github.com/vuejs/vue-router/issues/1212&quot;&gt;#1212&lt;/a&gt;。&lt;/p&gt;
&lt;h1&gt;router-link组件&lt;/h1&gt;
&lt;p&gt;router-link是vue-router提供的两个核心组件之一，它是一个普通组件，内部取消了&lt;code&gt;a标签&lt;/code&gt;的默认跳转行为，并控制了组件与&lt;code&gt;control&lt;/code&gt;、&lt;code&gt;meta&lt;/code&gt;等按键同时存在的兼容性问题，提供了当前激活路由匹配时的样式类；&lt;/p&gt;
&lt;p&gt;通过 &lt;code&gt;to&lt;/code&gt; 来决定点击事件跳转的目标路由，通过 &lt;code&gt;append&lt;/code&gt; &lt;code&gt;replace&lt;/code&gt;等属性改变默认路由跳转的行为。&lt;/p&gt;
&lt;h2&gt;通过slot分发内容&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const scopedSlot =
  !this.$scopedSlots.$hasNormal &amp;#x26;&amp;#x26;
  this.$scopedSlots.default &amp;#x26;&amp;#x26;
  this.$scopedSlots.default({
    href,
    route,
    navigate: handler,
    isActive: classes[activeClass],
    isExactActive: classes[exactActiveClass]
  })

if (scopedSlot) {
  if (scopedSlot.length === 1) {
    return scopedSlot[0]
  } else if (scopedSlot.length &gt; 1 || !scopedSlot.length) {
    if (process.env.NODE_ENV !== &apos;production&apos;) {
      warn(
        false,
        `RouterLink with to=&quot;${
          this.to
        }&quot; is trying to use a scoped slot but it didn&apos;t provide exactly one child. Wrapping the content with a span element.`
      )
    }
    return scopedSlot.length === 0 ? h() : h(&apos;span&apos;, {}, scopedSlot)
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/components/link.js#L91&quot;&gt;L91&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;统一处理点击事件兼容性&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function guardEvent (e) {
  // don&apos;t redirect with control keys
  if (e.metaKey || e.altKey || e.ctrlKey || e.shiftKey) return
  // don&apos;t redirect when preventDefault called
  if (e.defaultPrevented) return
  // don&apos;t redirect on right click
  if (e.button !== undefined &amp;#x26;&amp;#x26; e.button !== 0) return
  // don&apos;t redirect if `target=&quot;_blank&quot;`
  if (e.currentTarget &amp;#x26;&amp;#x26; e.currentTarget.getAttribute) {
    const target = e.currentTarget.getAttribute(&apos;target&apos;)
    if (/\b_blank\b/i.test(target)) return
  }
  // this may be a Weex event which doesn&apos;t have this method
  if (e.preventDefault) {
    e.preventDefault()
  }
  return true
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/components/link.js#L158&quot;&gt;L158&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;查找渲染的a标签&lt;/h2&gt;
&lt;p&gt;递归查找children中的a标签作为组件默认插槽的默认替换内容&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function findAnchor (children) {
  if (children) {
    let child
    for (let i = 0; i &amp;#x3C; children.length; i++) {
      child = children[i]
      if (child.tag === &apos;a&apos;) {
        return child
      }
      if (child.children &amp;#x26;&amp;#x26; (child = findAnchor(child.children))) {
        return child
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;源码：&lt;a href=&quot;https://github.com/vuejs/vue-router/blob/dev/src/components/link.js#L177&quot;&gt;L177&lt;/a&gt;&lt;/p&gt;
&lt;h1&gt;总结&lt;/h1&gt;
&lt;p&gt;经过以上的种种分析，vue-router中的核心特性的实现基本已经分析完成。由于目前作者水平有限，部分源码的分析还不够彻底，比如：router-view源码中涉及到与vue核心相关部分，甚至有地方存在疏漏或者错误，还请各位读者指正。&lt;/p&gt;</content:encoded><h:img src="/_astro/thumbnail.BUgTjp-a.png"/><enclosure url="/_astro/thumbnail.BUgTjp-a.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>深入理解 JS 模块</title><link>https://luopengcheng89.github.io/blog/js_module</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/js_module</guid><description>本文详细介绍了JavaScript模块化的发展历程，从早期的函数封装、对象封装，到CommonJS、AMD、CMD规范，再到ES6模块系统的全面解析。</description><pubDate>Tue, 14 Aug 2018 11:29:34 GMT</pubDate><content:encoded>&lt;h2&gt;引言&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;JavaScript&lt;/code&gt;的模块机制其实是借鉴的其他程序设计语言的, 如Java中package的概念, &lt;code&gt;import java.util.ArrayList;&lt;/code&gt;; package就是逻辑上相关的代码组织到同一个包内，包内是一个相对独立的作用域，不用担心命名冲突等等, 当需要在外部使用的时候直接import相应的package即可。&lt;/p&gt;
&lt;p&gt;由于&lt;code&gt;JavaScript&lt;/code&gt;在设计之初的定位原因, 并没有提供类似模块的功能, 随后便出现了各种&lt;strong&gt;模拟&lt;/strong&gt;类似功能的规范。到今天(2018-5-28)ES6已经十分普及, ES6的模块机制已经大规模使用, 我们完全可以使用ES6提供的模块化规范(机制)。&lt;/p&gt;
&lt;h2&gt;类模块化&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;类模块化&lt;/strong&gt;: 这是我自己理解的一个模块化概念, 指的是像&lt;strong&gt;函数封装&lt;/strong&gt;, &lt;strong&gt;对象&lt;/strong&gt;, &lt;strong&gt;立即执行函数包装&lt;/strong&gt;这样的类似模块化的规范。&lt;/p&gt;
&lt;h3&gt;函数封装&lt;/h3&gt;
&lt;p&gt;函数就是对实现特定逻辑的一组语句的打包, JS的作用域也是基于函数的, 所以函数可以很自然的作为模块化, 这也是最开始实现模块化的一种方法。&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;function func1(){
    ...
}
function func2(){
    ...
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;引用模块也即是调用函数, 这种函数定义的方式是直接采用&lt;code&gt;script&lt;/code&gt;来加载执行的(不经过模块打包器处理), 故函数是挂载在全局&lt;code&gt;window&lt;/code&gt;对象上, 因此&lt;strong&gt;存在污染全局变量的缺点和变量冲突等缺点&lt;/strong&gt;。&lt;/p&gt;
&lt;h3&gt;对象&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;var myModule = {
    var1: 1,
    var2: 2,
    func1: function(){
        ...
    },
    func2: function(){
        ...
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;将上面的函数封装在一个对象中, 引用模块即引用相应文件中对象上的属性, 如: &lt;code&gt;myModule.func1()&lt;/code&gt;, 通过对象名(模块名)避免了全局变量污染, 但是存在安全问题, 如: 外部可以随意修改模块内部的属性和方法等。&lt;/p&gt;
&lt;h3&gt;立即执行函数&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;var myModule = (function(){
    var var1 = 1;
    var var2 = 2;
    function func1(){
        ...
    }
    function func2(){
        ...
    }
    return {
        func1: func1,
        func2: func2
    };
})();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;在上面对象的基础之上, 用立即执行函数进行封装, 可以解决全局变量污染, 防止模块内部属性和方法被外部修改, 这是当前主流模块规范的基础。&lt;/p&gt;
&lt;p&gt;:::warning 注意
这里的随意修改指的是：可以通过&lt;code&gt;myModule.func1 = &apos;abc&apos;&lt;/code&gt;等赋值方式来修改掉模块的属性、变量等指向
:::&lt;/p&gt;
&lt;h2&gt;CommonJS(NodeJS)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;CommonJS&lt;/strong&gt;: 通用模块规范, 主要由NodeJS具体实现; 根据CommonJS规范, 一个单独的文件就是一个模块。每一个模块都是一个单独的作用域, 也就是说, &lt;strong&gt;在该模块内部定义的变量无法被其他模块读取&lt;/strong&gt;, 除非定义为global(浏览器中为window)对象的属性。&lt;/p&gt;
&lt;p&gt;CommonJS模块例子:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;// 模块定义 myModule.js
var name = &apos;myName&apos;;
function printName(){
    console.log(name);
}
function printFullName(firstName){
    console.log(firstName + name);
}
module.exports = {
    printName: printName,
    printFullName: printFullName
}
// 加载模块
var myModule = require(&apos;./myModule.js&apos;);
myModule.printName();
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;CommonJS模块存在的问题&lt;/h3&gt;
&lt;p&gt;require引入模块是同步的, 由于在浏览器环境下, JS都是通过script标签引入, 而这是天生异步的, 因此&lt;code&gt;CommonJS&lt;/code&gt;在浏览器环境下无法正常加载(无法处理依赖问题)。NodeJS广泛采用CommonJS的原因主要是NodeJS的require模块都是在本地, 完全不用担心异步过程(即使在服务器上也是如此)。因此, 针对浏览器端异步require模块出现了&lt;code&gt;AMD&lt;/code&gt;和&lt;code&gt;CMD&lt;/code&gt;规范。&lt;/p&gt;
&lt;h2&gt;AMD(RequireJS)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;AMD&lt;/strong&gt;: Asynchronous Module Definition(异步模块定义), 在浏览器端模块化开发的规范, 不是JavaScript原生支持, RequireJS是AMD规范的具体实现(严格上说是&lt;strong&gt;RequireJS的推广中产生的AMD规范&lt;/strong&gt;)。&lt;/p&gt;
&lt;p&gt;RequireJS模块例子:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;// 定义模块 myModule.js
define(&apos;myModule&apos;, [&apos;dependency&apos;], function(){
    var name = &apos;myName&apos;;
    function printName(){
        console.log(name);
    }
    return {
        printName: printName
    };
});

// 加载模块
require([&apos;myModule&apos;], function (my){
　 my.printName();
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;RequireJS定义了一个全局函数&lt;code&gt;define(id?, dependencies?, factory);&lt;/code&gt;来创建一个模块。
&lt;code&gt;AMD&lt;/code&gt;模块中所有的依赖都前置, 其模块是异步的, 该自定义的模块内用到的模块均等到异步加载完成之后才调用响应模块, 这样浏览器不会失去响应。require指定的回调函数，只有前面的模块都加载成功后，才会运行，解决了依赖性的问题。&lt;/p&gt;
&lt;p&gt;例如: 现需要在一个HTML页面中需要使用jQuery-fileupload插件, 并通过script标签的方式引入JS文件, 传统的方式是&lt;strong&gt;先引入jquery.min.js再引入jquery.fileupload.js&lt;/strong&gt;。由于jqery.fileupload.js是基于jQuery的, 必须保证首先引入jQuery, 加载JS时候页面会停止若此时网络较差, 会导致页面失去响应时间较长。&lt;/p&gt;
&lt;h3&gt;总结&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;RequireJS主要解决如问题:&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;多个JS文件可能有依赖关系，被依赖的文件需要早于依赖它的文件加载到浏览器;&lt;/li&gt;
&lt;li&gt;JS加载的时候浏览器会停止页面渲染，加载文件越多，页面失去响应时间越长。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;模块机制用途:&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;CommonJS&lt;/code&gt;是同步的, 主要用于服务器&lt;/li&gt;
&lt;li&gt;&lt;code&gt;AMD&lt;/code&gt;和&lt;code&gt;CMD&lt;/code&gt;是异步的, 两者的模块定义和加载机制稍有不同, 主要用于浏览器&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;AMD与CMD的区别&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;AMD&lt;/code&gt;推崇依赖前置，在定义模块的时候就要声明其依赖的模块&lt;code&gt;, &lt;/code&gt;CMD`推崇就近依赖，只有在用到某个模块的时候再去require&lt;/li&gt;
&lt;li&gt;两个都是定义的全局define函数来定义模块, define接收函数function(require, exports, module)保持一致&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CMD&lt;/code&gt;是懒加载, 仅在require时才会加载模块; &lt;code&gt;AMD&lt;/code&gt;是预加载, 在定义模块时就提前加载好所有依赖&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CMD&lt;/code&gt;保留了CommonJS风格&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;CMD(SeaJS)&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;CMD&lt;/strong&gt;: Common Module Definition通用模块定义, 由国内发展出来, SeaJS是其典型代表, 即SeaJS是通过浏览器对CMD的具体实现&lt;/p&gt;
&lt;p&gt;SeaJS模块例子:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;// 定义模块  myModule.js
define(function(require, exports, module) {
  var $ = require(&apos;jquery.js&apos;);
  var foo = require(&apos;foo&apos;);
  var out = foo.bar();
  $(&apos;div&apos;).addClass(&apos;active&apos;);
  module.exports = out;
});

// 加载模块
seajs.use([&apos;myModule.js&apos;], function(my){

});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;SeaJS定义了一个全局函数&lt;code&gt;define(id?, deps?, factory) &lt;/code&gt;来创建一个模块, define接受一个需要三个参数的函数, 分别为:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;require&lt;/strong&gt;: 一个方法, 接受模块标识 作为唯一参数，用来获取其他模块提供的接口：require(id)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;exports&lt;/strong&gt;: 一个对象, 用来向外提供模块接口&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;module&lt;/strong&gt;: 一个对象, 上面存储了与当前模块相关联的一些属性和方法&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;CMD推崇依赖就近原则(也就是&lt;strong&gt;懒加载&lt;/strong&gt;), 模块内部的依赖在需要引入的时候再引入, 如上例中的&lt;code&gt;var $ = require(&apos;jquery.js&apos;)&lt;/code&gt;, 这一点和通用的&lt;code&gt;CommonJS&lt;/code&gt;模块风格保持一致。&lt;/p&gt;
&lt;h2&gt;UMD&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;UMD&lt;/strong&gt;: 是一个既能在&lt;code&gt;seajs（CMD）&lt;/code&gt;环境里引入，又能在&lt;code&gt;requirejs（AMD）&lt;/code&gt;环境中引入，
当然也能在&lt;code&gt;Node.js（CommonJS）&lt;/code&gt;中使用，另外还可以在没有模块化的环境中用&lt;code&gt;script标签&lt;/code&gt;全局引入的&apos;模块规范&apos;&lt;/p&gt;
&lt;p&gt;&lt;code&gt;UMD&lt;/code&gt;模块其实就是在当前JS执行环境中对以上几种模块规范定义的&lt;code&gt;define&lt;/code&gt;, &lt;code&gt;module.exports&lt;/code&gt;等进行判断, 同一模块根据不同场所返回不同结果。&lt;/p&gt;
&lt;p&gt;UMD模块例子:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;;(function (global) {
    function factory () {
        // 具体业务方模块的定义
        var moduleName = {};
        return moduleName;
    }
    if (typeof module !== &apos;undefined&apos; &amp;#x26;&amp;#x26; typeof exports === &apos;object&apos;) {
        module.exports = factory();
    } else if (typeof define === &apos;function&apos; &amp;#x26;&amp;#x26; (define.cmd || define.amd)) {
        define(factory);
    } else {
        global.moduleName = factory();
    }
})(typeof window !== &apos;undefined&apos; ? window : global);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;UMD模块在不同环境引入:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;// RequireJs
define(&apos;moduleName&apos;, [&apos;otherModule&apos;], function (moduleName) {
  console.log(moduleName)
});

// SeaJs
define(function (require, exports, module) {
    var myModule = require(&apos;moduleName&apos;);
});

// Node.js
var myModule = require(&apos;moduleName&apos;);

// Browser global
&amp;#x3C;script src=&quot;moduleName.js&quot;&gt;&amp;#x3C;/script&gt;
&amp;#x3C;script type=&quot;text/javascript&quot;&gt;
  console.log(moduleName)
&amp;#x3C;/script&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;ES6模块(import,export)&lt;/h2&gt;
&lt;p&gt;ES6在语言标准的层面上, 实现了模块功能, 而且实现得相当简单, 完全可以取代&lt;code&gt;CommonJS&lt;/code&gt;和&lt;code&gt;AMD&lt;/code&gt;规范, 是浏览器和服务器通用的模块解决方案。&lt;/p&gt;
&lt;p&gt;ES6模块例子:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-js&quot;&gt;// 模块定义 myModule.js
const name = &apos;myName&apos;;
function printName(){
    console.log(name);
}
function printFullName(firstName){
    console.log(firstName + name);
}
const myModule = {
    printName: printName,
    printFullName: printFullName
};
export myModule;

// 加载模块
import myModule, { printFullName } from &apos;./myModule.js&apos;;
myModule.printName();
printFullName(&apos;myFullName&apos;);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;注意&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;ES6中的&lt;code&gt;export&lt;/code&gt;是ES6对于JS模块的一种新的规范, 不同于&lt;code&gt;CommonJS&lt;/code&gt;规范中的&lt;code&gt;module.exports&lt;/code&gt;和&lt;code&gt;exports&lt;/code&gt;;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;CommonJS&lt;/code&gt;规范中&lt;code&gt;exports&lt;/code&gt;可以理解为指向&lt;code&gt;module.exports&lt;/code&gt;的一个指针, 可以&lt;code&gt;exports.newModule = {...}&lt;/code&gt;, 但是这样写&lt;code&gt;exports={..}&lt;/code&gt;是不行的, 这会将&lt;code&gt;exports&lt;/code&gt;这个指针指向新的&lt;code&gt;{...}&lt;/code&gt;对象, 不再指向&lt;code&gt;module.exports&lt;/code&gt;;&lt;/li&gt;
&lt;li&gt;ES6语法一般都经过&lt;code&gt;babel&lt;/code&gt;转义为JS, 故可以在ES6中使用&lt;code&gt;CommonJS&lt;/code&gt;模块规范, 如: &lt;code&gt;var myModule = require(&apos;./myModule.js&apos;)&lt;/code&gt;。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;参考文章&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;a href=&quot;https://www.cnblogs.com/chenguangliang/p/5856701.html&quot;&gt;js模块化编程之彻底弄懂CommonJS和AMD/CMD&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;http://www.cnblogs.com/brandonchen/p/5550470.html&quot;&gt;写一个适应所有环境的js模块&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;http://www.zhangxinxu.com/sp/seajs/&quot;&gt;SeaJS&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;http://requirejs.org/&quot;&gt;RequireJS&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;http://es6.ruanyifeng.com/#docs/module&quot;&gt;阮一峰-ES6模块&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;</content:encoded><h:img src="/_astro/thumbnail.BlDuman0.png"/><enclosure url="/_astro/thumbnail.BlDuman0.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>React 和 Dva 的一些核心概念</title><link>https://luopengcheng89.github.io/blog/react_and_dva</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/react_and_dva</guid><description>本文介绍了React和Dva的核心概念，包括 React 的环境搭建、JSX 语法、组件（函数与类定义及规范）、state、props、生命周期、ContextAPI，以及 Dva 的项目搭建、数据管理（model 及使用）、路由管理、订阅（监听）等内容。</description><pubDate>Mon, 18 Mar 2019 11:21:15 GMT</pubDate><content:encoded>&lt;h2&gt;React&lt;/h2&gt;
&lt;h3&gt;环境搭建&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;Node.js&lt;/li&gt;
&lt;li&gt;创建项目
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx create-react-app react-demo
cd react-demo
npm start
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;JSX语法&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;jsx是js + XML，本质还是js，可通过js动态创建HTML标签和react组件&lt;/p&gt;
&lt;/blockquote&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;// jsx写法
const title = &apos;Hello World&apos;
&amp;#x3C;h1 className=&quot;title&quot; onClick={() =&gt; {
    console.log(title)
}}&gt;{title}&amp;#x3C;/title&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;// html写法
&amp;#x3C;h1 class=&quot;title&quot; onclick=&quot;console.log(title)&quot;&gt;Hello World&amp;#x3C;/h1&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;组件&lt;/h3&gt;
&lt;p&gt;用函数定义组件&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;const ComponentA = () =&gt; {
    return (
        &amp;#x3C;div&gt;ComponentA&amp;#x3C;/div&gt;
    )
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;用class定义组件&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;class ComponentB extends React.Component {
    render() {
        return (
            &amp;#x3C;div&gt;ComponentB&amp;#x3C;/div&gt;
        )
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;组件规范&lt;/h4&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;// 引入外组件/资源
import { Button } from &apos;antd&apos;
import styles from &apos;../style.less&apos;

// 定义并导出组件
export default class List extends React.Component {
    // 构造函数
    constructor(props) {
        super(props)
        this.state = {
            title: &apos;Hello World&apos;
        }
    }
    // 实现render方法
    render() {
        return (
            &amp;#x3C;div&gt;{this.state.title}&amp;#x3C;/div&gt;
        )
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;state&lt;/h3&gt;
&lt;p&gt;在一个组件内部定义的数据，可通过模板语法关联到jsx上，作用域是本组件&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;class ComponentC extends React.Component {
    constructor(props) {
        super(props)
        this.state = {
            title: &apos;ComponentC&apos;
        }
    }
    render() {
        return (
            &amp;#x3C;div&gt;{this.state.title}&amp;#x3C;/div&gt;
        )
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;props&lt;/h3&gt;
&lt;p&gt;jsx中使用组件的时候通过属性传递给组件的数据，挂载在本组件的this.props上，直接在本组件的render的jsx中使用即可&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;class ComponentD extends React.Component {
    constructor(props) {
        super(props)
        this.state = {
            title: &apos;ComponentD&apos;
        }
    }
    render() {
        return (
            &amp;#x3C;div&gt;
                &amp;#x3C;div&gt;{this.state.title}&amp;#x3C;/div&gt;
                &amp;#x3C;div&gt;{this.props.title}&amp;#x3C;/div&gt;
            &amp;#x3C;/div&gt;
        )
    }
}

class ComponentE extends React.Component {
    render() {
        return (
            &amp;#x3C;div&gt;
                &amp;#x3C;div&gt;ComponentE&amp;#x3C;/div&gt;
                &amp;#x3C;ComponentD title=&quot;props from ComponentE&quot;&gt;&amp;#x3C;/ComponentD&gt;
            &amp;#x3C;/div&gt;
        )
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;生命周期&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;componentDidMount&lt;/p&gt;
&lt;p&gt;组件挂载时候执行，常用于调用api接口获取数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;class ComponentF extends React.Component {
    constructor(props) {
        super(props)
        this.state = {}
    }
    async componentDidMount() {
        let data = await this.fetchData() // 调用接口获取数据
        this.setState({
            data
        })
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;componentWillUnmount&lt;/p&gt;
&lt;p&gt;组件卸载时执行，常用于清除componentDidMount时候创建的定时器&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;componentDidUpdate&lt;/p&gt;
&lt;p&gt;组件在更新时候执行，组件初次挂载不执行，可用于比较两次更新组件props的userID是否相同来决定是否重新调用接口&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;componentDidUpdate(prevProps) {
    if (this.props.userID !== prevProps.userID) {
        let data = this.fetchData(this.props.userID)// 根据userID获取用户信息
        this.setState({
            data
        })
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;ContextAPI&lt;/h3&gt;
&lt;p&gt;&lt;a href=&quot;https://codesandbox.io/s/q8l190v45j?fontsize=14&quot;&gt;demo&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;Dva&lt;/h2&gt;
&lt;h3&gt;搭建dva项目&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npm install dva-cli -g
dva new dva-demo
cd dva-demo
npm start
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;数据(状态)管理&lt;/h3&gt;
&lt;h4&gt;model&lt;/h4&gt;
&lt;p&gt;包含数据state 异步方法effects 同步方法reducers 监听数据源subscriptions&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;state：redux中的state，是组件/页面上数据(props)的来源&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;effects：调用service中的api接口操作服务器数据，然后根据接口返回值调用对应的reducers来更新state，实现页面数据的响应式更新&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;reducers：同步更新state的数据方法&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;使用model&lt;/h4&gt;
&lt;p&gt;定义好model之后需要将model中的state和effects等关联至页面组件，页面组件可通过派发相应的action来调用model中的业务逻辑&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;将model关联到页面&lt;/p&gt;
&lt;p&gt;使用&lt;code&gt;connect&lt;/code&gt;包装页面组件，被包装组件的props将携带相应model的state，dispatch也将被挂载在组件的props上&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;import { connect } from &apos;dva&apos;
mapStateToProps = ({ list }) =&gt; ({ list }) // 定义meodel的state到组件props的映射
connect：connect(mapStateToProps)(Component)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;使用model中的数据&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;render() {
    const { list } = this.props
    return &amp;#x3C;List data={list} /&gt;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;派发action调用接口/更新state&lt;/p&gt;
&lt;p&gt;通过dispatch函数来派发一个ation，action其实就是一个对象，结构为：&lt;code&gt;{ type: &apos;namespace/name&apos;, payload: payload }&lt;/code&gt;；action的type是model的namepsace+名称；payload是action需要携带的参数（可选）&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;render() {
    const { dispatch } = this.props
    return (
        &amp;#x3C;div&gt;
            &amp;#x3C;Button type=&quot;primary&quot; onClick={() =&gt; {
                dispatch({
                    type: &apos;list/delete&apos;,
                    payload: {
                        id: &apos;123456&apos;
                    }
                })
            }}&gt;删除&amp;#x3C;/Button&gt;
        &amp;#x3C;/div&gt;
    )
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;路由管理&lt;/h3&gt;
&lt;h4&gt;注册路由&lt;/h4&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;import { Router, Route } from &apos;dva/router&apos;;
app.router(({history}) =&gt;
  &amp;#x3C;Router history={history}&gt;
    &amp;#x3C;Route path=&quot;/&quot; component={HomePage} /&gt;
    &amp;#x3C;Route path=&quot;/list&quot; component={List} /&gt;
  &amp;#x3C;/Router&gt;
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;页面内部路由跳转&lt;/h4&gt;
&lt;p&gt;页面&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;render() {
    const { dispatch } = this.props
    return (
        &amp;#x3C;div&gt;
            &amp;#x3C;Button type=&quot;primary&quot; onClick={() =&gt; {
                dispatch({
                    type: &apos;app/goPage&apos;,
                    payload: {
                        pathname: &apos;/detail&apos;,
                        search: {
                            id: &apos;123456&apos;
                        }
                    }
                })
            }}&gt;查看详情&amp;#x3C;/Button&gt;
            &amp;#x3C;Button type=&quot;primary&quot; onClick={() =&gt; {
                dispatch({
                    type: &apos;app/goPage&apos;,
                    payload: {
                        pathname: &apos;/edit&apos;,
                        search: {
                            id: &apos;123456&apos;
                        },
                        type: &apos;replace&apos;
                    }
                })
            }}&gt;编辑&amp;#x3C;/Button&gt;
        &amp;#x3C;/div&gt;
    )
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;model&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;import { routerRedux } from &quot;dva/router&quot;

effects: {
    *goPage({ payload }, { put, select }) {
        // 路由跳转，包含普通push, 重定向replace，返回goBack, 由type控制

        const { pathname, search, type = &quot;push&quot; } = payload

        if (type === &quot;back&quot;) {
            window.history.back();
        } else {
            yield put(
                routerRedux[type]({
                    pathname,
                    search:
                    typeof search === &quot;object&quot; &amp;#x26;&amp;#x26;
                    encodeURI(queryString.stringify(search))
                })
            )
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;订阅(监听)&lt;/h3&gt;
&lt;p&gt;路由地址改变将会派发相应的action，以下为实现全局路由切换时进行登陆状态的判断&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;subscription: {
    setupHistory({ history, dispatch }) {
      history.listen(location =&gt; {
        // 检测登录状态
        dispatch({ type: &quot;isLogin&quot;, payload: { location } })
      })
    }
},
effects: {
    *isLogin({ payload }, { call, put }) {
        // 根据localStorage的token判断是否登陆
        // 若未登陆或登陆过期可派发action路由导航至登录页面
    }
}
&lt;/code&gt;&lt;/pre&gt;</content:encoded><h:img src="/_astro/thumbnail.8_6sE0-V.png"/><enclosure url="/_astro/thumbnail.8_6sE0-V.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>前端文件上传总结</title><link>https://luopengcheng89.github.io/blog/file_upload_in_frontend</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/file_upload_in_frontend</guid><description>本文详细总结了前端文件上传的完整实现方案，包括基于H5 File API的原生实现、FormData使用、图片压缩处理、兼容性问题解决方案以及多图并发上传控制策略。</description><pubDate>Sun, 20 May 2018 11:40:24 GMT</pubDate><content:encoded>&lt;h2&gt;0.图片上传思路&lt;/h2&gt;
&lt;blockquote&gt;
&lt;ol&gt;
&lt;li&gt;监听一个input(type=‘file’)的onchange事件，这样获取到文件file；&lt;/li&gt;
&lt;li&gt;将file转成dataUrl;&lt;/li&gt;
&lt;li&gt;然后根据dataUrl利用canvas绘制图片压缩，然后再转成新的dataUrl；&lt;/li&gt;
&lt;li&gt;再把dataUrl转成Blob；&lt;/li&gt;
&lt;li&gt;把Blob append进FormData中；&lt;/li&gt;
&lt;li&gt;xhr实现上传。&lt;/li&gt;
&lt;/ol&gt;
&lt;/blockquote&gt;
&lt;h2&gt;1.基于H5的File API和input实现&lt;/h2&gt;
&lt;p&gt;H5的File API实现代码如下:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;function uploadAndSubmit() {
  var form = document.forms[&quot;demoForm&quot;];
  if (form[&quot;file&quot;].files.length &gt; 0) { // 获取file文件对象 
    var file = form[&quot;file&quot;].files[0]; // try sending 
    var reader = new FileReader();
    reader.onloadstart = function() {
      // 这个事件在读取开始时触发 
      console.log(&quot;onloadstart&quot;);
      document.getElementById(&quot;bytesTotal&quot;).textContent = file.size;
    }
    reader.onprogress = function(p) {
      // 这个事件在读取进行中定时触发 
      console.log(&quot;onprogress&quot;);
      document.getElementById(&quot;bytesRead&quot;).textContent = p.loaded;
    }
    reader.onload = function() {
      // 这个事件在读取成功结束后触发 
      console.log(&quot;load complete&quot;);
    }
    reader.onloadend = function() {
      // 这个事件在读取结束后，无论成功或者失败都会触发 
      if (reader.error) { console.log(reader.error); } else {
        document.getElementById(&quot;bytesRead&quot;).textContent = file.size;
        // 构造 XMLHttpRequest 对象，发送文件 Binary 数据 
        var xhr = new XMLHttpRequest();
        xhr.open( /* method */ &quot;POST&quot;, /* target url */ &quot;upload.jsp?fileName=&quot; + file.name /*, async, default to true */ );
        // 设置MimeType为application/octet-stream, 即采用binary二进制传递文件
        xhr.overrideMimeType(&quot;application/octet-stream&quot;);
        xhr.sendAsBinary(reader.result);
        xhr.onreadystatechange = function() {
          if (xhr.readyState == 4) {
            if (xhr.status == 200) {
              console.log(&quot;upload complete&quot;);
              console.log(&quot;response: &quot; + xhr.responseText);
            }
          }
        }
      }
    }
    reader.readAsBinaryString(file);
  } else {
    alert(&quot;Please choose a file.&quot;);
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;2.FormData&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;通过FormData对象可以组装一组用 XMLHttpRequest发送请求的键/值对。它可以更灵活方便的发送表单数据，因为可以独立于表单使用。如果你把表单的编码类型设置为multipart/form-data ，则通过FormData传输的数据格式和表单通过submit() 方法传输的数据格式相同。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;3.base64&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;Base64是一种基于64个可打印字符来表示二进制数据的表示方法。 由于2的6次方等于64，所以每6个位元为一个单元，对应某个可打印字符。 三个字节有24个位元，对应于4个Base64单元，即3个字节可表示4个可打印字符
就是用一段字符串来描述一个二进制数据，所以很多时候也可以使用base64方式上传&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;4.Blob对象(File对象基于此)&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;一个 Blob对象表示一个不可变的, 原始数据的类似文件对象。Blob表示的数据不一定是一个JavaScript原生格式。 File 接口基于Blob，继承 blob功能并将其扩展为支持用户系统上的文件。
简单说Blob就是一个二进制对象，是原生支持的&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;5.webuploader插件&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/fex-team/webuploader&quot;&gt;Github: https://github.com/fex-team/webuploader&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;6.兼容性&lt;/h2&gt;
&lt;blockquote&gt;
&lt;ol&gt;
&lt;li&gt;部分安卓微信浏览器无法触发&lt;code&gt;onchange&lt;/code&gt;事件（第一步就特么遇到问题）
这其实安卓微信的一个遗留问题。 &lt;a href=&quot;https://www.zhihu.com/question/21452742&quot;&gt;查看讨论&lt;/a&gt; 解决办法也很简单：&lt;code&gt;input&lt;/code&gt;标签 &lt;code&gt;&amp;#x3C;input type=“file&quot; name=&quot;image&quot; accept=&quot;image/gif, image/jpeg, image/png”&gt;&lt;/code&gt;要写成&lt;code&gt;&amp;#x3C;input type=&quot;file&quot; name=&quot;image&quot; accept=“image/*”&gt;&lt;/code&gt;就没问题了。&lt;/li&gt;
&lt;li&gt;部分安卓微信不支持&lt;code&gt;Blob&lt;/code&gt;对象&lt;/li&gt;
&lt;li&gt;部分&lt;code&gt;Blob&lt;/code&gt;对象&lt;code&gt;append&lt;/code&gt;进&lt;code&gt;FormData&lt;/code&gt;中出现问题&lt;/li&gt;
&lt;li&gt;iOS 8不支持&lt;code&gt;new File Constructor&lt;/code&gt;，但是支持&lt;code&gt;input&lt;/code&gt;里的&lt;code&gt;file&lt;/code&gt;对象。&lt;/li&gt;
&lt;li&gt;iOS 上经过压缩后的图片可以上传成功 但是size是0 无法打开。&lt;/li&gt;
&lt;li&gt;部分手机出现图片上传转换问题，&lt;a href=&quot;https://github.com/xiangpaopao/blog/issues/7&quot;&gt;请移步&lt;/a&gt;。&lt;/li&gt;
&lt;li&gt;安卓手机不支持多选，原因在于multiple属性根本就不支持。&lt;/li&gt;
&lt;li&gt;多张图片转base64时候卡顿，因为调用了cpu进行了计算。&lt;/li&gt;
&lt;li&gt;上传图片可以使用base64上传或者formData上传&lt;/li&gt;
&lt;/ol&gt;
&lt;/blockquote&gt;
&lt;h2&gt;7.修改后方案&lt;/h2&gt;
&lt;p&gt;&lt;img src=&quot;https://luopengcheng89.github.io/_astro/d73b8d36-4e55-4028-b0e5-b5023d4465a9.CUQ6WYqR_1bU1MJ.webp&quot; alt=&quot;image&quot;&gt;&lt;/p&gt;
&lt;h2&gt;8.最终完善上传代码&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;// html文件
&amp;#x3C;input type=&quot;file&quot; name=&quot;image&quot; accept=“image/*” onchange=&apos;handleInputChange&apos;&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// js文件
// 全局对象，不同function使用传递数据
const imgFile = {};

function handleInputChange (event) {
    // 获取当前选中的文件
    const file = event.target.files[0];
    const imgMasSize = 1024 * 1024 * 10; // 10MB

    // 检查文件类型
    if([&apos;jpeg&apos;, &apos;png&apos;, &apos;gif&apos;, &apos;jpg&apos;].indexOf(file.type.split(&quot;/&quot;)[1]) &amp;#x3C; 0){
        // 自定义报错方式
        // Toast.error(&quot;文件类型仅支持 jpeg/png/gif！&quot;, 2000, undefined, false);
        return;
    }

    // 文件大小限制
    if(file.size &gt; imgMasSize ) {
        // 文件大小自定义限制
        // Toast.error(&quot;文件大小不能超过10MB！&quot;, 2000, undefined, false);
        return;
    }

    // 判断是否是ios
    if(!!window.navigator.userAgent.match(/\(i[^;]+;( U;)? CPU.+Mac OS X/)){
        // iOS
        transformFileToFormData(file);
        return;
    }

    // 图片压缩之旅
    transformFileToDataUrl(file);
}
// 将File append进 FormData
function transformFileToFormData (file) {
    const formData = new FormData();
    // 自定义formData中的内容
    // type
    formData.append(&apos;type&apos;, file.type);
    // size
    formData.append(&apos;size&apos;, file.size || &quot;image/jpeg&quot;);
    // name
    formData.append(&apos;name&apos;, file.name);
    // lastModifiedDate
    formData.append(&apos;lastModifiedDate&apos;, file.lastModifiedDate);
    // append 文件
    formData.append(&apos;file&apos;, file);
    // 上传图片
    uploadImg(formData);
}
// 将file转成dataUrl
function transformFileToDataUrl (file) {
    const imgCompassMaxSize = 200 * 1024; // 超过 200k 就压缩

    // 存储文件相关信息
    imgFile.type = file.type || &apos;image/jpeg&apos;; // 部分安卓出现获取不到type的情况
    imgFile.size = file.size;
    imgFile.name = file.name;
    imgFile.lastModifiedDate = file.lastModifiedDate;

    // 封装好的函数
    const reader = new FileReader();

    // file转dataUrl是个异步函数，要将代码写在回调里
    reader.onload = function(e) {
        const result = e.target.result;

        if(result.length &amp;#x3C; imgCompassMaxSize) {
            compress(result, processData, false );    // 图片不压缩
        } else {
            compress(result, processData);            // 图片压缩
        }
    };

    reader.readAsDataURL(file);
}
// 使用canvas绘制图片并压缩
function compress (dataURL, callback, shouldCompress = true) {
    const img = new window.Image();

    img.src = dataURL;

    img.onload = function () {
        const canvas = document.createElement(&apos;canvas&apos;);
        const ctx = canvas.getContext(&apos;2d&apos;);

        canvas.width = img.width;
        canvas.height = img.height;

        ctx.drawImage(img, 0, 0, canvas.width, canvas.height);

        let compressedDataUrl;

        if(shouldCompress){
            compressedDataUrl = canvas.toDataURL(imgFile.type, 0.2);
        } else {
            compressedDataUrl = canvas.toDataURL(imgFile.type, 1);
        }

        callback(compressedDataUrl);
    }
}

function processData (dataURL) {
    // 这里使用二进制方式处理dataUrl
    const binaryString = window.atob(dataUrl.split(&apos;,&apos;)[1]);
    const arrayBuffer = new ArrayBuffer(binaryString.length);
    const intArray = new Uint8Array(arrayBuffer);
    const imgFile = this.imgFile;

    for (let i = 0, j = binaryString.length; i &amp;#x3C; j; i++) {
        intArray[i] = binaryString.charCodeAt(i);
    }

    const data = [intArray];

    let blob;

    try {
        blob = new Blob(data, { type: imgFile.type });
    } catch (error) {
        window.BlobBuilder = window.BlobBuilder ||
            window.WebKitBlobBuilder ||
            window.MozBlobBuilder ||
            window.MSBlobBuilder;
        if (error.name === &apos;TypeError&apos; &amp;#x26;&amp;#x26; window.BlobBuilder){
            const builder = new BlobBuilder();
            builder.append(arrayBuffer);
            blob = builder.getBlob(imgFile.type);
        } else {
            // Toast.error(&quot;版本过低，不支持上传图片&quot;, 2000, undefined, false);
            throw new Error(&apos;版本过低，不支持上传图片&apos;);
        }
    }

    // blob 转file
    const fileOfBlob = new File([blob], imgFile.name);
    const formData = new FormData();

    // type
    formData.append(&apos;type&apos;, imgFile.type);
    // size
    formData.append(&apos;size&apos;, fileOfBlob.size);
    // name
    formData.append(&apos;name&apos;, imgFile.name);
    // lastModifiedDate
    formData.append(&apos;lastModifiedDate&apos;, imgFile.lastModifiedDate);
    // append 文件
    formData.append(&apos;file&apos;, fileOfBlob);

    uploadImg(formData);
}

// 上传图片
uploadImg (formData) {
    const xhr = new XMLHttpRequest();

    // 进度监听
    xhr.upload.addEventListener(&apos;progress&apos;, (e)=&gt;{console.log(e.loaded / e.total)}, false);
    // 加载监听
    // xhr.addEventListener(&apos;load&apos;, ()=&gt;{console.log(&quot;加载中&quot;);}, false);
    // 错误监听
    xhr.addEventListener(&apos;error&apos;, ()=&gt;{Toast.error(&quot;上传失败！&quot;, 2000, undefined, false);}, false);
    xhr.onreadystatechange = function () {
        if (xhr.readyState === 4) {
            const result = JSON.parse(xhr.responseText);
            if (xhr.status === 200) {
                // 上传成功
                

            } else {
                // 上传失败
            }
        }
    };
    xhr.open(&apos;POST&apos;, &apos;/uploadUrl&apos; , true);
    xhr.send(formData);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;9.多图并发上传&lt;/h2&gt;
&lt;p&gt;多张图片上传方式有三种：&lt;/p&gt;
&lt;blockquote&gt;
&lt;ol&gt;
&lt;li&gt;图片队列一张一张上传&lt;/li&gt;
&lt;li&gt;图片队列并发全部上传&lt;/li&gt;
&lt;li&gt;图片队列并发上传X个，其中一个返回了结果直接触发下一个上传，保证最多有X个请求。&lt;/li&gt;
&lt;/ol&gt;
&lt;/blockquote&gt;
&lt;p&gt;一张一张上传好解决，但是问题是上传事件太长，体验不佳；多张图片全部上传事件变短了，但是并发量太大，很可能出现问题；最后这个并发上传X个，体验最佳。&lt;/p&gt;
&lt;h3&gt;9.1并发上传实现&lt;/h3&gt;
&lt;p&gt;最后我们确定X = 3或者4。比如说上传9张图片，第一次上传个3个，其中一个请求回来了，立即去上传第四个，下一个回来上传第5个，以此类推。
这里我使用es6的generator函数来实现的，定义一个函数，返回需要上传的数组：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;*uploadGenerator (uploadQueue) {
        /**
         * 多张图片并发上传控制规则
         * 上传1-max数量的图片
         * 设置一个最大上传数量
         * 保证最大只有这个数量的上传请求
         *
         */

        // 最多只有三个请求在上传
        const maxUploadSize = 3;

        if(uploadQueue.length &gt; maxUploadSize){

            const result = [];

            for(let i = 0; i &amp;#x3C; uploadQueue.length; i++){
                // 第一次return maxUploadSize数量的图片
                if(i &amp;#x3C; maxUploadSize){
                    result.push(uploadQueue[i]);

                    if(i === maxUploadSize - 1){
                        yield result;
                    }
                } else {
                    yield [uploadQueue[i]];
                }
            }

        } else {
            yield uploadQueue.map((item)=&gt;(item));
        }
    }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;调用:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;// 通过该函数获取每次要上传的数组
        this.uploadGen = this.uploadGenerator(uploadQueue);
        // 第一次要上传的数量
        const firstUpload = this.uploadGen.next();


        // 真正开始上传流程
        firstUpload.value.map((item)=&gt;{
            /**
             * 图片上传分成5步
             * 图片转dataUrl
             * 压缩
             * 处理数据格式
             * 准备数据上传
             * 上传
             *
             * 前两步是回调的形式 后面是同步的形式
             */
            this.transformFileToDataUrl(item, this.compress, this.processData);
        });
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;9.2改进单张图片上传&lt;/h3&gt;
&lt;p&gt;图片上传分成5步，&lt;/p&gt;
&lt;blockquote&gt;
&lt;ol&gt;
&lt;li&gt;图片转dataUrl&lt;/li&gt;
&lt;li&gt;压缩&lt;/li&gt;
&lt;li&gt;处理数据格式&lt;/li&gt;
&lt;li&gt;准备数据上传&lt;/li&gt;
&lt;li&gt;上传&lt;/li&gt;
&lt;/ol&gt;
&lt;/blockquote&gt;
&lt;p&gt;这里面前两个是回调的形式，最后一个是异步形式。无法写成正常函数一个调用一个；而且各个function之间需要共享一些数据，之前把这个数据挂载到this.imgFile上了，但是这次是并发，一个对象没法满足需求了，改成数组也有很多问题。&lt;/p&gt;
&lt;p&gt;所以这次方案是：第一步创建一个要上传的对象，每次都通过参数交给下一个方法，直到最后一个方法上传。并且通过回调的方式，将各个步骤串联起来&lt;/p&gt;
&lt;p&gt;完整代码:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;import React from &apos;react&apos;
import classNames from &apos;classnames&apos;
import Touchable from &apos;rc-touchable&apos;
import Figure from &apos;./Figure&apos;
import Toast from &apos;../../../Feedback/Toast/components/Toast&apos;
import &apos;../style/index.scss&apos;

// 统计img总数 防止重复
let imgNumber = 0;

// 生成唯一的id
const getUuid = () =&gt; {
    return &quot;img-&quot; + new Date().getTime() + &quot;-&quot; + imgNumber++;
};

class Uploader extends React.Component{
    constructor (props) {
        super(props);
        this.state = {
            imgArray: [] // 图片已上传 显示的数组
        };
        this.handleInputChange = this.handleInputChange.bind(this);
        this.compress = this.compress.bind(this);
        this.processData = this.processData.bind(this);
    }
    componentDidMount () {
        // 判断是否有初始化的数据传入
        const {data} = this.props;

        if(data &amp;#x26;&amp;#x26; data.length &gt; 0){
            this.setState({imgArray: data});
        }
    }
    handleDelete(id) {
        this.setState((previousState)=&gt;{
            previousState.imgArray = previousState.imgArray.filter((item)=&gt;(item.id !== id));
            return previousState;
        });
    }
    handleProgress (id, e) {
        // 监听上传进度 操作DOM 显示进度
        const number = Number.parseInt((e.loaded / e.total) * 100) + &quot;%&quot;;
        const text = document.querySelector(&apos;#text-&apos;+id);
        const progress = document.querySelector(&apos;#progress-&apos;+id);

        text.innerHTML = number;
        progress.style.width = number;
    }
    handleUploadEnd (data, status) {
        // 准备一条标准数据
        const _this = this;
        const obj = {id: data.uuid, imgKey: &apos;&apos;, imgUrl: &apos;&apos;, name: data.file.name, dataUrl: data.dataUrl, status: status};

        // 更改状态
        this.setState((previousState)=&gt;{
            previousState.imgArray = previousState.imgArray.map((item)=&gt;{
                if(item.id === data.uuid){
                    item = obj;
                }

                return item;
            });
            return previousState;
        });

        // 上传下一个
        const nextUpload = this.uploadGen.next();
        if(!nextUpload.done){
            nextUpload.value.map((item)=&gt;{
                _this.transformFileToDataUrl(item, _this.compress, _this.processData);
            });
        }
    }
    handleInputChange (event) {
        const {typeArray, max, maxSize} = this.props;
        const {imgArray} = this.state;
        const uploadedImgArray = []; // 真正在页面显示的图片数组
        const uploadQueue = []; // 图片上传队列 这个队列是在图片选中到上传之间使用的 上传完成则清除

        // event.target.files是个类数组对象 需要转成数组方便处理
        const selectedFiles = Array.prototype.slice.call(event.target.files).map((item)=&gt;(item));

        // 检查文件个数 页面显示的图片个数不能超过限制
        if(imgArray.length + selectedFiles.length &gt; max){
            Toast.error(&apos;文件数量超出最大值&apos;, 2000, undefined, false);
            return;
        }

        let imgPass = {typeError: false, sizeError: false};

        // 循环遍历检查图片 类型、尺寸检查
        selectedFiles.map((item)=&gt;{
            // 图片类型检查
            if(typeArray.indexOf(item.type.split(&apos;/&apos;)[1]) === -1){
                imgPass.typeError = true;
            }
            // 图片尺寸检查
            if(item.size &gt; maxSize * 1024){
                imgPass.sizeError = true;
            }

            // 为图片加上位移id
            const uuid = getUuid();
            // 上传队列加入该数据
            uploadQueue.push({uuid: uuid, file: item});
            // 页面显示加入数据
            uploadedImgArray.push({ // 显示在页面的数据的标准格式
                id: uuid, // 图片唯一id
                dataUrl: &apos;&apos;, // 图片的base64编码
                imgKey: &apos;&apos;, // 图片的key 后端上传保存使用
                imgUrl: &apos;&apos;, // 图片真实路径 后端返回的
                name: item.name, // 图片的名字
                status: 1 // status表示这张图片的状态 1：上传中，2上传成功，3：上传失败
            });
        });

        // 有错误跳出
        if(imgPass.typeError){
            Toast.error(&apos;不支持文件类型&apos;, 2000, undefined, false);
            return;
        }

        if(imgPass.sizeError){
            Toast.error(&apos;文件大小超过限制&apos;, 2000, undefined, false);
            return;
        }

        // 没错误准备上传
        // 页面先显示一共上传图片个数
        this.setState({imgArray: imgArray.concat(uploadedImgArray)});

        // 通过该函数获取每次要上传的数组
        this.uploadGen = this.uploadGenerator(uploadQueue);
        // 第一次要上传的数量
        const firstUpload = this.uploadGen.next();


        // 真正开始上传流程
        firstUpload.value.map((item)=&gt;{
            /**
             * 图片上传分成5步
             * 图片转dataUrl
             * 压缩
             * 处理数据格式
             * 准备数据上传
             * 上传
             *
             * 前两步是回调的形式 后面是同步的形式
             */
            this.transformFileToDataUrl(item, this.compress, this.processData);
        });
    }
    *uploadGenerator (uploadQueue) {
        /**
         * 多张图片并发上传控制规则
         * 上传1-max数量的图片
         * 设置一个最大上传数量
         * 保证最大只有这个数量的上传请求
         *
         */

        // 最多只有三个请求在上传
        const maxUploadSize = 3;

        if(uploadQueue.length &gt; maxUploadSize){

            const result = [];

            for(let i = 0; i &amp;#x3C; uploadQueue.length; i++){
                // 第一次return maxUploadSize数量的图片
                if(i &amp;#x3C; maxUploadSize){
                    result.push(uploadQueue[i]);

                    if(i === maxUploadSize - 1){
                        yield result;
                    }
                } else {
                    yield [uploadQueue[i]];
                }
            }

        } else {
            yield uploadQueue.map((item)=&gt;(item));
        }
    }
    transformFileToDataUrl (data, callback, compressCallback) {
        /**
         * 图片上传流程的第一步
         * @param data file文件 该数据会一直向下传递
         * @param callback 下一步回调
         * @param compressCallback 回调的回调
         */
        const {compress} = this.props;
        const imgCompassMaxSize = 200 * 1024; // 超过 200k 就压缩

        // 封装好的函数
        const reader = new FileReader();

        // ⚠️ 这是个回调过程 不是同步的
        reader.onload = function(e) {
            const result = e.target.result;
            data.dataUrl = result;

            if(compress &amp;#x26;&amp;#x26; result.length &gt; imgCompassMaxSize){
                data.compress = true;

                callback(data, compressCallback); // 图片压缩
            } else {
                data.compress = false;

                callback(data, compressCallback); // 图片不压缩
            }
        };

        reader.readAsDataURL(data.file);
    }
    compress (data, callback) {
        /**
         * 压缩图片
         * @param data file文件 数据会一直向下传递
         * @param callback 下一步回调
         */
        const {compressionRatio} = this.props;
        const imgFile = data.file;
        const img = new window.Image();

        img.src = data.dataUrl;

        img.onload = function () {
            const canvas = document.createElement(&apos;canvas&apos;);
            const ctx = canvas.getContext(&apos;2d&apos;);

            canvas.width = img.width;
            canvas.height = img.height;

            ctx.drawImage(img, 0, 0, canvas.width, canvas.height);

            let compressedDataUrl;

            if(data.compress){
                compressedDataUrl = canvas.toDataURL(imgFile.type, (compressionRatio / 100));
            } else {
                compressedDataUrl = canvas.toDataURL(imgFile.type, 1);
            }

            data.compressedDataUrl = compressedDataUrl;

            callback(data);
        }
    }
    processData (data) {
        // 为了兼容性 处理数据
        const dataURL = data.compressedDataUrl;
        const imgFile = data.file;
        const binaryString = window.atob(dataURL.split(&apos;,&apos;)[1]);
        const arrayBuffer = new ArrayBuffer(binaryString.length);
        const intArray = new Uint8Array(arrayBuffer);

        for (let i = 0, j = binaryString.length; i &amp;#x3C; j; i++) {
            intArray[i] = binaryString.charCodeAt(i);
        }

        const fileData = [intArray];

        let blob;

        try {
            blob = new Blob(fileData, { type: imgFile.type });
        } catch (error) {
            window.BlobBuilder = window.BlobBuilder ||
                window.WebKitBlobBuilder ||
                window.MozBlobBuilder ||
                window.MSBlobBuilder;
            if (error.name === &apos;TypeError&apos; &amp;#x26;&amp;#x26; window.BlobBuilder){
                const builder = new BlobBuilder();
                builder.append(arrayBuffer);
                blob = builder.getBlob(imgFile.type);
            } else {
                throw new Error(&apos;版本过低，不支持上传图片&apos;);
            }
        }

        data.blob = blob;
        this.processFormData(data);
    }
    processFormData (data) {
        // 准备上传数据
        const formData = new FormData();
        const imgFile = data.file;
        const blob = data.blob;

        // type
        formData.append(&apos;type&apos;, blob.type);
        // size
        formData.append(&apos;size&apos;, blob.size);
        // append 文件
        formData.append(&apos;file&apos;, blob, imgFile.name);

        this.uploadImg(data, formData);
    }
    uploadImg (data, formData) {
        // 开始发送请求上传
        const _this = this;
        const xhr = new XMLHttpRequest();
        const {uploadUrl} = this.props;

        // 进度监听
        xhr.upload.addEventListener(&apos;progress&apos;, _this.handleProgress.bind(_this, data.uuid), false);

        xhr.onreadystatechange = function () {
            if (xhr.readyState === 4) {
                if (xhr.status === 200 || xhr.status === 201) {
                    // 上传成功
                    _this.handleUploadEnd(data, 2);
                } else {
                    // 上传失败
                    _this.handleUploadEnd(data, 3);
                }
            }
        };

        xhr.open(&apos;POST&apos;, uploadUrl , true);
        xhr.send(formData);
    }
    getImagesListDOM () {
        // 处理显示图片的DOM
        const {max} = this.props;
        const _this = this;
        const result = [];
        const uploadingArray = [];
        const imgArray = this.state.imgArray;

        imgArray.map((item)=&gt;{
            result.push(
                &amp;#x3C;Figure key={item.id} {...item} onDelete={_this.handleDelete.bind(_this)} /&gt;
            );

            // 正在上传的图片
            if(item.status === 1){
                uploadingArray.push(item);
            }
        });

        // 图片数量达到最大值
        if(result.length &gt;= max ) return result;

        let onPress = ()=&gt;{_this.refs.input.click();};

        //  或者有正在上传的图片的时候 不可再上传图片
        if(uploadingArray.length &gt; 0) {
            onPress = undefined;
        }

        // 简单的显示文案逻辑判断
        let text = &apos;上传图片&apos;;

        if(uploadingArray.length &gt; 0){
            text = (imgArray.length - uploadingArray.length) + &apos;/&apos; + imgArray.length;
        }

        result.push(
            &amp;#x3C;Touchable
                key=&quot;add&quot;
                activeClassName={&apos;zby-upload-img-active&apos;}
                onPress={onPress}
            &gt;
                &amp;#x3C;div className=&quot;zby-upload-img&quot;&gt;
                    &amp;#x3C;span key=&quot;icon&quot; className=&quot;fa fa-camera&quot; /&gt;
                    &amp;#x3C;p className=&quot;text&quot;&gt;{text}&amp;#x3C;/p&gt;
                &amp;#x3C;/div&gt;
            &amp;#x3C;/Touchable&gt;
        );

        return result;
    }
    render () {
        const imagesList = this.getImagesListDOM();
            
        return (
            &amp;#x3C;div className=&quot;zby-uploader-box&quot;&gt;
                {imagesList}
                &amp;#x3C;input ref=&quot;input&quot; type=&quot;file&quot; className=&quot;file-input&quot; name=&quot;image&quot; accept=&quot;image/*&quot; multiple=&quot;multiple&quot; onChange={this.handleInputChange} /&gt;
            &amp;#x3C;/div&gt;
        )
    }
}

Uploader.propTypes = {
    uploadUrl: React.PropTypes.string.isRequired, // 图上传路径
    compress: React.PropTypes.bool, // 是否进行图片压缩
    compressionRatio: React.PropTypes.number, // 图片压缩比例 单位：%
    data: React.PropTypes.array, // 初始化数据 其中的每个元素必须是标准化数据格式
    max: React.PropTypes.number, // 最大上传图片数
    maxSize: React.PropTypes.number, // 图片最大体积 单位：KB
    typeArray: React.PropTypes.array, // 支持图片类型数组
};

Uploader.defaultProps = {
    compress: true,
    compressionRatio: 20,
    data: [],
    max: 9,
    maxSize: 5 * 1024, // 5MB
    typeArray: [&apos;jpeg&apos;, &apos;jpg&apos;, &apos;png&apos;, &apos;gif&apos;],
};

export default Uploader
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;完整代码&lt;a href=&quot;https://github.com/Aus0049/react-component/blob/master/src/components/DataEntry/Uploader/components/Uploader.js&quot;&gt;Github&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;TODO&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;a href=&quot;https://www.cnblogs.com/wlink/articles/7792375.html&quot;&gt;桌面端采用node.js的fs,mime库上传文件 &lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;参考文章&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;a href=&quot;http://www.cnblogs.com/xiaoqian1993/p/5775135.html&quot;&gt;使用 JavaScript File API 实现文件上传&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://segmentfault.com/a/1190000010034177&quot;&gt;移动端H5实现图片上传的几种方案&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;</content:encoded><h:img src="/_astro/thumbnail.BdbzhB7A.png"/><enclosure url="/_astro/thumbnail.BdbzhB7A.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item><item><title>React 中使用 Video.js</title><link>https://luopengcheng89.github.io/blog/use_videojs_in_react</link><guid isPermaLink="true">https://luopengcheng89.github.io/blog/use_videojs_in_react</guid><description>在 React 项目中使用 video.js，实现 HTML5 视频播放器的自定制。</description><pubDate>Fri, 04 May 2018 14:27:53 GMT</pubDate><content:encoded>&lt;h2&gt;0-Intro&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;在 React 项目中使用 video.js，实现 HTML5 视频播放器的自定制。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;strong&gt;关于 &lt;a href=&quot;http://videojs.com/&quot;&gt;video.js&lt;/a&gt;：&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;引用官方的自我介绍：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;video.js is a free and open source HTML5 video player.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;这是个免费且开源的 HTML5 视频播放器。&lt;/p&gt;
&lt;h2&gt;1-着手开发&lt;/h2&gt;
&lt;p&gt;video.js 使用了 &lt;a href=&quot;https://gruntjs.com/&quot;&gt;Grunt&lt;/a&gt; 作为构建工具。所以，在开始写代码之前，请确保开发环境里已经装有 &lt;a href=&quot;https://nodejs.org/&quot;&gt;Node&lt;/a&gt; 和 &lt;a href=&quot;https://gruntjs.com/&quot;&gt;Grunt&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;安装 Grunt:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npm install -g grunt-cli
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;2-创建一个 React 项目&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;create-react-app myVideoPlayer
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;然后删去对我们没用的代码&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/BeijiYang/VideoJsCustomization/tree/06e79e32b3cbfb8571a788166279083729f40e6c&quot;&gt;commit&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;3-React项目中引入video.js&lt;/h2&gt;
&lt;p&gt;本节，我们将在 React 项目中引入 video.js ，为单调的页面上添加一个 HTML5 视频播放器。&lt;/p&gt;
&lt;h3&gt;安装video.js&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-sh&quot;&gt;npm install --save-dev video.js
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;项目中引入video.js&lt;/h3&gt;
&lt;p&gt;对于如何在 React 项目中使用 video.js ，官方文档就这一篇：&lt;a href=&quot;http://docs.videojs.com/tutorial-React.html&quot;&gt; video.js  and  ReactJS integration&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;我们参考文档中的基本方法，主要思路就是利用 React 组件的生命周期函数：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;在 &lt;code&gt;componentDidMount&lt;/code&gt; 阶段实例化一个 video.js 播放器&lt;/li&gt;
&lt;li&gt;在 &lt;code&gt;componentWillUnmount&lt;/code&gt; 阶段将其销毁&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;在我们的项目中，新建文件夹 &lt;code&gt;src/lib/VideoPlayer&lt;/code&gt;，在其中新建组件 &lt;code&gt;VideoPlayer.js&lt;/code&gt;:&lt;/p&gt;
&lt;p&gt;特别注意，需要加入对 css 文件的引用&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;import React from &apos;React&apos;
import videojs from &apos;video.js&apos;
import &apos;video.js/dist/video-js.css&apos;

export default class VideoPlayer extends React.Component {
  componentDidMount () {
    // instantiate video.js
    this.player = videojs(this.videoNode, this.props, function onPlayerReady () {
      console.log(&apos;onPlayerReady&apos;, this)
    })
  }

  // destroy player on unmount
  componentWillUnmount () {
    if (this.player) {
      this.player.dispose()
    }
  }

  // wrap the player in a div with a `data-vjs-player` attribute
  // so videojs won&apos;t create additional wrapper in the DOM
  // see https://github.com/videojs/ video.js /pull/3856
  render () {
    return (
      &amp;#x3C;div data-vjs-player&gt;
        &amp;#x3C;video ref={node =&gt; this.videoNode = node} className=&apos;video-js&apos; /&gt;
      &amp;#x3C;/div&gt;
    )
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;然后新建课程组件，它将引用 VideoPlayer 组件：&lt;/p&gt;
&lt;p&gt;根据 &lt;a href=&quot;https://medium.com/@dan_abramov/smart-and-dumb-components-7ca2f9a7c7d0&quot;&gt;Dan Abramov的思想&lt;/a&gt;，组件拆分为展示性组件和容器组件（Presentational and Container Components）&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;src/containers/App.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;import React, { Component } from &apos;React&apos;
import CourseContainer from &apos;./CourseContainer&apos;

class App extends Component {
  render () {
    return (
      &amp;#x3C;div className=&apos;app&apos;&gt;
        &amp;#x3C;CourseContainer /&gt;
      &amp;#x3C;/div&gt;
    )
  }
}

export default App
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;src/containers/CourseContainer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;import React, { Component } from &apos;React&apos;
import Course from &apos;../components/Course&apos;

class CourseContainer extends Component {
  render () {
    // VideoJsOptions for this Course
    const CourseVideoJsOptions = {
      autoplay: true,
      controls: true,
      sources: [{
        src: &apos;http://vjs.zencdn.net/v/oceans.mp4&apos;,
        type: &apos;video/mp4&apos;
      }]
    }

    return (
      &amp;#x3C;Course videoJsOptions={CourseVideoJsOptions} /&gt;
    )
  }
}

export default CourseContainer
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;src/components/Course.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;
import React, { Component } from &apos;React&apos;
import VideoPlayer from &apos;../lib/VideoPlayer/VideoPlayer&apos;

class Course extends Component {

  render () {
    return (
      &amp;#x3C;div className=&apos;course-container&apos;&gt;
        &amp;#x3C;h2&gt;CourseDemo&amp;#x3C;/h2&gt;
        &amp;#x3C;VideoPlayer {...this.props.videoJsOptions} /&gt;
      &amp;#x3C;/div&gt;
    )
  }
}

export default Course
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;至此，在课程页面上就已经有一个播放器了。我们在 React 项目中成功引入了 video.js 。&lt;/p&gt;
&lt;p&gt;很容易发现，页面加载完成后，播放器会自动播放视频。能否禁止这个默认行为呢？&lt;/p&gt;
&lt;p&gt;下一节我们就看如何对播放器的功能进行控制和扩展。&lt;/p&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/BeijiYang/VideoJsCustomization/tree/897f3ca7ccce59cb9490011e19bad0a7112088c8&quot;&gt;commit&lt;/a&gt;&lt;/p&gt;
&lt;h2&gt;4-用 options 控制功能&lt;/h2&gt;
&lt;p&gt;本节，我们用 options 实现对基本功能的控制。&lt;/p&gt;
&lt;p&gt;video.js 中，可以通过 &lt;a href=&quot;http://docs.videojs.com/tutorial-options.html&quot;&gt;options&lt;/a&gt; 对播放器实例进行控制，如循环播放、静音、以及宽高样式等方面。&lt;/p&gt;
&lt;p&gt;上面案例代码中的 &lt;code&gt;CourseVideoJsOptions&lt;/code&gt; 就是一个例子。定义一个 options 对象，将其作为参数传入 VideoPlayer 组件中：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;src/components/Course.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;&amp;#x3C;VideoPlayer {...this.props.videoJsOptions} /&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;案例代码中的 options 对象如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;const CourseVideoJsOptions = {
  autoplay: true,
  controls: true,
  sources: [{
    src: &apos;http://vjs.zencdn.net/v/oceans.mp4&apos;,
    type: &apos;video/mp4&apos;
  }]
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;其中有三个 key，对照 &lt;a href=&quot;http://docs.videojs.com/tutorial-options.html&quot;&gt;options&lt;/a&gt; 文档，不难知道&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;autoplay 是否自动播放&lt;/li&gt;
&lt;li&gt;controls 是否显示控制条&lt;/li&gt;
&lt;li&gt;sources 规定视频源&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;通过 options，我们可以对功能进行控制与添加：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;playbackRates：倍速播放&lt;/li&gt;
&lt;li&gt;poster： 视频播放前显示的图片&lt;/li&gt;
&lt;li&gt;volumePanel：音量条&lt;/li&gt;
&lt;li&gt;fluid： 播放器自动充满容器&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;const CourseVideoJsOptions = {
  autoplay: false,
  controls: true,
  sources: [{
    src: &apos;http://vjs.zencdn.net/v/oceans.mp4&apos;,
    type: &apos;video/mp4&apos;
  }, {
    src: &apos;http://vjs.zencdn.net/v/oceans.webm&apos;,
    type: &apos;video/webm&apos;
  }],
  poster: &apos;http://videojs.com/img/logo.png&apos;,
  fluid: &apos;true&apos;, // put the player in the VideoPlayerWrap box
  &apos;playbackRates&apos;: [0.75, 1, 1.5, 2],
  controlBar: {
    volumePanel: {
      inline: false // 将音量控制条垂直
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;注意：这里 &lt;code&gt;sources&lt;/code&gt; 对应的值是一个视频源对象数组。数组中每个 &lt;code&gt;src&lt;/code&gt; 都是同一个视频，但格式各异。&lt;/p&gt;
&lt;p&gt;这样可以解决不同浏览器之间的兼容性问题：Video.js 会检测当前浏览器所支持的视频格式，然后在数组中选择合适的视频源进行播放。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;src/components/Course.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;import React, { Component } from &apos;React&apos;
import VideoPlayer from &apos;../lib/VideoPlayer/VideoPlayer&apos;
import styled from &apos;styled-components&apos;

const VideoPlayerWrap = styled.div`
  margin: 10px;
  padding: 10px;
  border: 2px solid green;
`

class Course extends Component {

  render () {
    return (
      &amp;#x3C;div className=&apos;course-container&apos;&gt;
        &amp;#x3C;h2&gt;CourseDemo&amp;#x3C;/h2&gt;
        &amp;#x3C;VideoPlayerWrap&gt;
          &amp;#x3C;VideoPlayer {...this.props.videoJsOptions} /&gt;
        &amp;#x3C;/VideoPlayerWrap&gt;
      &amp;#x3C;/div&gt;
    )
  }
}

export default Course
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;注：这里使用了 &lt;a href=&quot;https://www.styled-components.com/&quot;&gt;styled-component&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;在播放器外套了一层 VideoPlayerWrap（其实就是 div ），这么做的好处在于：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;由于 VideoPlayer options 中打开了 &lt;code&gt;fluid&lt;/code&gt;，播放器可以自适应 VideoPlayerWrap 容器。如此，options 就可以专注于对功能进行控制。&lt;/li&gt;
&lt;li&gt;VideoPlayerWrap 的样式代码，同时也规定了播放器的样式。将样式代码集中写到展示性组件中，也符合 Dan Abramov 的思想&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;本节，我们用 video.js 的 options 机制，实现了播放器的倍速播放、添加 poster、样式控制等功能。&lt;/p&gt;
&lt;p&gt;以上都是对现有功能进行控制。下一节，我们来看看如何按照我们的想法，对播放器的功能进行扩展。&lt;/p&gt;
&lt;h2&gt;5-写插件(Plugin)扩展功能&lt;/h2&gt;
&lt;p&gt;本节，我们的目的是，扩展空格键控制播放/暂停的功能。&lt;/p&gt;
&lt;p&gt;video.js 的默认动作是，仅仅在 control bar 的播放键被鼠标选中时，才能用空格/回车键控制播放/暂停。不太方便。&lt;/p&gt;
&lt;p&gt;我们要将其改进为：点开视频后，就可以通过空格键控制视频的播放/暂停。&lt;/p&gt;
&lt;h3&gt;引入插件&lt;/h3&gt;
&lt;p&gt;我们可以通过写自己的&lt;a href=&quot;http://docs.videojs.com/docs/guides/plugins.html&quot;&gt;插件&lt;/a&gt;来实现额外的功能。参考这篇文档，尝试如下：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;render () {
   // 写插件：当监听到播放器实例的播放（play）事件，就输出一条语句
   function examplePlugin(options) {
       this.on(&apos;play&apos;, function(e) {
         console.log(&apos;playback has started!&apos;);
       });
     };

   // 注册该插件
   videojs.registerPlugin(&apos;examplePlugin&apos;, examplePlugin)

   return (
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;目前插件已经存在，可以根据需要打开或关闭它&lt;/p&gt;
&lt;p&gt;下面尝试使用&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;CourseContainer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;··· ···
const CourseVideoJsOptions = {
  autoplay: false,
  controls: true,

  ... ...

  // 使用该插件
  plugins: {
    setStateandFocusPlugin: true
  }
}
... ...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;完成以上步骤后，再次点开视频，发现控制台输出了 &lt;code&gt;playback has started!&lt;/code&gt; ，说明插件应用成功。&lt;/p&gt;
&lt;p&gt;关于代码中的事件监听，参考文档 &lt;a href=&quot;http://docs.videojs.com/tutorial-event-target.html&quot;&gt;Event Target&lt;/a&gt;&lt;/p&gt;
&lt;h3&gt;设置播放器实例的状态&lt;/h3&gt;
&lt;p&gt;在插件代码中加一条 console 语句如下。&lt;/p&gt;
&lt;p&gt;如此，在控制台中，就可以在点开视频时看到，这句代码输出了该播放器的实例对象&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;··· ···
function examplePlugin(options) {
    this.on(&apos;play&apos;, function(e) {
      console.log(this)
      console.log(&apos;playback has started!&apos;)
    });
  };
  ... ...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;仔细查看该对象的属性，发现有 &lt;code&gt;setState&lt;/code&gt; 和 &lt;code&gt;state&lt;/code&gt; 两条。
据此，我们尝试根据播放器的播放状态来设置 &lt;code&gt;state&lt;/code&gt; 值&lt;/p&gt;
&lt;p&gt;注意，这和 React 组件的 state 是两回事。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;··· ···
this.on(&apos;play&apos;, function (e) {
  console.log(this);
  console.log(&apos;playback has started!&apos;)
  this.setState({
    state: &apos;playing&apos;
  })
  console.log(this.state.state)
})

this.on(&apos;pause&apos;, function (e) {
  console.log(&apos;playback has paused&apos;)
  this.setState({
    state: &apos;pause&apos;
  })
  console.log(this.state.state)
})
... ...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;此时，再让视频播放/暂停，都会看到控制台输出的状态，说明设置成功。&lt;/p&gt;
&lt;h3&gt;初步改进空格键的功能&lt;/h3&gt;
&lt;p&gt;空格键的默认动作是:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;当鼠标选中 control bar 的播放键时，空格键可以切换播放/暂停；&lt;/li&gt;
&lt;li&gt;当鼠标选中全屏键时，空格键可以切换全屏；&lt;/li&gt;
&lt;li&gt;当鼠标选中静音键时，空格键可以切换静音；&lt;/li&gt;
&lt;li&gt;当鼠标什么也没选中时，空格键无法控制播放器的任何功能。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这样既麻烦又不实用，因为只有“播放/暂停”功能才是使用频率最高的功能。让空格键在任何情况下都能直接控制暂停功能，可以明显提升用户体验。&lt;/p&gt;
&lt;p&gt;接下来，我们需要监听“按下空格键”这个事件。这个需求可以分为两步：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;监听键盘事件&lt;/li&gt;
&lt;li&gt;判断是否为空格键&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;对于前者，我们可以使用 &lt;a href=&quot;http://www.w3school.com.cn/jsref/event_onkeydown.asp&quot;&gt;onKeyDown 事件&lt;/a&gt;，它会在用户按下一个键盘按键时发生。&lt;/p&gt;
&lt;p&gt;对于后者，涉及到 &lt;a href=&quot;&quot;&gt;keyCode/键码-文档链接待补充&lt;/a&gt;的知识。具体到这个案例，空格键的键码是32。&lt;/p&gt;
&lt;p&gt;推荐&lt;a href=&quot;http://keycode.info&quot;&gt;这个网站&lt;/a&gt;，它可以很方便地查询键盘各个按键的键码，十分好用。&lt;/p&gt;
&lt;p&gt;通过 onKeyDown 事件的文档可以看到，&lt;code&gt;&amp;#x3C;video&gt;&lt;/code&gt; 标签不支持它，所以我们把它写在包裹 &lt;code&gt;&amp;#x3C;video&gt;&lt;/code&gt; 标签的 &lt;code&gt;&amp;#x3C;div&gt;&lt;/code&gt; 标签中。代码如下&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;··· ···
videojs.registerPlugin(&apos;setStateandFocusPlugin&apos;, setStateandFocusPlugin)
// videojs.registerPlugin(&apos;handleKeyPress&apos;, handleKeyPress)

return (
  &amp;#x3C;div data-vjs-player
    onKeyDown={this.handleSpaceKeyDown}
    &gt;
    &amp;#x3C;video
    ref={node =&gt; this.videoNode = node}
    className=&apos;video-js vjs-hqcat&apos;
    /&gt;
    &amp;#x3C;/div&gt;
    )
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;由于 JS 的&lt;a href=&quot;&quot;&gt;事件冒泡-文档链接待补充&lt;/a&gt;机制，onKeyDown 事件是可以成功捕获的。&lt;/p&gt;
&lt;p&gt;事实上，如果在目前这个 div 标签的外层再套一层 div ，外层的 div 也同样能够捕获 onKeyDown 事件，有兴趣的可以试一下。&lt;/p&gt;
&lt;p&gt;接下来写监听到 onKeyDown 事件后调用的函数：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;首先，我们需要判断是否为空格键。&lt;/li&gt;
&lt;li&gt;如果是，我们需要禁止原来默认的动作。&lt;/li&gt;
&lt;li&gt;然后，利用之前设置的视频播放状态，进行相应的操作：
&lt;ul&gt;
&lt;li&gt;如果视频正在播放中，则暂停视频&lt;/li&gt;
&lt;li&gt;如果视频正在暂停中，则继续播放视频&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;··· ···
componentWillUnmount () {
  if (this.player) {
    this.player.dispose()
  }
}

handleSpaceKeyDown = (event) =&gt; {
  // 判断是否为空格键
  if (event.which === 32) {
    event.preventDefault()
    if (this.player) {
      // 根据播放状态的不同，进行相应的操作
      switch (this.player.state.state) {
        case &apos;playing&apos;:
        this.player.pause()
        break
        case &apos;pause&apos;:
        this.player.play()
        break
        default: return
      }
    } else {
      console.log(&apos;error&apos;)
    }
  }
}
... ...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;至此，我们完成了初步的改进。但目前扔有一些问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;点击视频播放后，必须再点一下视频，才能用空格键控制暂停/播放；&lt;/li&gt;
&lt;li&gt;如果在播放时，鼠标点一下页面上的其他地方，空格键又失效了。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;下一步来解决这些问题。&lt;/p&gt;
&lt;h3&gt;改进焦点管理&lt;/h3&gt;
&lt;p&gt;元素获得焦点的方式有页面加载、用户输入（通常是通过 Tab 按键）和在代码中调用 &lt;code&gt;focus()&lt;/code&gt; 方法。相应的文档获得了焦点，用户才能与页面交互。&lt;/p&gt;
&lt;p&gt;于是，我们是思路是：当视频播放时，调用 &lt;code&gt;focus()&lt;/code&gt; 方法，使得播放器获得焦点。&lt;/p&gt;
&lt;p&gt;如何获取播放器的 DOM 元素？可以使用 React 的 ref。
参考 [Refs and the DOM](https:// React js.org/docs/refs-and-the-dom.html)&lt;/p&gt;
&lt;p&gt;如何在插件代码中获取 React 组件？可以从它的外部传 &lt;code&gt;this&lt;/code&gt; 进去。&lt;/p&gt;
&lt;p&gt;尝试改进代码：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;··· ···
render () {
  // write a plugin
  var that = this
  const setStateandFocusPlugin = function (options) {
    this.on(&apos;play&apos;, function (e) {
      console.log(&apos;playback has started!&apos;)

      // 控制台输出结果表明成功获取VideoPlayer组件
      console.log(that)

      this.setState({
        state: &apos;playing&apos;
      })
      // 控制焦点
      that.refs.videoPlayerRef.focus()
    })
    ... ...
    return (
      &amp;#x3C;div data-vjs-player
        onKeyDown={this.handleSpaceKeyDown}
        ref=&apos;videoPlayerRef&apos;
        &gt;
        &amp;#x3C;video
          ref={node =&gt; this.videoNode = node}
          className=&apos;video-js vjs-hqcat&apos;
        /&gt;
      &amp;#x3C;/div&gt;
    )
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;现在再试用播放器，可以发现第一个问题已经解决了。点开视频后，直接按空格键，视频暂停。&lt;/p&gt;
&lt;p&gt;第二个问题依然存在。这是因为 &lt;code&gt;this.on(&apos;play&apos;, cb)&lt;/code&gt; 这个监听事件不是持续的。视频播放的过程中，再点击页面的其他地方，播放器会市区焦点。&lt;/p&gt;
&lt;p&gt;有没有更好的方法？ video.js 的 github issue 里，官方人员提及 video.js 支持全部原生  HTML5 media elements events, 仔细查阅 [W3C Recommendation 文档 media event summary部分](https://www.w3.org/TR/ HTML5 /embedded-content-0.html#mediaevents)，以及官方人员的 &lt;a href=&quot;http://docs.videojs.com/docs/api/player.html#events&quot;&gt;events文档&lt;/a&gt;，发现这一条：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;timeupdate&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;Fired when the current playback position has changed * During playback this is fired every &gt;15-250 milliseconds, depending on the playback technology in use.&lt;/p&gt;
&lt;p&gt;Defined in https://github.com/videojs/ video.js /blob/master/src/js/player.js line number: 2792&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;只要在播放，就会持续监听到该事件。十分适合用来解决我们的问题。有了它，&lt;code&gt;this.on(&apos;play&apos;, cb)&lt;/code&gt;的回调中也不需要进行焦点控制了。&lt;/p&gt;
&lt;p&gt;根据该插件的功能，将其命名为 &lt;code&gt;setStateandFocusPlugin&lt;/code&gt;，完整代码如下&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;··· ···
const setStateandFocusPlugin = function (options) {
  this.on(&apos;play&apos;, function (e) {
    console.log(&apos;playback has started!&apos;)
    console.log(that)
    this.setState({
      state: &apos;playing&apos;
    })
  })

  this.on(&apos;pause&apos;, function (e) {
    console.log(&apos;playback has paused&apos;)
    this.setState({
      state: &apos;pause&apos;
    })
  })

  this.on(&apos;timeupdate&apos;, function(e){
    that.refs.videoPlayerRef.focus()
  })
}
... ...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;注意，注册以及使用插件的代码中也需要同步改名。&lt;/p&gt;
&lt;p&gt;现在再来尝试，发现第二个问题也解决了。&lt;/p&gt;
&lt;p&gt;目前的代码可以实现：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;当视频播放的过程中，不论鼠标点哪里，都让焦点保持在播放器，以便通过空格键控制；&lt;/li&gt;
&lt;li&gt;看视频时也可能需要暂停下来做别的事情，那么当视频处于暂停状态，就可以通过鼠标点击等行为切换焦点。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;空格键控制播放/暂停的功能就比较完善了。&lt;/p&gt;
&lt;h3&gt;总结&lt;/h3&gt;
&lt;p&gt;这一小节，我们实现了一个 video.js 的插件（涉及到插件的注册，使用等），通过它监听播放器实例的事件，在不同的事件被触发时，进行相应的操作：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;当监听到播放（&lt;code&gt;play&lt;/code&gt;）事件的触发，设置播放器实例状态为 &lt;code&gt;playing&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;当监听到暂停（&lt;code&gt;pause&lt;/code&gt;）事件的触发，设置播放器实例状态为 &lt;code&gt;pause&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;当监听到 &lt;code&gt;timeupdate&lt;/code&gt; 事件的触发，让播放器组件获得焦点，涉及到
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;focus()&lt;/code&gt; 方法&lt;/li&gt;
&lt;li&gt;&lt;code&gt;ref&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;然后，监听键盘事件，当监听到用户按下空格键时，禁止原来的默认动作，再根据播放器实例此时的状态，进行相应的操作&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;若正在播放中，则暂停之&lt;/li&gt;
&lt;li&gt;若视频被暂停，则播放之&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;涉及到：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;event.preventDefault()&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;onKeyDown&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;事件冒泡&lt;/li&gt;
&lt;li&gt;键码&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;6-实现样式的自定制&lt;/h2&gt;
&lt;p&gt;以上内容都聚焦于功能。本节，对播放器的外观进行修改，创造自己的播放器皮肤文件。&lt;/p&gt;
&lt;p&gt;官方的 &lt;a href=&quot;http://docs.videojs.com/tutorial-skins.html&quot;&gt;Creating a Skin&lt;/a&gt; 文档中推荐的方法是直接覆盖掉原有的默认皮肤。&lt;/p&gt;
&lt;h3&gt;为播放器增加自定制的 &lt;code&gt;className&lt;/code&gt;&lt;/h3&gt;
&lt;p&gt;文档中的示例代码为&lt;code&gt;&amp;#x3C;video&gt;&lt;/code&gt;标签增加了一个新的 &lt;code&gt;class&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;类似的，在我们的 React 项目中，则需增加一个新的className：&lt;code&gt;vjs-hqcat&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;VideoPlayer.js&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-jsx&quot;&gt;&amp;#x3C;video
  ref={node =&gt; this.videoNode = node}
  className=&apos;video-js vjs-hqcat&apos;
/&gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;新建自定制的皮肤样式文件&lt;/h3&gt;
&lt;p&gt;我们将其命名为 &lt;code&gt;videojs-hqcat.css&lt;/code&gt;，放到 &lt;code&gt;src/lib/VideoPlayer&lt;/code&gt; 目录下。&lt;/p&gt;
&lt;h3&gt;创建自己的皮肤&lt;/h3&gt;
&lt;p&gt;现在，利用浏览器的开发工具，找到自己想修改的部分的选择器，就可以进行相应的修改了！&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;居中播放键&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;例如，在默认皮肤里，视频加载完成后，大播放键的位置是画面的左上角，看起来很别扭。&lt;/p&gt;
&lt;p&gt;通过 Chrome Dev tool，发现播放键的类名是 &lt;code&gt;vjs-big-play-butto&lt;/code&gt;。据此，有如下代码：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;src/lib/VideoPlayer/videojs-hqcat.css&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-css&quot;&gt;/*居中按钮*/
.vjs-hqcat .vjs-big-play-button {
  height: 1.5em;
  width: 3em;
  left: 50%;
  top: 50%;
  margin-left: -1.5em;
  margin-top: -0.75em;
  border-color: rgb(0, 188, 212);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;成功居中了播放键，顺便改了个好看的颜色。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;修改 controlBar 颜色&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;同理，给播放器的控制栏也换个好看的颜色&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;src/lib/VideoPlayer/videojs-hqcat.css&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-css&quot;&gt;/*颜色*/
.vjs-hqcat.video-js {
  color: rgb(0, 188, 212);
}

.vjs-hqcat:hover .vjs-big-play-button,
.vjs-hqcat .vjs-big-play-button:focus {
  border-color: rgb(0, 188, 212);
}

.vjs-hqcat .vjs-volume-level,
.vjs-hqcat .vjs-play-progress,
.vjs-hqcat .vjs-slider-bar {
  background:rgb(0, 188, 212);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这下颜色顺眼多了！&lt;/p&gt;
&lt;p&gt;还有些小问题，比如控制栏的字体大小，以及倍速选择栏的样式等。同理，找到相应的class名，在新皮肤文件中添加代码如下：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;src/lib/VideoPlayer/videojs-hqcat.css&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-css&quot;&gt;/*控制栏字体*/
.vjs-hqcat .vjs-control-bar {
  font-size: small;
}

.vjs-hqcat .vjs-menu-item {
  font-size: small;
}

.vjs-hqcat li.vjs-selected {
  background-color:rgb(0, 188, 212);
}

.vjs-hqcat .vjs-playback-rate-value {
  font-size: small;
  line-height: 3em;
}

/*完善倍速选择器样式*/
.vjs-menu li.vjs-selected,
.vjs-menu li.vjs-selected:focus,
.vjs-menu li.vjs-selected:hover {
  background-color: #2B333F;
  color: rgb(0, 188, 212); }

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;创建自定制皮肤文件成功！&lt;/p&gt;
&lt;h2&gt;7-总结&lt;/h2&gt;
&lt;p&gt;在这个案例中，我们在 React 项目里引入 video.js，实现了自定制的 HTML5 视频播放器。&lt;/p&gt;
&lt;p&gt;我们在播放器默认设置的基础之上，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;通过 options 控制现有功能；&lt;/li&gt;
&lt;li&gt;通过 plugins 扩展添加了自己想要的新功能；&lt;/li&gt;
&lt;li&gt;通过增加新的皮肤样式文件实现了外观的自定制。&lt;/li&gt;
&lt;/ul&gt;</content:encoded><h:img src="/_astro/thumbnail.BFbVXuhq.png"/><enclosure url="/_astro/thumbnail.BFbVXuhq.png"/><follow_challenge><feedId>180111202177880064</feedId><userId>157324722055452672</userId></follow_challenge></item></channel></rss>